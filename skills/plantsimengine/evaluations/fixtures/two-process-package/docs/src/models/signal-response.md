# Signal response

`LinearResponse` is a pedagogical, non-calibrated proportional-response
hypothesis. It uses an arbitrary illustrative unit, has no scientific
reference, and is validated only by structural tests.

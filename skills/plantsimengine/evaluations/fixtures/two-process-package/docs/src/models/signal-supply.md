# Signal supply

`ConstantSignal` is a pedagogical, non-calibrated constant-supply hypothesis.
It uses an arbitrary illustrative unit, has no scientific reference, and is
validated only by structural tests.

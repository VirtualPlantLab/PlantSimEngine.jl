var documenterSearchIndex = {"docs":
[{"category":"section","location":"guides/modelers/stateful_models/#State,-History,-And-Repeated-Updates","page":"Stateful models","text":"Object Status is current state, not timestep storage. Use PreviousTimeStep(:x) for a one-step lag, or keep a model-owned ring buffer when the algorithm requires deeper history. Accepted output streams provide simulation history.\n\nSeveral writers to one canonical variable are rejected unless the application declares Updates. Iterative trial execution belongs to calls; use publish=false until a state is accepted.","title":"State, History, And Repeated Updates"},{"category":"section","location":"introduction/why_plantsimengine/#Why-PlantSimEngine?","page":"Why PlantSimEngine ?","text":"PlantSimEngine was developed to address fundamental limitations in existing plant modeling tools. This framework emerged from the need for a system that could efficiently handle the complex dynamics of the soil-plant-atmosphere continuum while remaining accessible to researchers and practitioners from diverse disciplines.","title":"Why PlantSimEngine?"},{"category":"section","location":"introduction/why_plantsimengine/#The-Current-Landscape-of-Plant-Modeling","page":"Why PlantSimEngine ?","text":"Plant modeling has evolved significantly over the years, with different tools making different design tradeoffs to address specific research needs. These tools generally fall into three categories, each with their own strengths and limitations:","title":"The Current Landscape of Plant Modeling"},{"category":"section","location":"introduction/why_plantsimengine/#Monolithic-Systems","page":"Why PlantSimEngine ?","text":"Systems like APSIM[1], GroIMP[2], AMAPStudio[3], Helios[4], and CPlantBox[5] offer comprehensive functionality but present certain tradeoffs:\n\nThese systems provide robust, well-tested frameworks with established scientific validity, but their large, complex codebases can be challenging to navigate and modify without extensive programming expertise.\n\nTheir comprehensive architecture offers a wealth of integrated features but may require adaptation when implementing novel approaches that don't align with their predefined frameworks.\n\nThey excel at specific types of simulations but may require additional engineering effort for seamless multi-scale simulations and model coupling across the soil-plant-atmosphere continuum.\n\nThese platforms typically require dedicated engineering resources for maintenance and extension, with research teams often needing specialized technical staff to implement new models.","title":"Monolithic Systems"},{"category":"section","location":"introduction/why_plantsimengine/#Distributed-Systems","page":"Why PlantSimEngine ?","text":"Platforms like OpenAlea[6] and Crops in Silico[7] offer different advantages and tradeoffs:\n\nThese systems provide accessible interfaces (often in Python) that prioritize ease of use and flexibility, making them approachable for many researchers, though they may require performance optimization for large-scale simulations.\n\nTheir modular nature facilitates component reuse and integration, while sometimes requiring proficiency in multiple programming languages for extending computational backends.\n\nThey support diverse modeling paradigms but may involve a longer iteration cycle between design, implementation, and performance tuning compared to more specialized tools.\n\nWhile offering flexibility, implementing complex models often requires significant developer time, especially when optimizing performance using lower-level languages.","title":"Distributed Systems"},{"category":"section","location":"introduction/why_plantsimengine/#Architecture-Focused-Tools","page":"Why PlantSimEngine ?","text":"Tools like AMAPSim[8] make specific design choices that benefit certain applications:\n\nThese systems excel in their focused domains (such as structural modeling of plants) while requiring integration with other tools for comprehensive studies of plant physiology and environmental responses.\n\nTheir implementation in languages like C++ or Java delivers excellent performance but represents a tradeoff in terms of accessibility for researchers without expertise in these languages.\n\nThey provide sophisticated functionality in their target domains but may require additional work for rapid hypothesis testing and model prototyping across diverse aspects of plant science.","title":"Architecture-Focused Tools"},{"category":"section","location":"introduction/why_plantsimengine/#The-PlantSimEngine-Solution","page":"Why PlantSimEngine ?","text":"PlantSimEngine brings together innovative ideas to address these various tradeoffs, offering a unique combination of features:","title":"The PlantSimEngine Solution"},{"category":"section","location":"introduction/why_plantsimengine/#Automatic-Model-Coupling","page":"Why PlantSimEngine ?","text":"Seamless Integration: PlantSimEngine leverages Julia's multiple-dispatch capabilities to automatically compute the dependency graph between models. This allows researchers to effortlessly couple models without writing complex connection code or manually managing dependencies.\n\nIntuitive Multi-Scale Support: The framework naturally handles models operating at different scales—from organelle to ecosystem—connecting them with minimal effort and maintaining consistency across scales.","title":"Automatic Model Coupling"},{"category":"section","location":"introduction/why_plantsimengine/#Flexibility-with-Precision-Control","page":"Why PlantSimEngine ?","text":"Effortless Model Switching: Researchers can switch between different component models using a simple syntax without rewriting the underlying model code. This enables rapid comparison between different hypotheses and model versions, accelerating the scientific discovery process.\n\nFine-Grained Model Control: PlantSimEngine allows users to fix parameters, force variables to match observed values, or select simpler models for specific processes. This flexibility helps reduce overall system complexity while maintaining precision where it matters most.\n\nAdaptive Scalability: The same framework efficiently supports both simple prototypes for single-plant studies and complex ecosystem simulations, scaling computational resources appropriately to the problem at hand.","title":"Flexibility with Precision Control"},{"category":"section","location":"introduction/why_plantsimengine/#Outstanding-Performance","page":"Why PlantSimEngine ?","text":"High-Speed Computation: Benchmarks demonstrate operations completing in hundreds of nanoseconds, making PlantSimEngine suitable for computationally intensive applications. For example, the PlantBiophysics.jl implementation is over 38,000 times faster than equivalent implementations in R.\n\nComputational Efficiency: Julia's just-ahead-of-time compilation and native support for parallelism ensure that optimizations made during prototyping directly transfer to larger-scale applications, eliminating the need for reimplementation in a different language for performance gains.","title":"Outstanding Performance"},{"category":"section","location":"introduction/why_plantsimengine/#Developer-Efficiency","page":"Why PlantSimEngine ?","text":"Reduced Implementation Time: PlantSimEngine leverages Julia's dynamic language features while maintaining the performance of statically-compiled languages. This significantly reduces the time researchers spend implementing and optimizing models.\n\nModular Building Blocks: The component-based architecture allows models to be built as unit components that can be stacked like building blocks to create complex systems. This modularity dramatically increases code reuse and reduces redundant implementation efforts.\n\nNo Engineering Overhead: Unlike monolithic systems that require dedicated engineering teams or distributed platforms that need backend optimization, PlantSimEngine enables domain scientists to independently develop high-performance models without specialized programming expertise.\n\nRapid Prototyping to Production: The same code used for quick prototyping can transition directly to production-scale simulations without rewriting, eliminating the traditional gap between exploratory research and application.","title":"Developer Efficiency"},{"category":"section","location":"introduction/why_plantsimengine/#Key-Innovations","page":"Why PlantSimEngine ?","text":"PlantSimEngine's approach to plant modeling represents a paradigm shift in how scientists can build and use models:\n\nUniform API: Standardized interfaces make it easy to define new processes and component models, reducing the cognitive load on researchers.\nAutomatic Dependency Resolution: The system automatically determines the relationships between different models and processes, eliminating the need for manual coupling.\nConcrete Batched Execution: The model compiler groups compatible object targets into concrete execution batches. A public parallel or distributed executor is not currently provided; parallel execution remains planned work that requires explicit correctness and independence guarantees.\nFlexible Model Integration: The ability to easily combine models from different sources and at different scales facilitates more comprehensive and realistic simulations.\nUser-Centric Design: Emphasizing usability ensures that researchers with varied programming backgrounds can effectively engage with the system.\n\nBy offering solutions to the various tradeoffs present in existing modeling approaches, PlantSimEngine enables researchers to focus more on scientific questions and less on technical implementation details, accelerating the pace of discovery in plant science, agronomy, and related fields.\n\n[1]: Holzworth, D. P. et al. APSIM – Evolution towards a new generation of agricultural systems simulation. Environmental Modelling & Software 62, 327-350 (2014).\n\n[2]: Hemmerling, R., Kniemeyer, O., Lanwert, D., Kurth, W. & Buck-Sorlin, G. The rule-based language XL and the modelling environment GroIMP illustrated with simulated tree competition. Funct. Plant Biol. 35, 739 (2008).\n\n[3]: Griffon, S., and de Coligny, F. AMAPstudio: An editing and simulation software suite for plants architecture modelling. Ecological Modelling 290 (2014): 3‑10. https://doi.org/10.1016/j.ecolmodel.2013.10.037.\n\n[4]: Bailey, R. Spatial Modeling Environment for Enhancing Conifer Crown Management. Front. For. Glob. Change 3, 106 (2020).\n\n[5]: Schnepf, A., Leitner, D., Landl, M., Lobet, G., Mai, T. H., Morandage, S., Sheng, C., Zörner, M., Vanderborght, J., & Vereecken, H. CPlantBox: A whole-plant modelling framework for the simulation of water- and carbon-related processes. in silico Plants, 63 (2018).\n\n[6]: Pradal, C. et al. OpenAlea: A visual programming and component-based software platform for plant modeling. Funct. Plant Biol. 35, 751-760 (2008).\n\n[7]: Marshall-Colon, A. et al. Crops In Silico: Generating Virtual Crops Using an Integrative and Multi-Scale Modeling Platform. Frontiers in Plant Science 8 (2017). https://doi.org/10.3389/fpls.2017.00786.\n\n[8]: Barczi, J.-F., Rey, H., Caraglio, Y., Reffye, P. de, Barthélémy, D., Dong, Q. X., & Fourcaud, T. AmapSim: A Structural Whole-plant Simulator Based on Botanical Knowledge and Designed to Host External Functional Models. Annals of botany, 101(8), 1125-1138 (2008).","title":"Key Innovations"},{"category":"section","location":"step_by_step/implement_a_process/#Implementing-a-new-process","page":"Implement a process","text":"","title":"Implementing a new process"},{"category":"section","location":"step_by_step/implement_a_process/#Introduction","page":"Implement a process","text":"A process in this package defines a biological or physical phenomena. Think of any process happening in a system, such as light interception, photosynthesis, water, carbon and energy fluxes, growth, yield or even electricity produced by solar panels.\n\nPlantSimEngine.jl was designed to make the implementation of new processes and models easy and fast. The next section showcases how to implement a new process with a simple example: implementing a growth model.","title":"Introduction"},{"category":"section","location":"step_by_step/implement_a_process/#Implement-a-process","page":"Implement a process","text":"A process is \"declared\", meaning we define a process, and then implement models for its simulation. Declaring a process generates some boilerplate code for its simulation: \n\nan abstract type for the process\na method for the process function, that is used internally\n\nThe abstract process type is then used as a supertype of all models implementations for the process, and is named Abstract<process_name>Process, e.g. AbstractLight_InterceptionModel.\n\nFortunately, PlantSimEngine provides a macro to generate all that at once: @process. This macro takes only one argument: the name of the process.\n\nFor example, the photosynthesis process in PlantBiophysics.jl is declared using just this tiny line of code:\n\n@process \"photosynthesis\"\n\nIf we want to simulate the growth of a plant, we could add a new process called growth:\n\n@process \"growth\"\n\nAnd that's it! Note that the function guides you in the steps you can make after creating a process.","title":"Implement a process"},{"category":"section","location":"step_by_step/implement_a_process/#Implement-a-new-model-for-the-process","page":"Implement a process","text":"Once process implementation is done, you can write a corresponding model implementation. A tutorial page showcasing a light interception model implementation can be found here\n\nA full model implementation for this process is available in the example script ToyAssimGrowthModel.jl.","title":"Implement a new model for the process"},{"category":"section","location":"step_by_step/implement_a_process/#under_the_hood","page":"Implement a process","text":"The @process macro is just a shorthand reducing boilerplate.\n\nYou can in its stead directly define a process by hand by defining an abstract type that is a subtype of AbstractModel:\n\nabstract type AbstractGrowthModel <: PlantSimEngine.AbstractModel end\n\nAnd by adding a method for the process_ function that returns the name of the process:\n\nPlantSimEngine.process_(::Type{AbstractGrowthModel}) = :growth\n\nSo in the earlier example, a new process was created called growth. This defined a new abstract structure called AbstractGrowthModel, which is used as a supertype of the models. This abstract type is always named using the process name in title case (using titlecase()), prefixed with Abstract and suffixed with Model.","title":"Under the hood"},{"category":"section","location":"documentation_improvement/#Help-Improve-The-Documentation","page":"Help Improve The Documentation","text":"Good documentation is essential if PlantSimEngine is meant to stay usable for both new users and contributors.\n\nIf a page is unclear, incomplete, or out of date, the preferred way to report it is to open an issue or submit a pull request on GitHub.\n\nDocumentation fixes do not need to be large. Small corrections, clarified examples, and reports of missing release notes are all useful contributions.","title":"Help Improve The Documentation"},{"category":"section","location":"dev/distributed_output_ownership/#Distributed-output-ownership","page":"Distributed output ownership","text":"Status: implemented public and compiler contract.","title":"Distributed output ownership"},{"category":"section","location":"dev/distributed_output_ownership/#Problem","page":"Distributed output ownership","text":"Most PlantSimEngine applications compute outputs on the same objects on which they execute. Some scientifically useful models have a different shape:\n\none radiative-transfer model executes once on a complete scene and computes values for every simulated organ;\none plant allocation model executes once per plant and computes allocation and reserve values for many organs; or\none soil or microclimate model executes on a shared domain and computes local values for several plant objects.\n\nThese are not hard calls to per-object models. The scene or plant application owns the computation and cadence, while each destination object owns its local state value.\n\nPassing writable Many(...; from_status=true) carriers can modify those values, but it does not declare the producing application as their writer. Producer inference, scheduling, diagnostics, lifecycle refresh, and output retention would then lose the scientific ownership of the result.","title":"Problem"},{"category":"section","location":"dev/distributed_output_ownership/#Terms","page":"Distributed output ownership","text":"Execution target: the object on which an application runs. A scene light model has one Scene execution target.\nOutput destination: an object whose status owns one output computed by that application. The same scene light application can have many Leaf and Internode destinations.\nOutput binding: the compiled relationship between one application, execution target, destination selector, destination object IDs, and declared output variables.\nDestination ownership: the writer relationship (destination_object_id, variable) => application_id.\n\nExecution targets and output destinations are deliberately distinct. A model does not need fake per-organ applications merely to publish values computed elsewhere.","title":"Terms"},{"category":"section","location":"dev/distributed_output_ownership/#Public-declaration-and-scene-writer","page":"Distributed output ownership","text":"The scenario declares named destination groups with outputs_to:\n\nModelSpec(\n    SceneLightModel(solve_light);\n    name=:scene_light,\n    on=One(scale=:Scene),\n    outputs_to=(\n        organs=OutputTo(\n            Many(\n                scale=(:Leaf, :Internode),\n                within=SceneScope(),\n            );\n            vars=(\n                incident_par=Default(0.0),\n                absorbed_par=Default(0.0),\n                sky_fraction=Required(Float64),\n            ),\n        ),\n    ),\n)\n\nDefault(value) creates the destination status variable when needed. Required(T) requires it to exist on every selected destination. Only coverage=:exact, the default, is accepted.\n\nThe scene kernel looks up its compiled group and publishes an identified solver table:\n\nPlantSimEngine.@process \"scene light\" verbose = false\n\nstruct SceneLightModel{F} <: AbstractScene_LightModel\n    solve::F\nend\n\nPlantSimEngine.inputs_(::SceneLightModel) = NamedTuple()\nPlantSimEngine.outputs_(::SceneLightModel) = NamedTuple()\n\nfunction PlantSimEngine.run!(\n    model::SceneLightModel,\n    status,\n    environment,\n    constants,\n    context,\n)\n    targets = output_targets(context, :organs)\n    result = model.solve(\n        runtime_model(context),\n        environment,\n        object_ids(targets),\n    )\n    assign_outputs!(targets, result; id=:object_id)\n    return nothing\nend\n\nresult may be any Tables.jl-compatible row or column table. Its row order is independent of selector order because assignment uses ObjectId values.","title":"Public declaration and scene writer"},{"category":"section","location":"dev/distributed_output_ownership/#OutputTargets-runtime-surface","page":"Distributed output ownership","text":"output_targets(context, :organs) performs a typed lookup on the current RunContext; it does not resolve a selector or rebuild an identity index in the model call. The group name must be a Symbol declared by the current application.\n\nThe returned OutputTargets supports length, isempty, eachindex, and object_ids. Its destination carriers are available only through the explicit column namespace:\n\ntargets.columns.incident_par\ntargets.columns.absorbed_par\n\nOutput variables and compiled-binding metadata are deliberately not forwarded into the public property namespace: propertynames(targets) exposes only :columns. This keeps output names separate from implementation fields. An output named columns remains accessible as targets.columns.columns.\n\nobject_ids(targets) is an aligned, read-only identity carrier over the compiled destination IDs. Positions have no botanical meaning. Direct positional writes to targets.columns.<variable> are valid when the producing algorithm already uses this exact identity order; identified external results should use assign_outputs!.\n\nAn OutputTargets value belongs to the current model invocation and lifecycle generation. Kernels obtain it from RunContext on every call and do not store it on their model.","title":"OutputTargets runtime surface"},{"category":"section","location":"dev/distributed_output_ownership/#Identified-assignment","page":"Distributed output ownership","text":"The Tables.jl path is the general adapter:\n\nassign_outputs!(targets, result_table; id=:object_id)\n\nThe lower-level path accepts stable columns directly:\n\nassign_outputs!(targets, result_ids, result_columns)\n\nHere result_ids is an AbstractVector and result_columns is a NamedTuple. The lower-level overload avoids a Tables.jl adapter but uses the same validation, identity mapping, and assignment implementation.\n\nBoth forms require:\n\none result ID for every current destination;\nno unknown, duplicate, extra, or missing IDs;\nequal lengths for the ID and every declared result column; and\nevery variable declared by the OutputTo group.\n\nAdditional table or NamedTuple columns are metadata and are ignored. They can carry solver-facing values such as source_element, component_index, or geometry provenance without becoming status variables. Conversely, omitting a declared output is an error even when that destination status already has a value.\n\nNo subset or retain-last coverage mode exists. An adapter handles filtered, abscised, dead, or non-geometrized organs deliberately, or the destination selector excludes them.","title":"Identified assignment"},{"category":"section","location":"dev/distributed_output_ownership/#Identity-and-permutation-cache","page":"Distributed output ownership","text":"Each compiled output binding stores destination IDs, an ID-to-position index, and live reference-backed columns. The first assignment validates the result IDs and compiles either an exact-order marker or a row-to-destination permutation.\n\nThe runtime cache is keyed by object identity of the result ID column. Reusing the same ID-column object promises that its IDs and order are immutable; the cache does not rescan or hash its contents on every timestep. Value columns may be mutated and reused freely. Replace the ID-column object whenever its IDs or order changes.\n\nThis contract applies to both public overloads because the Tables.jl path extracts its ID column before entering the lower-level implementation. Reusing a table with the same ID carrier reuses the mapping; constructing a new ID carrier triggers validation and recompilation.","title":"Identity and permutation cache"},{"category":"section","location":"dev/distributed_output_ownership/#Atomic-validation-and-aliasing","page":"Distributed output ownership","text":"Coverage, column lengths, value conversions, and source/destination aliasing are checked before any destination status is changed. If mapping validation fails, the partially reused cache buffers are marked invalid so a later valid assignment recompiles cleanly.\n\nA result column may share destination storage only when all three conditions hold:\n\nthe result IDs are already in exact destination order;\nthe source is the same declared output column; and\nthe source and destination use the same complete mapping.\n\nPermuted views, partially overlapping views, cross-column aliases, and permuted self-assignment are rejected. Custom array wrappers that can share storage must implement Julia's Base.dataids and Base.mightalias contract so PlantSimEngine can detect that relationship before mutation.","title":"Atomic validation and aliasing"},{"category":"section","location":"dev/distributed_output_ownership/#Compilation,-ownership,-and-ordinary-consumers","page":"Distributed output ownership","text":"For every output group, compilation resolves:\n\nthe producing application and execution object;\nthe destination selector and current destination IDs;\ndeclared variables and their Required or Default initialization;\nconcrete reference-backed destination columns;\nthe destination ID index; and\nwriter ownership for every (destination_id, variable) pair.\n\nDestination status initialization occurs before consumer input compilation. Writer collisions are rejected unless an existing Updates(...; after=...) declaration establishes intentional ordering.\n\nA destination model consumes the value through its ordinary input contract:\n\nPlantSimEngine.inputs_(::LeafPhotosynthesis) = (\n    absorbed_par=Required(Float64),\n)\n\nModelSpec(\n    LeafPhotosynthesis();\n    name=:leaf_photosynthesis,\n    on=Many(scale=:Leaf),\n)\n\nProducer inference finds the scene application that owns (leaf_id, :absorbed_par), binds the leaf status field, and schedules the scene writer before the leaf consumer. The consumer does not use output_targets, from_status=true, a copy model, or an explicit after=:scene_light dependency.","title":"Compilation, ownership, and ordinary consumers"},{"category":"section","location":"dev/distributed_output_ownership/#Lifecycle","page":"Distributed output ownership","text":"The scenario application graph and selectors remain immutable while object membership may change. At a supported lifecycle barrier, PlantSimEngine:\n\nrebuilds affected destination memberships and reference columns;\nrebuilds the destination ID index;\nadvances the binding membership generation and invalidates result mappings;\nupdates writer ownership and consumer scheduling metadata;\ninitializes new destination status variables; and\nopens or closes retained streams as requested.\n\nAn empty destination group remains a typed OutputTargets group and can gain members after organ creation. The next model invocation receives the refreshed view and recompiles its result mapping. Ordinary timesteps do not resolve the selector or traverse the graph.","title":"Lifecycle"},{"category":"section","location":"dev/distributed_output_ownership/#Output-retention-and-diagnostics","page":"Distributed output ownership","text":"Retained streams are keyed by producing application, destination object, and variable. Distributed destinations change which object IDs are enumerated, not the scientific identity of the producer.\n\nUse the supported diagnostics rather than inspecting compiled fields:\n\nDiagnostics.explain_output_bindings shows execution objects, groups, destination IDs, column carrier types, coverage, and membership generation;\nDiagnostics.explain_writers shows the producing application for each destination variable;\nDiagnostics.explain_bindings and Diagnostics.explain_schedule show ordinary consumer coupling and execution order; and\nDiagnostics.explain_output_retention shows retained destination streams.\n\noutputs=:none does not allocate destination-history streams. Current values remain visible through final_state because assignment writes directly to destination statuses.","title":"Output retention and diagnostics"},{"category":"section","location":"dev/distributed_output_ownership/#Performance-characteristics","page":"Distributed output ownership","text":"The implementation keeps ordinary applications on their existing path. Applications without distributed outputs use the empty compiled marker and an empty output-target tuple; they do not resolve selectors or build assignment caches.\n\nFor distributed outputs:\n\ndestination selectors, IDs, indexes, ownership, and columns are compiled before execution;\nstable ID carriers reuse their exact-order marker or permutation;\nexact-order assignment avoids indexed destination lookup in the write loop;\nhomogeneous destination references produce typed RefVector{T} columns and a concrete recursive column loop;\nheterogeneous destination reference types fall back to ObjectRefVector, with conversion against each destination reference; and\nlifecycle changes rebuild only the affected compiled execution targets.\n\nAfter warm-up, the stable homogeneous exact-order and permuted columnar paths can execute without allocations. Heterogeneous destinations preserve correct per-object types but are an intentionally slower fallback. Benchmark compilation, lifecycle refresh, steady-state assignment, and output collection separately when changing these internals.","title":"Performance characteristics"},{"category":"section","location":"dev/distributed_output_ownership/#Alternatives-rejected","page":"Distributed output ownership","text":"","title":"Alternatives rejected"},{"category":"section","location":"dev/distributed_output_ownership/#A-second-Many-of-IDs","page":"Distributed output ownership","text":"This duplicates compiler-owned identity, lets selectors diverge, and treats identity as a biological input. It also does not solve writer ownership, scheduling, or retention.","title":"A second Many of IDs"},{"category":"section","location":"dev/distributed_output_ownership/#Fake-per-object-applications","page":"Distributed output ownership","text":"These misrepresent computation and cadence, enlarge the application graph, and make bookkeeping models appear scientifically meaningful.","title":"Fake per-object applications"},{"category":"section","location":"dev/distributed_output_ownership/#Public-CallTargets-reuse","page":"Distributed output ownership","text":"CallTargets represents executable callee applications and includes model, environment, status-view, and hard-call state. Distributed outputs need the lighter columnar OutputTargets view.","title":"Public CallTargets reuse"},{"category":"section","location":"dev/distributed_output_ownership/#Per-step-table-join","page":"Distributed output ownership","text":"A table join or MTG traversal in every model execution is avoidable work and makes ordering errors possible. PlantSimEngine compiles the identity mapping once per stable ID carrier and invalidates it when destination membership changes.","title":"Per-step table join"},{"category":"section","location":"guides/extensions/environment_backends/#Environment-Backend-Extensions","page":"Environment backend extensions","text":"This page is for package authors who connect a meteorology, canopy-layer, voxel, grid, or other spatial provider to PlantSimEngine. Simulation users normally configure an existing backend with Environment(...) and do not need this protocol.","title":"Environment Backend Extensions"},{"category":"section","location":"guides/extensions/environment_backends/#The-boundary","page":"Environment backend extensions","text":"An extension backend subtypes PlantSimEngine.EnvironmentAPI.AbstractEnvironmentBackend and extends functions in PlantSimEngine.EnvironmentAPI. Keep the concrete backend and handle types in the extension package. They do not belong in PlantSimEngine's root namespace.\n\nThe required methods are:\n\nbase_step_seconds(backend): duration of one base step;\nget_nsteps(backend): available number of steps;\nbind_environment(backend, object, context, config): compile one opaque handle for an application/object pair;\nsample(backend, handle, variable, time): read committed state.\n\nImplement environment_variables(backend) when the variable names can be enumerated cheaply. PlantSimEngine then validates model environment inputs while compiling.\n\nMutable or structurally indexed backends add only the capabilities they need:\n\nsample(backend, handle, trial_state, variable, time) for transient trial states supplied to run_call!;\ncommit_environment!(backend, handle, accepted_state, time) for accepted state;\nupdate_index!(backend, changed_entities, removed_object_ids) when topology or geometry changes require a spatial-index refresh. Initial compilation supplies all entities; later refreshes supply only the structural delta.\n\nToySpatialEnvironment is the small, tested implementation used by the user journeys.","title":"The boundary"},{"category":"section","location":"guides/extensions/environment_backends/#Compile-routing-into-the-handle","page":"Environment backend extensions","text":"bind_environment receives the target Object, an EnvironmentAPI.EnvironmentContext, and the payload configured with Environment(...). Resolve geometry, layer, voxel, provider, and commit sink there. Return a concrete handle containing everything later sampling and committing need.\n\nPlantSimEngine caches that handle. The hot sample methods receive it directly, so they should not search the object registry or repeat spatial routing. EnvironmentContext contains application id, object id, scale, and process; runtime status is deliberately absent.\n\nA controller that reads one provider and writes another can encode both routes in its handle. For example, the scenario may configure Environment(provider=:forcing, sink=:canopy), while the backend decides what those names mean.","title":"Compile routing into the handle"},{"category":"section","location":"guides/extensions/environment_backends/#Trial-and-commit-semantics","page":"Environment backend extensions","text":"Transient sampling and committing are separate backend operations:\n\na controller passes its typed trial state to run_call!(context, name; environment=trial_state, publish=false);\nPlantSimEngine invokes the transient sample overload through each target's already-compiled handle;\nthe controller accepts a state and calls commit_environment!(context, accepted_state);\nPlantSimEngine validates the caller's environment_outputs_ declaration, then invokes the backend commit through the caller's handle;\nthe controller publishes accepted called-model outputs explicitly with publish=true.\n\nThe model-facing commit_environment!(context, state) is root API. The backend-facing method belongs to PlantSimEngine.EnvironmentAPI; extension packages should qualify it when adding methods.","title":"Trial and commit semantics"},{"category":"section","location":"guides/extensions/environment_backends/#Refresh-and-diagnostics","page":"Environment backend extensions","text":"PlantSimEngine calls update_index! once per distinct spatial backend before binding or rebinding affected targets. Geometry changes invalidate the affected handles; structural changes can also change the set of bound targets.\n\nUse these public checks instead of reading compiler fields:\n\nvalidate_environment_inputs(model) checks declared variables;\nDiagnostics.explain_environment_bindings(model) reports each application/object handle and geometry source;\nDiagnostics.explain_environment(simulation) reports the active backend, variables, step count, and base-step duration.\n\nBackend tests should cover at least two objects with distinct handles, committed and transient sampling, rejected undeclared commits, and handle refresh after movement or geometry changes.","title":"Refresh and diagnostics"},{"category":"section","location":"guides/graph_visualizer_editor/#Visualize-And-Edit-A-CompositeModel","page":"Graph editor","text":"The CompositeModel graph shows how model applications, objects, and compiled value bindings fit together before a simulation runs. Use the static visualizer when you want an inspectable HTML artifact, and the interactive editor when you want browser actions to update a Julia CompositeModel.","title":"Visualize And Edit A CompositeModel"},{"category":"section","location":"guides/graph_visualizer_editor/#A-Small-CompositeModel","page":"Graph editor","text":"This example applies three toy models to one plant object. The compiler infers the same-object TT_cu and LAI bindings from the declared input and output names.\n\nusing PlantSimEngine\nusing PlantSimEngine.Examples\n\nmodel = CompositeModel(\n    ToyDegreeDaysCumulModel(),\n    ToyLAIModel(),\n    Beer(0.6);\n    status=(TT=12.0,),\n    id=:plant,\n    scale=:Plant,\n    kind=:plant,\n)\n\nview = GraphEditor.model_graph_view(model)\nview.metadata\n\nThe documentation build writes that graph as a self-contained HTML page and embeds it below.\n\n<iframe\n  id=\"pse-model-graph-example\"\n  title=\"Interactive PlantSimEngine CompositeModel graph example\"\n  style=\"width: 100%; height: 720px; border: 1px solid #d8cdbc; border-radius: 6px;\"\n  loading=\"lazy\">\n</iframe>\n<script>\n  document.getElementById(\"pse-model-graph-example\").src =\n    `${documenterBaseURL}/assets/model_graph_example.html`;\n</script>\n\nThe default Applications projection groups all concrete executions of one application into one card. Use Objects to inspect topology and Executions to inspect concrete (application, object) pairs. Search, diagnostics, initialization, selectors, parameters, and resolved edge details remain available in the static viewer. The topology projection includes model, template, and instance containers; selecting an instance or object subtree scopes the application and execution projections until the filter is cleared.","title":"A Small CompositeModel"},{"category":"section","location":"guides/graph_visualizer_editor/#Write-A-Static-Viewer","page":"Graph editor","text":"The static visualizer is part of PlantSimEngine core and does not load a web server:\n\npath = GraphEditor.write_model_graph_view(\"model-graph.html\", model)\n\nThe output bundles the graph payload, JavaScript, and CSS in one HTML file. It can be opened locally or embedded in Documenter documentation. A downstream package can generate the file from docs/make.jl and place it under docs/src/assets:\n\nmkpath(joinpath(@__DIR__, \"src\", \"assets\"))\nGraphEditor.write_model_graph_view(\n    joinpath(@__DIR__, \"src\", \"assets\", \"default_scene.html\"),\n    default_scene(),\n)\n\nThen embed it from a Markdown page with an HTML iframe. The graph is read-only, but its projection controls, search, inspector, and diagnostics are interactive in the browser.","title":"Write A Static Viewer"},{"category":"section","location":"guides/graph_visualizer_editor/#Start-The-Editor","page":"Graph editor","text":"The mutable editor is an optional package extension activated by HTTP.jl. Add HTTP once to the environment that will launch the editor:\n\nusing Pkg\nPkg.add(\"HTTP\")\n\nThen start a session:\n\nusing PlantSimEngine\nusing HTTP\n\nsession = GraphEditor.edit_graph(model)\n\nThe default browser opens automatically. The returned session also prints its URL and shutdown command. Julia remains authoritative: browser edits are sent as semantic commands, applied transactionally to a candidate CompositeModel, compiled, and returned as a fresh graph state.\n\nInspect the current result or stop the server with:\n\nedited_scene = GraphEditor.current_model(session)\nclose(session)\n\nCall GraphEditor.edit_graph() without a CompositeModel to start from an empty scenario. Use open_browser=false on remote machines or when a test controls the browser.","title":"Start The Editor"},{"category":"section","location":"guides/graph_visualizer_editor/#Templates-And-Several-Plants","page":"Graph editor","text":"A template is a reusable set of already coupled applications. Mounting the same template twice creates two instance-local application sets. Unqualified selectors remain inside their own plant, so a model in plant_a does not accidentally read values from plant_b.\n\nusing Dates\n\nplant_template = CompositeModelTemplate((\n    ModelSpec(\n        ToyDegreeDaysCumulModel();\n        name=:degree_days,\n        on=Many(scale=:Plant),\n        every=Hour(1),\n    ),\n    ModelSpec(\n        ToyLAIModel();\n        name=:leaf_area,\n        on=Many(scale=:Plant),\n    ),\n); kind=:plant, species=:oil_palm)\n\nplant_a = ObjectInstance(\n    :plant_a,\n    plant_template;\n    root=Object(:plant_a; name=:plant_a, scale=:Plant),\n)\nplant_b = ObjectInstance(\n    :plant_b,\n    plant_template;\n    root=Object(:plant_b; name=:plant_b, scale=:Plant),\n)\n\nmodel = CompositeModel(plant_a, plant_b)\nsession = GraphEditor.edit_graph(\n    model;\n    templates=(oil_palm=plant_template,),\n)\n\nThe Add instance wizard can mount a catalog template on an existing unclaimed root and its descendants, or create a minimal root and mount the template in one transaction. Preview the claimed subtree and resolved application targets before committing. Unmounting removes the applications but keeps the object subtree.\n\nCatalog templates are presets. The first edit to a mounted preset creates a model-local replacement shared by all instances that currently use it. The original preset remains available when adding another instance. Template application names are fixed because they are part of the template contract.","title":"Templates And Several Plants"},{"category":"section","location":"guides/graph_visualizer_editor/#Overrides","page":"Graph editor","text":"Use an override when one plant or organ needs a different parameterization without changing the shared template:\n\nplant_b = ObjectInstance(\n    :plant_b,\n    plant_template;\n    root=Object(:plant_b; name=:plant_b, scale=:Plant),\n    overrides=(\n        degree_days=ToyDegreeDaysCumulModel(T_base=12.0),\n    ),\n)\n\nThe editor offers the same operation at instance or object scope. Julia checks that the replacement implements the same process and variable contract.","title":"Overrides"},{"category":"section","location":"guides/graph_visualizer_editor/#Environment-Catalogs-And-Routing","page":"Graph editor","text":"Environment values remain in Julia. Give the editor a named catalog rather than serializing backends to the browser:\n\nsession = GraphEditor.edit_graph(\n    model;\n    templates=(oil_palm=plant_template,),\n    environments=(\n        weather=weather,\n        canopy=canopy_backend,\n    ),\n)\n\nThe scene environment can be replaced from this catalog. Each application can use the scene backend or a catalog backend and can configure provider, model-facing input-to-source mappings, sink, and backend-specific typed options. The editor shows environment_hint(model) and the effective compiled bindings read-only, then asks Julia to validate the candidate routing before it is committed.\n\nApplication cadence and temporal windows use Dates.Second, Dates.Minute, Dates.Hour, or Dates.Day. Whole-scene targeting is also explicit: SceneScope() must be selected deliberately. Omitting the scope keeps a template application local to each mounted instance.","title":"Environment Catalogs And Routing"},{"category":"section","location":"guides/graph_visualizer_editor/#What-Can-Be-Edited","page":"Graph editor","text":"The editor supports:\n\nmodel objects, metadata, status initialization, and parent topology;\nmodel applications, constructor parameters, target selectors, and cadence;\nexplicit value bindings, hard calls, output routing, and update ordering;\ntemplate catalogs, transactional instance mounting, shared template edits, and overrides;\nnamed scene and application environment backends, providers, sources, and sinks;\ndependency cycles through an explicit PreviousTimeStep break action;\nundo, redo, temporary recovery autosaves, and readable Julia CompositeModel scripts.\n\nApplication target and binding dialogs can ask Julia to preview the concrete objects selected by a declaration. This is important for Many, relative scopes such as SelfPlant, and composite models containing several plant instances.","title":"What Can Be Edited"},{"category":"section","location":"guides/graph_visualizer_editor/#Models-From-Other-Packages","page":"Graph editor","text":"The model browser reflects concrete AbstractModel subtypes currently loaded in Julia. There is no separate registration API. Loading a model package before starting the editor makes its models available automatically:\n\nusing PlantSimEngine\nusing PlantBiophysics\nusing HTTP\n\nsession = GraphEditor.edit_graph(model)\n\nThe + buttons next to ports use exact declared variable names only. For an input named LAI, the editor lists loaded models whose outputs_ contains LAI. For an output named LAI, it lists models whose inputs_ contains LAI, as well as compatible applications already present in the CompositeModel. This is a composition aid, not a scientific compatibility inference.\n\nWhen the CompositeModel is saved as Julia code, required package imports are emitted for the model types used by the CompositeModel.","title":"Models From Other Packages"},{"category":"section","location":"guides/graph_visualizer_editor/#Invalid-And-Cyclic-Composite-Models","page":"Graph editor","text":"Simulation compilation remains strict, but graph compilation preserves as much structure as possible and attaches diagnostics. This lets the editor display incomplete selectors, missing initialization, ambiguous writers, and cycles.\n\nCycle edges are shown in red. The break workflow asks which consumer input should read its previous accepted timestep value and asks for initial values when the affected target objects do not already provide one. The action changes the application input policy for every target selected by that application.\n\nwarning: Warning\nPreviousTimeStep changes model semantics. It disconnects the selected input from current-step producers during one run step. Use it only when that lag and its initialization value are scientifically intentional.","title":"Invalid And Cyclic Composite Models"},{"category":"section","location":"guides/graph_visualizer_editor/#Saving-And-Recovery","page":"Graph editor","text":"The Save action writes readable Julia code whose final binding is model = CompositeModel(...). Once a path is selected, every successful edit rewrites that file. The editor also keeps a temporary recovery file and lists recent CompositeModel scripts in Open.\n\nGenerated code is best effort for arbitrary Julia values and external runtime resources. Templates and instances are written inline. Named environment values are referenced through an editor_environments named tuple, and the generated header lists the keys that must be supplied when reopening the file:\n\nsession = GraphEditor.edit_graph(\n    ;\n    recover_path=\"model.jl\",\n    environments=(weather=weather, canopy=canopy_backend),\n)\n\nMissing environment keys fail while the file is opened. Review the generated code and keep important scenario scripts under Git.","title":"Saving And Recovery"},{"category":"section","location":"guides/data/environment_inputs/#Weather-And-Environment-Inputs","page":"Weather And Environment Inputs","text":"An environment may be a constant named tuple, one tabular row, or regular multi-row weather. Every row in a timed sequence needs a positive fixed duration; inconsistent base durations and application substeps are rejected.\n\nUse Environment(sources=...) to map model-facing environment names to provider columns. Values retain compatible user numeric types. Spatial providers implement the same model-facing contract and refresh bindings after object movement or geometry changes.","title":"Weather And Environment Inputs"},{"category":"section","location":"guides/time/hourly_daily_weekly/#Hourly,-Daily,-And-Weekly-Models","page":"Hourly, daily, and weekly","text":"Use one hourly leaf application, a daily plant application with Many(scale=:Leaf, within=Subtree(), policy=Integrate(), window=Day(1)), and a weekly application consuming the daily stream. Each application remains a normal ModelSpec; only its every value and input policy differ.\n\nRuntime dependency streams are retained because consumers need them. Output resampling is independent: create named OutputRequests for hourly, daily, and weekly analysis, then compare Diagnostics.explain_schedule sample counts with rows from collect_outputs.\n\nThe following reduced example checks the important physical contract: two leaf rates are integrated independently for 24 hourly samples and then summed on their plant.\n\nusing Dates\nusing PlantSimEngine\n\nPlantSimEngine.@process \"docs_hourly_flux\" verbose = false\nPlantSimEngine.@process \"docs_daily_total\" verbose = false\nstruct DocsHourlyFlux <: AbstractDocs_Hourly_FluxModel end\nstruct DocsDailyTotal <: AbstractDocs_Daily_TotalModel end\nPlantSimEngine.inputs_(::DocsHourlyFlux) = (rate=Required(Float64),)\nPlantSimEngine.outputs_(::DocsHourlyFlux) = (flux=0.0,)\nPlantSimEngine.run!(::DocsHourlyFlux, status, environment, constants, context) =\n    (status.flux = status.rate)\nPlantSimEngine.inputs_(::DocsDailyTotal) = (fluxes=Required(Vector{Float64}),)\nPlantSimEngine.outputs_(::DocsDailyTotal) = (total=0.0,)\nPlantSimEngine.run!(::DocsDailyTotal, status, environment, constants, context) =\n    (status.total = sum(status.fluxes))\n\nmodel = CompositeModel(\n    Object(:plant; scale=:Plant),\n    Object(:leaf_1; scale=:Leaf, parent=:plant, status=Status(rate=1.0)),\n    Object(:leaf_2; scale=:Leaf, parent=:plant, status=Status(rate=2.0));\n    applications=(\n        ModelSpec(DocsHourlyFlux(); name=:hourly, on=Many(scale=:Leaf), every=Hour(1)),\n        ModelSpec(\n            DocsDailyTotal();\n            name=:daily,\n            on=One(scale=:Plant),\n            inputs=(\n                :fluxes => Many(\n                scale=:Leaf, within=Subtree(), application=:hourly, var=:flux,\n                policy=Integrate(), window=Day(1),\n                ),\n            ),\n            every=Day(1),\n        ),\n    ),\n    environment=[(duration=Hour(1),) for _ in 1:25],\n)\nsimulation = run!(model; steps=25)\n@assert only(object.status.total for object in model_objects(model)\n             if object.id == ObjectId(:plant)) == 72.0\n\nA weekly consumer uses the same pattern with every=Week(1) and a seven-day window over the daily application. Keep Integrate for rates; choose Aggregate(reducer) for states or observations whose physical meaning is a mean, minimum, maximum, or custom statistic.","title":"Hourly, Daily, And Weekly Models"},{"category":"section","location":"step_by_step/implement_a_model/#model_implementation_page","page":"Implement a model","text":"For your own simulations, you might want to move beyond simple usage at some point and implement your own models. In this page, we'll go through the required steps for writing a new model. The detailed version is tailored for people less familiar with programming.","title":"Implementing a model"},{"category":"section","location":"step_by_step/implement_a_model/#Quick-version","page":"Implement a model","text":"Declare a new process : \n\n@process \"light_interception\" verbose = false\n\nDeclare your model struct, and its parameters : \n\nstruct Beer{T} <: AbstractLight_InterceptionModel\n    k::T\nend\n\nDeclare the inputs_ and outputs_ methods for that model (note the '_', these methods are distinct from inputs and outputs)\n\nfunction PlantSimEngine.inputs_(::Beer)\n    (LAI=Required(Float64),)\nend\n\nfunction PlantSimEngine.outputs_(::Beer)\n    (aPPFD=0.0,)\nend\n\nWrite the run! function that operates on a single timestep : \n\nfunction PlantSimEngine.run!(model::Beer, status, environment, constants, context)\n    status.aPPFD =\n        environment.Ri_PAR_f *\n        exp(-model.k * status.LAI) *\n        constants.J_to_umol\n    return nothing\nend\n\nAnd that is all you need to get going, for this example with a single parameter and no interdependencies. \n\nThe @process macro does some boilerplate work described here\n\nSome context utility functions can also be interesting to implement to make users' lives simpler. See the Model implementation additional notes page for details. If your custom model needs to handle more complex couplings than the simple input/output described in this example, check out the Coupling more complex models page.","title":"Quick version"},{"category":"section","location":"step_by_step/implement_a_model/#Detailed-version","page":"Implement a model","text":"PlantSimEngine.jl was designed to make new model implementation very simple. So let's learn about how to implement your own model with a simple example: implementing a new light interception model.\n\nThe model we'll (re)implement is available as an example model from the Examples sub-module. You can access the script from here: examples/Beer.jl. It is also available in the PlantBioPhysics.jl package.\n\nYou can import the model and PlantSimEngine's other example models into your environment with using:\n\n# Import the example models defined in the `Examples` sub-module:\nusing PlantSimEngine.Examples","title":"Detailed version"},{"category":"section","location":"step_by_step/implement_a_model/#Other-examples","page":"Implement a model","text":"PlantSimEngine's other toy models can be found in the examples folder.\n\nFor other examples, you can look at the code in PlantBiophysics.jl, where you will find e.g. a photosynthesis model, with the implementation of the FvCB model in src/photosynthesis/FvCB.jl; an energy balance model with the implementation of the Monteith model in src/energy/Monteith.jl; or a stomatal conductance model in src/conductances/stomatal/medlyn.jl.","title":"Other examples"},{"category":"section","location":"step_by_step/implement_a_model/#Requirements","page":"Implement a model","text":"If you have a look at example models, you'll see that in order to implement a new model you'll need to implement:\n\na structure, used to hold the parameter values and to dispatch to the right method\nthe actual model, developed as a method for the process it simulates\nsome helper functions used by the package and/or the users","title":"Requirements"},{"category":"section","location":"step_by_step/implement_a_model/#Example:-the-Beer-Lambert-model","page":"Implement a model","text":"","title":"Example: the Beer-Lambert model"},{"category":"section","location":"step_by_step/implement_a_model/#The-process","page":"Implement a model","text":"We start by declaring the light interception process at l.7 using @process: \n\n@process \"light_interception\" verbose = false\n\nSee Implementing a new process for more details on how that works and how to use the process.","title":"The process"},{"category":"section","location":"step_by_step/implement_a_model/#The-structure","page":"Implement a model","text":"To implement a model, the first thing to do is to define a structure. The purpose of this structure is two-fold:\n\nhold the parameter values\ndispatch to the right run! method when calling it\n\nThe structure of the model (or type) is defined as follows:\n\nstruct Beer{T} <: AbstractLight_InterceptionModel\n    k::T\nend\n\nThe first line defines the name of the model (Beer). It is good practice to use camel case for the name, i.e. using capital letters for the words and no separator LikeThis. \n\nThe Beer structure is defined as a subtype of AbstractLight_InterceptionModel indicating what kind of process the model simulates. The AbstractLight_InterceptionModel type is automatically created when defining the process \"light_interception\".\n\nWe can therefore infer from the declaration that Beer is a model to simulate the light interception process.\n\nThen come the parameters names, and their types. ","title":"The structure"},{"category":"section","location":"step_by_step/implement_a_model/#User-types-and-parametric-types","page":"Implement a model","text":"There is a little Julia specificity here, to enable the user to pass their own types to the simulation.\n\nBeer is a parameterized struct, indicated by the {T} annotation\nWe indicate the k parameter is of type T by adding ::T after the name.\n\nThe T is an arbitrary letter here. If you have parameters that you know will be of different types, you can either force their type, or make them parameterizable too, using another letter, e.g.:\n\nstruct CustomModel{T,S} <: AbstractLight_InterceptionModel\n    k::T\n    x::T\n    y::T\n    z::S\nend\n\nParameterized types are practical because they let the user choose the type of the parameters, and potentially change them at runtime. For example a user could use the Particles type from MonteCarloMeasurements.jl for automatic uncertainty propagation throughout the simulation. We refer you to the Parametric types subsection of the Model implementation additional notes page for more information on parametric types.","title":"User types and parametric types"},{"category":"section","location":"step_by_step/implement_a_model/#Inputs-and-outputs","page":"Implement a model","text":"When implementing a new model, it is necessary to declare what variables it reads and what variables it computes. Every input declaration must say whether the input is required or has a genuine model default. A required input is initialized by the user in a Status object or bound from another model. A defaulted input needs neither. Output variables may be retained as simulation outputs and/or used by other models.\n\nIn our case, the Beer model, computing light interception, has one input variable and one output variable:\n\nInputs: :LAI, the leaf area index (m² m⁻²)\nOutputs: :aPPFD, the photosynthetic photon flux density (μmol m⁻² s⁻¹)\n\nWe declare these inputs/outputs by adding methods for the underscore extension functions. inputs_ returns a NamedTuple whose values are Required(T) or Default(value) declarations. outputs_ returns a NamedTuple whose values are the initial output state:\n\nfunction PlantSimEngine.inputs_(::Beer)\n    (LAI=Required(Float64),)\nend\n\nfunction PlantSimEngine.outputs_(::Beer)\n    (aPPFD=0.0,)\nend\n\nLAI has no scientifically meaningful fallback, so it is required. If the model instead had an optional efficiency of 0.8, it would declare efficiency=Default(0.8). Do not use sentinel values such as -Inf to mean \"required\": they are ordinary values and hide the model contract.\n\nRequired(Float64) is an expected type, not an initialization value. A model that supports a broader or parameterized type can declare that type instead. PlantSimEngine does not convert status values to Float64.\n\nThese extension functions end with an \"_\". Simulation users instead use inputs, outputs, init_variables, and Diagnostics.explain_initialization to inspect the contract.","title":"Inputs and outputs"},{"category":"section","location":"step_by_step/implement_a_model/#The-run!-method","page":"Implement a model","text":"When running a simulation with run!, each model is run at its scheduled timestep, following the dependency order compiled from model applications, inputs, and manual calls. Each model has its own run! method for updating the current state. The function takes five arguments:\n\nfunction PlantSimEngine.run!(model::Beer, status, environment, constants, context)\n\nmodel: the current model instance, used for dispatch and parameter access.\nstatus: a Status object, which contains the current values (i.e. state) of the variables for one time-step (e.g. the value of the plant LAI at time t)\nenvironment: the sampled model-facing environment for the current target and timestep.\nconstants: a Constants object, or a NamedTuple, which contains the values of the constants for the simulation (e.g. the value of the Stefan-Boltzmann constant, unit-conversion constants...)\ncontext: PlantSimEngine's runtime context for hard calls and lifecycle operations.\n\nA typical run! function can therefore use simulation constants, input/output variables accessible through the Status object, or weather data.\n\nHere is the run! implementation of the light interception model. Note that the input and output variables are accessed through the status argument:\n\nfunction PlantSimEngine.run!(model::Beer, status, environment, constants, context)\n    status.aPPFD =\n        environment.Ri_PAR_f *\n        exp(-model.k * status.LAI) *\n        constants.J_to_umol\n    return nothing\nend","title":"The run! method"},{"category":"section","location":"step_by_step/implement_a_model/#Additional-notes","page":"Implement a model","text":"To use this model, simulation users must supply or bind every Required status input. Inputs declared with Default are initialized automatically. Required environment variables and constants must also be available through their respective contracts.\n\n!!! Note     Status objects contain the current state of the simulation. It is not, by default, possible to make use of earlier variable states, unless a custom model is written for that purpose.\n\nModel parameters are read directly from the current model instance. For example, the k parameter of the Beer model is model.k.\n\nwarning: Warning\nPrefix functions you extend with PlantSimEngine., or import them first, for example import PlantSimEngine: inputs_, outputs_. Otherwise Julia defines an unrelated function in your module instead of adding a method to PlantSimEngine's function.\n\nOK that's it! We now a full new model implementation for the light interception process! Other models might be more complex in terms of what computations they do, or how they couple with other models, but the approach remains the same.","title":"Additional notes"},{"category":"section","location":"step_by_step/implement_a_model/#Dependencies","page":"Implement a model","text":"If your model explicitly calls another model, you need to tell PlantSimEngine about it. This is called a hard dependency, in opposition to a soft dependency, which is when your model uses a variable from another model, but does not call it explicitly.\n\nTo do so, we can add a method to the dep function that tells PlantSimEngine which processes (and models) are needed for the model to run.\n\nOur example model does not call another model, so we don't need to implement it. But we can look at e.g. the implementation for Fvcb in PlantBiophysics.jl to see how it works:\n\nPlantSimEngine.dep(::Fvcb) = (stomatal_conductance=AbstractStomatal_ConductanceModel,)\n\nHere we say to PlantSimEngine that the Fvcb model needs a model of type AbstractStomatal_ConductanceModel in the stomatal conductance process.\n\nThis is intentionally process-based because dep(model) is a model-author contract. The model author cannot know which application name a future scenario will choose for stomatal conductance. In a concrete scenario, users should wire the selected producer or callee with application=... in ModelSpec(...; inputs=...) or ModelSpec(...; calls=...) when that application is known:\n\nModelSpec(ParentModel(); name=:parent, calls=(:stomata => One(scale=:Leaf, application=:stomatal_conductance)))\n\nYou can read more about hard dependencies in Coupling more complex models.","title":"Dependencies"},{"category":"section","location":"step_by_step/quick_and_dirty_examples/#Quick-Examples","page":"Quick Examples","text":"This page is for copy-paste experimentation with the native composite-model/object API. If you want a slower explanation of the same ideas, see Detailed Walkthrough Of A Simple Simulation.\n\nThe examples use one model object, but the same pattern scales to plants, organs, soil objects, and microclimate grids by adding more Objects and selecting them with ModelSpec(...; on=...) and ModelSpec(...; inputs=...).\n\nPages = [\"quick_and_dirty_examples.md\"]\nDepth = 2","title":"Quick Examples"},{"category":"section","location":"step_by_step/quick_and_dirty_examples/#One-Light-Interception-Model","page":"Quick Examples","text":"model = CompositeModel(\n    Beer(0.5);\n    status=(LAI=2.0,),\n    environment=meteo_day,\n)\n\nsim = run!(model; steps=3, outputs=:all)\nfirst(collect_outputs(sim), 3)","title":"One Light Interception Model"},{"category":"section","location":"step_by_step/quick_and_dirty_examples/#LAI-And-Light-Interception","page":"Quick Examples","text":"Here, ToyDegreeDaysCumulModel computes cumulative thermal time, ToyLAIModel computes LAI, and Beer consumes LAI. The compiler infers the same-object value bindings from model inputs and outputs.\n\nlai_scene = CompositeModel(\n    ToyDegreeDaysCumulModel(),\n    ToyLAIModel(),\n    Beer(0.5);\n    environment=meteo_day,\n)\n\nlai_sim = run!(lai_scene; steps=5, outputs=:all)\nfirst(collect_outputs(lai_sim), 8)\n\nInspect the inferred coupling:\n\nselect(\n    DataFrame(Diagnostics.explain_bindings(lai_scene)),\n    :application_id,\n    :input,\n    :source_application_ids,\n    :carrier_kind,\n)","title":"LAI And Light Interception"},{"category":"section","location":"step_by_step/quick_and_dirty_examples/#Add-Biomass-Growth","page":"Quick Examples","text":"ToyRUEGrowthModel consumes absorbed light and accumulates biomass. No extra input binding is needed because Beer is the unique producer of aPPFD on the same object.\n\ngrowth_scene = CompositeModel(\n    ToyDegreeDaysCumulModel(),\n    ToyLAIModel(),\n    Beer(0.5),\n    ToyRUEGrowthModel(0.2);\n    environment=meteo_day,\n)\n\ngrowth_sim = run!(growth_scene; steps=5)\ngrowth_status = final_state(growth_sim)\n(LAI=growth_status.LAI, aPPFD=growth_status.aPPFD, biomass=growth_status.biomass)","title":"Add Biomass Growth"},{"category":"section","location":"step_by_step/quick_and_dirty_examples/#Keep-Only-One-Requested-Output","page":"Quick Examples","text":"For larger simulations, request only the streams you want to keep:\n\nrequest = OutputRequest(\n    :Scene,\n    :biomass;\n    name=:biomass_daily,\n    application=:growth,\n    policy=HoldLast(),\n    clock=Day(1),\n)\n\nrequested_sim = run!(\n    growth_scene;\n    steps=5,\n    outputs=request,\n)\n\nfirst(collect_outputs(requested_sim, :biomass_daily), 5)","title":"Keep Only One Requested Output"},{"category":"section","location":"step_by_step/quick_and_dirty_examples/#PlantBiophysics","page":"Quick Examples","text":"The same composite-model/object API can host models from companion packages such as PlantBiophysics. A typical PlantBiophysics energy-balance setup uses ModelSpec(...; calls=...) so an iterative parent model can manually run photosynthesis and stomatal-conductance models, then call run_call!(target; publish=true) once for the accepted solution.\n\nSee MAESPA-style model example handoff for the current multi-plant energy-balance acceptance example.","title":"PlantBiophysics"},{"category":"section","location":"troubleshooting/dependency_cycles/#Diagnosing-Dependency-Cycles","page":"Dependency cycles","text":"A same-step value cycle is rejected because no valid execution order exists. Read the reported application, object, and variable edges. If the science uses yesterday's value, put PreviousTimeStep(:variable) on that input. If the science requires convergence in the current step, make one parent application own child trials with calls. Otherwise reformulate the coupled equations.\n\nApplication declaration order is not a cycle-resolution mechanism.\n\nFor example, if application :leaf reads same-step water from :root while :root reads same-step carbon from :leaf, compilation fails before either kernel runs. If root water scientifically affects tomorrow's leaf carbon, change only that edge:\n\nModelSpec(\n    LeafModel();\n    inputs=(\n        PreviousTimeStep(:water) =>\n            One(scale=:Root, application=:root, var=:water),\n    ),\n)\n\nThe receiving object's initial water value is used until the first accepted historical sample exists. If both values must converge within the same step, do not add a lag: make a parent model own calls to the two trial models, iterate with publish=false, and publish each accepted state once.","title":"Diagnosing Dependency Cycles"},{"category":"section","location":"step_by_step/implement_a_model_additional/#Model-implementation-additional-notes","page":"Additional notes","text":"Pages = [\"implement_a_model_additional.md\"]\nDepth = 3","title":"Model implementation additional notes"},{"category":"section","location":"step_by_step/implement_a_model_additional/#Parametric-types","page":"Additional notes","text":"In Implementing a model, the Beer model's structure was declared with a parametric type.\n\nstruct Beer{T} <: AbstractLight_InterceptionModel\n    k::T\nend\n\nWhy not force the type ? Float64 is more accurate than Float32, after all:\n\nstruct YourStruct <: AbstractLight_InterceptionModel\n    k::Float64\n    x::Float64\n    y::Float64\n    z::Int\nend\n\nDoing so would lose some flexibility in the way users can make use of your models. For example a user could use the Particles type from MonteCarloMeasurements.jl for automatic uncertainty propagation, and this is only possible if the model type is parameterizable. Forcing a Float64 type would render the model incompatible with Particles.","title":"Parametric types"},{"category":"section","location":"step_by_step/implement_a_model_additional/#Type-promotion","page":"Additional notes","text":"When implementing a new model, you can do a little optional extra work to help future users.\n\nYou can add a method for type promotion. It wouldn't make any sense for the previous Beer example because we have only one parameter. But we can make another example with a new model that would be called Beer2 that would take two parameters:\n\nstruct Beer2{T} <: AbstractLight_InterceptionModel\n    k::T\n    x::T\nend\n\nTo add type promotion to Beer2 we would do:\n\nfunction Beer2(k,x)\n    Beer2(promote(k,x)...)\nend\n\nnote: Note\npromote returns a NamedTuple, which needs to be splatted for the constructor, see the Julia docs for a more in-depth explanation, or our Getting started with Julia page for some links to other references discussing Julia concepts used in PlantSimEngine.\n\nThis would allow users to instantiate the model parameters using different types of inputs. For example users may write the following:\n\nBeer2(0.6,2)\n\nBeer2 is a parametric type, with all fields sharing the same type T. This is the T in Beer2{T} and then in k::T and x::T. And this forces the user to give all parameters with the same type.\n\nAnd in the example above, providin 0.6 for k, which is a Float64, and 2 for x, which is an Int. If you don't have type promotion, Julia will return an error because both should be either Float64 or Int. That's were type promotion comes in handy, as it will convert all your inputs to a common type (when possible). In our case it will convert 2 to 2.0.","title":"Type promotion"},{"category":"section","location":"step_by_step/implement_a_model_additional/#Other-helper-functions-and-constructors","page":"Additional notes","text":"","title":"Other helper functions and constructors"},{"category":"section","location":"step_by_step/implement_a_model_additional/#Default-parameter-values","page":"Additional notes","text":"You can simplify model usage by helping your user with default values for some parameters (if applicable). For example, in the Beer model a user will almost never change the value of k. So we can provide a default value like so:\n\nBeer() = Beer(0.6)\n\nNow the user can call Beer with no arguments, and k will default to 0.6.","title":"Default parameter values"},{"category":"section","location":"step_by_step/implement_a_model_additional/#Parameter-values-as-kwargs","page":"Additional notes","text":"Another useful thing is the ability to instantiate your model type with keyword arguments, i.e. naming the arguments. You can do it by adding the following method:\n\nBeer(;k) = Beer(k)\n\nThe ; syntax indicates that subsequent arguments are provided as keyword arguments, so now we can call Beer like this:\n\nBeer(k = 0.7)\n\nThis helps readability when there are a lot of parameters and some have default values.","title":"Parameter values as kwargs"},{"category":"section","location":"step_by_step/implement_a_model_additional/#eltype","page":"Additional notes","text":"The last optional utility function to implement is a method for the eltype function:\n\nBase.eltype(x::Beer{T}) where {T} = T\n\nThis one helps Julia know the type of the elements in the structure, and make it faster.","title":"eltype"},{"category":"section","location":"API/public_symbols/#Public-Symbol-Inventory","page":"Public symbol inventory","text":"This page records the supported default namespace and the four focused public submodules. Compiler representations and cache controls are intentionally listed separately under PlantSimEngine.Advanced.\n\nusing PlantSimEngine imports the ordinary model-author and simulation-user workflow plus the Diagnostics, GraphEditor, EnvironmentAPI, and Evaluation module names. Their members remain qualified unless a user explicitly imports one of those submodules.","title":"Public Symbol Inventory"},{"category":"section","location":"API/public_symbols/#Scenario-composition","page":"Public symbol inventory","text":"CompositeModel structure: CompositeModel, Object, ObjectId, CompositeModelTemplate, ObjectInstance, Override.\nApplications: ModelSpec, OutputTo, Environment, and Updates.\nApplication inspection: application_name, applies_to, value_inputs, model_calls, outputs_to, environment_config, output_routing, updates.\nDependency defaults: Input, Call, Initializer, PreviousTimeStep.","title":"Scenario composition"},{"category":"section","location":"API/public_symbols/#Object-selectors-and-queries","page":"Public symbol inventory","text":"Multiplicity: One, OptionalOne, Many.\nScope and topology: SceneScope, Self, Subtree, SelfPlant, Ancestor, Scope, Relation.\nLabel criteria are selector keywords: kind, species, scale, and name.\nIdentity and queries: object_id, object_ids, model_objects, resolve_object_ids, resolve_objects.\nObject data: geometry, position, bounds.","title":"Object selectors and queries"},{"category":"section","location":"API/public_symbols/#Execution,-lifecycle,-and-outputs","page":"Public symbol inventory","text":"Execution: run!, continue!, step!, Simulation, current_step, runtime_model, final_state.\nOutput selection and collection: OutputRequest, outputs, collect_outputs.\nDistributed output assignment: OutputTargets, output_targets, and assign_outputs!. Access destination carriers through targets.columns.<variable> and aligned identities through object_ids(targets).\nLifecycle: register_object!, add_organ!, remove_object!, reparent_object!, move_object!, update_geometry!, mark_environment_binding_dirty!, objects_from_mtg.\nHard calls: RunContext, CallTarget, CallTargets, call_model, call_targets, run_call!.\nNewborn initialization: Initializer, run_initializer!.","title":"Execution, lifecycle, and outputs"},{"category":"section","location":"API/public_symbols/#Diagnostics-namespace","page":"Public symbol inventory","text":"PlantSimEngine.Diagnostics owns structured explanations and supported inspection:\n\nStructure: Diagnostics.explain_objects, Diagnostics.explain_instances, Diagnostics.explain_scopes.\nCompilation: Diagnostics.explain_applications, Diagnostics.explain_bindings, Diagnostics.explain_calls, Diagnostics.explain_output_bindings, Diagnostics.explain_writers, Diagnostics.explain_schedule, Diagnostics.explain_execution_plan.\nInitialization, environment, and outputs: Diagnostics.explain_initialization, Diagnostics.explain_environment, Diagnostics.explain_environment_bindings, Diagnostics.explain_output_retention, Diagnostics.explain_outputs.\nSupported carrier inspection: Diagnostics.input_carrier, Diagnostics.input_value, Diagnostics.has_reference_carrier.\nNormalized selector addresses: Diagnostics.ObjectAddress, Diagnostics.object_address.","title":"Diagnostics namespace"},{"category":"section","location":"API/public_symbols/#Model-author-contract","page":"Public symbol inventory","text":"Model identity: AbstractModel, @process, process.\nState schema and initialization: Status, Required, Default, VariableContract, variable_contracts, init_variables, dep.\nModel IO inspection: inputs, outputs, variables, environment_inputs, environment_outputs, validate_environment_inputs.\nIdentity-aware many-input access: bound_input, BoundMany, and object_ids.\nTiming and routing traits: timespec, output_policy, timestep_hint, environment_hint, environment_bindings, environment_window.\n\nThe underscore declarations inputs_, outputs_, environment_inputs_, environment_outputs_, and variable_contracts_ are intentionally unexported extension functions. Model authors implement them with qualified definitions such as PlantSimEngine.inputs_(model) = .... inputs_ must return explicit Required(T) or Default(value) declarations; outputs_ returns initial output-state values; variable_contracts_ returns VariableContract metadata for declared status or environment variables.","title":"Model-author contract"},{"category":"section","location":"API/public_symbols/#Time-and-reducers","page":"Public symbol inventory","text":"Scheduling: ClockSpec, SchedulePolicy, HoldLast, Interpolate, Integrate, Aggregate.\nMeteorology reducers are not re-exported. Use qualified PlantMeteo names, for example PlantMeteo.MeanReducer or PlantMeteo.RadiationEnergy.","title":"Time and reducers"},{"category":"section","location":"API/public_symbols/#EnvironmentAPI-namespace","page":"Public symbol inventory","text":"Backend contract: EnvironmentAPI.AbstractEnvironmentBackend, EnvironmentAPI.EnvironmentContext, EnvironmentAPI.GlobalConstant, EnvironmentAPI.environment_backend, EnvironmentAPI.environment_variables, EnvironmentAPI.base_step_seconds, EnvironmentAPI.get_nsteps, and EnvironmentAPI.bind_environment.\nSampling and mutation: EnvironmentAPI.sample, EnvironmentAPI.sample_environment, EnvironmentAPI.commit_environment!, and EnvironmentAPI.update_index!.\nPlantMeteo conveniences: Atmosphere, Constants, Weather.","title":"EnvironmentAPI namespace"},{"category":"section","location":"API/public_symbols/#GraphEditor-namespace","page":"Public symbol inventory","text":"Discovery and DTOs: GraphEditor.available_models, GraphEditor.model_descriptor, GraphEditor.compile_model_report, GraphEditor.ModelGraphView, and GraphEditor.model_graph_view.\nSerialization and static views: GraphEditor.model_graph_view_json, GraphEditor.model_graph_view_html, and GraphEditor.write_model_graph_view.\nSemantic edits and sessions live under the same namespace, including GraphEditor.AddModelApplication, GraphEditor.apply_model_graph_edit, GraphEditor.edit_graph, GraphEditor.current_model, GraphEditor.undo!, and GraphEditor.redo!.","title":"GraphEditor namespace"},{"category":"section","location":"API/public_symbols/#Evaluation-namespace","page":"Public symbol inventory","text":"Fitting and metrics: Evaluation.fit, Evaluation.RMSE, Evaluation.NRMSE, Evaluation.EF, and Evaluation.dr.","title":"Evaluation namespace"},{"category":"section","location":"API/public_symbols/#Advanced-namespace","page":"Public symbol inventory","text":"PlantSimEngine.Advanced contains the qualified compiler and cache API:\n\nregistries and compiled representations: ObjectRegistry, CompiledCompositeModel, CompiledModelApplication, CompiledModelInputBinding, CompiledModelCallBinding, CompiledModelOutputDestinationPlan, CompiledModelOutputDestinationBinding, CompiledDistributedOutputPlans, CompiledDistributedOutputs, CompiledEnvironmentBinding, CompiledEnvironmentBindings;\ncarrier and adapter implementation types: ObjectRefVector, TimeStepTable;\ncompiler and cache operations: compile_composite_model, refresh_bindings!, refresh_environment_bindings!, compile_environment_bindings;\ncache diagnostics: bindings_dirty, environment_bindings_dirty, model_revision, environment_revision, compiled_bindings, compiled_environment_bindings.\n\nThese names require explicit qualification or using PlantSimEngine.Advanced. They are not part of the concise user namespace and may evolve with compiler implementation requirements.\n\nThe namespace-boundary test in test/test-model-api-stabilization.jl compares the complete default public-name set with an explicit inventory and separately checks every focused submodule. Adding or removing an export therefore requires an intentional inventory update.","title":"Advanced namespace"},{"category":"section","location":"tutorials/growing_plant/part1_growth/#Growing-A-Plant-CompositeModel","page":"Growing A Plant CompositeModel","text":"Begin with a plant object and leaf objects whose carbon production is gathered by a plant application through Many(scale=:Leaf, within=Subtree()). A growth model calls register_object! after its carbon or thermal threshold is met.\n\nStructural changes refresh compiled targets after the application that made the change. A new leaf may run applications that remain later in the same timestep. It runs an application that already completed only when its creator declares that application as an Initializer and explicitly initializes the newborn object. When callers mutate structure between step! calls, refresh occurs before the next step.\n\nBuild the initial registry explicitly so ownership remains visible:\n\nmodel = CompositeModel(\n    Object(:plant; scale=:Plant, status=Status(carbon=0.0)),\n    Object(:leaf_1; scale=:Leaf, parent=:plant, status=Status(area=1.0));\n    applications=(leaf_application, plant_balance, growth_application),\n    environment=weather,\n)\n\nThe plant balance gathers leaf production with Many(scale=:Leaf, within=Subtree()). The growth kernel obtains the live model with runtime_model(context), checks its carbon and thermal thresholds, creates a fully initialized Object, and calls register_object!. It should deduct the construction cost exactly once before registration.\n\nAfter each step, assert both biology and structure: remaining plant carbon, the number of leaf objects, each new leaf's parent, and accepted historical outputs. Diagnostics.explain_applications should show that the new leaf is absent during its creation step and present after the between-step refresh.","title":"Growing A Plant CompositeModel"},{"category":"section","location":"journeys/modelers/mutable_environment/#Implement-A-Mutable-Environment-Controller","page":"Mutable environment controllers","text":"New concept: accepted mutable environment state. A controller declares which variables it may commit, evaluates typed trial states, and commits one accepted state explicitly.\n\nSimulation users first encounter this workflow in Modify The Environment. Backend packages implement the separate Environment Backend Extensions contract.\n\nThe plain Julia blocks below are excerpts from the shipped, tested ToyEnvironmentControllerModel.","title":"Implement A Mutable Environment Controller"},{"category":"section","location":"journeys/modelers/mutable_environment/#Model-9:-declare-commit-permission","page":"Mutable environment controllers","text":"ToyEnvironmentControllerModel declares its reader hard dependency and the environment variable it may commit:\n\nPlantSimEngine.dep(::ToyEnvironmentControllerModel) = (\n    reader=Call(One(process=:toy_environment_reader)),\n)\nPlantSimEngine.environment_outputs_(\n    model::ToyEnvironmentControllerModel,\n) = (T=zero(model.accepted_temperature),)\n\nIts tested kernel keeps trial, commit, and accepted publication separate:\n\ntrial_environment = (T=model.trial_temperature,)\ntrial_target = only(run_call!(\n    context,\n    :reader;\n    environment=trial_environment,\n    publish=false,\n))\n\naccepted_environment = (T=model.accepted_temperature,)\ncommit_environment!(context, accepted_environment)\naccepted_target = only(run_call!(\n    context,\n    :reader;\n    environment=accepted_environment,\n    publish=true,\n))\n\nenvironment_outputs_ is commit permission, not object-status output. PlantSimEngine validates that the accepted state provides every declared variable before invoking the backend through the controller's compiled handle.","title":"Model 9: declare commit permission"},{"category":"section","location":"journeys/modelers/mutable_environment/#Compose-the-controller","page":"Mutable environment controllers","text":"The reader needs only a provider. The controller additionally receives sink=:cells; the backend defines what that sink means:\n\nusing PlantSimEngine, DataFrames\nusing PlantSimEngine.Examples\n\nenvironment = ToySpatialEnvironment(\n    Dict(:canopy => (T=20.0,));\n    step_seconds=3600.0,\n)\nmodel = CompositeModel(\n    Object(\n        :leaf;\n        scale=:Leaf,\n        geometry=(cell=:canopy,),\n    );\n    applications=(\n        ModelSpec(\n            ToyEnvironmentReaderModel();\n            name=:reader,\n            on=One(scale=:Leaf),\n            environment=Environment(backend=environment),\n        ),\n        ModelSpec(\n            ToyEnvironmentControllerModel(30.0, 22.0);\n            name=:controller,\n            on=One(scale=:Leaf),\n            environment=Environment(\n                backend=environment,\n                sink=:cells,\n            ),\n        ),\n    ),\n)\n\n(\n    call=DataFrame(Diagnostics.explain_calls(model)),\n    environment=DataFrame(\n        Diagnostics.explain_environment_bindings(model),\n    ),\n)\n\nsimulation = run!(model; outputs=:all)\n(\n    final=final_state(simulation),\n    committed=environment.cells[:canopy],\n    publications=filter(\n        row -> row.application_id == :reader,\n        DataFrame(Diagnostics.explain_outputs(simulation)),\n    ),\n)\n\nThe rejected T=30 trial mutates only the reader's trial status. The accepted T=22 state is committed once and produces the reader's only retained sample. If the controller itself runs as an unpublished ancestor call, PlantSimEngine also suppresses its descendant publications and environment writes.","title":"Compose the controller"},{"category":"section","location":"journeys/modelers/mutable_environment/#Model-author-recap","page":"Mutable environment controllers","text":"You implemented: declared commit variables, typed trial construction, acceptance logic, explicit commit, and one accepted publication.\nPlantSimEngine inferred: permission validation, backend/handle routing, nested trial suppression, and retained output history.\nThe scenario author keeps explicit: provider, commit sink, concrete backend, and any hard-call override.\nNew API names: environment_outputs_, commit_environment!, Environment, environment, and publish.","title":"Model-author recap"},{"category":"section","location":"journeys/users/advanced_execution/#Control-Advanced-Execution","page":"Control advanced execution","text":"","title":"Control Advanced Execution"},{"category":"section","location":"journeys/users/advanced_execution/#New-concept:-parent-controlled-execution-and-explicit-publication","page":"Control advanced execution","text":"Most coupling should remain a value dependency through inputs. Use a hard call only when a parent algorithm must decide whether, when, or how often another model runs—for example, while iterating toward an accepted leaf temperature.","title":"New concept: parent-controlled execution and explicit publication"},{"category":"section","location":"journeys/users/advanced_execution/#Declare-parent-controlled-targets","page":"Control advanced execution","text":"Start with one plant and two leaves. The reader application is selected by the controller's calls declaration, so it is call-only rather than independently scheduled:\n\nusing PlantSimEngine, DataFrames\nusing PlantSimEngine.Examples\n\nenvironment = ToySpatialEnvironment(\n    Dict(\n        :sun => (T=26.0,),\n        :shade => (T=18.0,),\n    );\n    step_seconds=3600.0,\n)\n\nmodel = CompositeModel(\n    Object(:plant; scale=:Plant, kind=:plant),\n    Object(\n        :sun_leaf;\n        scale=:Leaf,\n        kind=:leaf,\n        parent=:plant,\n        geometry=(cell=:sun,),\n    ),\n    Object(\n        :shade_leaf;\n        scale=:Leaf,\n        kind=:leaf,\n        parent=:plant,\n        geometry=(cell=:shade,),\n    );\n    applications=(\n        ModelSpec(\n            ToyEnvironmentReaderModel();\n            name=:reader,\n            on=Many(scale=:Leaf),\n            environment=Environment(backend=environment),\n        ),\n        ModelSpec(\n            ToySelectiveCallControllerModel(\n                (28.0, 31.0),\n                22.0;\n                selected_object=:sun_leaf,\n            );\n            name=:controller,\n            on=One(scale=:Plant),\n            calls=(\n                :readers => Many(\n                    scale=:Leaf,\n                    within=Subtree(),\n                    application=:reader,\n                ),\n            ),\n        ),\n    ),\n)\n\nselect(\n    DataFrame(Diagnostics.explain_calls(model)),\n    :application_id,\n    :call,\n    :callee_application_ids,\n    :callee_object_ids,\n    :publication_policy,\n)\n\nrun_call!(context, :readers) executes every resolved target and returns a vector-like CallTargets collection. One still returns a collection of one; OptionalOne returns zero or one; Many returns zero or more.","title":"Declare parent-controlled targets"},{"category":"section","location":"journeys/users/advanced_execution/#Inspect,-select,-iterate,-then-publish-once","page":"Control advanced execution","text":"ToySelectiveCallControllerModel needs different treatment per target, so its kernel first calls call_targets(context, :readers) without executing anything. It records the total, restricts the same declared call to :sun_leaf, runs two temperature trials, and accepts one result:\n\nfunction run_selected_trials!(\n    target,\n    trial_temperatures,\n    accepted_temperature,\n)\n    for temperature in trial_temperatures\n        run_call!(\n            target;\n            sampled_environment=(T=temperature,),\n            publish=false,\n        )\n    end\n    run_call!(\n        target;\n        sampled_environment=(T=accepted_temperature,),\n        publish=true,\n    )\nend\n\npublish=false is the default. Trials may update the called model's current status for convergence checks, but they neither append output samples nor commit mutable environment state. Publish exactly the accepted execution.\n\nsimulation = run!(model; outputs=:all)\n(\n    controller=final_state(simulation, :plant),\n    leaves=final_state(simulation, Many(scale=:Leaf)),\n)\n\nThe controller resolved two targets but selected only :sun_leaf. Object selection uses call_targets(context, name; objects=(ObjectId(:sun_leaf),)); it does not depend on iteration order. Two trials left no history; its accepted call published once, while :shade_leaf was never executed:\n\nfilter(\n    row -> row.application_id == :reader,\n    DataFrame(Diagnostics.explain_outputs(simulation)),\n)\n\nUse run_call!(context, name; environment=trial_state) when every target should sample the same provider-aware trial state through its own compiled handle. If the caller has already sampled the model-facing environment, use run_call!(context, name; sampled_environment=value) to execute all targets through cached typed batches. Use call_targets and run_call!(target; sampled_environment=...) only for selection, custom order, status inspection, or distinct already-sampled environments. For one target, call_model(context, name) provides allocation-free access to its concrete model when an algorithm must dispatch on model type or read its parameters.","title":"Inspect, select, iterate, then publish once"},{"category":"section","location":"journeys/users/advanced_execution/#Order-intentional-duplicate-writers","page":"Control advanced execution","text":"One canonical variable normally has one writer. Two applications that both claim stock therefore fail compilation unless their relationship is intentional and ordered. Updates makes that ownership explicit:\n\nwriter_model = CompositeModel(\n    Object(:reserve; scale=:Organ);\n    applications=(\n        ModelSpec(\n            ToyStockWriterModel(4);\n            name=:initial_stock,\n            on=One(scale=:Organ),\n        ),\n        ModelSpec(\n            ToyStockWriterModel(8);\n            name=:adjusted_stock,\n            on=One(scale=:Organ),\n            updates=Updates(:stock; after=:initial_stock),\n        ),\n        ModelSpec(\n            ToyStockWriterModel(99);\n            name=:alternative_stock,\n            on=One(scale=:Organ),\n            output_routing=(stock=:stream_only,),\n        ),\n    ),\n)\n\nselect(\n    DataFrame(Diagnostics.explain_writers(writer_model)),\n    :object_id,\n    :variable,\n    :application_ids,\n    :update_application_ids,\n    :update_after,\n)\n\ninitial_stock owns the first canonical write and adjusted_stock explicitly updates it afterward. The alternative value is useful as a retained comparison but must not replace canonical status, so output_routing marks it :stream_only.\n\nwriter_simulation = run!(writer_model; outputs=:all)\n(\n    canonical_stock=final_state(writer_simulation).stock,\n    published=collect_outputs(\n        writer_simulation,\n        :reserve,\n        :stock;\n        sink=nothing,\n    ),\n)\n\nThe canonical result is 8; the three application-specific streams retain 4, 8, and 99. A stream-only output is not a fallback writer and is not selected by an application-free OutputRequest; request its application explicitly when retaining only selected outputs.","title":"Order intentional duplicate writers"},{"category":"section","location":"journeys/users/advanced_execution/#Page-recap","page":"Control advanced execution","text":"You added: one declared hard call, selective trials, one accepted publication, explicit update ordering, and one stream-only alternative.\nPlantSimEngine inferred: the call-only schedule, concrete targets, canonical writer ownership, and update edge.\nYou keep explicit: when each target runs, publish, per-target forcing, intentional duplicate writers, and non-canonical streams.\nNew API names: calls, call_targets, run_call!, CallTargets, Updates, output_routing, and :stream_only.","title":"Page recap"},{"category":"section","location":"journeys/modelers/basic_model/#Implement-A-Basic-Model","page":"Basic contract and reuse","text":"New concept: the complete one-step model contract. This page then proves that the same kernel composes automatically on one object and runs unchanged over several objects.\n\nFor the simulation-user view of these scenarios, see Couple Models On One Object and Run The Coupling On Several Objects.","title":"Implement A Basic Model"},{"category":"section","location":"journeys/modelers/basic_model/#Model-1:-declare-the-complete-contract","page":"Basic contract and reuse","text":"ToyDevelopmentModel has one generic parameter, one required input, one true default, one output initial value, and the final five-argument kernel. This is the complete tested implementation from examples/ToyModelDeveloper.jl:\n\nstruct ToyDevelopmentModel{T} <: AbstractToy_DevelopmentModel\n    efficiency::T\nend\n\nPlantSimEngine.inputs_(::ToyDevelopmentModel) = (\n    TT=Required(Real),\n    stress=Default(1.0),\n)\nPlantSimEngine.outputs_(model::ToyDevelopmentModel) = (\n    growth=zero(model.efficiency),\n)\n\nfunction PlantSimEngine.run!(\n    model::ToyDevelopmentModel,\n    status,\n    environment,\n    constants,\n    context,\n)\n    status.growth = model.efficiency * status.TT * status.stress\n    return nothing\nend\n\nRequired(Real) is a contract, not an initialization value. Default(1.0) means the scientific model genuinely defines unstressed growth as its fallback. Output values initialize model state and should match the parameter's numeric type where practical.\n\nTest the kernel directly before involving a scenario:\n\nusing Dates, PlantMeteo, PlantSimEngine, DataFrames\nusing PlantSimEngine.Examples\n\ndevelopment = ToyDevelopmentModel(0.5)\nstatus = Status(TT=8.0, stress=0.75, growth=0.0)\nPlantSimEngine.run!(\n    development,\n    status,\n    nothing,\n    nothing,\n    nothing,\n)\nstatus.growth","title":"Model 1: declare the complete contract"},{"category":"section","location":"journeys/modelers/basic_model/#Model-2:-let-one-object-couple-it-automatically","page":"Basic contract and reuse","text":"ToyDegreeDaysCumulModel publishes TT; ToyDevelopmentModel requires TT. Because both applications target the same object and the producer is unique, the scenario needs no explicit inputs wiring:\n\none_object = CompositeModel(\n    Object(:leaf; scale=:Leaf);\n    applications=(\n        ModelSpec(\n            ToyDegreeDaysCumulModel(T_base=10.0);\n            name=:thermal_time,\n            on=One(scale=:Leaf),\n        ),\n        ModelSpec(\n            development;\n            name=:development,\n            on=One(scale=:Leaf),\n        ),\n    ),\n    environment=Atmosphere(\n        T=18.0,\n        Wind=1.0,\n        Rh=0.7,\n        duration=Day(1),\n    ),\n)\n\nselect(\n    DataFrame(Diagnostics.explain_bindings(one_object)),\n    :application_id,\n    :input,\n    :origin,\n    :source_application_ids,\n    :carrier_kind,\n)\n\none_simulation = run!(one_object)\nfinal_state(one_simulation)\n\nPlantSimEngine creates stress=1.0 from the model default and connects TT through a shared Ref. The development kernel knows neither fact.","title":"Model 2: let one object couple it automatically"},{"category":"section","location":"journeys/modelers/basic_model/#Model-3:-reuse-the-kernel-over-several-objects","page":"Basic contract and reuse","text":"Change only application multiplicity from One to Many:\n\nseveral_objects = CompositeModel(\n    Object(:leaf_1; scale=:Leaf),\n    Object(:leaf_2; scale=:Leaf);\n    applications=(\n        ModelSpec(\n            ToyDegreeDaysCumulModel(T_base=10.0);\n            name=:thermal_time,\n            on=Many(scale=:Leaf),\n        ),\n        ModelSpec(\n            development;\n            name=:development,\n            on=Many(scale=:Leaf),\n        ),\n    ),\n    environment=Atmosphere(\n        T=18.0,\n        Wind=1.0,\n        Rh=0.7,\n        duration=Day(1),\n    ),\n)\n\nfinal_state(run!(several_objects), Many(scale=:Leaf))\n\nDo not loop over objects inside ToyDevelopmentModel.run!. PlantSimEngine compiles homogeneous execution targets and invokes the one-target kernel for each selected object.","title":"Model 3: reuse the kernel over several objects"},{"category":"section","location":"journeys/modelers/basic_model/#Model-author-recap","page":"Basic contract and reuse","text":"You implemented: parameters, Required/Default inputs, output initial state, and one-target arithmetic.\nPlantSimEngine inferred: default initialization, same-object coupling, execution order, and repeated targets.\nThe scenario author keeps explicit: objects, application names, multiplicity, and environment data.\nNew API names: AbstractModel, inputs_, outputs_, Required, Default, and run!.","title":"Model-author recap"},{"category":"section","location":"dev/composite_model_completion_audit/#Unified-CompositeModel/Object-Completion-Audit","page":"Composite model/object completion audit","text":"This audit records the evidence used to assess the breaking composite-model/object redesign against composite_model_design.md and composite_model_implementation_plan.md.\n\nAudit date: June 12, 2026.","title":"Unified CompositeModel/Object Completion Audit"},{"category":"section","location":"dev/composite_model_completion_audit/#Result","page":"Composite model/object completion audit","text":"The unified composite-model/object redesign is implemented as the primary public configuration API.\n\nThe superseded mapping implementation and the unreleased intermediate prototype were removed after the composite-model/object runtime replaced them.","title":"Result"},{"category":"section","location":"dev/composite_model_completion_audit/#Public-Contract","page":"Composite model/object completion audit","text":"The exported scenario vocabulary centers on:\n\nCompositeModel\nObject\nModelSpec\nUpdates\nEnvironment\n\nLegacy scenario constructors such as ModelMapping and MultiScaleModel were removed.\n\nEvidence:\n\nsrc/PlantSimEngine.jl\ndocs/src/API/API_public.md\ndocs/src/migration_composite_model.md\nREADME.md","title":"Public Contract"},{"category":"section","location":"dev/composite_model_completion_audit/#Requirement-Evidence","page":"Composite model/object completion audit","text":"Requirement Evidence\nOne model object registry without prescribing plant topology CompositeModel, Object, selectors, relations, scopes, and objects_from_mtg in src/composite_model_api.jl; selector and MTG adapter tests in test/test-unified-model-object-api.jl\nReusable species models and repeated plant instances CompositeModelTemplate, ObjectInstance, Override, shared model ownership, and homogeneous/heterogeneous batches; four-instance and override tests\nSoft/value dependencies Compiled ModelSpec(...; inputs=...) bindings, same-object inference, scalar references, RefVector, Advanced.ObjectRefVector, temporal carriers, renaming, and optional inputs\nManual hard dependencies Compiled ModelSpec(...; calls=...), vector-like CallTargets, run_call!(context, name), and fine-grained run_call!(target); trial calls default to publish=false and accepted calls publish explicitly\nModel-author dependency defaults Input(...) and Call(...) entries from dep(model), with ModelSpec overrides and binding provenance in explanations\nMultirate execution every=Dates.Period, model timespec, input policies, explicit windows, PreviousTimeStep, and stable compiled scheduling\nGeneric value types Reference and temporal tests using ModelObjectDualLike{BigFloat} and BigFloat interpolation/integration without Float64 conversion\nReference semantics and low-copy execution Shared scalar references, typed many-object carriers, preinstalled status bindings, zero-allocation materialization tests, and a zero-allocation warmed 128-object execution batch\nDuplicate writers Canonical writer validation and Updates(:variable; after=...), including pruning-after-allocation tests\nGrowth, pruning, reparenting, and movement Central lifecycle APIs, structural/environment cache invalidation, dynamic target/carrier rebuilding, removed-object output history, and object-scoped geometry refresh tests\nAutomatic meteorology and microclimate environment_inputs_, global and spatial backends, ancestor geometry fallback, source remapping, model hints, tabular aggregation, cached opaque handles, run_call!(...; environment=trial_state) trial sampling, and commit_environment! accepted mutable commits\nStructured agent explanations Object, instance, scope, application, binding, call, model-bundle, environment, schedule, writer, execution-plan, output, and retention explanations returning structured rows\nInitialization workflow Required(T) and Default(value) make the input contract explicit; Diagnostics.explain_initialization classifies required, defaulted, supplied, generated, producer-bound, and environment-bound variables before strict compilation\nRuntime model access runtime_model is the public accessor used by lifecycle-capable kernels and accepts CompositeModel, RunContext, and Simulation\nOne-object ergonomics CompositeModel(model, models...; status=...) lowers to the same object/application compiler and runtime as explicit construction\nOutput ownership and retention Application-qualified streams, output_routing, OutputRequest(application=...), dynamic-object exports, and bounded policy-specific dependency histories\nMAESPA acceptance case build_maespa_scene and run_maespa_example use CompositeModelTemplate, ObjectInstance, on, inputs, calls, and every; verified by test/test-maespa-model-example.jl\nDocumentation and migration CompositeModel/object-first README, home page, quickstart, execution guide, public API, migration guide, and explicitly labeled legacy reference sections","title":"Requirement Evidence"},{"category":"section","location":"dev/composite_model_completion_audit/#Verification","page":"Composite model/object completion audit","text":"The following gates passed from a clean, controllable Kaimon Julia session:\n\ntest/test-unified-model-object-api.jl  576 passed\ntest/runtests.jl                       885 passed\ndocs/make.jl                           passed\nPlantBiophysics/test/runtests.jl       117 passed\ngit diff --check                       passed\n\nThe complete package suite includes Aqua, all focused restoration files, the broad unified runtime regression, examples, fitting, and doctests. The documentation build completed its executable examples, doctests, cross-references, document checks, and HTML rendering successfully.\n\nThe current uncommitted XPalm CompositeModel/Object migration loads this PlantSimEngine worktree and executes 74 of 75 downstream assertions. Its remaining assertion expects the removed runtime's first-step LAI (0.000272); the current compiled dependency order correctly runs leaf area before plant and model aggregation, so the initial zero-biomass leaf produces 0.0. This is a downstream fixture expectation in a dirty migration worktree, not a package-specific behavior to restore in PlantSimEngine. No XPalm workaround was added here.","title":"Verification"},{"category":"section","location":"dev/composite_model_completion_audit/#Compatibility-Boundary","page":"Composite model/object completion audit","text":"Historical mapping source and tests were removed. Migration guidance remains in the documentation for downstream packages moving to the composite-model/object API. The branch-only intermediate prototype was also removed because it was never released and has no compatibility boundary.\n\nRequested output histories are still materialized after a run. Dependency-only temporal histories are bounded, but a fully online output sink would be an additional optimization rather than a missing requirement of this redesign.","title":"Compatibility Boundary"},{"category":"section","location":"guides/time/advanced_time_environment/#Advanced-Time-And-Environment-Configuration","page":"Advanced configuration","text":"Disambiguate a producer with an explicit selector containing application, var, and within. Put temporal policy and window on that input. Configure environment source renaming and reducers with Environment; scenario values override model-level environment_hint entries.\n\nUse Diagnostics.explain_bindings, Diagnostics.explain_environment_bindings, and Diagnostics.explain_schedule to inspect the final source, reducer, window, cadence, and clock origin. All periods that require seconds must be fixed Dates periods; Month(1) is intentionally rejected.","title":"Advanced Time And Environment Configuration"},{"category":"section","location":"dev/maespa_model_handoff/#MAESPA-Style-CompositeModel-Example-Handoff","page":"MAESPA-style composite-model example handoff","text":"The executable acceptance example is examples/maespa_model_example.jl, with focused coverage in test/test-maespa-model-example.jl.","title":"MAESPA-Style CompositeModel Example Handoff"},{"category":"section","location":"dev/maespa_model_handoff/#CompositeModel-Shape","page":"MAESPA-style composite-model example handoff","text":"One :Scene object owns canopy microclimate and model-scale fluxes.\nOne shared :Soil object owns soil water state.\nSpecies A and B are reusable CompositeModelTemplates mounted as independent ObjectInstances.\nEach plant instance contains one plant object, one internode object, and its own leaf objects.\nSpecies parameters differ while the model application structure is shared.\n\nLeaf applications use the copied PlantBiophysics subsample models:\n\nMonteith for :energy_balance;\nFvcb for :photosynthesis;\nTuzet for :stomatal_conductance.","title":"CompositeModel Shape"},{"category":"section","location":"dev/maespa_model_handoff/#Coupling","page":"MAESPA-style composite-model example handoff","text":"The model energy-balance application controls iterative canopy-air, leaf, and soil calls. ModelSpec(...; calls=...) expresses execution ownership only: the scene model decides when subprocesses run.\n\nModelSpec(scene_model; name=:scene_eb, on=One(scale=:Scene), inputs=(:psi_soil =>\n            One(kind=:soil, scale=:Soil, application=:soil_water, var=:psi_soil),), calls=(:energy_balance =>\n            Many(kind=:plant, scale=:Leaf, process=:energy_balance),\n        :soil =>\n            One(kind=:soil, scale=:Soil, application=:soil_water),), environment=Environment(provider=:forcing, sink=:canopy), every=Dates.Hour(1))\n\nThe scene receives above-canopy forcing from the :forcing provider and has :canopy as its explicit commit sink. Trial leaf calls use run_call!(context, :energy_balance; environment=trial_environment), so all hard-called leaves sample the trial canopy atmosphere through their compiled handles without committing it. After convergence, the scene commits the accepted canopy atmosphere with commit_environment!(context, accepted_environment) and publishes one accepted leaf call against that committed environment.\n\nScene/soil values are wired declaratively with ModelSpec(...; inputs=...), not by manually writing another object's status. The soil model receives accepted scene fluxes through live references:\n\nModelSpec(SoilWater(...); name=:soil_water, on=One(kind=:soil, scale=:Soil), inputs=(:transpiration =>\n            One(\n                scale=:Scene,\n                within=SceneScope(),\n                application=:scene_eb,\n                var=:scene_transpiration,\n            ),\n        :infiltration =>\n            One(\n                scale=:Scene,\n                within=SceneScope(),\n                application=:scene_eb,\n                var=:scene_infiltration,\n            ),), every=Dates.Hour(1))\n\nThis creates a parent-controlled feedback loop: the scene reads mapped psi_soil when it starts its energy-balance solve, computes accepted scene water fluxes, writes scene_transpiration and scene_infiltration, then calls the soil model. The soil call sees those scene values through input carriers and publishes the updated soil state. If the intended science is an explicit lag rather than same-step parent control, use PreviousTimeStep(:psi_soil) on the scene input.\n\nCompositeModel LAI receives live references to every leaf area:\n\nModelSpec(LAIModel(ground_area); name=:lai_dynamic, on=One(scale=:Scene), inputs=(:leaf_areas => Many(\n            kind=:plant,\n            scale=:Leaf,\n            within=SceneScope(),\n            process=:leaf_state,\n            var=:leaf_area,\n        ),), every=Dates.Day(1))\n\nThe scene energy-balance model uses the same mapping mechanism for leaf-scale values needed during the hard-call solve. It maps leaf area, leaf carbon, trial leaf inputs (Ra_SW_f, aPPFD, Ψₗ), and accepted leaf fluxes (Rn, λE, H, A) into scene-level vector inputs. The scene model then writes or reads those vectors, while the referenced leaf statuses remain the single source of truth.\n\nModelSpec(scene_model; name=:scene_eb, on=One(scale=:Scene), inputs=(:leaf_areas => Many(kind=:plant, scale=:Leaf, within=SceneScope(), var=:leaf_area),\n        :leaf_carbon => Many(kind=:plant, scale=:Leaf, within=SceneScope(), var=:leaf_carbon),\n        :leaf_Ra_SW_f => Many(kind=:plant, scale=:Leaf, within=SceneScope(), var=:Ra_SW_f),\n        :leaf_aPPFD => Many(kind=:plant, scale=:Leaf, within=SceneScope(), var=:aPPFD),\n        :Ψₗ => Many(kind=:plant, scale=:Leaf, within=SceneScope(), var=:Ψₗ),\n        :leaf_rn => Many(kind=:plant, scale=:Leaf, within=SceneScope(), policy=HoldLast(), var=:Rn),\n        :leaf_lambda_e => Many(kind=:plant, scale=:Leaf, within=SceneScope(), policy=HoldLast(), var=:λE),\n        :leaf_h => Many(kind=:plant, scale=:Leaf, within=SceneScope(), policy=HoldLast(), var=:H),\n        :leaf_a => Many(kind=:plant, scale=:Leaf, within=SceneScope(), policy=HoldLast(), var=:A),))\n\nHoldLast() is intentional for the leaf flux vectors: it asks the compiler for live references to the current held status values, so the parent scene solve can iterate hard-call trial states without materializing temporal streams.\n\nAllocation is plant-local because its leaf selector uses within=Subtree():\n\nModelSpec(allocation; name=:allocation, on=One(scale=:Plant), inputs=(:leaf_carbon => Many(scale=:Leaf, within=Subtree(), var=:leaf_carbon)), every=Dates.Day(1))","title":"Coupling"},{"category":"section","location":"dev/maespa_model_handoff/#Meteorology","page":"MAESPA-style composite-model example handoff","text":"Input meteorology is above-canopy forcing wrapped in a MaespaSingleLayerEnvironment. The backend stores two meteorological states:\n\nforcing: the above-canopy Weather/time series sampled by the scene;\ncanopy: the mutable canopy Atmosphere sampled by every leaf.\n\nThe scene application uses Environment(provider=:forcing, sink=:canopy). Leaf energy-balance applications use Environment(provider=:canopy). The one-layer backend does not look at process names, geometry, or cells; all leaves intentionally sample the same current canopy atmosphere.\n\ncanopy_air_update(...) is a plain helper, not a model application. It reads canopy-scale leaf fluxes aggregated in the scene, computes the MAESPA-style canopy air update, and returns a new Atmosphere. The accepted solution is committed directly with:\n\ncommit_environment!(context, accepted_environment)\n\nCompositeModel status also stores diagnostics for the resulting below-canopy microclimate:\n\ncanopy_tair;\ncanopy_vpd;\ncanopy_rh;\ncanopy_htot;\ncanopy_gcanop.\n\nTrial iterations pass the candidate atmosphere through run_call!, preserving the leaf applications' compiled provider handles. The accepted state is the only state committed to the mutable environment backend.","title":"Meteorology"},{"category":"section","location":"dev/maespa_model_handoff/#Acceptance-Checks","page":"MAESPA-style composite-model example handoff","text":"The focused test verifies:\n\nfive leaves across two species and one shared soil object;\ninstance membership and mounted application ids;\nmodel calls to all leaf energy-balance applications and the soil model;\nnested Monteith -> Fvcb -> Tuzet call bundles;\nlive-reference LAI and plant-local allocation bindings;\nhourly energy balance and daily LAI/allocation schedules;\nexactly one accepted publication per manually called target and timestep;\nfinite canopy microclimate, leaf energy, photosynthesis, soil feedback, and species-specific allocation after a 25-hour run.","title":"Acceptance Checks"},{"category":"section","location":"introduction/why_julia/#The-choice-of-using-Julia","page":"Why Julia ?","text":"PlantSimEngine is implemented in Julia. It arose from a particular combination of needs and requirements that Julia addresses effectively.\n\nOther modelling frameworks, FSPMs and crop models are -often- written in combinations of Java, C++, Python, or Fortran. Given that Julia isn't the language many researchers (and developers!) are most familiar with yet, this page provides a short explanation of the reasoning behind that language choice. Another nice resource is this discourse post by Alejandro Morales Sierra, the creator and maintainer of Virtual Plant Lab.","title":"The choice of using Julia"},{"category":"section","location":"introduction/why_julia/#From-research-to-real-world-applications","page":"Why Julia ?","text":"PlantSimEngine was originally a goal-oriented framework. Its features arose -and continue to evolve- out of necessity for more and more complex simulation setups.\n\nWhile PlantSimEngine primarily helps researchers prototype and test their models efficiently, we consistently work with the vision of making it suitable for real-world applications. Our goal is to build a bridge between academic plant modeling and practical field applications. Ideally, researchers should be able to develop and refine their models in a comfortable environment, and these models could eventually be deployed in production environments.\n\nThis vision of dual-purpose functionality drives our focus on performance optimization. We aspire for the models you develop to be useful beyond academic papers, potentially serving reliably in production environments where efficiency and accuracy are crucial. Julia's strong performance characteristics support this vision in ways other languages would struggle to match.\n\nPlantSimEngine aims to balance scientific rigor with developer productivity, with the long-term goal of ensuring that models can be deployed at scale. Julia provides an environment where researchers can express complex mathematical concepts directly in code with good performance potential, creating a pathway for these models to potentially reach practical implementation.","title":"From research to real-world applications"},{"category":"section","location":"introduction/why_julia/#PlantSimEngine's-constraints","page":"Why Julia ?","text":"","title":"PlantSimEngine's constraints"},{"category":"section","location":"introduction/why_julia/#Performance","page":"Why Julia ?","text":"While computers have gained several orders of magnitude of power and memory over the past few decades, to the point where many prior performance bottlenecks have vanished, performance can still be a limiting factor.\n\nSimulating multiple processes with user-provided variables over many plants with tens of thousands of leaves requires a lot of computation. Using a higher-level language such as Python or R would not lead to adequate simulation times.\n\nIn fact, part of the initial motivation to commit to Julia happened after porting a model from R to Julia and getting several orders of magnitude difference in performance 'out-of-the-box'. Seeing computations that previously took minutes suddenly completing in seconds was quite convincing (see also this benchmark showing a difference of 5 orders of magnitude).\n\nJulia, with its well-designed 'Just-ahead-of-time' compilation model and its flexibility allowing to do some lower-level optimisation, doesn't suffer from the limitations one would encounter by using only Python or R.","title":"Performance"},{"category":"section","location":"introduction/why_julia/#Flexibility,-ease-of-use","page":"Why Julia ?","text":"PlantSimEngine was also developed with a few goals in mind, one of them being to make hypothesis testing quite easy. It is currently difficult to validate FSPM, crop model or ecophysiological hypotheses in many existing frameworks due to their rigid structure or steep learning curve.\n\nSimilarly, when developing a full-featured FSPM, there might be a need to test different models for a specific process, or to switch a model for a more complex one. API and language ease of use is as much of a factor as automated model coupling in keeping these changes smooth.","title":"Flexibility, ease of use"},{"category":"section","location":"introduction/why_julia/#Packages-destined-to-be-used-by-a-wider-community","page":"Why Julia ?","text":"As mentioned earlier, PlantSimEngine is intended for a wide audience. Only few of them are expected to have a strong development background. Many other potential users might be researchers more well-versed in ecophysiology or plant architecture and only know a little bit of Python, Matlab or R. Reducing friction for these users is paramount.\n\nOpen-source libraries/packages, ease of installation and low entry barrier also factor in the decision.","title":"Packages destined to be used by a wider community"},{"category":"section","location":"introduction/why_julia/#Modularity-and-flexibility-while-retaining-performance","page":"Why Julia ?","text":"One approach could be to combine, say, Python, with a more performant language such as C++ or Fortran. The slower but flexible language being used for prototyping, and when performance is required, some chunks are reimplmented in the other language.\n\nThis fits the performance constraint, but has a few caveats.","title":"Modularity and flexibility while retaining performance"},{"category":"section","location":"introduction/why_julia/#Low-developer-bandwidth","page":"Why Julia ?","text":"And of course, budget, time and resources are a concern. The more autonomous researchers and modelers are, and the less specialist developer/engineering resources are required, the easier it is for the project to keep evolving.","title":"Low developer bandwidth"},{"category":"section","location":"introduction/why_julia/#Comparison","page":"Why Julia ?","text":"","title":"Comparison"},{"category":"section","location":"introduction/why_julia/#The-Two-Language-Problem","page":"Why Julia ?","text":"Combining two different languages requires a lot of language expertise, with constant knowledge refreshing, as one might only occasionally work with and debug with the lower-level language. Or more engineering resources.\n\nSpeed of iteration is also lost whenever performance is a concern, which happens often in our context. However modular and easy-to-use a language like Python might be, whenever it's time to switch to a low-level language, development speed will slow down.\n\nJulia effectively solves this problem. While it might be a little harder to learn than Python, and require extra knowledge to properly make use of its flexibility and performance capabilities, it leads to a smoother development experience.\n\nEverything can be done using Julia exclusively, so there is no need to learn two languages. No need to interface between them. Iteration speed doesn't suddenly grind to a halt if a low-level implementation is needed. A competent researcher-developer can move seamlessly from prototype to production, while still being able to focus on modeling and the actual plant side of things.\n\n(Image: Language usage comparison for different ML packages (source: https://pde-on-gpu.vaw.ethz.ch/lecture1/)) (Language usage comparison for different ML packages; source: https://pde-on-gpu.vaw.ethz.ch/lecture1/)\n\nIt seems we aren't the only ones to find Julia a good tool for our job. Other niches where Julia is gaining traction tend to be other computationally heavy areas with much active research, such as machine learning and climate modeling - areas where this balance of expressivity and performance is equally valuable.","title":"The Two-Language Problem"},{"category":"section","location":"introduction/why_julia/#A-good-balance-in-terms-of-accessibility","page":"Why Julia ?","text":"Another argument in favour of Julia is that one of the aims for PlantSimEngine is to be easy-to-use for researchers wishing to test hypothesis, or reproduce results from other papers. Scientific reproducibility is greatly enhanced when the barrier to running and modifying simulations is lowered.\n\nMany researchers are not developers by trade or heart, and a Java-only or C++-only implementation, on top of the earlier points, would not be accessible enough and would not gain much traction.\n\nJulia, while less ubiquitous than other languages in research circles, resembles Python and R and is more beginner-friendly than Java or C++. It is easier for a Python user to learn to use a simple Julia package than a C++ one.\n\nUsers will also find it easier to quickly implement new models without the potential hurdle of a low-level implementation, or some language interfacing also being required. The prototyping phase doesn't require a subsequent performance tuning phase.","title":"A good balance in terms of accessibility"},{"category":"section","location":"introduction/why_julia/#Ease-of-environment-setup","page":"Why Julia ?","text":"Similarly, Julia's language and package installation is -mostly- fairly straightforward and requires little additional knowledge.\n\nThe package manager is built directly into the language, making dependency management straightforward. This is particularly important for reproducible scientific workflows, where consistent environments are crucial.","title":"Ease of environment setup"},{"category":"section","location":"introduction/why_julia/#Downsides-acceptable","page":"Why Julia ?","text":"While very practical for a 'researcher-developer', Julia is of course far from being the perfect language in every discipline. It is massive in terms of features, has a heavy runtime, is more involved to learn and master quickly compared to Python, has a few hurdles for beginners, some quirks that can be awkward for developers, tools that aren't fully mature, no clear 'recommended' workflow, and so on. \n\nThe cost for switching may not be worth it in many other circumstances. However, several of these downsides, while very relevant for embedded systems, or game development, are much less relevant regarding PlantSimEngine. And others can be mitigated with, hopefully, adequate learning resources and documentation.","title":"Downsides acceptable"},{"category":"section","location":"introduction/why_julia/#Conclusion","page":"Why Julia ?","text":"For PlantSimEngine's specific requirements—balancing performance with flexibility, enabling rapid iteration while maintaining computational efficiency, and providing an accessible interface for both researchers and field practitioners—Julia represents a suitable choice. The language allows us to build an ecosystem where plant modeling can advance through collaborative, efficient, and scientifically rigorous development while delivering real-world value through production deployments.\n\nWhile no language solution is perfect, Julia's combination of features makes it well-suited to the challenges of modern plant modeling and simulation, both in research and practical applications. We're optimistic about the possibilities it offers for the future of plant modeling.","title":"Conclusion"},{"category":"section","location":"journeys/users/several_objects/#Run-The-Coupling-On-Several-Objects","page":"Several same-scale objects","text":"","title":"Run The Coupling On Several Objects"},{"category":"section","location":"journeys/users/several_objects/#New-concept:-stable-object-identity-and-Many","page":"Several same-scale objects","text":"The previous page ran one model chain on one automatically created object. The smallest extension is to create two same-scale objects and target both with one reusable Many selector.\n\nusing PlantSimEngine, PlantMeteo, Dates, DataFrames\nusing PlantSimEngine.Examples\n\nweather = read_weather(\n    joinpath(pkgdir(PlantSimEngine), \"examples/meteo_day.csv\");\n    duration=Day,\n)\n\nplants = (\n    Object(\n        :plant_a;\n        scale=:Plant,\n        kind=:plant,\n        status=Status(TT_cu=0.0),\n    ),\n    Object(\n        :plant_b;\n        scale=:Plant,\n        kind=:plant,\n        status=Status(TT_cu=200.0),\n    ),\n)\nplant_targets = Many(scale=:Plant)\n\nmodel = CompositeModel(\n    plants...;\n    applications=(\n        ModelSpec(\n            ToyDegreeDaysCumulModel();\n            name=:degree_days,\n            on=plant_targets,\n        ),\n        ModelSpec(ToyLAIModel(); name=:lai, on=plant_targets),\n        ModelSpec(Beer(0.6); name=:light, on=plant_targets),\n    ),\n    environment=weather,\n)\n\n:plant_a and :plant_b are stable object identities. Their initial cumulative thermal times differ, but the same three model kernels execute for both. The model implementations contain no loop over plants.\n\nThe application diagnostic confirms that each application compiled to both objects:\n\nselect(\n    DataFrame(Diagnostics.explain_applications(model)),\n    :application_id,\n    :target_ids,\n)\n\nRun five steps and inspect each independent final status:\n\nsimulation = run!(model; steps=5, outputs=:all)\nstates = final_state(simulation, Many(scale=:Plant))\nDict(\n    id => (TT_cu=state.TT_cu, LAI=state.LAI, aPPFD=state.aPPFD)\n    for (id, state) in states\n)\n\nRetained streams are keyed by application, object, and variable, so the two objects do not overwrite one another:\n\nrows = collect_outputs(simulation)\nlai_rows = rows[rows.variable .== :LAI, [\n    :timestep,\n    :application_id,\n    :object_id,\n    :value,\n]]\nfirst(lai_rows, 6)\n\nThis remains a same-scale simulation. Parent/child topology and cross-object value selection are introduced on the next journey, after independent object execution is established.","title":"New concept: stable object identity and Many"},{"category":"section","location":"journeys/users/several_objects/#Page-recap","page":"Several same-scale objects","text":"You added: two explicit Objects, stable ids, one shared Many selector, and named ModelSpec applications.\nPlantSimEngine inferred: two targets per application plus independent same-object TT_cu and LAI connections for each plant.\nYou keep explicit: which objects exist, their initial status, application names, and the selector describing the target set.\nNew API names: Object, Status, ModelSpec, Many, and Diagnostics.explain_applications.","title":"Page recap"},{"category":"section","location":"guides/multiscale/import_mtg/#Importing-An-MTG","page":"Importing an MTG","text":"objects_from_mtg(root) converts MTG topology and labels into ordinary model objects. CompositeModel(root; applications=...) performs the same adaptation and then uses the normal CompositeModel compiler. The MTG is an input representation, not a second runtime.\n\nFor growth, prefer add_organ!: it creates the MTG node, applies the model's status policy by default, attaches status, and registers the corresponding object.","title":"Importing An MTG"},{"category":"section","location":"tutorials/growing_plant/part3_debugging/#Debugging-Growth-And-Resource-Ordering","page":"Part 3: debugging","text":"If an organ appears to spend resources before it exists, inspect activation timing and the compiled schedule. If two models intentionally update one stock, declare Updates(:stock; after=:producer). If a parent must test several child states before accepting one, use calls and publish only the accepted call.\n\nFor cycles, choose a scientific meaning: lag one edge with PreviousTimeStep, put convergence under a parent-owned hard call, or reformulate the equations. Do not resolve a cycle by incidental application ordering.\n\nUse this debugging order:\n\nDiagnostics.explain_initialization(model) for missing state or environment values.\nDiagnostics.explain_bindings(model) for source scope and multiplicity.\nDiagnostics.explain_writers(model) for competing canonical outputs.\nDiagnostics.explain_calls(model) for call-only targets and target cardinality.\nDiagnostics.explain_schedule(model) for cadence and root ordering.\nDiagnostics.explain_outputs(simulation) after execution for publication history.\n\nA trial call must not mutate accepted output history or scatter mutable environment outputs. Nested trials inherit the outer publication decision. Convergence and failure policy belongs to the parent model: it decides the iteration limit, tolerance, fallback, and whether any state is accepted.\n\nStructural mutation is also transactional at the timestep boundary. A new organ is registered immediately in the model registry but does not recursively run merely because it was created. A creator may explicitly initialize one newborn object through a declared Initializer; otherwise, compilation refreshes targets, carriers, calls, writer validation, schedules, and requested outputs at the safe barrier. Geometry-only movement refreshes only affected spatial bindings where possible.","title":"Debugging Growth And Resource Ordering"},{"category":"section","location":"journeys/users/environments/#Understand-Environments","page":"Read an environment","text":"","title":"Understand Environments"},{"category":"section","location":"journeys/users/environments/#New-concept:-declared-sampling-from-global-and-spatial-sources","page":"Read an environment","text":"The first simulation used a weather file as supplied forcing. This page now makes that contract explicit. A model declares the names it reads from its model-facing environment:\n\nusing PlantSimEngine, Dates, DataFrames\nusing PlantSimEngine.Examples\n\n(\n    degree_days=PlantSimEngine.environment_inputs_(\n        ToyDegreeDaysCumulModel(),\n    ),\n    light=PlantSimEngine.environment_inputs_(Beer(0.6)),\n)\n\nToyDegreeDaysCumulModel reads environment.T; Beer reads environment.Ri_PAR_f. These are not status inputs and are not outputs owned by the target object.","title":"New concept: declared sampling from global and spatial sources"},{"category":"section","location":"journeys/users/environments/#Global-sampling-and-source-names","page":"Read an environment","text":"The source does not need to use the model-facing names. Here a global provider has air_temperature and incident_par; Environment(...; sources=...) remaps them for the two model applications.\n\nforcing = (\n    air_temperature=20.0,\n    incident_par=300.0,\n    duration=Day(1),\n)\n\nglobal_model = CompositeModel(\n    Object(:plant; scale=:Plant, kind=:plant);\n    applications=(\n        ModelSpec(\n            ToyDegreeDaysCumulModel();\n            name=:degree_days,\n            on=One(scale=:Plant),\n            environment=Environment(\n                provider=:global,\n                sources=(T=:air_temperature,),\n            ),\n        ),\n        ModelSpec(\n            ToyLAIModel();\n            name=:lai,\n            on=One(scale=:Plant),\n        ),\n        ModelSpec(\n            Beer(0.6);\n            name=:light,\n            on=One(scale=:Plant),\n            environment=Environment(\n                provider=:global,\n                sources=(Ri_PAR_f=:incident_par,),\n            ),\n        ),\n    ),\n    environment=forcing,\n)\n\nvalidate_environment_inputs(global_model)\nglobal_simulation = run!(global_model)\nglobal_state = final_state(global_simulation)\n(TT_cu=global_state.TT_cu, LAI=global_state.LAI, aPPFD=global_state.aPPFD)\n\nThe environment diagnostic distinguishes the variables seen by each model from the actual source names:\n\nselect(\n    DataFrame(Diagnostics.explain_environment_bindings(global_model)),\n    :application_id,\n    :object_id,\n    :required_inputs,\n    :source_inputs,\n    :handle,\n)\n\nGlobal sampling has no spatial handle. The forcing above is intentionally strict: apart from timeline duration, it exposes only the two remapped source variables. Removing either source makes validate_environment_inputs fail before simulation.","title":"Global sampling and source names"},{"category":"section","location":"journeys/users/environments/#Spatial-sampling","page":"Read an environment","text":"Spatial backends keep the same model-facing declaration. They additionally compile an opaque handle for each application/object target. The small ToySpatialEnvironment example maps object geometry to either a sunny or shaded cell:\n\nspatial_environment = ToySpatialEnvironment(\n    Dict(\n        :sun => (Ri_PAR_f=400.0,),\n        :shade => (Ri_PAR_f=100.0,),\n    );\n    step_seconds=3600.0,\n)\n\nspatial_model = CompositeModel(\n    Object(\n        :sun_leaf;\n        scale=:Leaf,\n        kind=:leaf,\n        geometry=(cell=:sun,),\n        status=Status(LAI=2.0),\n    ),\n    Object(\n        :shade_leaf;\n        scale=:Leaf,\n        kind=:leaf,\n        geometry=(cell=:shade,),\n        status=Status(LAI=2.0),\n    );\n    applications=(\n        ModelSpec(\n            Beer(0.6);\n            name=:light,\n            on=Many(scale=:Leaf),\n            environment=Environment(backend=spatial_environment),\n        ),\n    ),\n)\n\nspatial_simulation = run!(spatial_model)\nspatial_states = final_state(spatial_simulation, Many(scale=:Leaf))\nDict(id => state.aPPFD for (id, state) in spatial_states)\n\nThe one Many application retains two distinct compiled handles:\n\nselect(\n    DataFrame(Diagnostics.explain_environment_bindings(spatial_model)),\n    :application_id,\n    :object_id,\n    :geometry_source,\n    :handle,\n)\n\nThe scientific Beer kernel is unchanged. It sees only environment.Ri_PAR_f; the backend owns the meaning of each handle. Backend authors can inspect the implementation of ToySpatialEnvironment in the environment extension reference.","title":"Spatial sampling"},{"category":"section","location":"journeys/users/environments/#Page-recap","page":"Read an environment","text":"You added: explicit environment declarations, global source remapping, and then a spatial backend with object geometry.\nPlantSimEngine inferred: global sampling, validation of required source names, and one cached spatial handle per application/object target.\nYou keep explicit: model-facing environment names, scenario source remaps, provider/backend choice, and geometry used by a spatial backend.\nNew API names: environment_inputs_, Environment, validate_environment_inputs, ToySpatialEnvironment, and Diagnostics.explain_environment_bindings.","title":"Page recap"},{"category":"section","location":"API/API_examples/#Example-models","page":"Example models","text":"PlantSimEngine provides example processes and models to users. They are available from a sub-module called Examples. To get access to these models in a working environment with PlantSimEngine, you can simply use this sub-module:\n\nusing PlantSimEngine.Examples","title":"Example models"},{"category":"section","location":"API/API_examples/#List","page":"Example models","text":"Pages = [\"API_examples.md\"]","title":"List"},{"category":"section","location":"API/API_examples/#Details","page":"Example models","text":"","title":"Details"},{"category":"module","location":"API/API_examples/#PlantSimEngine.Examples","page":"Example models","text":"A sub-module with example models.\n\nExamples used in the documentation for a set of multiscale models. The models can be found in the examples folder of the package, and are stored  in the following files:\n\nToyAssimModel.jl\nToyCDemandModel.jl\nToyCAllocationModel.jl\nToySoilModel.jl\n\nExamples\n\nusing PlantSimEngine\nusing PlantSimEngine.Examples\nToyAssimModel()\n\n\n\n\n\n","title":"PlantSimEngine.Examples"},{"category":"type","location":"API/API_examples/#PlantSimEngine.Examples.AbstractCarbon_AllocationModel","page":"Example models","text":"carbon_allocation process abstract model. \n\nAll models implemented to simulate the carbon_allocation process must be a subtype of this type, e.g.  struct MyCarbon_AllocationModel <: AbstractCarbon_AllocationModel end.\n\nYou can list all models implementing this process using subtypes:\n\nExamples\n\nsubtypes(AbstractCarbon_AllocationModel)\n\n\n\n\n\n","title":"PlantSimEngine.Examples.AbstractCarbon_AllocationModel"},{"category":"type","location":"API/API_examples/#PlantSimEngine.Examples.AbstractCarbon_AssimilationModel","page":"Example models","text":"carbon_assimilation process abstract model. \n\nAll models implemented to simulate the carbon_assimilation process must be a subtype of this type, e.g.  struct MyCarbon_AssimilationModel <: AbstractCarbon_AssimilationModel end.\n\nYou can list all models implementing this process using subtypes:\n\nExamples\n\nsubtypes(AbstractCarbon_AssimilationModel)\n\n\n\n\n\n","title":"PlantSimEngine.Examples.AbstractCarbon_AssimilationModel"},{"category":"type","location":"API/API_examples/#PlantSimEngine.Examples.AbstractCarbon_BiomassModel","page":"Example models","text":"carbon_biomass process abstract model. \n\nAll models implemented to simulate the carbon_biomass process must be a subtype of this type, e.g.  struct MyCarbon_BiomassModel <: AbstractCarbon_BiomassModel end.\n\nYou can list all models implementing this process using subtypes:\n\nExamples\n\nsubtypes(AbstractCarbon_BiomassModel)\n\n\n\n\n\n","title":"PlantSimEngine.Examples.AbstractCarbon_BiomassModel"},{"category":"type","location":"API/API_examples/#PlantSimEngine.Examples.AbstractCarbon_DemandModel","page":"Example models","text":"carbon_demand process abstract model. \n\nAll models implemented to simulate the carbon_demand process must be a subtype of this type, e.g.  struct MyCarbon_DemandModel <: AbstractCarbon_DemandModel end.\n\nYou can list all models implementing this process using subtypes:\n\nExamples\n\nsubtypes(AbstractCarbon_DemandModel)\n\n\n\n\n\n","title":"PlantSimEngine.Examples.AbstractCarbon_DemandModel"},{"category":"type","location":"API/API_examples/#PlantSimEngine.Examples.AbstractDegreedaysModel","page":"Example models","text":"Degreedays process abstract model. \n\nAll models implemented to simulate the Degreedays process must be a subtype of this type, e.g.  struct MyDegreedaysModel <: AbstractDegreedaysModel end.\n\nYou can list all models implementing this process using subtypes:\n\nExamples\n\nsubtypes(AbstractDegreedaysModel)\n\n\n\n\n\n","title":"PlantSimEngine.Examples.AbstractDegreedaysModel"},{"category":"type","location":"API/API_examples/#PlantSimEngine.Examples.AbstractGrowthModel","page":"Example models","text":"growth process abstract model. \n\nAll models implemented to simulate the growth process must be a subtype of this type, e.g.  struct MyGrowthModel <: AbstractGrowthModel end.\n\nYou can list all models implementing this process using subtypes:\n\nExamples\n\nsubtypes(AbstractGrowthModel)\n\n\n\n\n\n","title":"PlantSimEngine.Examples.AbstractGrowthModel"},{"category":"type","location":"API/API_examples/#PlantSimEngine.Examples.AbstractLai_DynamicModel","page":"Example models","text":"LAI_Dynamic process abstract model. \n\nAll models implemented to simulate the LAI_Dynamic process must be a subtype of this type, e.g.  struct MyLai_DynamicModel <: AbstractLai_DynamicModel end.\n\nYou can list all models implementing this process using subtypes:\n\nExamples\n\nsubtypes(AbstractLai_DynamicModel)\n\n\n\n\n\n","title":"PlantSimEngine.Examples.AbstractLai_DynamicModel"},{"category":"type","location":"API/API_examples/#PlantSimEngine.Examples.AbstractLeaf_SurfaceModel","page":"Example models","text":"leaf_surface process abstract model. \n\nAll models implemented to simulate the leaf_surface process must be a subtype of this type, e.g.  struct MyLeaf_SurfaceModel <: AbstractLeaf_SurfaceModel end.\n\nYou can list all models implementing this process using subtypes:\n\nExamples\n\nsubtypes(AbstractLeaf_SurfaceModel)\n\n\n\n\n\n","title":"PlantSimEngine.Examples.AbstractLeaf_SurfaceModel"},{"category":"type","location":"API/API_examples/#PlantSimEngine.Examples.AbstractLight_InterceptionModel","page":"Example models","text":"light_interception process abstract model. \n\nAll models implemented to simulate the light_interception process must be a subtype of this type, e.g.  struct MyLight_InterceptionModel <: AbstractLight_InterceptionModel end.\n\nYou can list all models implementing this process using subtypes:\n\nExamples\n\nsubtypes(AbstractLight_InterceptionModel)\n\n\n\n\n\n","title":"PlantSimEngine.Examples.AbstractLight_InterceptionModel"},{"category":"type","location":"API/API_examples/#PlantSimEngine.Examples.AbstractLight_PartitioningModel","page":"Example models","text":"light_partitioning process abstract model. \n\nAll models implemented to simulate the light_partitioning process must be a subtype of this type, e.g.  struct MyLight_PartitioningModel <: AbstractLight_PartitioningModel end.\n\nYou can list all models implementing this process using subtypes:\n\nExamples\n\nsubtypes(AbstractLight_PartitioningModel)\n\n\n\n\n\n","title":"PlantSimEngine.Examples.AbstractLight_PartitioningModel"},{"category":"type","location":"API/API_examples/#PlantSimEngine.Examples.AbstractMaintenance_RespirationModel","page":"Example models","text":"maintenance_respiration process abstract model. \n\nAll models implemented to simulate the maintenance_respiration process must be a subtype of this type, e.g.  struct MyMaintenance_RespirationModel <: AbstractMaintenance_RespirationModel end.\n\nYou can list all models implementing this process using subtypes:\n\nExamples\n\nsubtypes(AbstractMaintenance_RespirationModel)\n\n\n\n\n\n","title":"PlantSimEngine.Examples.AbstractMaintenance_RespirationModel"},{"category":"type","location":"API/API_examples/#PlantSimEngine.Examples.AbstractProcess1Model","page":"Example models","text":"process1 process abstract model. \n\nAll models implemented to simulate the process1 process must be a subtype of this type, e.g.  struct MyProcess1Model <: AbstractProcess1Model end.\n\nYou can list all models implementing this process using subtypes:\n\nExamples\n\nsubtypes(AbstractProcess1Model)\n\n\n\n\n\n","title":"PlantSimEngine.Examples.AbstractProcess1Model"},{"category":"type","location":"API/API_examples/#PlantSimEngine.Examples.AbstractProcess2Model","page":"Example models","text":"process2 process abstract model. \n\nAll models implemented to simulate the process2 process must be a subtype of this type, e.g.  struct MyProcess2Model <: AbstractProcess2Model end.\n\nYou can list all models implementing this process using subtypes:\n\nExamples\n\nsubtypes(AbstractProcess2Model)\n\n\n\n\n\n","title":"PlantSimEngine.Examples.AbstractProcess2Model"},{"category":"type","location":"API/API_examples/#PlantSimEngine.Examples.AbstractProcess3Model","page":"Example models","text":"process3 process abstract model. \n\nAll models implemented to simulate the process3 process must be a subtype of this type, e.g.  struct MyProcess3Model <: AbstractProcess3Model end.\n\nYou can list all models implementing this process using subtypes:\n\nExamples\n\nsubtypes(AbstractProcess3Model)\n\n\n\n\n\n","title":"PlantSimEngine.Examples.AbstractProcess3Model"},{"category":"type","location":"API/API_examples/#PlantSimEngine.Examples.AbstractProcess4Model","page":"Example models","text":"process4 process abstract model. \n\nAll models implemented to simulate the process4 process must be a subtype of this type, e.g.  struct MyProcess4Model <: AbstractProcess4Model end.\n\nYou can list all models implementing this process using subtypes:\n\nExamples\n\nsubtypes(AbstractProcess4Model)\n\n\n\n\n\n","title":"PlantSimEngine.Examples.AbstractProcess4Model"},{"category":"type","location":"API/API_examples/#PlantSimEngine.Examples.AbstractProcess5Model","page":"Example models","text":"process5 process abstract model. \n\nAll models implemented to simulate the process5 process must be a subtype of this type, e.g.  struct MyProcess5Model <: AbstractProcess5Model end.\n\nYou can list all models implementing this process using subtypes:\n\nExamples\n\nsubtypes(AbstractProcess5Model)\n\n\n\n\n\n","title":"PlantSimEngine.Examples.AbstractProcess5Model"},{"category":"type","location":"API/API_examples/#PlantSimEngine.Examples.AbstractProcess6Model","page":"Example models","text":"process6 process abstract model. \n\nAll models implemented to simulate the process6 process must be a subtype of this type, e.g.  struct MyProcess6Model <: AbstractProcess6Model end.\n\nYou can list all models implementing this process using subtypes:\n\nExamples\n\nsubtypes(AbstractProcess6Model)\n\n\n\n\n\n","title":"PlantSimEngine.Examples.AbstractProcess6Model"},{"category":"type","location":"API/API_examples/#PlantSimEngine.Examples.AbstractProcess7Model","page":"Example models","text":"process7 process abstract model. \n\nAll models implemented to simulate the process7 process must be a subtype of this type, e.g.  struct MyProcess7Model <: AbstractProcess7Model end.\n\nYou can list all models implementing this process using subtypes:\n\nExamples\n\nsubtypes(AbstractProcess7Model)\n\n\n\n\n\n","title":"PlantSimEngine.Examples.AbstractProcess7Model"},{"category":"type","location":"API/API_examples/#PlantSimEngine.Examples.AbstractSoil_WaterModel","page":"Example models","text":"soil_water process abstract model. \n\nAll models implemented to simulate the soil_water process must be a subtype of this type, e.g.  struct MySoil_WaterModel <: AbstractSoil_WaterModel end.\n\nYou can list all models implementing this process using subtypes:\n\nExamples\n\nsubtypes(AbstractSoil_WaterModel)\n\n\n\n\n\n","title":"PlantSimEngine.Examples.AbstractSoil_WaterModel"},{"category":"type","location":"API/API_examples/#PlantSimEngine.Examples.AbstractToy_Daily_DevelopmentModel","page":"Example models","text":"toy_daily_development process abstract model. \n\nAll models implemented to simulate the toy_daily_development process must be a subtype of this type, e.g.  struct MyToy_Daily_DevelopmentModel <: AbstractToy_Daily_DevelopmentModel end.\n\nYou can list all models implementing this process using subtypes:\n\nExamples\n\nsubtypes(AbstractToy_Daily_DevelopmentModel)\n\n\n\n\n\n","title":"PlantSimEngine.Examples.AbstractToy_Daily_DevelopmentModel"},{"category":"type","location":"API/API_examples/#PlantSimEngine.Examples.AbstractToy_DevelopmentModel","page":"Example models","text":"toy_development process abstract model. \n\nAll models implemented to simulate the toy_development process must be a subtype of this type, e.g.  struct MyToy_DevelopmentModel <: AbstractToy_DevelopmentModel end.\n\nYou can list all models implementing this process using subtypes:\n\nExamples\n\nsubtypes(AbstractToy_DevelopmentModel)\n\n\n\n\n\n","title":"PlantSimEngine.Examples.AbstractToy_DevelopmentModel"},{"category":"type","location":"API/API_examples/#PlantSimEngine.Examples.AbstractToy_Environment_ControllerModel","page":"Example models","text":"toy_environment_controller process abstract model. \n\nAll models implemented to simulate the toy_environment_controller process must be a subtype of this type, e.g.  struct MyToy_Environment_ControllerModel <: AbstractToy_Environment_ControllerModel end.\n\nYou can list all models implementing this process using subtypes:\n\nExamples\n\nsubtypes(AbstractToy_Environment_ControllerModel)\n\n\n\n\n\n","title":"PlantSimEngine.Examples.AbstractToy_Environment_ControllerModel"},{"category":"type","location":"API/API_examples/#PlantSimEngine.Examples.AbstractToy_Environment_ReaderModel","page":"Example models","text":"toy_environment_reader process abstract model. \n\nAll models implemented to simulate the toy_environment_reader process must be a subtype of this type, e.g.  struct MyToy_Environment_ReaderModel <: AbstractToy_Environment_ReaderModel end.\n\nYou can list all models implementing this process using subtypes:\n\nExamples\n\nsubtypes(AbstractToy_Environment_ReaderModel)\n\n\n\n\n\n","title":"PlantSimEngine.Examples.AbstractToy_Environment_ReaderModel"},{"category":"type","location":"API/API_examples/#PlantSimEngine.Examples.AbstractToy_Selective_Call_ControllerModel","page":"Example models","text":"toy_selective_call_controller process abstract model. \n\nAll models implemented to simulate the toy_selective_call_controller process must be a subtype of this type, e.g.  struct MyToy_Selective_Call_ControllerModel <: AbstractToy_Selective_Call_ControllerModel end.\n\nYou can list all models implementing this process using subtypes:\n\nExamples\n\nsubtypes(AbstractToy_Selective_Call_ControllerModel)\n\n\n\n\n\n","title":"PlantSimEngine.Examples.AbstractToy_Selective_Call_ControllerModel"},{"category":"type","location":"API/API_examples/#PlantSimEngine.Examples.AbstractToy_Stock_WriterModel","page":"Example models","text":"toy_stock_writer process abstract model. \n\nAll models implemented to simulate the toy_stock_writer process must be a subtype of this type, e.g.  struct MyToy_Stock_WriterModel <: AbstractToy_Stock_WriterModel end.\n\nYou can list all models implementing this process using subtypes:\n\nExamples\n\nsubtypes(AbstractToy_Stock_WriterModel)\n\n\n\n\n\n","title":"PlantSimEngine.Examples.AbstractToy_Stock_WriterModel"},{"category":"type","location":"API/API_examples/#PlantSimEngine.Examples.Beer","page":"Example models","text":"Beer(k)\n\nBeer-Lambert law for light interception.\n\nRequired inputs: LAI in m[leaf]² m[ground]⁻². Required environment input: Ri_PAR_f, the incident flux of atmospheric radiation in the PAR, in W m[ground]⁻² (== J m[ground]⁻² s⁻¹).\n\nOutput: aPPFD, the canopy-absorbed Photosynthetic Photon Flux Density in μmol[PAR] m[ground]⁻² s⁻¹. It is not a mean leaf-area-basis PPFD.\n\n\n\n\n\n","title":"PlantSimEngine.Examples.Beer"},{"category":"type","location":"API/API_examples/#PlantSimEngine.Examples.Process1Model","page":"Example models","text":"Process1Model(a)\n\nA dummy model implementing a \"process1\" process for testing purposes.\n\n\n\n\n\n","title":"PlantSimEngine.Examples.Process1Model"},{"category":"type","location":"API/API_examples/#PlantSimEngine.Examples.Process2Model","page":"Example models","text":"Process2Model()\n\nA dummy model implementing a \"process2\" process for testing purposes.\n\n\n\n\n\n","title":"PlantSimEngine.Examples.Process2Model"},{"category":"type","location":"API/API_examples/#PlantSimEngine.Examples.Process3Model","page":"Example models","text":"Process3Model()\n\nA dummy model implementing a \"process3\" process for testing purposes.\n\n\n\n\n\n","title":"PlantSimEngine.Examples.Process3Model"},{"category":"type","location":"API/API_examples/#PlantSimEngine.Examples.Process4Model","page":"Example models","text":"Process4Model()\n\nA dummy model implementing a \"process4\" process for testing purposes. It computes the inputs needed for the coupled processes 1-2-3.\n\n\n\n\n\n","title":"PlantSimEngine.Examples.Process4Model"},{"category":"type","location":"API/API_examples/#PlantSimEngine.Examples.Process5Model","page":"Example models","text":"Process5Model()\n\nA dummy model implementing a \"process5\" process for testing purposes. It needs the outputs from the coupled processes 1-2-3.\n\n\n\n\n\n","title":"PlantSimEngine.Examples.Process5Model"},{"category":"type","location":"API/API_examples/#PlantSimEngine.Examples.Process6Model","page":"Example models","text":"Process6Model()\n\nA dummy model implementing a \"process6\" process for testing purposes. It needs the outputs from the coupled processes 1-2-3, but also from process 7 that is itself independant.\n\n\n\n\n\n","title":"PlantSimEngine.Examples.Process6Model"},{"category":"type","location":"API/API_examples/#PlantSimEngine.Examples.Process7Model","page":"Example models","text":"Process7Model()\n\nA dummy model implementing a \"process7\" process for testing purposes. It is independent (needs :var0 only as for Process4Model), but its outputs are used by Process6Model, so it is a soft-coupling.\n\n\n\n\n\n","title":"PlantSimEngine.Examples.Process7Model"},{"category":"type","location":"API/API_examples/#PlantSimEngine.Examples.ToyAssimGrowthModel","page":"Example models","text":"ToyAssimGrowthModel(Rm_factor, Rg_cost)\nToyAssimGrowthModel(; LUE=0.2, Rm_factor = 0.5, Rg_cost = 1.2)\n\nComputes the biomass growth of a plant.\n\nArguments\n\nLUE=0.2: the light use efficiency, in gC mol[PAR]⁻¹\nRm_factor=0.5: the fraction of assimilation that goes into maintenance respiration\nRg_cost=1.2: the cost of growth maintenance, in gram of carbon biomass per gram of assimilate\n\nInputs\n\naPPFD: the absorbed photosynthetic photon flux density, in mol[PAR] m⁻² time-step⁻¹\n\nOutputs\n\ncarbon_assimilation: the assimilation, in gC m⁻² time-step⁻¹\nRm: the maintenance respiration, in gC m⁻² time-step⁻¹\nRg: the growth respiration, in gC m⁻² time-step⁻¹\nbiomass_increment: the daily biomass increment, in gC m⁻² time-step⁻¹\nbiomass: the plant biomass, in gC m⁻² time-step⁻¹\n\n\n\n\n\n","title":"PlantSimEngine.Examples.ToyAssimGrowthModel"},{"category":"type","location":"API/API_examples/#PlantSimEngine.Examples.ToyAssimModel","page":"Example models","text":"ToyAssimModel(LUE)\n\nComputes the assimilation of a plant (= photosynthesis).\n\nArguments\n\nLUE=0.2: the light use efficiency, in gC mol[PAR]⁻¹\n\nInputs\n\naPPFD: the absorbed photosynthetic photon flux density, in mol[PAR] m⁻² time-step⁻¹\nsoil_water_content: the soil water content, in %\n\nOutputs\n\ncarbon_assimilation: the assimilation or photosynthesis, also sometimes denoted A, in gC m⁻² time-step⁻¹\n\nDetails\n\nThe assimilation is computed as the product of the absorbed photosynthetic photon flux density (aPPFD) and the light use efficiency (LUE), so the units of the assimilation usually are in gC m⁻² time-step⁻¹, but they could be in another spatial or temporal unit depending on the unit of aPPFD, e.g.  if aPPFD is in mol[PAR] plant⁻¹ time-step⁻¹, the assimilation will be in gC plant⁻¹ time-step⁻¹.\n\n\n\n\n\n","title":"PlantSimEngine.Examples.ToyAssimModel"},{"category":"type","location":"API/API_examples/#PlantSimEngine.Examples.ToyCAllocationModel","page":"Example models","text":"ToyCAllocationModel()\n\nComputes the carbon allocation to each organ of a plant based on the plant total carbon offer and individual organ demand. This model should be used at the plant scale, because it first computes the carbon availaible for allocation as the minimum between the total demand  (sum of organs' demand) and total carbon offer (sum of organs' assimilation - total maintenance respiration), and then allocates the carbon relative  to each organ's demand.\n\nInputs\n\ncarbon_assimilation: a vector of the assimilation of all photosynthetic organs, usually in gC m⁻² time-step⁻¹\nRm: the maintenance respiration of the plant, usually in gC m⁻² time-step⁻¹\ncarbon_demand: a vector of the carbon demand of the organs, usually in gC m⁻² time-step⁻¹\n\nOutputs\n\ncarbon_assimilation: the carbon assimilation, usually in gC m⁻² time-step⁻¹\n\nDetails\n\nThe units usually are in gC m⁻² time-step⁻¹, but they could be in another spatial or temporal unit depending on the unit of the inputs, e.g. in gC plant⁻¹ time-step⁻¹.\n\n\n\n\n\n","title":"PlantSimEngine.Examples.ToyCAllocationModel"},{"category":"type","location":"API/API_examples/#PlantSimEngine.Examples.ToyCBiomassModel","page":"Example models","text":"ToyCBiomassModel(construction_cost)\n\nComputes the carbon biomass of an organ based on the carbon allocation and construction cost.\n\nArguments\n\nconstruction_cost: the construction cost of the organ, usually in gC gC⁻¹. Should be understood as the amount of carbon needed to build 1g of carbon biomass.\n\nInputs\n\ncarbon_allocation: the carbon allocation to the organ for the time-step, usually in gC m⁻² time-step⁻¹\n\nOutputs\n\ncarbon_biomass_increment: the increment of carbon biomass, usually in gC time-step⁻¹\ncarbon_biomass: the carbon biomass, usually in gC\ngrowth_respiration: the growth respiration, usually in gC time-step⁻¹\n\n\n\n\n\n","title":"PlantSimEngine.Examples.ToyCBiomassModel"},{"category":"type","location":"API/API_examples/#PlantSimEngine.Examples.ToyCDemandModel","page":"Example models","text":"ToyCDemandModel(optimal_biomass, development_duration)\nToyCDemandModel(; optimal_biomass, development_duration)\n\nComputes the carbon demand of an organ depending on its biomass under optimal conditions and the duration of its development in degree days. The model assumes that the carbon demand is linear througout the duration of the development.\n\nArguments\n\noptimal_biomass: the biomass of the organ under optimal conditions, in gC\ndevelopment_duration: the duration of the development of the organ, in degree days\n\nInputs\n\nTT: the thermal time, in degree days\n\nOutputs\n\ncarbon_demand: the carbon demand, in gC\n\n\n\n\n\n","title":"PlantSimEngine.Examples.ToyCDemandModel"},{"category":"type","location":"API/API_examples/#PlantSimEngine.Examples.ToyDailyDevelopmentModel","page":"Example models","text":"ToyDailyDevelopmentModel(increment)\n\nAccumulate one configured growth increment every 24 simulation steps. The model declares this default cadence and hold-last output semantics.\n\n\n\n\n\n","title":"PlantSimEngine.Examples.ToyDailyDevelopmentModel"},{"category":"type","location":"API/API_examples/#PlantSimEngine.Examples.ToyDegreeDaysCumulModel","page":"Example models","text":"ToyDegreeDaysCumulModel(;init_TT=0.0, T_base=10.0, T_max=43.0)\n\nComputes the thermal time in degree days and cumulated degree-days based on the average daily temperature (T), the initial cumulated degree days, the base temperature below which there is no growth, and the maximum  temperature for growh.\n\n\n\n\n\n","title":"PlantSimEngine.Examples.ToyDegreeDaysCumulModel"},{"category":"type","location":"API/API_examples/#PlantSimEngine.Examples.ToyDevelopmentModel","page":"Example models","text":"ToyDevelopmentModel(efficiency)\n\nCompute one growth increment from required thermal time and an optional stress factor.\n\nInputs\n\nTT: required thermal time for the current step.\nstress: dimensionless stress factor, defaulting to 1.0.\n\nOutputs\n\ngrowth: growth increment for the current step.\n\n\n\n\n\n","title":"PlantSimEngine.Examples.ToyDevelopmentModel"},{"category":"type","location":"API/API_examples/#PlantSimEngine.Examples.ToyEnvironmentControllerModel","page":"Example models","text":"ToyEnvironmentControllerModel(trial_temperature, accepted_temperature)\n\nDemonstrate a typed trial environment followed by one accepted environment commit and publication.\n\n\n\n\n\n","title":"PlantSimEngine.Examples.ToyEnvironmentControllerModel"},{"category":"type","location":"API/API_examples/#PlantSimEngine.Examples.ToyEnvironmentHandle","page":"Example models","text":"ToyEnvironmentHandle\n\nOpaque compiled handle returned by ToySpatialEnvironment.\n\n\n\n\n\n","title":"PlantSimEngine.Examples.ToyEnvironmentHandle"},{"category":"type","location":"API/API_examples/#PlantSimEngine.Examples.ToyEnvironmentReaderModel","page":"Example models","text":"ToyEnvironmentReaderModel()\n\nRead temperature from the model-facing environment.\n\n\n\n\n\n","title":"PlantSimEngine.Examples.ToyEnvironmentReaderModel"},{"category":"type","location":"API/API_examples/#PlantSimEngine.Examples.ToyLAIModel","page":"Example models","text":"ToyLAIModel(;max_lai=8.0, dd_incslope=800, inc_slope=110, dd_decslope=1500, dec_slope=20)\n\nComputes the Leaf Area Index (LAI) based on a sigmoid function of thermal time.\n\nArguments\n\nmax_lai: the maximum LAI value\ndd_incslope: the thermal time at which the LAI starts to increase\ninc_slope: the slope of the increase\ndd_decslope: the thermal time at which the LAI starts to decrease\ndec_slope: the slope of the decrease\n\nInputs\n\nTT_cu: the cumulated thermal time since the beginning of the simulation, usually in °C days\n\nOutputs\n\nLAI: the Leaf Area Index, usually in m² m⁻²\n\n\n\n\n\n","title":"PlantSimEngine.Examples.ToyLAIModel"},{"category":"type","location":"API/API_examples/#PlantSimEngine.Examples.ToyLAIfromLeafAreaModel","page":"Example models","text":"ToyLAIfromLeafAreaModel()\n\nComputes the Leaf Area Index (LAI) of the model based on the plants leaf area.\n\nArguments\n\nscene_area: the area of the model, usually in m²\n\nInputs\n\nsurface: a vector of plant leaf surfaces, usually in m²\n\nOutputs\n\nLAI: the Leaf Area Index of the model, usually in m² m⁻²\ntotal_surface: the total surface of the plants, usually in m²\n\n\n\n\n\n","title":"PlantSimEngine.Examples.ToyLAIfromLeafAreaModel"},{"category":"type","location":"API/API_examples/#PlantSimEngine.Examples.ToyLeafSurfaceModel","page":"Example models","text":"ToyLeafSurfaceModel(SLA)\n\nComputes the individual leaf surface from its biomass using the SLA.\n\nArguments\n\nSLA: the specific leaf area, usually in m² gC⁻¹. Should be understood as the surface area of a leaf per unit of carbon biomass.\n\nValues typically range from 0.002 to 0.027 m² gC⁻¹.\n\nInputs\n\ncarbon_biomass: the carbon biomass of the leaf, usually in gC\n\nOutputs\n\nsurface: the leaf surface, usually in m²\n\n\n\n\n\n","title":"PlantSimEngine.Examples.ToyLeafSurfaceModel"},{"category":"type","location":"API/API_examples/#PlantSimEngine.Examples.ToyLightPartitioningModel","page":"Example models","text":"ToyLightPartitioningModel()\n\nComputes the light partitioning based on relative surface.\n\nInputs\n\naPPFD: the absorbed photosynthetic photon flux density at the larger scale (e.g. model), in mol[PAR] m⁻² time-step⁻¹\n\nOutputs\n\naPPFD: the assimilation or photosynthesis, also sometimes denoted A, in gC time-step⁻¹\n\nDetails\n\n\n\n\n\n","title":"PlantSimEngine.Examples.ToyLightPartitioningModel"},{"category":"type","location":"API/API_examples/#PlantSimEngine.Examples.ToyMaintenanceRespirationModel","page":"Example models","text":"RmQ10FixedN(Q10, Rm_base, T_ref, P_alive, nitrogen_content)\n\nMaintenance respiration based on a Q10 computation with fixed nitrogen values  and proportion of living cells in the organs.\n\nArguments\n\nQ10: Q10 factor (values should usually range between: 1.5 - 2.5, with 2.1 being the most common value)\nRm_base: Base maintenance respiration (gC gDM⁻¹ time-step⁻¹). Should be around 0.06.\nT_ref: Reference temperature at which Q10 was measured (usually around 25.0°C)\nP_alive: proportion of living cells in the organ\nnitrogen_content: nitrogen content of the organ (gN gC⁻¹)\n\nInputs\n\ncarbon_biomass: the carbon biomass of the organ in gC\n\n\n\n\n\n","title":"PlantSimEngine.Examples.ToyMaintenanceRespirationModel"},{"category":"type","location":"API/API_examples/#PlantSimEngine.Examples.ToyPlantLeafSurfaceModel","page":"Example models","text":"ToyPlantLeafSurfaceModel()\n\nComputes the leaf surface at plant scale by summing the individual leaf surfaces.\n\nInputs\n\nleaf_surfaces: a vector of leaf surfaces, usually in m²\n\nOutputs\n\nsurface: the leaf surface at plant scale, usually in m²\n\n\n\n\n\n","title":"PlantSimEngine.Examples.ToyPlantLeafSurfaceModel"},{"category":"type","location":"API/API_examples/#PlantSimEngine.Examples.ToyPlantRmModel","page":"Example models","text":"ToyPlantRmModel()\n\nTotal plant maintenance respiration based on the sum of Rm_organs, the maintenance respiration of the organs.\n\nInputs\n\nRm_organs: a vector of maintenance respiration from all organs in the plant in gC time-step⁻¹\n\nOutputs\n\nRm: the total plant maintenance respiration in gC time-step⁻¹\n\n\n\n\n\n","title":"PlantSimEngine.Examples.ToyPlantRmModel"},{"category":"type","location":"API/API_examples/#PlantSimEngine.Examples.ToyRUEGrowthModel","page":"Example models","text":"ToyRUEGrowthModel(efficiency)\n\nComputes the carbon biomass increment of a plant based on the radiation use efficiency principle.\n\nArguments\n\nefficiency: the radiation use efficiency, in gC[biomass] mol[PAR]⁻¹\n\nInputs\n\naPPFD: the absorbed photosynthetic photon flux density, in mol[PAR] m⁻² time-step⁻¹\n\nOutputs\n\nbiomass_increment: the daily biomass increment, in gC[biomass] m⁻² time-step⁻¹\nbiomass: the plant biomass, in gC[biomass] m⁻² time-step⁻¹\n\n\n\n\n\n","title":"PlantSimEngine.Examples.ToyRUEGrowthModel"},{"category":"type","location":"API/API_examples/#PlantSimEngine.Examples.ToySelectiveCallControllerModel","page":"Example models","text":"ToySelectiveCallControllerModel(\n    trial_temperatures,\n    accepted_temperature;\n    selected_object,\n)\n\nResolve several hard-call targets, run selected_object for several unpublished trials, then publish one accepted result.\n\n\n\n\n\n","title":"PlantSimEngine.Examples.ToySelectiveCallControllerModel"},{"category":"type","location":"API/API_examples/#PlantSimEngine.Examples.ToySoilWaterModel","page":"Example models","text":"ToySoilWaterModel(values=[0.5])\n\nA toy model to compute the soil water content. The model simply take a random value in the values range using rand.\n\nOutputs\n\nsoil_water_content: the soil water content (%).\n\nArguments\n\nvalues: a range of soil_water_content values to sample from. Can be a vector of values [0.5,0.6] or a range 0.1:0.1:1.0. Default is [0.5].\n\n\n\n\n\n","title":"PlantSimEngine.Examples.ToySoilWaterModel"},{"category":"type","location":"API/API_examples/#PlantSimEngine.Examples.ToySpatialEnvironment","page":"Example models","text":"ToySpatialEnvironment(cells; step_seconds=3600.0)\n\nA minimal spatial environment for examples and tests.\n\ncells maps cell ids to named tuples of environment variables. Objects select a cell with geometry such as (cell=:sun,). PlantSimEngine compiles that cell id into a ToyEnvironmentHandle, so sampling does not resolve geometry inside the model kernel loop. An application configured with sink=:cells may also commit an accepted named-tuple state to its bound cell.\n\n\n\n\n\n","title":"PlantSimEngine.Examples.ToySpatialEnvironment"},{"category":"type","location":"API/API_examples/#PlantSimEngine.Examples.ToyStockWriterModel","page":"Example models","text":"ToyStockWriterModel(value)\n\nWrite one configured stock value. Several named applications of this model can demonstrate canonical writer ordering and stream-only output routing.\n\n\n\n\n\n","title":"PlantSimEngine.Examples.ToyStockWriterModel"},{"category":"method","location":"API/API_examples/#PlantSimEngine.Examples.import_mtg_example-Tuple{}","page":"Example models","text":"import_mtg_example()\n\nReturns an example multiscale tree graph (MTG) with a model, a soil, and a plant with two internodes and two leaves.\n\nExamples\n\njulia> using PlantSimEngine.Examples\n\njulia> import_mtg_example()\n/ 1: Scene\n├─ / 2: Soil\n└─ + 3: Plant\n   └─ / 4: Internode\n      ├─ + 5: Leaf\n      └─ < 6: Internode\n         └─ + 7: Leaf\n\n\n\n\n\n","title":"PlantSimEngine.Examples.import_mtg_example"},{"category":"method","location":"API/API_examples/#PlantSimEngine.fit-Tuple{Type{Beer}, Any}","page":"Example models","text":"fit(::Type{Beer}, df; J_to_umol=PlantMeteo.Constants().J_to_umol)\n\nCompute the k parameter of the Beer-Lambert law from measurements.\n\nArguments\n\n::Type{Beer}: the model type\ndf: a DataFrame with the following columns:\naPPFD: canopy-absorbed Photosynthetic Photon Flux Density in μmol[PAR] m[ground]⁻² s⁻¹\nLAI: leaf area index in m[leaf]² m[ground]⁻²\nRi_PAR_f: incident PAR flux in W m[ground]⁻² (== J m[ground]⁻² s⁻¹)\n\naPPFD and Ri_PAR_f * J_to_umol must use the same ground-area basis. LAI and the converted incident flux must be finite and strictly positive, and the implied absorbed fraction must satisfy 0 ≤ f_abs < 1.\n\nExamples\n\nImport the example models defined in the Examples sub-module:\n\nusing PlantSimEngine, PlantMeteo, DataFrames\nusing PlantSimEngine.Examples\n\nCreate a CompositeModel with one canopy model on a plant object, then fit Beer to the data:\n\nmeteo = Atmosphere(\n    T=20.0,\n    Wind=1.0,\n    P=101.3,\n    Rh=0.65,\n    Ri_PAR_f=300.0,\n)\nmodel = CompositeModel(\n    Beer(0.6);\n    status=(LAI=2.0,),\n    id=:plant,\n    scale=:Plant,\n    environment=meteo,\n)\nsimulation = run!(model)\nplant = final_state(simulation, One(scale=:Plant))\ndf = DataFrame(\n    aPPFD=[plant.aPPFD],\n    LAI=[plant.LAI],\n    Ri_PAR_f=[meteo.Ri_PAR_f[1]],\n)\nEvaluation.fit(Beer, df)\n\n\n\n\n\n","title":"PlantSimEngine.fit"},{"category":"method","location":"API/API_examples/#PlantSimEngine.run!-Tuple{Beer, Vararg{Any, 4}}","page":"Example models","text":"run!(model::Beer, status, environment, constants, context)\n\nComputes the canopy-absorbed photosynthetic photon flux density (aPPFD, µmol[PAR] m[ground]⁻² s⁻¹) from the incoming PAR radiation flux (Ri_PAR_f, W m[ground]⁻²) and the Beer-Lambert law of light extinction.\n\nArguments\n\nmodel: the current Beer model instance.\nstatus: the application-local view of the target Object status.\nenvironment: sampled environment, such as an Atmosphere row.\nconstants: physical constants supplied by the CompositeModel run.\ncontext: runtime context; this kernel does not use it.\n\nExamples\n\nmodel = CompositeModel(\n    Beer(0.5);\n    status=(LAI=2.0,),\n    id=:plant,\n    scale=:Plant,\n    environment=Atmosphere(\n        T=20.0,\n        Wind=1.0,\n        P=101.3,\n        Rh=0.65,\n        Ri_PAR_f=300.0,\n        duration=Hour(1),\n    ),\n)\nrun!(model)\nonly(model_objects(model; scale=:Plant)).status.aPPFD\n\n\n\n\n\n","title":"PlantSimEngine.run!"},{"category":"section","location":"prerequisites/julia_basics/#Getting-started-with-Julia","page":"Julia language basics","text":"PlantSimEngine (as well as its related packages) is written in Julia. The reasons why Julia was chosen are briefly discussed here : The choice of using Julia.\n\nJulia is a language that is gaining traction, but it isn't the most widely used in research and data science. \n\nMany elements will be familiar to those with an R, Python or Matlab background, but there are some noteworthy differences, and if you are new to the language, there will be a few hurdles you might have to overcome to be comfortable using the language.\n\nThis page is here to list to the parts of Julia that are most relevant regarding usage of PlantSimEngine, and point to resources that can help you grasp those basics.","title":"Getting started with Julia"},{"category":"section","location":"prerequisites/julia_basics/#New-to-programming","page":"Julia language basics","text":"It is not meant as a full-fledged from-scratch Julia tutorial. If you are completely new to programming, you may wish to check some other resources first, such as ones found here. The video course Julia Programming for Nervous Beginners is tailored for people with no programming experience.","title":"New to programming"},{"category":"section","location":"prerequisites/julia_basics/#Installing-packages-and-setting-up-and-environment","page":"Julia language basics","text":"For PlantSimEngine, you can check our documentation page on the topic:  Installing PlantSimEngine.","title":"Installing packages and setting up and environment"},{"category":"section","location":"prerequisites/julia_basics/#Cheatsheets","page":"Julia language basics","text":"You can also find a few cheatsheets here as well as a short introductory notebook along with its install instructions.","title":"Cheatsheets"},{"category":"section","location":"prerequisites/julia_basics/#Troubleshooting","page":"Julia language basics","text":"For more Julia learning-related difficulties, you will find quick responses on the Discourse forum: https://discourse.julialang.org.","title":"Troubleshooting"},{"category":"section","location":"prerequisites/julia_basics/#Noteworthy-differences-with-other-languages:","page":"Julia language basics","text":"If you wish to compare Julia to a specific language, the noteworthy differences section will provide you with a quick overview of the differences.\n\n(Array indexing starts at 1, for example)","title":"Noteworthy differences with other languages:"},{"category":"section","location":"prerequisites/julia_basics/#Essential-Julia-concepts-for-PlantSimEngine","page":"Julia language basics","text":"Here's a list of the main aspects of the Julia language required (beyond package management) to understand how to use PlantSimEngine to its potential:\n\nStandard notions and constructs:\n\nStandard concepts of a variable, arrays, functions, function arguments\nThe typing system and custom types\nDictionaries and NamedTuple objects are used throughout the codebase\n\nThe Julia manual goes more in-depth than lighter introductions to some of these topics, so might be more useful as a reference than a starting point. You might find other guides or courses, such as the first section in https://julia.quantecon.org/intro.html, chapters 0-4 and 7 of the Learn Julia the Hard Way draft or the interactive Mathigon course.\n\nAlso of importance:\n\nKeyword arguments (kwargs) are present in many API functions\nType promotion, splatting, broadcasting, and comprehensions are also very useful, but not compulsory to get started\n\nMany of these are also briefly presented in this Julia Data Science guide, which also happens to focus on the DataFrames.jl package.\n\nUnderstanding more about methods, parametric types and the typing system is usually worthwhile, when working with Julia packages.","title":"Essential Julia concepts for PlantSimEngine"},{"category":"section","location":"prerequisites/installing_plantsimengine/#Installing-PlantSimEngine","page":"Installing PlantSimEngine","text":"Install Julia from the official download page, create a project environment, and add PlantSimEngine:\n\nusing Pkg\nPkg.activate(\"my_simulation\")\nPkg.add(\"PlantSimEngine\")\n\nMost simulations also use PlantMeteo:\n\nPkg.add(\"PlantMeteo\")","title":"Installing PlantSimEngine"},{"category":"section","location":"prerequisites/installing_plantsimengine/#First-Simulation","page":"Installing PlantSimEngine","text":"using PlantSimEngine, PlantMeteo, Dates\nusing PlantSimEngine.Examples\n\nmeteo = Atmosphere(\n    T=20.0,\n    Wind=1.0,\n    Rh=0.65,\n    Ri_PAR_f=500.0,\n    duration=Hour(1),\n)\n\nmodel = CompositeModel(\n    Beer(0.5);\n    status=(LAI=2.0,),\n    id=:leaf,\n    scale=:Leaf,\n    environment=meteo,\n)\n\nsimulation = run!(model)\nfinal_state(simulation, One(scale=:Leaf)).aPPFD\n\nExample models are provided by the PlantSimEngine.Examples submodule. They are useful for learning and tests but are not part of the core modeling API.\n\nFor local package development, use Pkg.develop(path=\"...\"). Run the package tests with Pkg.test(\"PlantSimEngine\").","title":"First Simulation"},{"category":"section","location":"guides/modelers/port_existing_model/#Port-An-Existing-Model","page":"Port an existing model","text":"Start with a one-step scientific function and separate four concerns: immutable parameters, object state, environment forcing, and produced values. Keep the arithmetic generic; a model should not convert compatible values to Float64.\n\nusing Dates\nusing PlantSimEngine\n\nPlantSimEngine.@process \"docs_lai_growth\" verbose = false\n\nstruct DocsLAIGrowth{T} <: AbstractDocs_Lai_GrowthModel\n    rate::T\nend\nPlantSimEngine.inputs_(::DocsLAIGrowth) = (lai=Required(Float64),)\nPlantSimEngine.outputs_(::DocsLAIGrowth) = (lai_next=0.0,)\nPlantSimEngine.environment_inputs_(::DocsLAIGrowth) = (T=0.0,)\nfunction PlantSimEngine.run!(m::DocsLAIGrowth, status, environment, constants, context)\n    status.lai_next = status.lai + m.rate * environment.T\nend\n\nmodel = CompositeModel(\n    DocsLAIGrowth(0.02);\n    status=(lai=1.0,),\n    environment=(T=10.0, duration=Day(1)),\n)\nsimulation = run!(model)\n@assert final_state(simulation).lai_next == 1.2\n\nTest the scientific function first, then the kernel directly with a Status, and finally the same model through a model. These three levels separate a scientific error from a model-contract error and a scenario-binding error. Diagnostics.explain_initialization(model) should show lai as supplied, T as environment-bound, and lai_next as generated.\n\nUse Default(value) only when the scientific model really defines a fallback. Do not translate an old sentinel such as -Inf into a default: use Required(T) when the value must come from the scenario.\n\nThe concise constructor lowers to the ordinary CompositeModel compiler; it is not a separate runtime. Move to explicit Object and ModelSpec construction only when the scenario needs multiple objects, selectors, or named applications.","title":"Port An Existing Model"},{"category":"section","location":"model_execution/#Model-Execution","page":"Model execution","text":"This page describes how the native composite-model/object runtime executes model applications. Use this path for new multi-object, multi-plant, soil, microclimate, and multirate simulations.\n\nThe public configuration surface has one application constructor:\n\nModelSpec(\n    model;\n    name=:application,\n    on=Many(scale=:Leaf),\n    inputs=(...),\n    calls=(...),\n    every=Dates.Hour(1),\n    environment=Environment(...),\n    output_routing=(...),\n    updates=Updates(...),\n)\n\nScenarios start from CompositeModel and model applications.","title":"Model Execution"},{"category":"section","location":"model_execution/#Model-Kernels-And-Applications","page":"Model execution","text":"A model kernel is still an ordinary PlantSimEngine model:\n\ninputs_(model) declares each status input as Required(T) or Default(value);\noutputs_(model) declares variables the model computes and their initial output-state values;\nenvironment_inputs_(model) declares environment variables it reads;\ncommit_environment!(context, state) commits accepted mutable environment state when the model intentionally controls microclimate;\ndep(model) may declare model-author defaults;\nrun!(model, status, environment, constants, context) contains the model\n\nequations.\n\nRequired(T) has no initialization value: object state or a producer application must satisfy it. Default(value) is installed only when the target does not already have the input. Plain input literals are rejected because they are ambiguous.\n\nThe composite-model/object layer does not change that kernel contract. It adds a scenario-specific application around the kernel:\n\nModelSpec(\n    LeafEnergyBalance();\n    name=:leaf_energy,\n    on=Many(kind=:plant, scale=:Leaf),\n    inputs=(...),\n    calls=(...),\n    every=Dates.Hour(1),\n    environment=Environment(provider=:canopy),\n)\n\nModelSpec decides where the model runs, where its inputs come from, which models it may call manually, which timestep it uses, and which environment provider is bound to it. The model implementation stays reusable.","title":"Model Kernels And Applications"},{"category":"section","location":"model_execution/#Compilation-Before-Runtime","page":"Model execution","text":"Before the timestep loop, PlantSimEngine compiles the model into concrete runtime carriers:\n\nModelSpec(...; on=...) selectors are resolved to stable object ids.\nModelSpec(...; inputs=...) selectors are resolved to source object/application ids.\nSame-rate inputs are wired as shared Refs, RefVectors, or heterogeneous object-reference vectors.\nTemporal inputs are compiled as stream lookups with a policy such as HoldLast, Interpolate, Integrate, or Aggregate.\nModelSpec(...; calls=...) declarations are compiled to callable target lists.\nEnvironment(...) is bound to backend cells, layers, voxels, or global weather providers.\nThe root application order is topologically sorted from value inputs and Updates(...) ordering.\nRoot execution batches are grouped by concrete model/status/environment types where possible.\n\nSelectors are not resolved in the hot loop. Runtime execution uses the compiled indexes and carriers.\n\nUseful inspection helpers:\n\nDiagnostics.explain_applications(model)\nDiagnostics.explain_bindings(model)\nDiagnostics.explain_calls(model)\nDiagnostics.explain_environment_bindings(model)\nDiagnostics.explain_schedule(model)\nDiagnostics.explain_execution_plan(model)\nDiagnostics.explain_writers(model)\n\nThese explanations are intended for both users and agents. They report the compiled object ids, applications, carriers, clocks, environment bindings, and manual-call targets that the runtime will use.","title":"Compilation Before Runtime"},{"category":"section","location":"model_execution/#Soft-Dependencies-With-Inputs","page":"Model execution","text":"Soft dependencies are value dependencies. A consumer model reads a variable produced by another model through ModelSpec(...; inputs=...).\n\nModelSpec(SceneLAI(ground_area); name=:scene_lai, on=One(scale=:Scene), inputs=(:leaf_areas => Many(\n            kind=:plant,\n            scale=:Leaf,\n            within=SceneScope(),\n            application=:leaf_state,\n            var=:leaf_area,\n        ),))\n\nFor same-rate inputs, the runtime installs a reference carrier into the consumer status during compilation. A model-scale model reading all leaf areas therefore sees a RefVector-like object: reading pulls current values from source leaf statuses, and writing through the carrier mutates source refs when the carrier supports it.\n\nIf an input is not explicitly declared with ModelSpec(...; inputs=...), the compiler can infer simple same-object bindings when exactly one producer on the same object outputs the same variable. Ambiguous producers are errors and should be disambiguated with application=... and, when names differ, var=....\n\nUse PreviousTimeStep(:x) => selector when a feedback dependency should read the previous sample instead of creating a same-timestep scheduling edge.","title":"Soft Dependencies With Inputs"},{"category":"section","location":"model_execution/#Hard-Calls-With-Calls","page":"Model execution","text":"Hard dependencies are manual calls. Use ModelSpec(...; calls=...) when a parent model must control the call stack, for example during an iterative energy-balance solve.\n\nModelSpec(SceneEnergyBalance(); name=:scene_energy, on=One(scale=:Scene), calls=(:leaf_energy => Many(\n            kind=:plant,\n            scale=:Leaf,\n            within=SceneScope(),\n            application=:energy_balance,\n        ),\n        :soil => One(\n            kind=:soil,\n            scale=:Soil,\n            within=SceneScope(),\n            application=:soil_water,\n        ),), every=Dates.Hour(1))\n\nInside run!, the parent can execute every resolved target directly. The return value is always vector-like: One returns one element, OptionalOne returns zero or one, and Many returns zero or more.\n\nsoil_targets = run_call!(context, :soil; publish=true)\nsoil_status = only(soil_targets).status\n\nFor finer-grained iterative control, retrieve targets without executing them and decide when to publish the accepted state:\n\nfunction PlantSimEngine.run!(model::SceneEnergyBalance, status, environment,\n                             constants, context)\n    trial = trial_environment(model, status)\n    run_call!(context, :leaf_energy; environment=trial, publish=false)\n\n    accepted = accepted_environment(model, status)\n    commit_environment!(context, accepted)\n    run_call!(context, :leaf_energy; publish=true)\n\n    return nothing\nend\n\nrun_call! defaults to publish=false. Trial calls mutate target statuses but do not publish temporal samples or commit mutable environment updates. Use environment=trial_state when hard-called descendants should sample temporary state through their compiled environment handles. Call commit_environment! and run_call!(...; publish=true) once for the accepted state.\n\nApplications selected only by ModelSpec(...; calls=...) are marked manual-call-only in Diagnostics.explain_schedule(model) and are skipped by the root run!(model) loop.","title":"Hard Calls With Calls"},{"category":"section","location":"model_execution/#Duplicate-Writers-With-Updates","page":"Model execution","text":"By default, one application owns each (object, output variable) canonical writer. If a scenario intentionally lets several models update the same variable, later writers must declare that order explicitly:\n\nModelSpec(CarbonAllocation(); name=:carbon_allocation, on=Many(scale=:Leaf))\n\nModelSpec(LeafPruning(); name=:leaf_pruning, on=Many(scale=:Leaf), updates=Updates(:leaf_biomass; after=:carbon_allocation))\n\nThis keeps ordinary duplicate outputs as errors while allowing cases such as allocation followed by pruning. Diagnostics.explain_writers(model) reports writer groups and the Updates(...) declarations that validate them. The after value is the canonical application identifier shown by Diagnostics.explain_applications(model), not the process name.","title":"Duplicate Writers With Updates"},{"category":"section","location":"model_execution/#Multirate-Execution","page":"Model execution","text":"Use ModelSpec(...; every=...) with Dates.Period values for model application clocks:\n\nModelSpec(HourlyLeafAssimilation(); name=:leaf_assim, on=Many(scale=:Leaf), every=Dates.Hour(1))\n\nModelSpec(DailyPlantAllocation(); name=:allocation, on=Many(scale=:Plant), inputs=(:leaf_assimilation => Many(\n            scale=:Leaf,\n            within=Subtree(),\n            application=:leaf_assim,\n            var=:A,\n            policy=Integrate(),\n            window=Dates.Day(1),\n        ),), every=Dates.Day(1))\n\nClock precedence is:\n\nexplicit ModelSpec(...; every=...) on the ModelSpec;\nnon-default timespec(model) trait;\nthe model environment base step.\n\ntimestep_hint(model) is a compatibility constraint and explanation hint. It does not silently choose a clock. If a model uses the environment base step and that step violates timestep_hint.required, model compilation errors.\n\nTemporal input policy precedence is:\n\nexplicit selector policy, such as policy=Integrate();\nproducer output_policy(model) for that output;\nHoldLast().\n\nSupported policies are:\n\nHoldLast(): use the latest producer sample;\nInterpolate(): interpolate or extrapolate from producer samples;\nIntegrate(): reduce values over a window, defaulting to SumReducer();\nAggregate(): reduce values over a window, defaulting to MeanReducer().\n\nIntegrate(...) and Aggregate(...) accept reducer objects or callables that take either (values) or (values, durations_seconds). For duration-aware reducers, each producer value is held until the next producer execution and weighted by the portion of that interval overlapping the consumer window. This includes the last value published before the window when it remains active inside the window.\n\nTemporal windows are duration-based rolling windows. Calendar-aligned civil days and \"previous complete period\" selection are not part of the public API; there is no CalendarWindow compatibility type.","title":"Multirate Execution"},{"category":"section","location":"model_execution/#Environment-Sampling","page":"Model execution","text":"Environment(...) chooses a provider and optional source-variable remapping:\n\nModelSpec(CO2Probe(); name=:co2_probe, on=Many(scale=:Leaf), environment=Environment(provider=:canopy, sources=(CO2=:Ca,)))\n\nThe compiler binds each application/object pair to the selected backend before runtime. Constant weather, global tabular meteorology, grid, layer, voxel, or octree-style microclimate backends all use the same contract:\n\nenvironment_inputs_(model) says what the model reads;\nenvironment_outputs_(model) says what the model may commit;\ncommit_environment!(context, accepted_environment) commits accepted mutable meteorology from a controller model;\nrun_call!(context, name; environment=trial_state) exposes non-committing trial state while preserving every target's compiled backend handle;\nEnvironment(; sources=...) maps model-facing names to backend names;\ngeometry and position are used by spatial backends when available;\nobject-to-environment links are cached and refreshed when objects move.\n\nBackend authors implement an opaque-handle protocol:\n\nhandle = EnvironmentAPI.bind_environment(backend, object, context, config)\n\nEnvironmentAPI.sample(backend, handle, variable, time)               # committed state\nEnvironmentAPI.sample(backend, handle, trial_state, variable, time)  # transient state\ncommit_environment!(backend, handle, accepted_state, time)\n\nEnvironmentAPI.EnvironmentContext identifies the application, object, scale, and process while the handle is compiled. Runtime status and geometry are not passed to sampling: a spatial backend resolves them once in EnvironmentAPI.bind_environment and stores the resulting provider, layer, voxel, or other routing data in its concrete handle. A controller that reads from one provider and commits to another should encode both routes in the handle, for example Environment(provider=:forcing, sink=:canopy).\n\nModel-level environment_hint(...) can provide default source bindings and aggregation rules. Scenario-level Environment(...) keeps precedence for source names, while explicit sampling policy on ModelSpec(...; inputs=...) controls model-to-model temporal values.","title":"Environment Sampling"},{"category":"section","location":"model_execution/#Running-And-Outputs","page":"Model execution","text":"Run a model with:\n\nsim = run!(model; steps=30)\n\nThe returned Simulation contains the mutated model, compiled bindings, environment bindings, execution plan, and retained temporal output streams.\n\nBy default, model runs retain no user output streams. Pass outputs=:all to retain every published stream, or pass OutputRequest values to retain only selected outputs and required temporal dependency streams:\n\nrequest = OutputRequest(\n    Many(scale=:Leaf),\n    :A;\n    name=:leaf_assimilation_daily,\n    application=:leaf_assimilation,\n    policy=Integrate(),\n    clock=Dates.Day(1),\n)\n\nsim = run!(model; steps=72, outputs=request)\ncollect_outputs(sim, :leaf_assimilation_daily; sink=nothing)\nDiagnostics.explain_output_retention(sim)\n\nWhen several applications publish the same process and variable, use application=:application_name in the request. This selects the named application directly and can also request an explicitly named :stream_only publisher.\n\noutputs=:none retains no user output streams. Histories required by temporal dependencies are still maintained with bounded retention.\n\nrun!(model; ...) always starts a fresh result timeline. Continue an existing simulation without resetting its step index, environment position, multirate phase, or temporal histories with:\n\ncontinue!(sim; steps=24)\nstep!(sim)\ncurrent_step(sim)\n\nTemporal dependency streams that are not explicitly requested retain only the history required by their input policy. HoldLast keeps the latest sample, Integrate and Aggregate keep their input window, and Interpolate and PreviousTimeStep keep sufficient recent source samples. Requested streams retain complete histories for post-run export. Diagnostics.explain_output_retention(sim) reports retention_steps for bounded dependency-only streams and nothing for full-history streams.","title":"Running And Outputs"},{"category":"section","location":"model_execution/#Lifecycle-Changes","page":"Model execution","text":"CompositeModel objects may be added, removed, reparented, moved, or have their geometry updated between or during timesteps:\n\nregister_object!(model, Object(:new_leaf; scale=:Leaf); parent=:plant_1)\nleaf_status = add_organ!(\n    parent_node,\n    model,\n    :+,\n    :Leaf,\n    3;\n    index=4,\n    attributes=(area=0.01,),\n    initial_status=(biomass=0.0,),\n)\nremove_object!(model, :old_leaf)\nreparent_object!(model, :leaf_3, :plant_2)\nmove_object!(model, :leaf_4, new_geometry)\nupdate_geometry!(model, :leaf_5, new_geometry)\n\nUse add_organ! for an MTG-backed model. It creates the MTG node, initializes and attaches its Status, registers the model object, and invalidates the affected bindings. By default, status initialization reuses the model's MTG policy. A framework that already supplies the complete creation attributes and initial status may set use_status_adapter=false; doing so is an explicit assertion that the configured status accessor contributes no additional fields or side effects for that node. register_object! is the low-level operation for callers that already own a complete Object.\n\nStructural changes invalidate compiled object/model bindings. Movement and geometry changes invalidate environment bindings without rebuilding structural input carriers. Scenario-level application, dependency, selector, cadence, environment-sampling, and output-retention plans remain immutable; only the affected object targets and buffers are refreshed.\n\nDo not mutate Object topology, labels, or geometry fields directly. Direct field mutation bypasses registry indexes and cache invalidation and is unsupported. Use the lifecycle functions above. They validate prerequisites before mutating; in particular, reparent_object! rejects self-parenting and descendant cycles without changing existing links. ObjectInstance roots are immutable lifecycle anchors: removing or reparenting a root, or an ancestor whose subtree contains one, is rejected atomically. Ordinary descendants may still be added, removed, or reparented.\n\nInside a lifecycle-capable model kernel, use runtime_model(context) to obtain the live model. Objects created during a kernel call do not recursively execute inside that call. At the safe barrier after the mutating application, PlantSimEngine refreshes affected structural targets, value carriers, hard-call targets, writer validation, temporal storage, execution groups, output-request matches, and environment handles. A new object can therefore run an application that remains later in the same timestep. It does not retroactively run an application that already completed unless its creator declares that application as an Initializer and explicitly calls run_initializer! on the newborn object. Already published streams remain available for removed objects.","title":"Lifecycle Changes"},{"category":"section","location":"developers/#Developer-Guidelines","page":"Developer guidelines","text":"This page is for contributors working on PlantSimEngine itself. It focuses on the local development workflow, the checks worth running before opening a pull request, and a few implementation details that are easy to miss.","title":"Developer Guidelines"},{"category":"section","location":"developers/#Working-on-PlantSimEngine","page":"Developer guidelines","text":"Clone the repository from GitHub and develop against a checked-out local copy, typically through Pkg.develop(path=\"...\").\n\nWe mostly follow the Julia manual's style guide. Questions, bug reports, and design discussions should go through GitHub issues or the related pull request.\n\nThe Roadmap summarizes longer-term work that is not yet complete.","title":"Working on PlantSimEngine"},{"category":"section","location":"developers/#Local-environments","page":"Developer guidelines","text":"PlantSimEngine currently has three main local environments:\n\ntest/ for the package test suite and doctests run from test/runtests.jl;\ndocs/ for the Documenter build;\nbenchmark/ for benchmark scripts used to compare performance locally.","title":"Local environments"},{"category":"section","location":"developers/#Running-checks-locally","page":"Developer guidelines","text":"","title":"Running checks locally"},{"category":"section","location":"developers/#Main-test-suite","page":"Developer guidelines","text":"Run the standard test suite from the repository root:\n\njulia --project=test test/runtests.jl\n\nThe current public runtime is sequential. Running with multiple Julia threads does not enable a parallel Composite model executor; parallel execution remains roadmap work and requires dedicated correctness tests before it becomes public.","title":"Main test suite"},{"category":"section","location":"developers/#Documentation","page":"Developer guidelines","text":"Build the documentation from the repository root with:\n\njulia --project=docs docs/make.jl\n\nThe docs environment includes the extra packages needed for examples and API documentation, such as Documenter, CairoMakie, PlantMeteo, and MultiScaleTreeGraph.","title":"Documentation"},{"category":"section","location":"developers/#Benchmarks","page":"Developer guidelines","text":"Benchmark scripts live in benchmark/. They are useful when a change may alter runtime characteristics, but they are not a substitute for the main test suite or downstream integration checks.\n\nFor phase-oriented diagnostics, opt in explicitly:\n\nsimulation = run!(model; steps=48, outputs=:none, performance=true)\nDiagnostics.explain_runtime_performance(simulation)\n\nThe returned rows distinguish immutable-plan compilation, object-target instantiation, lifecycle-buffer updates, steady-state execution, output collection, and whole-initialization totals. Use these counters to establish where work occurred, not as a microbenchmark. Timing instrumentation calls time_ns() at runtime boundaries, so benchmark ordinary execution separately with performance=false after warming the simulation.","title":"Benchmarks"},{"category":"section","location":"developers/#CI-workflows","page":"Developer guidelines","text":"The repository currently relies on these GitHub Actions workflows:\n\nCI.yml for the main test matrix, docs build, and coverage;\nIntegration.yml for downstream checks against packages that depend on PlantSimEngine;\nBenchmarks.yml for pull-request benchmark runs;\nregister.yml and TagBot.yml for release automation.\n\nIf a change affects public APIs or execution behavior, check both CI and Integration before merging. Benchmark results are useful for regressions, but should be interpreted alongside the test results.","title":"CI workflows"},{"category":"section","location":"developers/#Graph-Viewer-Frontend","page":"Developer guidelines","text":"The static viewer and HTTP editor share the React application under frontend/. PlantSimEngine releases include the production bundle in frontend/dist, because Julia package installations do not run Node or Vite. The content hash in asset filenames is intentional: it prevents browsers and documentation hosts from reusing stale JavaScript after a release.\n\nInstall the frontend development dependencies from the repository root:\n\ncd frontend\nnpm ci\n\nRun the fast checks while developing:\n\nnpm run typecheck\nnpm test\n\nBuild the production assets after changing TypeScript, CSS, or frontend dependencies:\n\nnpm run build\n\nCommit the resulting frontend/dist changes together with the source changes. Do not commit frontend/node_modules, Playwright reports, screenshots, videos, or local test output.\n\nThe end-to-end suite starts a real Julia GraphEditor.edit_graph session and controls it with Chromium:\n\nnpx playwright install chromium\nnpm run test:e2e\n\nUse npm run test:e2e:ui for a headed local debugging session. The tests use stable data-testid attributes for commands and confirm mutations through the Julia /state endpoint. Avoid assertions against generated CSS classes or implementing PlantSimEngine selector semantics in TypeScript.\n\nCore graph DTO and edit tests live in test/test-model-graph-view.jl. HTTP-extension tests live in test/test-model-graph-editor-extension.jl. When changing the graph schema, update those Julia tests, frontend types, unit tests, Playwright scenarios, and the committed production bundle in the same change.","title":"Graph Viewer Frontend"},{"category":"section","location":"developers/#Documentation-impact","page":"Developer guidelines","text":"Changes in PlantSimEngine often require documentation updates beyond the page you were editing.\n\nUser-facing errors often require updates to the troubleshooting pages.\nNew examples should ideally become doctests or rendered examples.\nAPI or behavior changes may require updates to the roadmap, migration notes, and example pages.\nIf a feature remains experimental, say so clearly in the docs instead of letting examples imply stable support.","title":"Documentation impact"},{"category":"section","location":"developers/#Pull-request-checklist","page":"Developer guidelines","text":"Make sure the change is covered by tests.\nRun the main test suite locally.\nBuild the documentation locally if docstrings, examples, or APIs changed.\nReview the affected docs pages and update them in the same pull request.\nCheck GitHub Actions after pushing.\nIf the change is breaking or deprecates an old path, document the migration path before merging.","title":"Pull request checklist"},{"category":"section","location":"developers/#Implementation-notes","page":"Developer guidelines","text":"","title":"Implementation notes"},{"category":"section","location":"developers/#Coverage-gaps-to-keep-in-mind","page":"Developer guidelines","text":"Not every combination of weather structure, status shape, mapping layout, and downstream usage is covered directly in PlantSimEngine. When changing the public API or runtime semantics, treat downstream integration results as part of the validation surface, not as optional extra signal.","title":"Coverage gaps to keep in mind"},{"category":"section","location":"dev/composite_model_implementation_plan/#Unified-CompositeModel/Object-Implementation-Plan","page":"Composite model/object implementation plan","text":"This plan is the persistent handoff for replacing the historical multiscale-mapping system with one composite-model/object address system.\n\nThe implementation can be incremental internally, but the target API is breaking. Do not preserve experimental intermediate APIs as user-facing concepts in the final design.\n\nThe target public surface should be centered on a small set of concepts:\n\nModelSpec(\n    model;\n    name=:application,\n    on=Many(scale=:Leaf),\n    inputs=(...),\n    calls=(...),\n    every=Dates.Hour(1),\n    environment=Environment(...),\n    output_routing=(...),\n    updates=Updates(...),\n)\n\nThis is the API memory target for users, modelers, and agents. Additional types should be selectors, model traits, or internal compiled carriers.","title":"Unified CompositeModel/Object Implementation Plan"},{"category":"section","location":"dev/composite_model_implementation_plan/#Implementation-Progress","page":"Composite model/object implementation plan","text":"Started Phase 0 by adding typed application metadata, now constructed directly with ModelSpec keywords.\nAdded ModelSpec(model; name=...) application names and getters: application_name, applies_to, value_inputs, model_calls, and environment_config.\nAdded selector and address types: SceneScope, Self, Subtree, SelfPlant, Ancestor, Scope, Relation, One, OptionalOne, Many, and Diagnostics.ObjectAddress.\nAdded initial CompositeModel/Object registry types and lifecycle hooks: register_object!, remove_object!, reparent_object!, move_object!, and Advanced.refresh_bindings!.\nAdded registry-backed selector resolution with resolve_object_ids and resolve_objects for SceneScope(), Self(), SelfPlant(), Ancestor(...), Scope(...), label keywords such as kind=:plant and scale=:Leaf, and One/OptionalOne/Many cardinality checks.\nAdded Diagnostics.explain_scopes(model) for agent-readable scope diagnostics. It reports the global model scope, each object subtree, each named Scope(...), and label groups by scale, kind, and species with concrete resolved object ids.\nSelector cardinality and named-scope failures now report the consumer context, matched object ids, requested criteria, available scales, kinds, species, and names inside the resolved scope, plus bounded edit-distance suggestions.\nRelation(...) selectors now resolve :self, :parent, :children, :ancestors, :descendants, and :siblings relative to the consuming object. Explicit scopes constrain relation results, default dependency scopes do not erase parent/sibling queries, and compiled ModelSpec(...; inputs=...) can use these relations without runtime selector resolution.\nDiagnostics.ObjectAddress(selector) normalizes scope, relation, label, routing, temporal-policy, and status-ordering fields into one structured diagnostic record.\nStarted the object-address compiler with Advanced.compile_composite_model(model, specs) and compiled model application/binding carriers. The compiler now resolves ModelSpec(...; on=...) target object ids, object-relative ModelSpec(...; inputs=...) source object ids, and object-relative ModelSpec(...; calls=...) callee object/application ids before runtime.\nAdded Diagnostics.explain_applications, Diagnostics.explain_bindings, and Diagnostics.explain_calls for the compiled model view. These explanations expose application ids, processes, target ids, input source ids, call callee ids, temporal policy, window, and carrier hints.\nAdded status-backed compiled input carriers. When source objects already hold Status values, ModelSpec(...; inputs=...) bindings now precompile a scalar shared Ref, a homogeneous RefVector, or an Advanced.ObjectRefVector fallback for heterogeneous reference-preserving vectors. Diagnostics.input_carrier, Diagnostics.input_value, and Diagnostics.has_reference_carrier expose these carriers, and Diagnostics.explain_bindings reports carrier kind, copy/reference semantics, carrier type, and reference availability.\nAdded conservative same-object input inference in the model compiler. When a model declares an inputs_ variable that is not covered by explicit/default ModelSpec(...; inputs=...), and exactly one other application on the same object outputs the same variable, Advanced.compile_composite_model creates an inferred reference binding. Diagnostics.explain_bindings now reports binding origin values such as :model_default, :model_spec, and :inferred_same_object.\nCompiled input bindings now carry producer metadata. When an ModelSpec(...; inputs=...) selector uses process= or application=, Advanced.compile_composite_model validates that a matching source application exists for the selected source objects. Diagnostics.explain_bindings reports source_application_ids, process, and application for agent-readable dependency diagnostics.\nDependency selectors in ModelSpec(...; inputs=...) and ModelSpec(...; calls=...) now infer a default scope from the consumer object when no explicit within=... is provided: model objects default to SceneScope(), while non-model objects default to Self(). Shared model/soil dependencies from organs should therefore use within=SceneScope() explicitly.\nAdvanced.compile_composite_model now validates Required(T) status inputs from inputs_(model). Each required input must either have a compiled binding or already exist on the target object Status; otherwise compilation errors with the concrete application id, object id, and input variable.\nCompositeModel compilation creates an empty Status for model-targeted objects when status is omitted, inserts missing outputs_ fields and Default(value) inputs from their initial values, and installs explicitly or implicitly bound input carriers. Unbound Required(T) inputs remain compilation errors.\nAdvanced.compile_composite_model now rejects ModelSpec(...; inputs=...) declarations whose left-hand variable is not declared by the target model's inputs_. This catches misspelled or stale scenario bindings before they create silent unused metadata.\nAdvanced.compile_composite_model now validates source availability for status-backed non-temporal ModelSpec(...; inputs=...) bindings. When selected source objects already have Status values, the requested source variable must resolve to references instead of silently compiling to an unused/no-op binding.\nCarrier compilation preserves source Status references and arbitrary value types; tests cover scalar refs, heterogeneous many-object vectors, and a homogeneous dual-like BigFloat value through RefVector, model arithmetic, source mutation, and typed output publication.\nSame-object renaming is supported directly by ModelSpec(...; inputs=...), for example inputs=(:renamed_signal => One(within=Self(), var=:signal),). The compiler aliases the source Ref, records the renamed source variable in Diagnostics.explain_bindings, and schedules the producer before the consumer.\nSame-rate input carriers are installed directly into consumer Status reference cells during model compilation. Scalar bindings share the source Ref; many-object bindings store the compiled RefVector or Advanced.ObjectRefVector once. The timestep runtime performs no assignment for these bindings, and a focused Many(...) materialization gate verifies zero allocations after compilation.\nReference wiring adds a missing bound input field to the consumer Status schema when needed, instead of requiring users to duplicate compiler-owned input placeholders.\nAdded call ambiguity validation in the compiled model view: a call can select by process when unique, and must use application=:name when several model applications with the same process match the same object.\nAdded a model binding cache with Advanced.refresh_bindings!, Advanced.bindings_dirty, Advanced.compiled_bindings, and Advanced.model_revision. Object creation, removal, and reparenting now invalidate the compiled binding cache and bump a model revision before the next refresh.\nAdded an environment binding cache with Advanced.refresh_environment_bindings!, Advanced.compile_environment_bindings, Advanced.CompiledEnvironmentBinding, Advanced.CompiledEnvironmentBindings, Advanced.environment_bindings_dirty, Advanced.compiled_environment_bindings, Advanced.environment_revision, and Diagnostics.explain_environment_bindings. The compiler resolves each application/object environment provider, backend, required environment_inputs_, support descriptor, and backend cell before runtime.\nAdded the minimal model geometry contract: geometry(object_or_status), position(object_or_status), and bounds(object_or_status). Environment binding refreshes now call EnvironmentAPI.update_index!(backend, changed_entities, removed_object_ids) once per distinct backend before Advanced.EnvironmentAPI.bind_environment, giving spatial backends a current model-wide object/entity list for precomputed microclimate lookup.\nAutomatic spatial binding now uses the nearest ancestor geometry when a target object has no geometry of its own. Existing backends still receive an Object carrying the target id/status, while its binding-time geometry comes from the ancestor. Explanations report geometry_source=:self, :ancestor, or :global and the source object id.\nMoving an object invalidates environment bindings for descendants that inherit its geometry, stopping at descendants with their own geometry. This preserves unaffected cached bindings.\nEnvironment refresh now reconciles model environment contracts against cached spatial bindings. If only environment_inputs_ changes while application id, object, process, provider, backend, status, and geometry provenance remain unchanged, required metadata is updated while the cached cell is reused without EnvironmentAPI.update_index! or Advanced.EnvironmentAPI.bind_environment.\nvalidate_environment_inputs(model) and validate_environment_inputs(compiled_scene, environment_or_backend) now validate composite-model/object model application environment_inputs_ against the active environment or an explicit replacement. Errors report model application ids, so duplicate process applications remain diagnosable.\nEnvironment(; sources=(target=:source,)) now remaps model-facing environment variables to backend source variables. Environment binding refresh validates missing source variables when the backend can enumerate its variables, and Diagnostics.explain_environment_bindings reports both required_inputs and source_inputs.\nCompositeModel applications now infer model-author default environment source remaps from environment_hint(...).bindings when the scenario does not provide explicit environment bindings. Scenario Environment(; sources=...) keeps precedence over the trait.\nGlobal tabular meteorology is now sampled at each model application's compiled clock. environment_hint(...).bindings reducers and windows are applied through PlantMeteo, while Environment(; sources=...) replaces only the source variable and preserves the selected reducer. Prepared weather samplers are shared by applications using the same weather table, and each application/timestep sample is cached once per run so all target objects reuse it.\nObject creation, removal, and reparenting invalidate both structural and environment bindings. Object movement invalidates only environment bindings, so moving a leaf or changing its geometry can refresh microclimate lookup without rebuilding object/model binding carriers.\nAdded public geometry invalidation helpers: update_geometry!(model, object, geometry; invalidate_environment=true) and object-scoped mark_environment_binding_dirty!(model, object). Geometry-only changes record the affected object ids and refresh only their compiled environment bindings; descendants inheriting moved ancestor geometry are invalidated as part of the same object-scoped refresh.\nStarted composite-model/object execution with run!(model; steps=...). The runtime refreshes compiled object bindings and environment bindings, materializes precompiled ModelSpec(...; inputs=...) carriers into consumer Status fields, samples the bound environment backend, and calls generic model kernels through the existing run! contract.\nCompositeModel/object execution now publishes model outputs to model-local temporal streams. Compiled ModelSpec(...; inputs=...) bindings marked as :temporal_stream can materialize HoldLast, Interpolate, Integrate, and Aggregate values before the consumer runs, using selector source ids, source variables, windows, and the model base timestep.\nCompositeModel temporal ModelSpec(...; inputs=...) now honor producer output_policy(...) traits when the selector omits policy=... and resolves to a unique source application. Explicit selector policies remain scenario-level overrides.\nCompositeModel Interpolate(...) matches the established multirate runtime: bracketed samples use linear interpolation, online consumers use linear extrapolation from the last two samples when requested, and insufficient or non-interpolable values fall back to hold-last. mode=:hold and extrapolation=:hold are supported, invalid modes fail during model compilation, and interpolation arithmetic preserves generic numeric value types without converting model values to Float64.\nUnified ModelSpec(...; inputs=...) now supports explicit lagged dependencies with PreviousTimeStep(:input) => selector. Lagged bindings use temporal streams, read source samples at or before t - 1, preserve the initialized consumer status value until history exists, and do not add a same-timestep scheduling edge. This allows feedback cycles to compile without changing generic model kernels.\nCompositeModel/object execution now exposes explicit mutable environment commits through commit_environment!(context, accepted_state) and non-committing trial sampling through run_call!(context, name; environment=trial_state). Meteorological state stays in the environment backend instead of being staged through same-named status values.\nAdded root application scheduling from ModelSpec(...; every=...) using Dates.Period values and the model environment base step. Diagnostics.explain_schedule on a Advanced.CompiledCompositeModel now reports each application clock, phase, timestep in base steps, timestep duration in seconds, and whether the application is scheduled as a root application or is manual-call-only.\nCompositeModel application scheduling now also honors a model's timespec(...) trait when ModelSpec(...; every=...) is omitted. Scenario-level ModelSpec(...; every=...) keeps precedence over the model trait, matching the established multirate runtime.\nCompositeModel application scheduling now validates timestep_hint(...) required bounds for base-step-derived clocks. Hints remain compatibility constraints; they do not override explicit ModelSpec(...; every=...) or non-default timespec(...).\nAdvanced.compile_composite_model now computes a stable topological application order from resolved ModelSpec(...; inputs=...) producer edges and Updates(...) writer-order edges. Inputs produced by manual-call-only applications are redirected to the parent application that owns the ModelSpec(...; calls=...) call stack. run!(model) uses this precompiled order instead of user declaration order, cycles fail at compile time, and Diagnostics.explain_schedule reports execution_index.\nAdvanced.CompiledCompositeModel now pre-indexes input and call bindings by (application_id, object_id). Per-object input materialization and call_targets lookup uses these indexes instead of scanning every binding in the model at each model call.\nAdvanced.CompiledCompositeModel now also pre-indexes applications by application id. Hard-call target resolution and stable ordered-application materialization use this index instead of scanning or rebuilding lookup dictionaries.\nAdvanced.CompiledEnvironmentBindings now pre-indexes environment bindings by (application_id, object_id). Environment sampling and mutable environment output scattering use direct lookup instead of scanning all environment bindings for every model invocation.\nAdded RunContext and CallTarget. Models can retrieve manual ModelSpec(...; calls=...) targets with call_targets(context, :name) and execute them with run_call!, preserving explicit call-stack control in the composite-model/object runtime. Manual calls execute immediately under the parent call stack; applications selected by ModelSpec(...; calls=...) are skipped by the root run!(model) loop and only execute through run_call!.\nAdded composite-model/object duplicate-writer validation. During Advanced.compile_composite_model, each (object, output variable) now has one canonical writer unless later writers declare Updates(:var; after=...). The after token can match a previous application id/name or process, so scenario authors can express cases such as pruning after carbon allocation without changing either model implementation.\nAdded Diagnostics.explain_writers(compiled). It reports each object/variable writer group, duplicate-writer status, writer application ids/processes, and the Updates(...) declarations used to validate ordered updates.\nExtended explain_model_specs rows with application name, target selector, value inputs, manual calls, and environment metadata.\nStarted Phase 3 by compiling simple ModelSpec(...; inputs=...) declarations to typed scale/variable carriers, for example inputs=(:x => Many(scale=:Leaf, var=:y),).\nAdded model-level Input(...) defaults from dep(model) into ModelSpec value inputs. Scenario-level ModelSpec(...; inputs=(...)) overrides those defaults before the native binding is compiled.\nRemoved the intermediate scenario bridge after the composite-model/object compiler gained native ModelSpec(...; inputs=...) support. Manual value-transfer carriers are not retained as user-authored API.\nRemoved the intermediate dependency resolver after ModelSpec(...; calls=...) became native composite-model/object metadata. Manual model execution now goes through CallTargets, call_targets, and run_call!.\nAdded model-level Call(...) defaults from dep(model) into ModelSpec manual-call metadata. Scenario-level ModelSpec(...; calls=(...)) overrides those defaults, and dep(::ModelSpec) excludes raw Call(...) trait entries so default calls are normalized through the same bridge as explicit calls.\nMigrated the MAESPA example's model energy-balance hard calls to scenario-level ModelSpec(scene_model; calls=(...)).\nMigrated the MAESPA example's model LAI leaf-area transfer to consumer-side ModelSpec(LAIModel(...); inputs=(...)).\nStarted Phase 5 with CompositeModelTemplate and ObjectInstance. A template stores reusable composite-model/object ModelSpecs plus default object labels, and an instance mounts those specs inside one named object subtree.\nCompositeModel(...) accepts ObjectInstance values directly or through its instances keyword. An instance root can be an owned Object or the id of an object supplied separately to the model.\nMounted template applications receive stable instance-prefixed application names and an implicit Scope(instance_name) on unqualified ModelSpec(...; on=...) selectors. Their ModelSpec(...; inputs=...), ModelSpec(...; calls=...), scheduling, writer validation, and execution use the normal compiled composite-model/object path.\nInstance overrides can replace one template application by application name or process. Overrides must be unambiguous and preserve process identity. Instances without overrides retain the exact shared model object from the template.\nTemplate labels fill missing kind and species metadata throughout the mounted subtree, while the root receives the instance name used by Scope(...). Tests cover four instances, plant-local aggregation, shared model storage, and one process-level model override.\nAdded explicit exceptional-organ overrides with Override(object=..., application=..., model=...) through ObjectInstance(...; object_overrides=...). The override must resolve to one template application, belong to the instance subtree, and preserve process, input, output, and environment-variable declarations.\nObject overrides remain one logical model application: the compiler stores the selected replacement model by target object id. Dependency bindings, writer ownership, application names, and manual calls therefore remain unchanged, and no selector resolution occurs in the runtime loop.\nParameter/model ownership is explicit. Templates retain user-supplied model and parameters objects by reference; unchanged instances share them. Instance and object overrides retain their user-supplied replacement model by reference. PlantSimEngine does not copy models or mutate model fields to merge parameter overrides.\nSame-concrete-type object overrides use a concretely typed object-to-model table. Structured application explanations report shared/per-object storage, concrete versus heterogeneous dispatch, overridden object ids, and model types.\nCompositeModel retains mounted instance metadata and Diagnostics.explain_instances(model) reports each instance root, current subtree object ids, mounted application ids, instance/object overrides, template labels, and parameter ownership. Diagnostics.explain_objects(model) also reports instance membership.\nNew objects registered below an instance automatically inherit missing template kind and species labels. Instance explanations derive membership from the current topology, so growth, pruning, and reparenting do not leave a separate stale membership list.\nCompositeModel hard calls can run under temporary local meteorology with run_call!(context, name; environment=local_state). Descendants sample the temporary state through normal environment bindings, while publish=false suppresses output publication and environment commits. This supports iterative microclimate solvers such as the MAESPA model energy-balance loop.\nModel kernels read their own parameters from the model argument. Generic hard-dependency kernels such as Monteith and Fvcb discover declared call targets through call_targets and execute them through run_call!.\nCompositeModel duplicate-writer validation now ignores manual-call-only applications when validating canonical root writers. This keeps hard-dependency children from being treated as independent root writers when they intentionally update the same object status inside their parent call stack.\nAdded a unified composite-model/object MAESPA example path: build_maespa_scene(...) and run_maespa_example(...). It uses CompositeModelTemplate, ObjectInstance, on, inputs, calls, and every=Dates.Period with two plant species, one shared soil object, model LAI, and model energy balance.\ntest/test-maespa-model-example.jl verifies the unified composite-model/object MAESPA path.\nrun!(model) now returns a Simulation wrapper with the mutated CompositeModel, compiled model bindings, compiled environment bindings, and the model-local temporal output streams. This keeps existing status mutation behavior but makes model outputs inspectable after a run.\nAdded outputs(sim::Simulation), collect_outputs(sim), and Diagnostics.explain_outputs(sim) for composite-model/object runs. The explanation reports object ids, variables, publishing application ids, sample counts, time bounds, and value types.\nrun!(model; tracked_outputs=...) now accepts OutputRequest and returns requested model outputs through collect_outputs(sim) or collect_outputs(sim, :request_name). CompositeModel requests are materialized from retained typed temporal streams after the run. They support HoldLast, Interpolate, Integrate, and Aggregate, Dates.Period export clocks, canonical-publisher inference when unique, explicit process=... selection, and dynamic objects over each object's own published sample interval.\nCompositeModel output requests now compile a publisher-level retention plan. With tracked_outputs=nothing, the model runtime retains all output streams for historical inspection. With explicit tracked_outputs, including an empty request vector, it retains only requested publisher streams plus streams required by temporal ModelSpec(...; inputs=...). Dependency-only streams are pruned after publication to the compiled policy horizon: latest-only for HoldLast, the input window for Integrate/Aggregate, and enough source history for Interpolate/PreviousTimeStep. Explicitly requested streams retain their complete histories for post-run export. Export is therefore not yet fully online, but unrequested temporal dependencies no longer grow for the full simulation.\nAdded Diagnostics.explain_output_retention(sim) to report which application/variable streams are retained and whether the reason is default retention, an output request, or a temporal dependency. Dependency-only rows also report their compiled retention_steps; unbounded requested/default rows report nothing.\nCompositeModel temporal streams are now keyed by application id, object id, and variable. Multiple applications can publish the same variable on the same object without overwriting each other's stream samples.\nCompositeModel output-export tests now cover requested-output DataFrame materialization, canonical publisher inference when process= is omitted, rejection when only stream-only publishers exist, and ambiguity when an explicit process matches both a stream-only and a canonical publisher.\nCompositeModel OutputRequest(...) now accepts application=... to select an explicit application id/name when the same process is mounted more than once. Explicit application selection can retain and export a :stream_only publisher.\nEach model temporal stream owns a concrete Vector{Tuple{Float64,T}} selected from its first published value rather than boxing all values as Any. Output type changes fail explicitly, and Interpolate/Integrate tests verify BigFloat histories and reduced values remain BigFloat.\nCompositeModel output_routing=(var=:stream_only,) now matches the unified graph semantics: stream-only outputs are excluded from canonical writer validation and same-object input inference, while remaining available in output streams and explicit inputs=(... One(application=:name), ...) selections.\nrun!(model; steps=...) now refreshes dirty structural bindings at timestep boundaries. Objects created, removed, or reparented by a model during one timestep update ModelSpec(...; on=...) target sets, input carriers, call targets, writer validation, and scheduling before the next timestep.\nGeometry-only mutations refresh environment bindings at the next timestep without recompiling structural bindings. The returned Simulation always contains final compiled structural and environment bindings, including mutations performed on the last step.\nEnvironment dirty tracking is now object-scoped for geometry-only changes. move_object!, update_geometry!, and mark_environment_binding_dirty!(model, object) retain unaffected compiled bindings and re-run Advanced.EnvironmentAPI.bind_environment only for applications targeting the changed object. Structural changes and provider-wide invalidation still rebuild the complete environment cache.\nRuntime lifecycle tests cover a model-created leaf joining a leaf application and plant-local RefVector, a pruned leaf leaving both before the next step, and a moved leaf switching mock microclimate cells.\nRoot model execution now compiles contiguous homogeneous target batches. Each target prebinds its concrete model, Status, input-binding tuple, and environment binding. Runtime dispatch occurs once at the batch function barrier; the inner object loop is specialized on a concrete target type.\nExceptional object overrides with another concrete model implementation become separate batches without changing stable object execution order. Structural or environment binding refresh recompiles the execution plan before the next timestep.\nAdded Diagnostics.explain_execution_plan(scene_or_simulation). It reports batch object ids, concrete model/status/carrier types, batch sizes, and inner-loop dispatch semantics. A focused 128-leaf gate verifies zero allocations inside a warmed homogeneous no-output batch.\nAdded objects_from_mtg(root; ...) and CompositeModel(root::MultiScaleTreeGraph.Node; ...). Existing MTG topology is traversed once into the unified registry, preserving stable node-derived ids, parent relations, labels, geometry, and explicitly imported runtime status through configurable accessors; the canonical adapter does not read or write runtime objects in MTG attributes.\n\nThe composite-model/object compiler is executable: selectors normalize to object addresses, resolve before runtime, and compile into reference, temporal, call, writer, and environment carriers. The historical mapping compiler has been removed.","title":"Implementation Progress"},{"category":"section","location":"dev/composite_model_implementation_plan/#Phase-0:-Public-Contract-Freeze","page":"Composite model/object implementation plan","text":"Goal: decide the small public vocabulary before implementing internals.\n\nDefine:\n\nModelSpec(model; name=nothing) as the model-application wrapper.\nModelSpec(...; on=selector) as the target object-set declaration.\nModelSpec(...; inputs=...) for value dependencies.\nModelSpec(...; calls=...) for manual call-stack dependencies.\nUpdates(...) for rare ordered duplicate writers.\nevery=period::Dates.Period and related multirate policies.\nEnvironment(...) for optional environment resolver/backend overrides.\n\nRules:\n\na model kernel remains generic and declares inputs_, outputs_, optional dep, optional environment_inputs_, and run!;\na model application decides where the kernel runs, at what rate, and how its inputs, calls, updates, outputs, and environment are bound;\napplication ids are stable and can be generated from explicit name, process, object selector, and occurrence index;\nif several applications provide the same process on the same object set, selectors must disambiguate by application name or another explicit filter.\n\nAcceptance tests:\n\na model can be applied twice to the same leaf objects with different names;\na dependency selector can choose by process when unique and by name when not;\nstructured explanations expose model kernel type, process, application name, and target object ids.","title":"Phase 0: Public Contract Freeze"},{"category":"section","location":"dev/composite_model_implementation_plan/#Phase-1:-CompositeModel-Object-Registry","page":"Composite model/object implementation plan","text":"Goal: introduce the internal object model without changing public behavior yet.\n\nImplement:\n\nObjectId as the stable identity key for every runtime object.\nModelObject metadata with labels: scale, kind, species, optional name, parent id, child ids, and optional geometry/position handle.\nAdvanced.ObjectRegistry storing objects, parent/child relations, and indexes by label.\nadapters from existing MTG state into the registry: each selected root and each MTG node gets an object id; single-status simulations get one object with scale=:Default.\nobject lifecycle hooks for add/remove/reparent that mirror the existing MTG runtime reindexing.\n\nAcceptance tests:\n\nthe MAESPA example registers five leaf objects, two plant objects, one soil object, and one model object;\nstatus(sim, :plant_A, :Leaf) and status(sim, :Leaf) can be expressed as registry queries;\nadd/remove/reparent updates object registry relations and status views.","title":"Phase 1: CompositeModel Object Registry"},{"category":"section","location":"dev/composite_model_implementation_plan/#Phase-2:-Selector-And-Scope-Language","page":"Composite model/object implementation plan","text":"Goal: make \"which objects?\" explicit and reusable.\n\nImplement selector types:\n\nSceneScope()\nSelf()\nSubtree()\nSelfPlant()\nAncestor(scale=:Plant)\nScope(name)\nRelation(...)\n\nObject labels use keyword criteria such as kind=:plant, species=:oil_palm, scale=:Leaf, and name=:leaf_1.\n\nImplement multiplicity wrappers:\n\nOne(selector...)\nOptionalOne(selector...)\nMany(selector...)\n\nSelectors must normalize to Diagnostics.ObjectAddress objects with enough context to be resolved relative to a consuming object.\n\nImplement ModelSpec(...; on=...) using the same selector system. The target object set of a model application must never be hidden inside a mapping key or implicit scale table.\n\nDefinitions:\n\nSelf() means only the current object: the object on which the consuming model application runs. It is a plant only when that object is the plant.\nSubtree() means the current object and its descendants.\nSelfPlant() means the nearest containing plant scope.\nAncestor(scale=:Plant) is the generic selector form for SelfPlant().\nSceneScope() means the whole model.\nScope(name) means a named scope or object collection.\n\nRules:\n\nunqualified selectors inside a reusable plant application bundle default to within=Self();\nmodel-level selectors default to within=SceneScope();\nOne(...) errors unless exactly one object resolves per consumer;\nMany(...) preserves stable object-id order;\nobject-id order replaces incidental traversal order as the semantic default.\nselectors are resolved during compilation or binding refresh, not inside the inner model loop.\n\nAcceptance tests:\n\nplant allocation on four oil palms reads only leaves under each plant;\nmodel LAI reads leaves across all plant objects;\na species-specific model model can read only species=:oil_palm leaves;\na model application target set declared with ModelSpec(...; on=...) produces stable application/object pairs;\nselector errors report available labels and near matches.","title":"Phase 2: Selector And Scope Language"},{"category":"section","location":"dev/composite_model_implementation_plan/#Phase-3:-Unified-Value-Inputs","page":"Composite model/object implementation plan","text":"Goal: use ModelSpec(...; inputs=...) as the only user-facing value-dependency declaration. Historical MultiScaleModel(...) mappings are migration sources only.\n\nTarget API:\n\nModelSpec(AllocationModel(); on=Many(kind=:plant, scale=:Plant), inputs=(:leaf_carbon => Many(scale=:Leaf, within=Subtree(), var=:leaf_carbon)))\n\nModelSpec(LAIModel(area); on=One(scale=:Scene), inputs=(:leaf_areas => Many(kind=:plant, scale=:Leaf, within=SceneScope(), var=:leaf_area)))\n\nImplement:\n\nModelSpec(...; inputs=...) as ModelSpec configuration.\nInput(...) or an equivalent internal wrapper that lets dep(model) provide default value-input bindings.\nnormalized input bindings from target variable to Diagnostics.ObjectAddress.\ncompiler pass that decides carrier: direct reference, RefVector, temporal stream, or materialization.\nstatus-default insertion for materialized target variables using the consumer model's inputs_ default.\ntemporal policies on value inputs: HoldLast, Interpolate, Integrate, Aggregate.\nDates.Period windows on value inputs, for example window=Day(1).\ncopy/reference semantics reporting for every compiled input binding.\n\nRules:\n\nmodel authors still declare inputs_; scenario authors decide where those inputs come from;\ndep(model) may provide defaults for common value-input bindings in composite-model/object composition;\nscenario-level ModelSpec(...; inputs=(...)) always wins over dep(model) defaults;\nsame-rate local links should keep reference semantics where possible;\ncross-rate links always go through temporal state;\nduplicate source candidates are errors unless the selector disambiguates;\nmaterialization carriers, when needed, are internal compiler details and are not user-authored structs;\nsame-rate scalar and many-object links should avoid copies when they can use aliases, shared refs, RefVector, or an equivalent typed carrier;\nPlantSimEngine must preserve arbitrary value types, including units, automatic differentiation numbers, uncertainty wrappers, and other numeric-like values.\n\nCarrier expectations:\n\nBinding kind Runtime carrier\nsame-rate scalar shared Ref or local alias\nsame-rate many-object RefVector or equivalent typed reference collection\ncross-rate temporal stream sample\nintegrate/aggregate temporal window reduction\nmaterialized cross-object input generated pre-run status assignment\nenvironment cached environment binding sample\n\nAcceptance tests:\n\nthe MAESPA model LAI cross-object input is declared with ModelSpec(...; inputs=...) and produces the same lai and leaf_area;\nhistorical plant allocation MultiScaleModel([:leaf_carbon => [:Leaf => :leaf_carbon]]) becomes ModelSpec(...; inputs=...) and remains plant-local;\na same-scale rename currently expressed with SameScale() works through ModelSpec(...; inputs=...);\nmulti-rate value inputs integrate object streams by object id.\nsame-rate many-object bindings do not allocate per timestep in a benchmarked hot loop beyond unavoidable model work.\nunitful or dual-number status values survive ModelSpec(...; inputs=...) without forced conversion to Float64.","title":"Phase 3: Unified Value Inputs"},{"category":"section","location":"dev/composite_model_implementation_plan/#Phase-4:-Unified-Model-Calls","page":"Composite model/object implementation plan","text":"Goal: use ModelSpec(...; calls=...) as the only user-facing manual model-call declaration. The same mechanism must also be usable from dep(model) so hard-dependency traits become default call declarations.\n\nTarget API:\n\nModelSpec(\n    SceneEB();\n    on=One(scale=:Scene),\n    calls=(\n        :leaf_energy =>\n            Many(kind=:plant, scale=:Leaf, process=:energy_balance),\n        :soil => One(kind=:soil, application=:soil_water),\n    ),\n)\n\nImplement:\n\nModelSpec(...; calls=...) as ModelSpec configuration.\nCall(...) or an equivalent internal wrapper that lets dep(model) provide default manual-call dependencies.\ncall resolution from Diagnostics.ObjectAddress to concrete ModelCall handles, or an equivalent callable runtime object if the final internal type name differs.\nsame-status hard dependency calls using the same public API.\npublication semantics: trial run_call!(call) mutates status only; final run_call!(call; publish=true) appends outputs and temporal streams.\nstructured call explanations with parent application id, selected callee application ids, selected object ids, selector, and publication behavior.\n\nRules:\n\ncalls are manual call-stack dependencies and are not independently scheduled under the parent;\ndep(model) call defaults are model-author defaults, not final wiring;\nscenario-level ModelSpec(...; calls=(...)) overrides dep(model) defaults;\nhard target outputs still participate in dependency graph compilation through the owning parent when needed;\ncall selection must be visible through explanation helpers.\n\nAcceptance tests:\n\nMAESPA model energy balance uses ModelSpec(...; calls=...) and still controls iterative leaf energy calls;\nmissing call selectors report kind, scale, process, and available matches;\nfinal accepted calls publish exactly once per timestep.\nan iterative model model can run selected leaf and soil calls several times with publish=false and publish only the accepted state.\n\nImplemented:\n\nrun_call!(::CallTarget) defaults to publish=false, matching the iterative manual-call contract.\nOne-shot accepted calls use publish=true explicitly.\nAn iterative hard-call regression executes two default non-publishing trials followed by one accepted call and verifies exactly one environment write and one temporal output sample for the accepted state.\nDiagnostics.explain_calls(compiled) reports publication_policy=:explicit_accept, default_publish=false, and accepted_publish=true for every compiled call edge.\nModelSpec now retains per-binding provenance for value inputs and manual calls. Bindings from dep(model) are reported as :model_default, scenario-level ModelSpec(...; inputs=...) and ModelSpec(...; calls=...) are reported as :model_spec, and compiler-created same-object value links are reported as :inferred_same_object. Diagnostics.explain_bindings, Diagnostics.explain_calls, and explain_model_specs expose these origins for agent-readable diagnostics.\nZero-match OptionalOne(...) dependencies remain compiled and visible. Optional inputs retain the consumer inputs_ default with carrier_kind=:optional_default; optional calls expose an empty target set and resolved=false instead of failing compilation.","title":"Phase 4: Unified Model Calls"},{"category":"section","location":"dev/composite_model_implementation_plan/#Phase-5:-Object-Templates,-Instances,-And-Overrides","page":"Composite model/object implementation plan","text":"Goal: support several plants of the same species with shared default models and selective per-instance differences.\n\nTarget API:\n\noil_palm = CompositeModelTemplate(\n    kind=:plant,\n    species=:oil_palm,\n    mapping=oil_palm_mapping,\n)\n\nmodel = CompositeModel(\n    ObjectInstance(:palm_1, oil_palm; root=node1),\n    ObjectInstance(:palm_2, oil_palm; root=node2, overrides=(\n        stomatal_conductance = Tuzet(; g1=3.2),\n    )),\n)\n\nImplement:\n\ntemplate-level model specs and parameters;\ninstance-level model/parameter overrides by process;\nobject-level overrides for exceptional organs;\nconflict validation when two overrides target the same process/object.\nshared parameter/model storage when template instances do not override anything, with explicit copy/ownership behavior when they do.\n\nRules:\n\ntemplates do not prescribe topology; they attach mappings to whatever object tree the instance provides;\ndefault Self() selectors resolve inside the current instance;\nmodel-wide models must opt into wider scope.\n\nAcceptance tests:\n\nfour oil palm instances share model objects/parameters when not overridden;\none palm instance can override one process parameter;\nallocation remains per plant while model LAI sees all leaves.\n\nMAESPA status:\n\nthe unified composite-model/object MAESPA path uses CompositeModelTemplate and ObjectInstance for species A and B.","title":"Phase 5: Object Templates, Instances, And Overrides"},{"category":"section","location":"dev/composite_model_implementation_plan/#Phase-5B:-Object-Lifecycle-And-Cache-Invalidation","page":"Composite model/object implementation plan","text":"Goal: make growth, pruning, and moving organs update every compiled binding through one mutation path.\n\nImplement public lifecycle hooks:\n\nregister_object!(model, object; parent)\nremove_object!(model, object)\nreparent_object!(model, object, new_parent)\nmove_object!(model, object, geometry_or_position)\nAdvanced.refresh_bindings!(model)\n\nImplement invalidation for:\n\nobject selector caches;\nmodel application target sets;\nRefVector or equivalent many-object carriers;\ntemporal stream ownership;\nwriter validation;\nenvironment bindings.\n\nRules:\n\ntopology and geometry changes do not silently leave stale carriers;\nobject creation should bind the new object to model applications selected by ModelSpec(...; on=...) before the next timestep;\nmoving an object should refresh environment bindings without rebuilding unrelated model bindings unless the move changes object relations or labels.\n\nAcceptance tests:\n\ncreating a new leaf adds it to plant-local allocation and model LAI before the next timestep;\npruning/removing a leaf removes it from many-object carriers and temporal stream ownership;\nchanging a leaf insertion angle can refresh only the affected environment binding when topology is unchanged.","title":"Phase 5B: Object Lifecycle And Cache Invalidation"},{"category":"section","location":"dev/composite_model_implementation_plan/#Phase-6:-Environment-Binding-Cache","page":"Composite model/object implementation plan","text":"Goal: make environment and microclimate sampling automatic and fast.\n\nImplement:\n\nEnvironmentBinding cache: object id, backend/provider id, cell/layer id, required variables.\ndefault environment resolver: global environment data for non-spatial backends; object position for spatial backends; parent position fallback; global fallback or validation error.\ndirty flags and batched refresh: mark_environment_binding_dirty!; update_geometry!(...; invalidate_environment=true); automatic dirty marking on object creation, removal, reparenting, and environment grid rebuild.\nexplanation helper: Diagnostics.explain_environment_bindings(sim).\nminimal geometry accessors or traits: position, geometry, and bounds.\nbackend protocol: Advanced.EnvironmentAPI.bind_environment, opaque handles, committed/transient sample, commit_environment!, and EnvironmentAPI.update_index!.\nEnvironment(...) overrides for scenario-specific resolver/backend choices.\nEnvironment(; sources=(CO2=:Ca,)) for scenario-specific environment source remapping without changing model kernels.\n\nRuntime rule:\n\nobject -> cached binding -> backend cell/layer -> current environment values\n\nSpatial lookup must happen only during binding refresh, not inside every model call.\n\nAcceptance tests:\n\nglobal environment data gives the same values to all objects;\nmissing global environment variables fail during environment binding refresh when the backend can enumerate variables;\nEnvironment(; sources=...) remaps backend variables to model-facing environment_inputs_ names and is visible in explanations;\na model running every two hours over hourly global meteorology receives a windowed weather sample rather than only the current raw row;\nmodel environment_hint reducers/windows are honored, and an Environment(; sources=...) override changes the source without discarding the reducer;\nall objects targeted by one application reuse one global weather sample per application/timestep;\nmock grid backend binds leaves to cells once at initialization;\nmoving one leaf marks only that leaf binding dirty and refreshes it before the next timestep;\nmodel environment_inputs_ changes update required variables without recomputing spatial links unless necessary.\ncommit_environment!(context, accepted_state) commits mutable microclimate state back to the active backend.","title":"Phase 6: Environment Binding Cache"},{"category":"section","location":"dev/composite_model_implementation_plan/#Phase-7:-Compiler,-Scheduler,-And-Explanation-Cleanup","page":"Composite model/object implementation plan","text":"Goal: make the unified graph the source of truth.\n\nImplement:\n\none compiler that builds a global dependency graph over object addresses;\nmaterialization and multiscale reference wiring as internal carriers;\nobject/scope dependency scheduling;\nwriter validation through the same graph, including Updates(...);\nmodel application scheduling from ModelSpec(...; on=...) target sets;\nmultirate scheduling based on Dates.Period values in ModelSpec(...; every=...) and input windows;\ntyped compiled bindings that avoid selector resolution in timestep hot loops;\ntyped homogeneous execution batches that move dynamic dispatch outside the per-object inner loop while preserving ordered heterogeneous overrides;\narbitrary value type preservation through status, input carriers, temporal storage, and environment samples;\nstructured explanation: Diagnostics.explain_objects, Diagnostics.explain_scopes, Diagnostics.explain_bindings, Diagnostics.explain_calls, Diagnostics.explain_environment_bindings, Diagnostics.explain_schedule, Diagnostics.explain_writers.\n\nAcceptance tests:\n\nold MultiScaleModel examples rewritten with ModelSpec(...; inputs=...) produce matching outputs;\nhistorical cross-object examples rewritten with ModelSpec(...; inputs=...) produce matching outputs;\nMAESPA hard-call example rewritten with ModelSpec(...; calls=...) produces matching outputs;\nexplanation helpers include enough concrete object ids, scales, processes, and variables for an AI agent to repair bad mappings.\nDiagnostics.explain_bindings(sim) reports whether each dependency came from inference, dep(model), or ModelSpec, and reports carrier/copy semantics.\nno selector resolution occurs inside the per-object, per-model timestep loop for static composite models.\na warmed homogeneous execution batch performs no allocations beyond model, output-stream, or backend work requested by the application itself;\nmultirate simulations use the same object-address graph as same-rate simulations.","title":"Phase 7: Compiler, Scheduler, And Explanation Cleanup"},{"category":"section","location":"dev/composite_model_implementation_plan/#Phase-8:-Breaking-API-Removal-And-Migration-Docs","page":"Composite model/object implementation plan","text":"Goal: remove the old configuration surface once parity is proven.\n\nRemoved:\n\nMultiScaleModel(...) as public scenario configuration;\nsuperseded scenario containers and value-transfer authoring;\nsuperseded manual-dependency selectors.\n\nWrite migration notes:\n\nMultiScaleModel([:x => [:Leaf => :y]]) -> inputs=(:x => Many(scale=:Leaf, var=:y),);\ncross-object value declarations -> consumer ModelSpec(...; inputs=...);\nmanual dependency declarations -> ModelSpec(...; calls=...);\nrepeated species assemblies -> CompositeModelTemplate plus ObjectInstance;\nexplicit environment wiring -> environment resolver/binding backend.\nInputBindings(...) -> source and temporal policy information inside ModelSpec(...; inputs=...);\nMeteoBindings(...) and MeteoWindow(...) -> Environment(...) and environment sampling/window policy;\nModelSpec(...; output_routing=...) -> model-application output policy;\nPreviousTimeStep(...) -> temporal policy/cycle-breaking marker in the unified graph;\nScopeModel(...) -> ModelSpec(...; on=...) plus selector scope.\n\nRegression tests must cover all migrated examples before removal.\n\nMigration documentation progress:\n\nAdded docs/src/migration_composite_model.md with direct translations for MultiScaleModel, repeated object assemblies, TimeStepModel, InputBindings, MeteoBindings, ScopeModel, and SameScale.\nDocumentation navigation and the home page now identify the composite-model/object API as the target for new multiscale and multi-plant work.\nThe documentation home page now uses executable composite-model/object examples as the primary quickstart. It shows CompositeModel, Object, ModelSpec, on, inputs, every, automatic same-object binding inference, multi-object Many(...) inputs, and manual ModelSpec(...; calls=...) syntax.\nThe repository README now mirrors the composite-model/object entry point instead of teaching ModelMapping first. It includes smoke-tested CompositeModel/Object quickstart code, ModelSpec(...; inputs=...) multi-object coupling, conceptual ModelSpec(...; calls=...) syntax, and links to the migration guide.\nAdded docs/src/composite_model/quickstart.md as the first native composite-model/object tutorial page and promoted it in the documentation navigation. The page contains docs-tested examples for one-object model chaining, inferred same-object bindings, OutputRequest retention, multi-object ModelSpec(...; inputs=...), RefVector carrier explanations, and manual ModelSpec(...; calls=...) syntax.\nThe repository agent skill teaches the unified public vocabulary.\nThe public API page now starts with curated composite-model/object groups for scenario construction, selectors, coupling, lifecycle, environment, runtime, and structured explanations.\n\nCurrent removal audit:\n\nThe unreleased intermediate scenario and runtime subsystem has been deleted, including its carriers, dependency selectors, target helpers, tests, examples, and documentation.\nPublic manual-call control now uses vector-like CallTargets, run_call!(context, name), call_targets, and run_call!(target).\nRunContext defines Symbol-named run_call! and call_targets directly.\nThe legacy mapping transforms are removed: MultiScaleModel, SameScale, TimeStepModel, InputBindings, MeteoBindings, MeteoWindow, and ScopeModel are not retained as compatibility constructors.\nModelMapping is removed. Retained documentation mentions it only as historical migration context.\nHistorical MTG mapping and mapping-level multirate pages were removed from the active documentation navigation. A future documentation cleanup can replace these historical pages with equivalent composite-model/object tutorials rather than retaining them as migration reference.\nThe model execution page has been rewritten as a composite-model/object-first guide. It now documents compilation, same-rate reference carriers, temporal ModelSpec(...; inputs=...), manual ModelSpec(...; calls=...), Updates(...), ModelSpec(...; every=...), environment binding, output retention, lifecycle cache invalidation, and compatibility translations from the historical mapping runtime.\nThe detailed first simulation tutorial now starts from the composite-model/object API instead of ModelMapping. It introduces model kernels, object status, compiled applications, inferred same-object bindings, model outputs, and a short compatibility note for historical mapping examples.\nThe quick examples page now uses copy-pasteable composite-model/object examples for Beer light interception, degree-days/LAI/light coupling, biomass growth, and retained OutputRequest exports. ModelMapping appears only in the compatibility note.\nThe standard model coupling, model switching, and coupling more complex models step-by-step tutorials now teach CompositeModel, Object, ModelSpec, on, every, inferred soft ModelSpec(...; inputs=...), and manual ModelSpec(...; calls=...) first. Historical PlantSimEngine.ModelMapping(...) appears only in compatibility notes on those pages.\nThe home page has been replaced by native composite-model/object examples. The repository README has also been replaced by native composite-model/object examples, and a dedicated composite-model/object quickstart is now available in the main documentation navigation.\nCompositeModel/object tests cover scheduling, temporal policies, binding inference and overrides, environment contracts and aggregation, output routing and application-qualified export, and structured explanations. Legacy mapping regression tests were removed with the old runtime.\nCompositeModel/object tests now include public environment-contract validation parity for missing environment variables, explicit Environment(; sources=...) remapping, model-author environment_hint source defaults, and validation against an explicit replacement environment object/backend.\nTest code uses the canonical ModelSpec(...; every=...) spelling and composite-model/object modifiers. Legacy transform tests were removed with the old compatibility constructors.\nThe unified MAESPA path is implemented and tested through on, inputs, calls, call_targets, and run_call!.","title":"Phase 8: Breaking API Removal And Migration Docs"},{"category":"section","location":"dev/composite_model_implementation_plan/#Resolved-API-Decisions","page":"Composite model/object implementation plan","text":"SceneScope, Self, Subtree, SelfPlant, Ancestor, Scope, and Relation are the public topology selector names. Object labels use kind=, species=, scale=, and name= keyword criteria.\nModelSpec(...; inputs=...) is the only scenario-level value-binding construction form.\nModelSpec(...; every=...) is the canonical timestep configuration.\nEnvironment(...) owns provider/resolver/source configuration. Temporal value windows belong to the consuming ModelSpec(...; inputs=...) selector.\nObject templates own reusable model applications and parameters, not plant topology construction. They consume explicit object trees or MTGs adapted through objects_from_mtg.\nThe old multiscale and mapping-transform implementations have been removed.","title":"Resolved API Decisions"},{"category":"section","location":"dev/composite_model_implementation_plan/#Completion-Evidence","page":"Composite model/object implementation plan","text":"The requirement-by-requirement evidence and final verification commands are recorded in composite_model_completion_audit.md.","title":"Completion Evidence"},{"category":"section","location":"journeys/users/one_plant/#Build-One-Multiscale-Plant","page":"One multiscale plant","text":"","title":"Build One Multiscale Plant"},{"category":"section","location":"journeys/users/one_plant/#New-concept:-topology-and-cross-object-values","page":"One multiscale plant","text":"The previous journey used independent objects at one scale. A multiscale plant adds parent/child topology: one plant object owns two leaf objects.\n\nThe scope picture for this page is:\n\n:plant — Subtree() from here contains :plant, :leaf_1, and :leaf_2<br> ├─ :leaf_1 — Self() is :leaf_1; SelfPlant() resolves to :plant<br> └─ :leaf_2 — Self() is :leaf_2; SelfPlant() resolves to :plant\n\nSelectors still filter that scope. For example, Many(scale=:Leaf, within=Subtree()) selects the two leaves when evaluated for the plant application.","title":"New concept: topology and cross-object values"},{"category":"section","location":"journeys/users/one_plant/#First-pass:-one-scalar-value-from-plant-to-leaves","page":"One multiscale plant","text":"Start with leaf surfaces and total plant surface supplied as status. The only new value connection sends the plant-level absorbed light to each leaf.\n\nusing PlantSimEngine, DataFrames\nusing PlantSimEngine.Examples\n\nobjects = (\n    Object(\n        :plant;\n        scale=:Plant,\n        kind=:plant,\n        status=Status(aPPFD=120.0, surface=3.0),\n    ),\n    Object(\n        :leaf_1;\n        scale=:Leaf,\n        kind=:leaf,\n        parent=:plant,\n        status=Status(surface=1.0),\n    ),\n    Object(\n        :leaf_2;\n        scale=:Leaf,\n        kind=:leaf,\n        parent=:plant,\n        status=Status(surface=2.0),\n    ),\n)\n\nscalar_model = CompositeModel(\n    objects...;\n    applications=(\n        ModelSpec(\n            ToyLightPartitioningModel();\n            name=:leaf_light,\n            on=Many(scale=:Leaf),\n            inputs=(\n                :aPPFD_larger_scale => One(\n                    scale=:Plant,\n                    within=SelfPlant(),\n                    var=:aPPFD,\n                ),\n                :total_surface => One(\n                    scale=:Plant,\n                    within=SelfPlant(),\n                    var=:surface,\n                ),\n            ),\n        ),\n    ),\n)\n\nscalar_simulation = run!(scalar_model; outputs=:all)\nscalar_states = final_state(scalar_simulation, Many(scale=:Leaf))\nDict(id => state.aPPFD for (id, state) in scalar_states)\n\nThe leaf model reads its own surface directly from each leaf status. SelfPlant() makes the other two scalar sources plant-local:\n\nselect(\n    DataFrame(Diagnostics.explain_bindings(scalar_model)),\n    :consumer_id,\n    :input,\n    :source_ids,\n    :carrier_kind,\n)","title":"First pass: one scalar value from plant to leaves"},{"category":"section","location":"journeys/users/one_plant/#Second-pass:-compute-and-aggregate-leaf-surfaces","page":"One multiscale plant","text":"Now replace the supplied surfaces with two existing models:\n\nToyLeafSurfaceModel computes each leaf surface from its carbon biomass;\nToyPlantLeafSurfaceModel sums those leaf surfaces on the plant.\n\nThis is the first vector-like cross-object input. It comes after the scalar connection above, and differs only in the new :leaf_surfaces binding.\n\ncomputed_objects = (\n    Object(\n        :plant;\n        scale=:Plant,\n        kind=:plant,\n        status=Status(aPPFD=120.0),\n    ),\n    Object(\n        :leaf_1;\n        scale=:Leaf,\n        kind=:leaf,\n        parent=:plant,\n        status=Status(carbon_biomass=50.0),\n    ),\n    Object(\n        :leaf_2;\n        scale=:Leaf,\n        kind=:leaf,\n        parent=:plant,\n        status=Status(carbon_biomass=100.0),\n    ),\n)\n\ncomputed_model = CompositeModel(\n    computed_objects...;\n    applications=(\n        ModelSpec(\n            ToyLeafSurfaceModel(0.02);\n            name=:leaf_surface,\n            on=Many(scale=:Leaf),\n        ),\n        ModelSpec(\n            ToyPlantLeafSurfaceModel();\n            name=:plant_surface,\n            on=One(scale=:Plant),\n            inputs=(\n                :leaf_surfaces => Many(\n                    scale=:Leaf,\n                    within=Subtree(),\n                    application=:leaf_surface,\n                    var=:surface,\n                ),\n            ),\n        ),\n        ModelSpec(\n            ToyLightPartitioningModel();\n            name=:leaf_light,\n            on=Many(scale=:Leaf),\n            inputs=(\n                :aPPFD_larger_scale => One(\n                    scale=:Plant,\n                    within=SelfPlant(),\n                    var=:aPPFD,\n                ),\n                :total_surface => One(\n                    scale=:Plant,\n                    within=SelfPlant(),\n                    application=:plant_surface,\n                    var=:surface,\n                ),\n            ),\n        ),\n    ),\n)\n\ncomputed_simulation = run!(computed_model; outputs=:all)\nplant_state = final_state(computed_simulation, One(scale=:Plant))\nleaf_states = final_state(computed_simulation, Many(scale=:Leaf))\n(\n    plant_surface=plant_state.surface,\n    leaf_surfaces=Dict(id => state.surface for (id, state) in leaf_states),\n    leaf_light=Dict(id => state.aPPFD for (id, state) in leaf_states),\n)\n\nThe plant aggregation uses a live RefVector; the scalar connections remain single references:\n\nselect(\n    DataFrame(Diagnostics.explain_bindings(computed_model)),\n    :application_id,\n    :consumer_id,\n    :input,\n    :source_ids,\n    :carrier_kind,\n)","title":"Second pass: compute and aggregate leaf surfaces"},{"category":"section","location":"journeys/users/one_plant/#Page-recap","page":"One multiscale plant","text":"You added: parent/child topology, plant-to-leaf scalar connections, and one plant-local vector aggregation.\nPlantSimEngine inferred: same-leaf surface coupling, application order, and live scalar/vector reference carriers.\nYou keep explicit: object parentage, the scope of cross-object searches, and the source application when selecting a produced value.\nNew API names: parent, Self, SelfPlant, Subtree, and the application and var selector fields.","title":"Page recap"},{"category":"section","location":"guides/coupling/#Coupling-Models","page":"Coupling Models","text":"Use inputs when a model reads a value produced by another application. A unique same-object producer is inferred; cross-object sources should use an explicit One, OptionalOne, or Many selector. Inspect the resolved references with Diagnostics.explain_bindings.\n\nUse calls only when a parent algorithm owns child execution or iteration. Use run_call!(context, :name) to execute every resolved target. Pass sampled_environment=value to this bulk path when the caller already has one model-facing environment for all targets. Use call_model(context, :name) to inspect a singular dependency model without materializing a public target. For selection, target status access, custom ordering, or distinct environments, retrieve the vector-like collection with call_targets(context, :name) and execute individual targets. Trial calls use publish=false; accepted state is published once. Nested calls inherit publication suppression, so a descendant cannot publish inside an unpublished ancestor trial. Diagnostics.explain_calls and Diagnostics.explain_schedule show call-only targets and ordering.\n\nDiagnostics.explain_initialization(model) classifies values as supplied, generated, producer-bound, defaulted, required, or environment-bound before execution.","title":"Coupling Models"},{"category":"section","location":"guides/coupling/#Value-coupling","page":"Coupling Models","text":"A consumer on the same object needs no scenario syntax when exactly one canonical producer exists. Make cross-object intent explicit:\n\nModelSpec(PlantBalance(); name=:balance, on=Many(scale=:Plant), inputs=(:assimilation => Many(\n            scale=:Leaf,\n            within=Subtree(),\n            application=:photosynthesis,\n            var=:carbon,\n        ),\n        :soil_water => One(\n            scale=:Soil,\n            within=SceneScope(),\n            application=:soil,\n            var=:water,\n        ),))\n\nOne is a contract: zero or multiple matches are errors. Use OptionalOne only when absence has a scientific meaning, and Many when aggregation is part of the consumer model. within=Subtree() searches descendants of the current target; within=SelfPlant() anchors repeated plant instances; and SceneScope() is deliberately global.\n\nBy default, an input selector also identifies applications that produce the selected variable, and those producers are scheduled before the consumer. Use from_status=true only when the input deliberately reads the objects' current Status references independently of any producer:\n\nModelSpec(\n    ReserveConsumer();\n    inputs=(\n        :organ_reserves => Many(\n            scale=(:Leaf, :Internode),\n            within=Subtree(),\n            var=:reserve,\n            from_status=true,\n            after=:plant_allocation,\n        ),\n    ),\n)\n\nThis is a same-step live-reference binding. It cannot be combined with process, application, policy, or window. It does not infer a producer edge; use after=:application_id when the state must be read or mutated after a particular application. Otherwise, the scenario's application order is preserved.","title":"Value coupling"},{"category":"section","location":"guides/coupling/#Manual-calls","page":"Coupling Models","text":"ModelSpec(Optimizer(); name=:optimizer, on=Many(scale=:Plant), calls=(:leaf_energy => Many(scale=:Leaf, within=Subtree())))\n\nInside Optimizer, iterate over call_targets(context, :leaf_energy). Run candidate states with run_call!(target; publish=false) and the accepted state with publish=true. A call-only target is excluded from root scheduling, and an unpublished outer call suppresses publication by every nested descendant.\n\nAfter compilation, inspect Diagnostics.explain_bindings(compiled) for source identity and carrier type, Diagnostics.explain_calls(compiled) for call-only targets, and Diagnostics.explain_schedule(compiled) for root execution order. These rows are the supported diagnostic surface; compiled fields are internal.","title":"Manual calls"},{"category":"section","location":"guides/multiscale/from_one_object/#From-One-Object-To-A-Multiscale-CompositeModel","page":"From One Object To A Multiscale CompositeModel","text":"First run all models on one object with the concise CompositeModel(models...; status=...) constructor. Then move organ-specific applications to leaf objects and aggregate their outputs on a plant with Many(..., within=SelfPlant()) or an instance-local selector.\n\nCompare collected, application-qualified outputs with isapprox. Exact bitwise identity is not generally a valid requirement after changing reduction order.","title":"From One Object To A Multiscale CompositeModel"},{"category":"section","location":"planned_features/#Roadmap","page":"Roadmap","text":"PlantSimEngine now has one composite-model/object runtime for single-object, multiscale, multi-plant, soil, microclimate, and multirate simulations.\n\nCurrent priorities are:\n\nmigrate downstream model packages to CompositeModel, CompositeModelTemplate, ObjectInstance, and ModelSpec;\nstrengthen type-stability and allocation tests for million-object workloads;\nadd broader lifecycle tests for object creation, removal, movement, and environment-index refresh;\nimprove diagnostics for ambiguous selectors, writer conflicts, and temporal policies;\nvalidate mutable voxel, layer, and octree microclimate backends;\nexpand downstream release gates and performance benchmarks;\nevaluate parallel execution for independent compiled application batches.","title":"Roadmap"},{"category":"section","location":"planned_features/#Environment-and-microclimate-work","page":"Roadmap","text":"","title":"Environment and microclimate work"},{"category":"section","location":"planned_features/#Trial-environment-sampling","page":"Roadmap","text":"Coupled microclimate solvers can iterate on local environmental state before accepting a timestep. A canopy energy-balance model, for example, may need to:\n\npropose a trial canopy air temperature and humidity;\nrun leaf models against that trial air state;\nupdate the trial air state from leaf sensible and latent heat fluxes;\nrepeat until convergence;\ncommit only the accepted canopy or voxel air state to the mutable environment backend.\n\nPass non-committing trial state through run_call!:\n\nrun_call!(context, :leaf_energy; environment=trial_environment, publish=false)\n\nThen commit the accepted state through the model-facing environment API:\n\ncommit_environment!(context, accepted_environment)\nrun_call!(context, :leaf_energy; publish=true)\n\nThe transient state is interpreted by each target backend through its opaque compiled handle, so one call can sample different cells for different leaves. commit_environment! commits only the accepted state to a mutable backend. Future work is to validate full voxel, layer, and octree implementations on this same model-side API.\n\nThe full issue list is available on GitHub.","title":"Trial environment sampling"},{"category":"section","location":"agent_skill/#AI-agent-skill","page":"AI agent skill","text":"PlantSimEngine includes an optional Codex/OpenAI-style skill for users who want an AI agent to help write simulations or implement models.\n\nThe skill file is stored in the repository at:\n\nskills/plantsimengine/SKILL.md\n\nUsers can download the skills/plantsimengine folder and tell their agent to use the plantsimengine skill when working with PlantSimEngine.jl. The skill gives agents the package-specific conventions they need for:\n\nbuilding object graphs with CompositeModel, Object, CompositeModelTemplate, and ObjectInstance;\napplying models with ModelSpec and on;\ncoupling values and manual model calls with inputs and calls;\nusing cached hard-call plans through bulk run_call!, singular call_model, and lifecycle-maintained object target buffers;\nconfiguring multirate simulations with every, Dates.Period values, and temporal policies;\nbinding global or spatial microclimate through Environment;\nreasoning about model-clock weather aggregation, environment_hint reducers, and scenario source overrides;\ninspecting compiled scenarios with structured explanation helpers;\nreporting supplied, generated, bound, and unresolved variables with Diagnostics.explain_initialization;\naccessing the live model from lifecycle-capable kernels with runtime_model(context);\ninspecting homogeneous runtime batches with Diagnostics.explain_execution_plan;\ncollecting raw or requested model outputs with outputs, OutputRequest, collect_outputs, and Diagnostics.explain_output_retention;\nimplementing or wrapping models with @process, inputs_, outputs_, run!, hard dependencies, and model traits.\n\nThe canonical source is skills/plantsimengine/SKILL.md.\n\nAgents should still inspect the local package code before making changes. The skill is a usage and modeling guide, not a replacement for the current API definitions in src/.","title":"AI agent skill"},{"category":"section","location":"API/API_public/#Public-API","page":"Public API","text":"","title":"Public API"},{"category":"section","location":"API/API_public/#Unified-CompositeModel/Object-API","page":"Public API","text":"","title":"Unified CompositeModel/Object API"},{"category":"section","location":"API/API_public/#Scenario-and-model-applications","page":"Public API","text":"CompositeModel stores objects, model applications, instances, and environment.\nCompositeModel(model, models...; status=..., timestep=...) is the concise one-object form and lowers to the same object/application representation.\nObject represents one runtime entity with stable identity and status.\nobject_id(model, source) resolves an ObjectId, registered Object or Status, MTG node, or raw identifier against the live registry. MTG nodes retain the exact identity assigned by the model's id= accessor during adaptation or organogenesis; object_id does not reevaluate that accessor, and copied or foreign nodes are rejected. The same methods accept a RunContext or Simulation.\nCompositeModelTemplate and ObjectInstance reuse a model across instances.\nModelSpec(model; name=..., on=..., inputs=..., calls=..., outputs_to=..., every=..., environment=..., output_routing=..., updates=...) is the one application construction form.","title":"Scenario and model applications"},{"category":"section","location":"API/API_public/#Coupling","page":"Public API","text":"ModelSpec(...; inputs=...) declares value dependencies.\nbound_input(context, :name) opts a model kernel into an identity-aware BoundMany view for one of its declared Many inputs; object_ids(view) returns the aligned object identities without copying them.\nModelSpec(...; calls=...) declares manually executable child models.\nModelSpec(...; outputs_to=(name=OutputTo(selector; vars=...),)) declares status variables owned by the application but stored on selected destination objects. Each variable uses Required(T) or Default(value); the compiler resolves identities and rejects ambiguous writers before initializing statuses.\noutput_targets(context, :name) returns the compiled OutputTargets view for one named outputs_to group. Destination columns are exposed explicitly as targets.columns.<variable>, and object_ids(targets) returns their aligned, read-only identities.\nassign_outputs!(targets, table; id=:object_id) assigns a Tables.jl-compatible result by identity. The lower-level assign_outputs!(targets, ids, columns) overload accepts an ID vector and a NamedTuple of columns directly.\nUpdates(:variable; after=:application_id) orders intentional duplicate writers.\nInput(...) and Call(...) express model defaults through dep(model).\nInitializer(One(application=:name, ...)) declares one normally scheduled application that may initialize a newly registered object during its creation event.\nrun_call!(context, :name; publish=false) executes every resolved hard-call target and always returns a vector-like CallTargets collection.\nrun_call!(context, :name; sampled_environment=value) forwards one already sampled model-facing environment through cached typed execution batches.\ncall_model(context, :name) returns the concrete model when a call resolves to exactly one target.\ncall_targets(context, :name) returns the same non-executing collection for fine-grained execution with run_call!(target; ...).\nrun_initializer!(context, :name, object) runs an Initializer binding once on that newborn object, initializes canonical local status without an extra mid-step output sample, and returns its canonical Status. It is not a trial-call or existing-object API.\n\nDistributed assignment requires exact destination coverage. Every selected object ID must occur exactly once and every declared output column must be present; additional table or NamedTuple columns are treated as metadata and ignored. Result columns may alias destination storage only for direct self-assignment of the same column in exact destination order.\n\nObtain OutputTargets inside each model invocation and do not retain it across a lifecycle barrier. Reusing the same ID-column object lets PlantSimEngine reuse its compiled row permutation and promises that the IDs and their order have not been mutated. Replace the ID-column object when either changes.","title":"Coupling"},{"category":"section","location":"API/API_public/#Model-input-schema","page":"Public API","text":"Required(T) declares an input that object state or another application must supply. T is an expected type and may be generic.\nDefault(value) declares a true model fallback that needs no user initialization.\ninputs_(model) uses only these explicit declarations; plain literals are rejected.\noutputs_(model) literals remain initial output-state values.\ninit_variables(model) returns only genuine input defaults and initial output values.\nVariableContract records a variable's unit, spatial or object basis, temporal basis, aggregation meaning, and intensive/extensive character without wrapping its runtime value.\nvariable_contracts(model) returns validated declarations from the package-extension trait PlantSimEngine.variable_contracts_. A compiled producer-consumer binding must have identical contracts once either side declares one.","title":"Model input schema"},{"category":"section","location":"API/API_public/#Selectors","page":"Public API","text":"Multiplicity: One(...), OptionalOne(...), and Many(...).\nScope: SceneScope(), Self(), Subtree(), SelfPlant(), Ancestor(...), and Scope(name).\nLabel criteria: kind=..., species=..., scale=..., and name=....\nTopology relations: Relation(...).\n\nSelf() always means the current object: the object on which the consuming application runs. It means a plant only when that object is itself the plant.\n\nSelector fields are checked where the selector is used:\n\nContext Accepted criteria\nModelSpec(...; on=...) kind, species, scale, name, and a scene or named scope\nModelSpec(...; inputs=...) object criteria plus process, application, var, policy, window, from_status, and after\nModelSpec(...; calls=...) object criteria plus process and application\nOutputTo(...) in ModelSpec(...; outputs_to=...) object criteria only\nobject queries and OutputRequest selectors object criteria only\n\nUnsupported or misspelled fields fail when the selector is constructed. Object-relative scopes and relations require a current object, so they belong in inputs, calls, or contextual object/output queries rather than application targets.","title":"Selectors"},{"category":"section","location":"API/API_public/#Time-and-environment","page":"Public API","text":"ModelSpec(...; every=period) sets an application cadence.\nHoldLast, Interpolate, Integrate, and Aggregate define temporal input policies.\nEnvironment(...) configures environment providers and source remapping.\nModels declare sampled environment variables with environment_inputs_.\nMutable environment controllers pass trial state with run_call!(context, name; environment=trial_state) and commit accepted state with commit_environment!.\nOutputRequest(selector, variable; ...) selects retained and optionally resampled streams using the same object selector grammar.","title":"Time and environment"},{"category":"section","location":"API/API_public/#Lifecycle","page":"Public API","text":"objects_from_mtg and CompositeModel(mtg; ...) adapt an MTG into the object registry.\nadd_organ! creates and initializes a new organ in an MTG-backed model.\nruntime_model(context) gives lifecycle-capable kernels sanctioned access to the live model from their RunContext.\nobject_id(context), model_object(context), model_status(context), and source_node(context) resolve the current execution target. The status accessor returns canonical registry state rather than the application-local status view passed to a kernel.\nregister_object!, remove_object!, and reparent_object! change topology.\nmove_object! and update_geometry! change spatial state.\nSupported lifecycle operations automatically invalidate and refresh the affected structural or spatial bindings before the next timestep.\nA creator that must run an application which already completed on existing objects declares an Initializer call. The compiler orders the scheduled target before the creator and the creator before direct non-temporal same-step consumers; run_initializer! admits exactly one target from the current pure-addition event and rejects repeat, existing, reparented, manual-call, and refresh-fallback execution. Each initialized output must have one potential canonical writer across local and distributed destinations. Because run_initializer! emits no mid-step stream sample, downstream temporal consumers of a possible newborn output are rejected at compilation; a PreviousTimeStep input used by the initializer itself remains supported.\nrun!(model; steps=..., outputs=:none) starts a fresh result timeline and returns a Simulation.\ncontinue!(simulation; steps=...) and step!(simulation) advance an existing timeline without resetting temporal state.\ncurrent_step(simulation) reports the accepted timeline position.\nfinal_state(simulation) returns a latest-state snapshot without requiring output retention; pass an object id or selector for multi-object simulations.\ncollect_outputs(sim) materializes retained output streams.","title":"Lifecycle"},{"category":"section","location":"API/API_public/#Explanations","page":"Public API","text":"Use the Diagnostics namespace instead of inspecting internals:\n\nDiagnostics.explain_objects\nDiagnostics.explain_instances\nDiagnostics.explain_scopes\nDiagnostics.explain_applications\nDiagnostics.explain_bindings\nDiagnostics.explain_calls\nDiagnostics.explain_output_bindings\nDiagnostics.explain_environment_bindings\nDiagnostics.explain_schedule\nDiagnostics.explain_writers\nDiagnostics.explain_execution_plan\nDiagnostics.explain_output_retention\nDiagnostics.explain_outputs\nDiagnostics.explain_initialization\nDiagnostics.input_carrier, Diagnostics.input_value, and Diagnostics.has_reference_carrier\nDiagnostics.object_address\n\nSee Migrating To The CompositeModel/Object API for translations from removed APIs.","title":"Explanations"},{"category":"section","location":"API/API_public/#CompositeModel-graph-visualization-and-editing","page":"Public API","text":"GraphEditor.compile_model_report(model; strict=false) preserves partial graph state and structured diagnostics for incomplete or cyclic composite models.\nGraphEditor.model_graph_view(model; level=:applications) returns the typed graph view.\nGraphEditor.model_graph_view_json(model) serializes the same DTO used by the browser.\nGraphEditor.write_model_graph_view(path, model) writes a self-contained static viewer.\nGraphEditor.edit_graph(model; templates=..., environments=...) starts the optional HTTP editor after using HTTP and keeps catalog values authoritative in Julia.\nGraphEditor.current_model(session), GraphEditor.undo!(session), GraphEditor.redo!(session), and close(session) control an interactive session from Julia.\n\nSee Visualize And Edit A CompositeModel for the runnable workflow, model discovery, selector previews, cycle breaking, and Documenter embedding.","title":"CompositeModel graph visualization and editing"},{"category":"section","location":"API/API_public/#Environment-backend-extensions","page":"Public API","text":"Backend packages extend the protocol under EnvironmentAPI, including EnvironmentAPI.AbstractEnvironmentBackend, EnvironmentAPI.bind_environment, EnvironmentAPI.sample, EnvironmentAPI.commit_environment!, and EnvironmentAPI.update_index!. The root-level commit_environment! remains part of the ordinary model-kernel workflow for committing an accepted controller state.","title":"Environment backend extensions"},{"category":"section","location":"API/API_public/#Fitting-and-evaluation","page":"Public API","text":"Generic fitting and metrics live under Evaluation: Evaluation.fit, Evaluation.RMSE, Evaluation.NRMSE, Evaluation.EF, and Evaluation.dr. PlantMeteo reducers are accessed from PlantMeteo directly rather than being re-exported by PlantSimEngine.","title":"Fitting and evaluation"},{"category":"section","location":"API/API_public/#Advanced-compiler-API","page":"Public API","text":"Compiler representations, cache refresh operations, and low-level binding compilers live under PlantSimEngine.Advanced. They are intended for package integration, diagnostics development, and compiler work rather than ordinary scenario composition. Prefer Diagnostics.explain_*, which accepts a CompositeModel directly, over manually compiling and inspecting fields.\n\nExamples include Advanced.compile_composite_model, Advanced.refresh_bindings!, and the Advanced.CompiledCompositeModel family. These qualified APIs may evolve more quickly than the default modeling interface.","title":"Advanced compiler API"},{"category":"section","location":"API/API_public/#Index","page":"Public API","text":"Pages = [\"API_public.md\"]","title":"Index"},{"category":"section","location":"API/API_public/#API-Documentation","page":"Public API","text":"","title":"API Documentation"},{"category":"module","location":"API/API_public/#PlantSimEngine.Advanced","page":"Public API","text":"PlantSimEngine.Advanced\n\nQualified compiler, cache, and low-level runtime extension APIs. These symbols are available for diagnostics, package integration, and compiler development, but are intentionally not part of the default user namespace.\n\n\n\n\n\n","title":"PlantSimEngine.Advanced"},{"category":"type","location":"API/API_public/#PlantSimEngine.AbstractModel","page":"Public API","text":"Abstract model type. All models are subtypes of this one.\n\n\n\n\n\n","title":"PlantSimEngine.AbstractModel"},{"category":"type","location":"API/API_public/#PlantSimEngine.Aggregate","page":"Public API","text":"Reduce a producer window, using a mean by default.\n\n\n\n\n\n","title":"PlantSimEngine.Aggregate"},{"category":"type","location":"API/API_public/#PlantSimEngine.BoundMany","page":"Public API","text":"BoundMany <: AbstractVector\n\nIdentity-aware, reference-backed view of a compiled Many input.\n\nUse bound_input inside a model kernel to obtain this view. Ordinary positional indexing, iteration, broadcasting, and mutation operate on the same carrier already installed in the model status. object_ids returns the aligned, read-only object identities without copying them. Wrap an identity in ObjectId for unambiguous identity-based indexing; integer indexing remains positional.\n\nBoth aligned carriers use the one-based indexing contract of PlantSimEngine's compiled input storage.\n\n\n\n\n\n","title":"PlantSimEngine.BoundMany"},{"category":"type","location":"API/API_public/#PlantSimEngine.Call","page":"Public API","text":"Call(selector::AbstractObjectMultiplicity)\nCall(; kwargs...)\n\nDeclare the target selector for a manual hard call. Applications reached only through Call are removed from the root schedule and execute when their owner invokes run_call! or call_targets.\n\n\n\n\n\n","title":"PlantSimEngine.Call"},{"category":"type","location":"API/API_public/#PlantSimEngine.CallTarget","page":"Public API","text":"CallTarget\n\nOne resolved executable target of a declared hard call. Obtain targets from a CallTargets collection with call_targets, then pass an individual target to run_call!(::CallTarget). A target is a runtime view owned by its compiled simulation; construct hard-call relationships with ModelSpec(...; calls=...) rather than constructing this type directly.\n\n\n\n\n\n","title":"PlantSimEngine.CallTarget"},{"category":"type","location":"API/API_public/#PlantSimEngine.CallTargets","page":"Public API","text":"CallTargets <: AbstractVector{CallTarget}\n\nA cached vector-like view of the compiled targets for one declared hard call. Retrieving it does not allocate a replacement collection. Obtain it with call_targets or as the result of run_call!(::RunContext, ::Symbol).\n\n\n\n\n\n","title":"PlantSimEngine.CallTargets"},{"category":"type","location":"API/API_public/#PlantSimEngine.ClockSpec","page":"Public API","text":"ClockSpec(dt, phase=0)\n\nExecution interval and phase, expressed in simulation steps.\n\n\n\n\n\n","title":"PlantSimEngine.ClockSpec"},{"category":"method","location":"API/API_public/#PlantSimEngine.CompositeModel-Tuple{AbstractModel, Vararg{AbstractModel}}","page":"Public API","text":"CompositeModel(model::AbstractModel, models::AbstractModel...;\n      status=NamedTuple(), id=:scene, scale=:Scene, kind=nothing,\n      name=id, environment=nothing, timestep=nothing)\n\nConstruct a concise one-object simulation. This is syntax lowering only: it creates one ordinary Object, one normal ModelSpec per model, and a regular CompositeModel. The returned model therefore uses the same compiler, scheduler, diagnostics, lifecycle, and output system as explicitly assembled composite models.\n\nUse explicit Object, ModelSpec, and selector construction when applications need names, different cadences, explicit coupling, or different target sets. Use timestep to apply one common cadence to every supplied model.\n\n\n\n\n\n","title":"PlantSimEngine.CompositeModel"},{"category":"method","location":"API/API_public/#PlantSimEngine.CompositeModel-Tuple{CompositeModelTemplate}","page":"Public API","text":"CompositeModel(template::CompositeModelTemplate;\n               root, objects=(), name=nothing, overrides=NamedTuple(),\n               object_overrides=(), applications=(), environment=nothing)\n\nBuild an executable composite model from a reusable template mounted on one concrete object subtree. root may be the owned root Object, or its id when the root is included in objects. When name is omitted, it is inferred from the root object's name or id.\n\nThis constructor is syntax lowering for an ObjectInstance passed to the regular CompositeModel constructor. Use explicit ObjectInstance values when the same composite model contains several mounted templates.\n\n\n\n\n\n","title":"PlantSimEngine.CompositeModel"},{"category":"method","location":"API/API_public/#PlantSimEngine.CompositeModel-Tuple{MultiScaleTreeGraph.Node}","page":"Public API","text":"CompositeModel(root::MultiScaleTreeGraph.Node; applications=(), instances=(),\n      environment=nothing, id=node_id, scale=symbol, status=..., ...)\n\nBuild a unified model directly from an MTG subtree. The MTG accessors are retained and reused by add_organ! when the topology grows.\n\n\n\n\n\n","title":"PlantSimEngine.CompositeModel"},{"category":"method","location":"API/API_public/#PlantSimEngine.CompositeModel-Tuple{Vararg{Union{Object, ObjectInstance}}}","page":"Public API","text":"CompositeModel(items...; applications=(), instances=(), environment=nothing)\n\nCreate a model from Object and ObjectInstance values. Global applications and applications mounted from object instances are compiled through the same composite-model/object dependency graph.\n\n\n\n\n\n","title":"PlantSimEngine.CompositeModel"},{"category":"type","location":"API/API_public/#PlantSimEngine.CompositeModelTemplate","page":"Public API","text":"CompositeModelTemplate(applications=(); kind=nothing, species=nothing, parameters=NamedTuple())\n\nReusable model-application bundle for one kind of model object, such as a plant species. Each mounted ObjectInstance scopes unqualified ModelSpec(...; on=...) selectors to its own object subtree. Model objects are shared between instances unless an instance supplies an override.\n\nparameters stores template-level metadata. Parameter-field merging is not implicit: use an instance model override when model parameters differ.\n\n\n\n\n\n","title":"PlantSimEngine.CompositeModelTemplate"},{"category":"type","location":"API/API_public/#PlantSimEngine.Default","page":"Public API","text":"Default(value)\n\nDeclare a status input with a genuine model-provided default. The compiler adds value to a target object's status only when the variable is neither supplied by the user nor already present.\n\n\n\n\n\n","title":"PlantSimEngine.Default"},{"category":"type","location":"API/API_public/#PlantSimEngine.HoldLast","page":"Public API","text":"Use the latest available producer value.\n\n\n\n\n\n","title":"PlantSimEngine.HoldLast"},{"category":"type","location":"API/API_public/#PlantSimEngine.Initializer","page":"Public API","text":"Initializer(selector::One)\nInitializer(; application, ...)\n\nDeclare that a normally scheduled application may also initialize one newly registered object during the lifecycle event that created it. Initializers are executed with run_initializer!; unlike Call targets, their callee application remains in the root schedule and retains canonical writer ownership.\n\n\n\n\n\n","title":"PlantSimEngine.Initializer"},{"category":"type","location":"API/API_public/#PlantSimEngine.Integrate","page":"Public API","text":"Reduce a producer window, using a sum by default.\n\n\n\n\n\n","title":"PlantSimEngine.Integrate"},{"category":"type","location":"API/API_public/#PlantSimEngine.Interpolate","page":"Public API","text":"Interpolate(; mode=:linear, extrapolation=:linear)\n\nInterpolate a slower producer stream. Both mode and extrapolation accept :linear or :hold.\n\n\n\n\n\n","title":"PlantSimEngine.Interpolate"},{"category":"type","location":"API/API_public/#PlantSimEngine.ModelSpec","page":"Public API","text":"ModelSpec(model; name=nothing, on=nothing, inputs=NamedTuple(),\n          calls=NamedTuple(), outputs_to=NamedTuple(),\n          environment=nothing, every=nothing,\n          environment_bindings=NamedTuple(), environment_window=nothing,\n          output_routing=NamedTuple(), updates=())\n\nConfiguration for one model application in a CompositeModel.\n\nModelSpec is the single scenario-construction form. on selects the target objects, inputs and calls declare coupling, and a calls entry may wrap its selector in Initializer for targeted newborn initialization. outputs_to declares status outputs owned by this application but stored on selected destination objects, every selects application cadence, and environment accepts an Environment configuration. Output routing and intentional duplicate-writer ordering are declared directly with output_routing and updates.\n\nExample\n\nModelSpec(\n    model;\n    name=:leaf_energy,\n    on=Many(scale=:Leaf),\n    inputs=(:soil_water => One(scale=:Soil, var=:water),),\n    calls=(:stomata => One(within=Self(), application=:stomata),),\n    every=Dates.Hour(1),\n    environment=Environment(provider=:canopy),\n    output_routing=(temperature=:stream_only,),\n    updates=Updates(:temperature; after=:radiation),\n)\n\n\n\n\n\n","title":"PlantSimEngine.ModelSpec"},{"category":"type","location":"API/API_public/#PlantSimEngine.ObjectId","page":"Public API","text":"ObjectId(value)\n\nStable identity of one Object in a CompositeModel. Strings are normalized to symbols; an existing ObjectId is returned unchanged.\n\n\n\n\n\n","title":"PlantSimEngine.ObjectId"},{"category":"type","location":"API/API_public/#PlantSimEngine.ObjectInstance","page":"Public API","text":"ObjectInstance(name, template; root, objects=(), overrides=NamedTuple(), object_overrides=())\n\nMount a CompositeModelTemplate on one concrete object subtree.\n\nroot may be an Object owned by the instance or the id of an object supplied separately to CompositeModel. objects contains additional owned descendants. overrides maps one template application name to a replacement model implementing the same process. object_overrides contains Override entries for exceptional organs.\n\n\n\n\n\n","title":"PlantSimEngine.ObjectInstance"},{"category":"type","location":"API/API_public/#PlantSimEngine.OutputRequest","page":"Public API","text":"OutputRequest(selector, var; name=var, application=nothing, context=nothing,\n              policy=HoldLast(), clock=nothing)\nOutputRequest(scale, var; kwargs...)\n\nRequest retention and optional resampling of one model output stream.\n\nThe first form accepts the same One, OptionalOne, or Many selector used by ModelSpec selectors/bindings and object queries. Passing a scale is a convenience for Many(scale=scale).\n\n\n\n\n\n","title":"PlantSimEngine.OutputRequest"},{"category":"type","location":"API/API_public/#PlantSimEngine.OutputTargets","page":"Public API","text":"OutputTargets\n\nIdentity-aware, columnar destination view for one named OutputTo group. Obtain it inside a model kernel with output_targets, inspect its stable identities with object_ids, and assign identified result tables with assign_outputs!.\n\nDestination carriers are exposed explicitly as targets.columns.<variable>.\n\nThe view is valid for the current model invocation and lifecycle generation. Do not retain it across a lifecycle barrier.\n\n\n\n\n\n","title":"PlantSimEngine.OutputTargets"},{"category":"type","location":"API/API_public/#PlantSimEngine.OutputTo","page":"Public API","text":"OutputTo(selector; vars, coverage=:exact)\n\nDeclare status outputs computed by one model application and stored on other objects selected by selector.\n\nvars is a non-empty NamedTuple whose values are Required or Default declarations. coverage=:exact requires the application to publish every declared variable for every selected destination; no partial coverage policy is currently supported.\n\nExample\n\nOutputTo(\n    Many(scale=(:Leaf, :Internode), within=SceneScope());\n    vars=(\n        incident_par=Default(0.0),\n        absorbed_par=Required(Float64),\n    ),\n)\n\n\n\n\n\n","title":"PlantSimEngine.OutputTo"},{"category":"type","location":"API/API_public/#PlantSimEngine.Override","page":"Public API","text":"Override(; object, application, model)\n\nReplace one template model application on one exceptional object. Select the template application by its application name. The replacement must implement the same process and variable contract.\n\n\n\n\n\n","title":"PlantSimEngine.Override"},{"category":"type","location":"API/API_public/#PlantSimEngine.PreviousTimeStep","page":"Public API","text":"PreviousTimeStep(variable)\n\nA structure to flag a model input as using the value computed on the previous model timestep. This breaks same-timestep coupling cycles. The value can be initialized in the Status if needed.\n\n\n\n\n\n","title":"PlantSimEngine.PreviousTimeStep"},{"category":"type","location":"API/API_public/#PlantSimEngine.Required","page":"Public API","text":"Required(T)\n\nDeclare a status input that must be supplied by object state or resolved from another model application.\n\nT is an expected type, not an initialization value. It may be abstract or parametric so model declarations remain generic.\n\n\n\n\n\n","title":"PlantSimEngine.Required"},{"category":"type","location":"API/API_public/#PlantSimEngine.RunContext","page":"Public API","text":"RunContext\n\nRuntime context passed as the final argument to model kernels. Use runtime_model, bound_input, output_targets, call_targets, and run_call! instead of inspecting its fields.\n\n\n\n\n\n","title":"PlantSimEngine.RunContext"},{"category":"type","location":"API/API_public/#PlantSimEngine.Self","page":"Public API","text":"Self()\n\nSelect the current object: the object on which the consuming model application runs. Self() means a plant only when that object is itself the plant.\n\n\n\n\n\n","title":"PlantSimEngine.Self"},{"category":"type","location":"API/API_public/#PlantSimEngine.Simulation","page":"Public API","text":"Simulation\n\nResult of running a CompositeModel. Use outputs, collect_outputs, final_state, and PlantSimEngine.Diagnostics to inspect it.\n\n\n\n\n\n","title":"PlantSimEngine.Simulation"},{"category":"type","location":"API/API_public/#PlantSimEngine.Status","page":"Public API","text":"Status(vars)\n\nStatus type used to store the values of the variables during simulation. It is mainly used as the structure to store the variables in the TimeStepRow of a TimeStepTable (see  PlantMeteo.jl docs).\n\nMost of the code is taken from MasonProtter/MutableNamedTuples.jl, so Status is a MutableNamedTuples with a few modifications, so in essence, it is a stuct that stores a NamedTuple of the references to the values of the variables, which makes it mutable.\n\nExamples\n\nA leaf with one value for all variables will make a status with one time step:\n\njulia> using PlantSimEngine\n\njulia> st = PlantSimEngine.Status(Ra_SW_f=13.747, sky_fraction=1.0, d=0.03, aPPFD=1500.0);\n\nAll these indexing methods are valid:\n\njulia> st[:Ra_SW_f]\n13.747\n\njulia> st.Ra_SW_f\n13.747\n\njulia> st[1]\n13.747\n\nSetting a Status variable is very easy:\n\njulia> st[:Ra_SW_f] = 20.0\n20.0\n\njulia> st.Ra_SW_f = 21.0\n21.0\n\njulia> st[1] = 22.0\n22.0\n\n\n\n\n\n","title":"PlantSimEngine.Status"},{"category":"type","location":"API/API_public/#PlantSimEngine.Updates","page":"Public API","text":"Updates(vars...; after=nothing)\n\nScenario-level declaration that a model updates variables which may also be computed by another model at the same scale.\n\nafter contains canonical application identifiers. It is intentionally scenario-level metadata: the model implementation stays reusable, while the simulation setup can declare ordering constraints that only exist in this coupling.\n\n\n\n\n\n","title":"PlantSimEngine.Updates"},{"category":"type","location":"API/API_public/#PlantSimEngine.VariableContract","page":"Public API","text":"VariableContract(; unit, basis=nothing, temporal=nothing,\n                   aggregation=nothing, extent=nothing)\n\nScientific meaning attached to a model variable without wrapping its runtime numeric value.\n\nunit names the physical numerator unit, for example :mol_photon.\nbasis names the normalization basis, for example :leaf_area, :ground_area, or :plant.\ntemporal names the time basis, for example :second, :day, or :step.\naggregation distinguishes values such as :instantaneous, :rate, :mean, :total, and :accumulated.\nextent distinguishes :intensive and :extensive quantities when useful.\n\nTokens are intentionally open Symbols so packages can extend the vocabulary. The compiler compares complete contracts exactly; two connected model variables must therefore use the same tokens. Runtime payloads remain ordinary numbers or arrays.\n\n\n\n\n\n","title":"PlantSimEngine.VariableContract"},{"category":"method","location":"API/API_public/#PlantSimEngine.Environment-Tuple{Any}","page":"Public API","text":"Environment(config)\nEnvironment(; kwargs...)\n\nEnvironment configuration for a ModelSpec.\n\nPass the result directly with environment=Environment(...).\n\n\n\n\n\n","title":"PlantSimEngine.Environment"},{"category":"method","location":"API/API_public/#PlantSimEngine.add_organ!-Tuple{MultiScaleTreeGraph.Node, Any, Any, Any, Integer}","page":"Public API","text":"add_organ!(parent, runtime, link, symbol, scale; index=0, id, attributes=(),\n           initial_status=(), use_status_adapter=true, kind=nothing,\n           species=nothing, name=nothing)\n\nCreate an MTG node and register its corresponding model object as one operation. runtime may be a CompositeModel, RunContext, or Simulation. The model reuses the MTG accessors and status initializer supplied when it was constructed, then overlays initial_status.\n\nSet use_status_adapter=false only when the caller guarantees that the new node's attributes and initial_status completely define its initial status. In that mode the configured MTG status accessor is not called for the new node. The node status field is always forced to the newly created node.\n\nThis is the public growth API. register_object! remains the low-level registry operation for callers that already own a fully initialized Object.\n\n\n\n\n\n","title":"PlantSimEngine.add_organ!"},{"category":"method","location":"API/API_public/#PlantSimEngine.application_name-Tuple{ModelSpec}","page":"Public API","text":"application_name(spec::ModelSpec)\n\nOptional stable name for one model application in the unified composite-model/object API.\n\n\n\n\n\n","title":"PlantSimEngine.application_name"},{"category":"method","location":"API/API_public/#PlantSimEngine.applies_to-Tuple{ModelSpec}","page":"Public API","text":"applies_to(spec::ModelSpec)\n\nObject selector where a model application runs in the unified composite-model/object API.\n\n\n\n\n\n","title":"PlantSimEngine.applies_to"},{"category":"method","location":"API/API_public/#PlantSimEngine.assign_outputs!-Tuple{OutputTargets, AbstractVector, NamedTuple}","page":"Public API","text":"assign_outputs!(targets::OutputTargets, ids, columns::NamedTuple)\n\nLower-level identified-column assignment. columns must contain every output declared by targets; its extra fields are ignored. This overload avoids a Tables.jl adapter on the stable columnar path.\n\n\n\n\n\n","title":"PlantSimEngine.assign_outputs!"},{"category":"method","location":"API/API_public/#PlantSimEngine.assign_outputs!-Tuple{OutputTargets, Any}","page":"Public API","text":"assign_outputs!(targets::OutputTargets, table; id=:object_id)\n\nAssign every declared output column from an identified Tables.jl-compatible table. Destination coverage is exact: unknown, duplicate, extra, and missing IDs are rejected before any status is changed. Additional table metadata columns are ignored, while every output declared by the target group is required.\n\nResult columns must not alias output destination storage, except for a direct self-assignment in exact destination order. Custom array types that wrap shared storage must implement Julia's Base.dataids/Base.mightalias contract so aliasing can be rejected before any status is changed.\n\nThe row permutation is cached by identity of the ID column. Reusing that column promises that its IDs and order remain unchanged; replace the ID column object when either changes.\n\n\n\n\n\n","title":"PlantSimEngine.assign_outputs!"},{"category":"method","location":"API/API_public/#PlantSimEngine.bound_input-Tuple{RunContext, Symbol}","page":"Public API","text":"bound_input(context::RunContext, input)\n\nReturn an identity-aware BoundMany view for the declared Many input named input on the application currently executing. The view reuses the compiler-owned object identities and the live vector already installed in the model status.\n\n\n\n\n\n","title":"PlantSimEngine.bound_input"},{"category":"method","location":"API/API_public/#PlantSimEngine.call_targets-Tuple{RunContext, Symbol}","page":"Public API","text":"call_targets(context::RunContext, name::Symbol; objects=nothing)\n\nReturn a cached, non-executing vector-like view of the targets declared for name with ModelSpec(...; calls=...). The collection is empty for an unresolved OptionalOne, has one element for One, and contains every resolved target for Many.\n\nWhen objects is provided, resolve the declared call against the current object topology and restrict the result to those objects. This explicit form is intended for models that create objects and immediately initialize selected manual-call-only applications on them. Structural refresh still occurs at the safe barrier after a pure-addition event. If the pending event also removes or reparents objects, this accessor performs a full binding/environment refresh before resolving the explicit targets. Use Initializer and run_initializer! instead when the target application must remain in the normal schedule. Each requested object may be an ObjectId, Object, an MTG node, or the Status returned by add_organ!.\n\nUse this accessor with run_call!(::CallTarget) when targets need different sampled environments, selective execution, a controlled order, or separate trial and accepted publication.\n\n\n\n\n\n","title":"PlantSimEngine.call_targets"},{"category":"method","location":"API/API_public/#PlantSimEngine.commit_environment!-Tuple{PlantSimEngine.AbstractEnvironmentBackend, Any, Any, Any}","page":"Public API","text":"commit_environment!(backend, handle, state, time)\n\nCommit an accepted environment state through an opaque compiled backend handle. Model kernels call commit_environment!(context, state); backend authors implement this method for their concrete mutable environment.\n\n\n\n\n\n","title":"PlantSimEngine.commit_environment!"},{"category":"method","location":"API/API_public/#PlantSimEngine.commit_environment!-Tuple{RunContext, Any}","page":"Public API","text":"commit_environment!(context::RunContext, state)\n\nCommit an accepted environment state through the opaque handle compiled for the currently running model application/object. The model must declare the variables it commits with environment_outputs_.\n\nCommits are ignored while the current model is executing as a non-publishing hard call. Trial environment states should instead be passed to run_call!(context, name; environment=state).\n\n\n\n\n\n","title":"PlantSimEngine.commit_environment!"},{"category":"method","location":"API/API_public/#PlantSimEngine.continue!-Tuple{Simulation}","page":"Public API","text":"continue!(simulation; steps=1)\n\nAdvance an existing Simulation without resetting its timeline, retained streams, temporal dependency history, or environment position.\n\n\n\n\n\n","title":"PlantSimEngine.continue!"},{"category":"method","location":"API/API_public/#PlantSimEngine.dep-Tuple{AbstractModel}","page":"Public API","text":"dep(model)\n\nReturn model-level default Input(...), Call(...), and Initializer(...) declarations. Models without explicit coupling requirements return an empty NamedTuple.\n\n\n\n\n\n","title":"PlantSimEngine.dep"},{"category":"method","location":"API/API_public/#PlantSimEngine.environment_bindings-Tuple{ModelSpec}","page":"Public API","text":"environment_bindings(spec::ModelSpec)\n\nOptional explicit weather aggregation bindings used by the model runtime. Each key is the target environment variable exposed to the model at execution time. Each value can be:\n\nPlantMeteo reducer instance/type (e.g. MeanWeighted(), MaxReducer)\nFunction: custom reducer callable\nNamedTuple: optional fields source and reducer\n\n\n\n\n\n","title":"PlantSimEngine.environment_bindings"},{"category":"method","location":"API/API_public/#PlantSimEngine.environment_config-Tuple{ModelSpec}","page":"Public API","text":"environment_config(spec::ModelSpec)\n\nOptional composite-model/object environment configuration declared with ModelSpec(...; environment=Environment(...)).\n\n\n\n\n\n","title":"PlantSimEngine.environment_config"},{"category":"method","location":"API/API_public/#PlantSimEngine.environment_inputs-Tuple{AbstractModel}","page":"Public API","text":"environment_inputs(model::AbstractModel)\nenvironment_inputs_(model::AbstractModel)\n\nEnvironment variables read directly by a model.\n\nThis trait is separate from inputs_ because meteorology may be constant, table-backed, or produced by a microclimate backend. The default is empty.\n\n\n\n\n\n","title":"PlantSimEngine.environment_inputs"},{"category":"method","location":"API/API_public/#PlantSimEngine.environment_outputs-Tuple{AbstractModel}","page":"Public API","text":"environment_outputs(model::AbstractModel)\nenvironment_outputs_(model::AbstractModel)\n\nEnvironment variables that a controller model is allowed to commit with commit_environment!, for example local microclimate variables computed over a canopy, voxel, or octree backend.\n\nThese declarations are environment capabilities, not status outputs. Declare diagnostic status values separately with outputs_.\n\n\n\n\n\n","title":"PlantSimEngine.environment_outputs"},{"category":"method","location":"API/API_public/#PlantSimEngine.environment_window-Tuple{ModelSpec}","page":"Public API","text":"environment_window(spec::ModelSpec)\n\nOptional weather window-selection strategy used by the model runtime. Defaults to nothing (runtime falls back to PlantMeteo.RollingWindow() behavior).\n\n\n\n\n\n","title":"PlantSimEngine.environment_window"},{"category":"method","location":"API/API_public/#PlantSimEngine.final_state-Tuple{Simulation}","page":"Public API","text":"final_state(simulation)\nfinal_state(simulation, object_id)\nfinal_state(simulation, selector; context=nothing)\n\nReturn a NamedTuple snapshot of the latest canonical object status. The no-selector form requires the simulation to contain exactly one object. One returns one snapshot, OptionalOne returns one snapshot or nothing, and Many returns a dictionary from object ids to snapshots.\n\nThis accessor reports final state, independently of output retention. Use collect_outputs for retained history.\n\n\n\n\n\n","title":"PlantSimEngine.final_state"},{"category":"method","location":"API/API_public/#PlantSimEngine.init_variables-Tuple{AbstractModel}","page":"Public API","text":"init_variables(model)\n\nReturn the merged genuine input defaults and initial output-state values declared by model. Inputs declared with Required(T) are omitted.\n\n\n\n\n\n","title":"PlantSimEngine.init_variables"},{"category":"method","location":"API/API_public/#PlantSimEngine.inputs-Tuple{T} where T<:AbstractModel","page":"Public API","text":"inputs(model::AbstractModel)\ninputs(...)\n\nGet the inputs of one or several models.\n\nReturns an empty tuple by default for AbstractModels (no inputs) or Missing models.\n\nExamples\n\nusing PlantSimEngine;\n\n# Load the dummy models given as example in the package:\nusing PlantSimEngine.Examples;\n\ninputs(Process1Model(1.0))\n\n# output\n(:var1, :var2)\n\n\n\n\n\n","title":"PlantSimEngine.inputs"},{"category":"method","location":"API/API_public/#PlantSimEngine.model_calls-Tuple{ModelSpec}","page":"Public API","text":"model_calls(spec::ModelSpec)\n\nUnified composite-model/object call bindings declared with the ModelSpec(...; calls=...) keyword. Ordinary object selectors declare manual calls; Initializer wraps a selector for targeted newborn initialization while leaving the callee normally scheduled.\n\n\n\n\n\n","title":"PlantSimEngine.model_calls"},{"category":"method","location":"API/API_public/#PlantSimEngine.model_object-Tuple{CompositeModel, Any}","page":"Public API","text":"model_object(model::CompositeModel, id) -> Object\n\nReturn the single object registered under id. The identifier may be either an ObjectId or the value used to construct one. An error is raised when the registry contains no matching object.\n\n\n\n\n\n","title":"PlantSimEngine.model_object"},{"category":"method","location":"API/API_public/#PlantSimEngine.model_status-Tuple{CompositeModel, Any}","page":"Public API","text":"model_status(model::CompositeModel, source)\n\nReturn the runtime Status, or nothing, owned by the model object represented by source. source accepts the same identities as object_id, including an exact MTG node. Runtime status belongs to the model registry and is not stored in MTG attributes.\n\n\n\n\n\n","title":"PlantSimEngine.model_status"},{"category":"method","location":"API/API_public/#PlantSimEngine.object_id-Tuple{CompositeModel, ObjectId}","page":"Public API","text":"object_id(model::CompositeModel, source) -> ObjectId\n\nReturn the registered ObjectId represented by source.\n\nsource may be an ObjectId, a registered Object, a registered Status, an MTG node, or the raw value used to construct an ObjectId. For an MTG model, the id= accessor is evaluated during initial adaptation or organogenesis and the exact node-to-ObjectId association is retained; object_id does not reevaluate the accessor. Later changes to a node's attributes or raw MTG id therefore do not change its model identity. A node from a copied or foreign MTG is rejected even when its raw node id is identical.\n\nEvery result is checked against the live object registry. Removed or unknown objects therefore raise an error instead of returning a stale identity. RunContext, CallTarget, and Simulation delegate to their live model.\n\n\n\n\n\n","title":"PlantSimEngine.object_id"},{"category":"method","location":"API/API_public/#PlantSimEngine.object_id-Tuple{Union{CallTarget, RunContext}}","page":"Public API","text":"object_id(context::Union{RunContext,CallTarget})\nmodel_object(context::Union{RunContext,CallTarget})\nmodel_status(context::Union{RunContext,CallTarget})\nsource_node(context::Union{RunContext,CallTarget})\n\nResolve the current execution target. model_status(context) returns the canonical registry Status, not the application-local status view passed to the kernel. source_node(context) is available for MTG-backed models and avoids requiring a topology node inside that local status view.\n\n\n\n\n\n","title":"PlantSimEngine.object_id"},{"category":"method","location":"API/API_public/#PlantSimEngine.object_ids-Tuple{BoundMany}","page":"Public API","text":"object_ids(values::BoundMany)\n\nReturn the live, read-only ObjectId view aligned with values. The view is maintained by compiled lifecycle refresh and does not copy the identity vector.\n\n\n\n\n\n","title":"PlantSimEngine.object_ids"},{"category":"method","location":"API/API_public/#PlantSimEngine.objects_from_mtg-Tuple{MultiScaleTreeGraph.Node}","page":"Public API","text":"objects_from_mtg(root; id=node_id, scale=symbol, kind=..., species=...,\n                 name=..., geometry=..., status=...)\n\nAdapt one MTG subtree to model Object values. The MTG is traversed once; node ids and parent relations become stable model-object identities and relations. Accessors may attach labels and geometry without prescribing a plant architecture. Runtime status is not read from MTG attributes. Pass an explicit status= accessor only at a deliberate import boundary. This standalone projection retains no lifecycle identity index; construct CompositeModel(root) when later node resolution or organogenesis is required.\n\n\n\n\n\n","title":"PlantSimEngine.objects_from_mtg"},{"category":"method","location":"API/API_public/#PlantSimEngine.output_policy-Tuple{AbstractModel}","page":"Public API","text":"output_policy(model::AbstractModel)\noutput_policy(::Type{<:AbstractModel})\n\nPer-output scheduling policy for a model. Default is empty, meaning all outputs fallback to hold-last behaviour.\n\nWhen multi-rate input bindings are inferred automatically, this trait also provides the default cross-clock policy (HoldLast, Integrate, Aggregate, or Interpolate) for each producer output.\n\n\n\n\n\n","title":"PlantSimEngine.output_policy"},{"category":"method","location":"API/API_public/#PlantSimEngine.output_routing-Tuple{ModelSpec}","page":"Public API","text":"output_routing(spec::ModelSpec)\n\nPer-output routing mode for multi-rate runs. Allowed values are:\n\n:canonical (default): output participates in canonical status publication.\n:stream_only: output is only tracked in temporal streams.\n\n\n\n\n\n","title":"PlantSimEngine.output_routing"},{"category":"method","location":"API/API_public/#PlantSimEngine.output_targets-Tuple{RunContext, Symbol}","page":"Public API","text":"output_targets(context::RunContext, group)\n\nReturn the compiled OutputTargets view for the named outputs_to group on the application currently executing. The lookup is a typed field access; selectors and destination indexes were resolved before the kernel.\n\n\n\n\n\n","title":"PlantSimEngine.output_targets"},{"category":"method","location":"API/API_public/#PlantSimEngine.outputs-Tuple{T} where T<:AbstractModel","page":"Public API","text":"outputs(model::AbstractModel)\noutputs(...)\n\nGet the outputs of one or several models.\n\nReturns an empty tuple by default for AbstractModels (no outputs) or Missing models.\n\nExamples\n\nusing PlantSimEngine;\n\n# Load the dummy models given as example in the package:\nusing PlantSimEngine.Examples;\n\noutputs(Process1Model(1.0))\n\n# output\n(:var3,)\n\n\n\n\n\n","title":"PlantSimEngine.outputs"},{"category":"method","location":"API/API_public/#PlantSimEngine.outputs_to-Tuple{ModelSpec}","page":"Public API","text":"outputs_to(spec::ModelSpec)\n\nNamed distributed-output destinations declared with the ModelSpec(...; outputs_to=...) keyword.\n\n\n\n\n\n","title":"PlantSimEngine.outputs_to"},{"category":"method","location":"API/API_public/#PlantSimEngine.register_object!-Tuple{CompositeModel, Object}","page":"Public API","text":"register_object!(model, object; parent=object.parent)\n\nRegister a fully initialized Object in model. Structural bindings are marked dirty and become visible to execution after the next lifecycle refresh boundary. Prefer add_organ! for MTG-backed growth.\n\n\n\n\n\n","title":"PlantSimEngine.register_object!"},{"category":"method","location":"API/API_public/#PlantSimEngine.run!-Tuple{CompositeModel}","page":"Public API","text":"run!(model; steps=1, constants=Constants(), outputs=:none, performance=false)\n\nRun a fresh simulation timeline while mutating object status in model. Choose outputs=:none, outputs=:all, one OutputRequest, or a vector of requests. Use continue! on the returned Simulation to advance without resetting time. Set performance=true to record coarse compiler/runtime timing and work counters for diagnostics and performance regression tests.\n\n\n\n\n\n","title":"PlantSimEngine.run!"},{"category":"method","location":"API/API_public/#PlantSimEngine.run_call!-Tuple{RunContext, Symbol}","page":"Public API","text":"run_call!(context::RunContext, name::Symbol;\n          environment, sampled_environment, publish=false)\n\nExecute every target of the hard call declared as name and return its CallTargets collection. The return shape is always vector-like: One produces one element, OptionalOne zero or one, and Many zero or more. Initializer bindings are rejected here; execute those with run_initializer!.\n\nWhen environment is supplied, every target keeps its own opaque compiled backend handle and samples that transient backend-specific state. The state is inherited by nested hard calls. Omit it to sample the committed backend state.\n\nWhen sampled_environment is supplied, that already sampled model-facing value is forwarded directly to every target through the cached typed execution path. It is not resampled and is not inherited as a backend state by nested calls. environment and sampled_environment are mutually exclusive.\n\nFor finer-grained target selection, order, or direct per-target sampled environments, use call_targets and run_call!(::CallTarget). Commit an accepted mutable state with commit_environment! before publishing the accepted descendants.\n\n\n\n\n\n","title":"PlantSimEngine.run_call!"},{"category":"method","location":"API/API_public/#PlantSimEngine.run_initializer!-Tuple{RunContext, Symbol, Any}","page":"Public API","text":"run_initializer!(context::RunContext, name::Symbol, object)\n\nRun the application declared by name=Initializer(...) exactly once on one object registered during the current lifecycle event. The target application remains normally scheduled and retains canonical writer ownership. Its model mutates the newborn's canonical local status, but the targeted initializer does not publish an extra mid-step output sample or distributed/environment update. Consequently, only direct non-temporal downstream consumers may observe its newborn output in that step; downstream temporal consumers are rejected during scenario compilation.\n\nThe initializer target must use the caller's cadence and global environment, must not declare nested calls, distributed outputs, or stream-only outputs, and must be the sole potential canonical writer of each initialized output. It may use PreviousTimeStep as its only temporal input policy. The initialized object's canonical Status is returned. Calling the same initializer again for that application/object pair, or passing an existing or reparented object, is an error. The application/object pair is reserved before model code runs, so a failed attempt remains marked and cannot be retried in the same lifecycle event after an unknown partial mutation.\n\n\n\n\n\n","title":"PlantSimEngine.run_initializer!"},{"category":"method","location":"API/API_public/#PlantSimEngine.runtime_model-Tuple{CompositeModel}","page":"Public API","text":"runtime_model(runtime)\n\nReturn the live CompositeModel owned by a CompositeModel, RunContext, or Simulation. Lifecycle-capable models should call this accessor instead of reaching through runtime implementation fields.\n\n\n\n\n\n","title":"PlantSimEngine.runtime_model"},{"category":"method","location":"API/API_public/#PlantSimEngine.source_node-Tuple{CompositeModel, Any}","page":"Public API","text":"source_node(model::CompositeModel, source) -> MultiScaleTreeGraph.Node\n\nReturn the exact MTG node associated with a registered object identity. This is available only for a model constructed from an MTG; copied, foreign, and removed identities are rejected.\n\n\n\n\n\n","title":"PlantSimEngine.source_node"},{"category":"method","location":"API/API_public/#PlantSimEngine.step!-Tuple{Simulation}","page":"Public API","text":"step!(simulation)\n\nAdvance an existing Simulation by one timestep.\n\n\n\n\n\n","title":"PlantSimEngine.step!"},{"category":"method","location":"API/API_public/#PlantSimEngine.timespec-Tuple{AbstractModel}","page":"Public API","text":"timespec(model::AbstractModel)\ntimespec(::Type{<:AbstractModel})\n\nClock definition for a model. Default is single-rate behaviour (dt=1.0, phase=0.0).\n\n\n\n\n\n","title":"PlantSimEngine.timespec"},{"category":"method","location":"API/API_public/#PlantSimEngine.updates-Tuple{ModelSpec}","page":"Public API","text":"updates(spec::ModelSpec)\n\nScenario-level metadata for variables intentionally updated by this model after another producer on the same object.\n\n\n\n\n\n","title":"PlantSimEngine.updates"},{"category":"method","location":"API/API_public/#PlantSimEngine.validate_environment_inputs-Tuple{Dict{Symbol, Dict{Symbol, ModelSpec}}, Any}","page":"Public API","text":"validate_environment_inputs(model_specs, environment)\n\nValidate declared environment_inputs_ against the available environment fields.\n\nThe check is intentionally field-based and independent from units/backends. When Environment source bindings remap a declared model input from another variable; the source variable is checked on the raw environment object.\n\n\n\n\n\n","title":"PlantSimEngine.validate_environment_inputs"},{"category":"method","location":"API/API_public/#PlantSimEngine.validate_environment_inputs-Tuple{PlantSimEngine.CompiledCompositeModel, Any}","page":"Public API","text":"validate_environment_inputs(model::CompositeModel)\nvalidate_environment_inputs(compiled::CompiledCompositeModel)\nvalidate_environment_inputs(compiled::CompiledCompositeModel, environment_or_backend)\n\nValidate declared composite-model/object environment_inputs_.\n\nThe one-argument methods validate each application against its actual compiled environment backend, including application-level Environment(...) overrides. The two-argument method validates every compiled application against an explicit replacement environment backend. Diagnostics report model application ids, so several applications for the same process can be diagnosed independently.\n\n\n\n\n\n","title":"PlantSimEngine.validate_environment_inputs"},{"category":"method","location":"API/API_public/#PlantSimEngine.value_inputs-Tuple{ModelSpec}","page":"Public API","text":"value_inputs(spec::ModelSpec)\n\nUnified composite-model/object value-input bindings declared with the ModelSpec(...; inputs=...) keyword.\n\n\n\n\n\n","title":"PlantSimEngine.value_inputs"},{"category":"method","location":"API/API_public/#PlantSimEngine.variable_contracts-Tuple{Union{Missing, AbstractModel}}","page":"Public API","text":"variable_contracts(model)\n\nReturn the structurally validated variable-contract declaration for model. Compilation additionally checks each key against the concrete ModelSpec, including its distributed output declarations.\n\n\n\n\n\n","title":"PlantSimEngine.variable_contracts"},{"category":"method","location":"API/API_public/#PlantSimEngine.variables-Tuple{Module}","page":"Public API","text":"variables(pkg::Module)\n\nReturns a dataframe of all variables, their description and units in a package that has PlantSimEngine as a dependency (if implemented by the authors).\n\nNote to developers\n\nDevelopers of a package that depends on PlantSimEngine should  put a csv file in \"data/variables.csv\", then this file will be  returned by the function.\n\nExamples\n\nHere is an example with the PlantBiophysics package:\n\n#] add PlantBiophysics\nusing PlantBiophysics\nvariables(PlantBiophysics)\n\n\n\n\n\n","title":"PlantSimEngine.variables"},{"category":"method","location":"API/API_public/#PlantSimEngine.variables-Union{Tuple{T}, Tuple{T, Vararg{Any}}} where T<:Union{Missing, AbstractModel}","page":"Public API","text":"variables(model)\nvariables(model, models...)\n\nReturn the values PlantSimEngine can initialize without user input: genuine input defaults declared with Default(value) and initial output-state values. Required inputs have no initialization value and are therefore omitted.\n\nNote\n\nEach model can (and should) have a method for this function.\n\n\nusing PlantSimEngine;\n\n# Load the dummy models given as example in the package:\nusing PlantSimEngine.Examples;\n\nvariables(Process1Model(1.0))\n\nvariables(Process1Model(1.0), Process2Model())\n\n# output\n\n(var3 = -Inf, var4 = -Inf, var5 = -Inf)\n\nSee also\n\ninputs, outputs and variables_typed\n\n\n\n\n\n","title":"PlantSimEngine.variables"},{"category":"macro","location":"API/API_public/#PlantSimEngine.@process-Tuple{Any, Vararg{Any}}","page":"Public API","text":"@process(process::String, doc::String=\"\"; verbose::Bool=true)\n\nThis macro generate the abstract type and some boilerplate code for the simulation of a process, along  with its documentation. It also prints out a short tutorial for implementing a model if verbose=true.\n\nThe abstract process type is then used as a supertype of all models implementations for the  process, and is named \"Abstract<ProcessName>Model\", e.g. AbstractGrowthModel for a process called growth.\n\nThe first argument to @process is the new process name,  the second is any additional documentation that should be added  to the Abstract<ProcessName>Model type, and the third determines whether  the short tutorial should be printed or not.\n\nNewcomers are encouraged to use this macro because it explains in detail what to do next with the process. But more experienced users may want to directly define their process without  printing the tutorial. To do so, you can just define a new abstract type and define it as a  subtype of AbstractModel:\n\nabstract type MyNewProcess <: AbstractModel end\n\nExamples\n\n@process \"dummy_process\" \"This is a dummy process that shall not be used\"\n\n\n\n\n\n","title":"PlantSimEngine.@process"},{"category":"module","location":"API/API_public/#PlantSimEngine.Diagnostics","page":"Public API","text":"PlantSimEngine.Diagnostics\n\nStructured compiler/runtime explanations and supported carrier inspection.\n\n\n\n\n\n","title":"PlantSimEngine.Diagnostics"},{"category":"module","location":"API/API_public/#PlantSimEngine.GraphEditor","page":"Public API","text":"PlantSimEngine.GraphEditor\n\nStatic graph DTOs, model discovery, graph edits, and interactive editor sessions.\n\n\n\n\n\n","title":"PlantSimEngine.GraphEditor"},{"category":"module","location":"API/API_public/#PlantSimEngine.EnvironmentAPI","page":"Public API","text":"PlantSimEngine.EnvironmentAPI\n\nExtension protocol for global and spatial environment backends.\n\n\n\n\n\n","title":"PlantSimEngine.EnvironmentAPI"},{"category":"module","location":"API/API_public/#PlantSimEngine.Evaluation","page":"Public API","text":"PlantSimEngine.Evaluation\n\nGeneric model-fitting interface and simulation evaluation metrics.\n\n\n\n\n\n","title":"PlantSimEngine.Evaluation"},{"category":"section","location":"troubleshooting/common_errors/#Common-Errors","page":"Common errors","text":"A missing-input error means a Required(T) input has neither supplied state nor a producer binding; start with Diagnostics.explain_initialization. A plain-input-declaration error means inputs_ must replace each literal with Required(T) or Default(value). A cardinality error lists selector matches; correct the scope or choose the intended OptionalOne/Many multiplicity. An ambiguity requires an explicit application/object selector.\n\nDuplicate-writer errors require either distinct output routing or an explicit Updates order. Cadence errors require fixed Dates periods compatible with the environment base step. Extend package functions as PlantSimEngine.run!(...), including the package qualification.","title":"Common Errors"},{"category":"section","location":"#PlantSimEngine","page":"Home","text":"(Image: Build Status) (Image: Coverage) (Image: ColPrac: Contributor's Guide on Collaborative Practices for Community Packages) (Image: Aqua QA) (Image: DOI) (Image: JOSS)\n\nPages = [\"index.md\"]\nDepth = 4","title":"PlantSimEngine"},{"category":"section","location":"#Overview","page":"Home","text":"PlantSimEngine is a Julia framework for building soil-plant-atmosphere simulations from small process models. A modeler writes reusable kernels with inputs_, outputs_, optional dependency traits, and run!. A simulation author then assembles those kernels on objects in a CompositeModel.\n\nThe public scenario API has one application-construction form:\n\nModelSpec(\n    model;\n    name=:application,\n    on=Many(scale=:Leaf),\n    inputs=(...),\n    calls=(...),\n    every=Dates.Hour(1),\n    environment=Environment(...),\n    output_routing=(...),\n    updates=Updates(...),\n)","title":"Overview"},{"category":"section","location":"#Models-And-Applications","page":"Home","text":"A model is a reusable implementation of a process. A model application is one configured use of that model in a model: it gives the use a name, selects its target objects, and configures its inputs, calls, timestep, and environment.\n\nConcept Meaning\nProcess The biological or physical operation, such as light interception\nModel An implementation of that process, such as Beer\nApplication One configured use of a model in a CompositeModel\nTarget An object on which that application executes\n\nOne application can target many objects. The same model can also be used in several named applications with different parameters, selectors, or cadence. During compilation, PlantSimEngine resolves each application into its concrete (application, object) executions.\n\nThis means the same model can be reused on one object, many leaves, several plant species, a shared soil object, or a model-scale energy-balance solver without changing the model implementation.","title":"Models And Applications"},{"category":"section","location":"#Why-PlantSimEngine?","page":"Home","text":"Modular models: each process model can be developed, tested, calibrated, and replaced independently.\nExplicit coupling: ModelSpec(...; inputs=...) declares value dependencies, while calls=... gives iterative parent solvers manual control over hard model calls.\nObject-based multiscale composite models: scales are labels on objects, so a plant can be described as plants, axes, internodes, leaves, roots, voxels, or any topology the model requires.\nMultirate execution: use every=Dates.Hour(1) or every=Dates.Day(1), with temporal policies such as Integrate() or HoldLast() in the same model.\nAutomatic environment binding: global weather and spatial microclimate backends are bound through Environment(...), model environment_inputs_, and explicit accepted-state commits with commit_environment!.\nPerformance-oriented internals: selectors and bindings are compiled before the timestep loop, same-rate inputs use references when possible, and homogeneous object batches are specialized.\nGeneric values: status, model parameters, meteorology, and outputs can carry units, automatic-differentiation values, uncertainty wrappers, or other compatible Julia types.","title":"Why PlantSimEngine?"},{"category":"section","location":"#Installation","page":"Home","text":"To install the package, enter Julia package mode by pressing ] in the REPL, then run:\n\nadd PlantSimEngine\n\nUse it from Julia with:\n\nusing PlantSimEngine","title":"Installation"},{"category":"section","location":"#Quickstart:-One-CompositeModel-Object","page":"Home","text":"This example runs three existing toy models on one model object:\n\nToyDegreeDaysCumulModel computes daily thermal time.\nToyLAIModel consumes cumulative thermal time and computes LAI.\nBeer consumes LAI and meteorology to compute absorbed PAR.\n\nThe model kernels are unchanged; the model application layer says where they run. Since no every is specified, these applications use the daily cadence of meteo_day.\n\nusing PlantSimEngine, PlantMeteo, Dates, DataFrames\nusing PlantSimEngine.Examples\n\nmeteo_day = read_weather(\n    joinpath(pkgdir(PlantSimEngine), \"examples/meteo_day.csv\");\n    duration=Dates.Day,\n)\n\nmodel = CompositeModel(\n    ToyDegreeDaysCumulModel(),\n    ToyLAIModel(),\n    Beer(0.6);\n    environment=meteo_day,\n)\n\nsim = run!(model; steps=30, outputs=:all)\nout = collect_outputs(sim; sink=DataFrame)\nfirst(out, 6)\n\nToyLAIModel does not know where TT_cu comes from, and Beer does not know where LAI comes from. The compiler infers the unambiguous same-object bindings from each model's declared inputs and outputs:\n\nselect(\n    DataFrame(Diagnostics.explain_bindings(model)),\n    :application_id,\n    :input,\n    :source_application_ids,\n    :carrier_kind,\n    :copy_semantics,\n)\n\nThe outputs can be plotted like any other tabular result:\n\nusing CairoMakie\n\nlai = out[out.variable .== :LAI, :value]\nappfd = out[out.variable .== :aPPFD, :value]\ntt_cu = out[out.variable .== :TT_cu, :value]\n\nfig = Figure(resolution=(800, 600))\nax = Axis(fig[1, 1], ylabel=\"LAI (m² m⁻²)\")\nlines!(ax, tt_cu, lai, color=:mediumseagreen)\n\nax2 = Axis(fig[2, 1], xlabel=\"Cumulated growing degree days since sowing (°C)\", ylabel=\"aPPFD (mol m⁻² d⁻¹)\")\nlines!(ax2, tt_cu, appfd, color=:firebrick1)\nfig","title":"Quickstart: One CompositeModel Object"},{"category":"section","location":"#Multi-Object-Inputs","page":"Home","text":"Use ModelSpec(...; inputs=...) when a model needs values from selected objects. Here the model-scale LAI model reads live references to all plant surfaces in the model:\n\nplant_scene = CompositeModel(\n    Object(:scene; scale=:Scene, kind=:scene),\n    Object(:plant_1; scale=:Plant, kind=:plant, parent=:scene,\n           status=Status(surface=12.0)),\n    Object(:plant_2; scale=:Plant, kind=:plant, parent=:scene,\n           status=Status(surface=8.0));\n    applications=(\n        ModelSpec(ToyLAIfromLeafAreaModel(100.0); name=:scene_lai, on=One(scale=:Scene), inputs=(:plant_surfaces => Many(\n                    scale=:Plant,\n                    within=SceneScope(),\n                    var=:surface,\n                ),)),\n    ),\n)\n\nplant_sim = run!(plant_scene)\nscene_status = final_state(plant_sim, One(scale=:Scene))\nscene_status\n\nThe same Many(...) selector would be plant-local if the consumer ran on a plant and used within=Subtree(). This is the same mechanism used for plant allocation models that sum their own leaves, model models that aggregate all plants, and microclimate solvers that select objects inside one environment cell.\n\nWhen a selector reads a value produced by another model application, prefer application=... to identify the concrete producer. A process name describes the reusable scientific contract; an application name identifies the mounted producer in this scenario. This matters as soon as several applications implement the same process or publish the same variable on different objects.","title":"Multi-Object Inputs"},{"category":"section","location":"#Manual-Calls-For-Iterative-Solvers","page":"Home","text":"Use ModelSpec(...; calls=...) when a parent model must directly run another model, for example a model energy-balance solver that iterates leaf temperatures until convergence:\n\nModelSpec(SceneEnergyBalance(); name=:scene_energy, on=One(scale=:Scene), calls=(:leaf_energy => Many(\n            kind=:plant,\n            scale=:Leaf,\n            within=SceneScope(),\n            application=:energy_balance,\n        ),\n        :soil => One(\n            kind=:soil,\n            scale=:Soil,\n            within=SceneScope(),\n            application=:soil_water,\n        ),), every=Hour(1))\n\nThe same rule applies to manual calls: scenario wiring should select the concrete callee application with application=.... Model authors use process requirements in dep(model) when they declare generic dependencies, because they cannot know the application names that a user will choose later.\n\nInside run!, use run_call!(context, :leaf_energy) to execute every target and receive a vector-like collection. Pass sampled_environment=value to the same bulk call when the parent has already sampled one environment for all targets. Use call_model(context, :leaf_energy) when a singular call's concrete model must guide an iterative algorithm. Reserve call_targets and run_call!(target; publish=false) for selection, custom ordering, target status inspection, or different per-target environments. Publish the accepted state once, so temporal outputs and mutable environment writes are recorded exactly once.","title":"Manual Calls For Iterative Solvers"},{"category":"section","location":"#Where-To-Go-Next","page":"Home","text":"A mental model introduces the seven ideas used throughout the framework without configuration details.\nCouple models on one object is the first executable journey and runs a coupled simulation over many timesteps.\nRun the coupling on several objects introduces stable object identity and Many.\nPublic API lists the composite-model/object constructors, selectors, lifecycle hooks, and explanation helpers.\nModel traits explains inputs_, outputs_, dep, timespec, output_policy, and environment_inputs_.\nMigration guide covers upgrades from earlier PlantSimEngine releases.","title":"Where To Go Next"},{"category":"section","location":"#Performance","page":"Home","text":"PlantSimEngine keeps model kernels close to regular Julia functions while the runtime handles dependency scheduling, object selection, temporal aggregation, and environment sampling. On an M1 MacBook Pro, toy daily simulations run in hundreds of microseconds, and PlantBiophysics.jl models using PlantSimEngine have been measured much faster than equivalent implementations in typical scientific scripting languages.\n\nFor performance-sensitive composite models, inspect the supported structured explanations:\n\nDiagnostics.explain_bindings(model)\nDiagnostics.explain_schedule(model)\nDiagnostics.explain_execution_plan(model)\n\nThese helpers expose resolved objects, carriers, copy/reference semantics, application clocks, and homogeneous execution batches.","title":"Performance"},{"category":"section","location":"#Ask-Questions","page":"Home","text":"If you have questions or feedback, open an issue or ask on discourse.","title":"Ask Questions"},{"category":"section","location":"#Projects-That-Use-PlantSimEngine","page":"Home","text":"PlantBiophysics.jl\nXPalm","title":"Projects That Use PlantSimEngine"},{"category":"section","location":"#Make-It-Yours","page":"Home","text":"PlantSimEngine is distributed under the MIT license. If you develop a package or model suite that uses it and want it listed here, please open a pull request or contact the maintainers.","title":"Make It Yours"},{"category":"section","location":"guides/data/numerical_reliability/#Numerical-Reliability","page":"Numerical reliability","text":"Use exact assertions for deliberately exact integer/rational scenarios and isapprox for floating-point scientific results. Splitting a computation across objects may change reduction order without changing the model.\n\nPlantSimEngine preserves compatible numeric types through parameters, status, carriers, meteorology, and streams. Avoid forced Float64 conversion. For long or ill-conditioned sums, use pairwise or compensated accumulation inside the scientific model and test its error tolerance explicitly.","title":"Numerical Reliability"},{"category":"section","location":"prerequisites/key_concepts/#Key-Concepts","page":"Key Concepts","text":"","title":"Key Concepts"},{"category":"section","location":"prerequisites/key_concepts/#Processes-And-Models","page":"Key Concepts","text":"A process identifies a biological or physical phenomenon, such as photosynthesis, growth, water balance, or energy balance. A model is one implementation of a process.\n\nModels subtype AbstractModel and declare:\n\ninputs_: values read from object status, each declared as Required(T) or Default(value);\noutputs_: values written to object status;\nenvironment_inputs_: values sampled from the environment;\nenvironment_outputs_: environment variables a controller may commit;\ncommit_environment!: accepted meteorological state committed to a mutable environment by controller models;\ndep: processes called manually by the model, when required.\n\nThe numerical kernel is implemented with:\n\nPlantSimEngine.run!(model, status, environment, constants, context)\n\nRequired(T) describes an input that must be supplied by object state or another application. Default(value) is a true model fallback that PlantSimEngine can initialize automatically. Output literals are initial output-state values.","title":"Processes And Models"},{"category":"section","location":"prerequisites/key_concepts/#Composite-Models-And-Objects","page":"Key Concepts","text":"A CompositeModel contains objects and model applications. An Object can represent a model, plant, soil volume, axis, internode, leaf, sensor, or any other simulated entity. PlantSimEngine does not impose one plant architecture.\n\nObjects can carry:\n\na stable identifier;\nscale, kind, species, and name metadata;\nparent-child relationships;\ngeometry and position;\nmutable Status;\nobject-local model applications.\n\nCompositeModelTemplate packages reusable applications for a species or object type. ObjectInstance mounts the template in a model. Several instances can share models and parameters while declaring targeted overrides for exceptional objects.","title":"Composite Models And Objects"},{"category":"section","location":"prerequisites/key_concepts/#Model-Applications","page":"Key Concepts","text":"ModelSpec configures one use of a model:\n\nModelSpec(...; on=...) selects target objects;\nModelSpec(...; inputs=...) selects producers for value dependencies;\nModelSpec(...; calls=...) binds manually controlled model calls;\nModelSpec(...; every=...) selects the execution cadence;\nEnvironment(...) configures environment sampling;\nUpdates(...; after=:application_id) orders intentional additional writers;\nModelSpec(...; output_routing=...) controls output publication.\n\nThis keeps model implementations generic. Models do not need to know which model, object, timestep, or coupling scenario will use them.","title":"Model Applications"},{"category":"section","location":"prerequisites/key_concepts/#Soft-And-Manual-Dependencies","page":"Key Concepts","text":"Ordinary dependencies are inferred by matching model inputs with outputs and are compiled into an acyclic execution order. ModelSpec(...; inputs=...) is used when the source is cross-object, renamed, temporal, or otherwise ambiguous.\n\nSome algorithms need direct call-stack control. For example, a model energy balance may repeatedly call leaf energy-balance models until canopy microclimate converges. Such dependencies are bound with ModelSpec(...; calls=...); the parent invokes them with run_call!.","title":"Soft And Manual Dependencies"},{"category":"section","location":"prerequisites/key_concepts/#Status-And-References","page":"Key Concepts","text":"Status stores variables in references. Same-rate coupling normally shares those references instead of copying values. A many-object input uses a reference vector, so aggregation models read current source values directly.\n\nTemporal coupling uses published streams when producer and consumer clocks differ. Policies include HoldLast, Interpolate, Integrate, Aggregate, and PreviousTimeStep.","title":"Status And References"},{"category":"section","location":"prerequisites/key_concepts/#Environment","page":"Key Concepts","text":"The active environment backend may be:\n\none constant atmosphere shared by all objects;\na time-indexed weather table;\na mutable layer, voxel, grid, or octree microclimate.\n\nObject-to-environment support is compiled and cached. Geometry changes mark the binding dirty so it can be refreshed without recomputing spatial lookup at every timestep.","title":"Environment"},{"category":"section","location":"prerequisites/key_concepts/#Multiscale-Plant-Structure","page":"Key Concepts","text":"PlantSimEngine treats scale and object hierarchy as scenario data. A plant may use leaves directly under a plant, or axes, segments, internodes, roots, and other intermediate levels. Selectors express relationships such as one source, many descendants, the current plant, an ancestor, or all matching objects in the model.\n\nMultiScaleTreeGraph objects can be imported with objects_from_mtg, but the runtime operates on the same composite-model/object representation afterward.","title":"Multiscale Plant Structure"},{"category":"section","location":"dev/release_notes_handoff/#Release-Notes-Handoff","page":"Release notes handoff","text":"This page is the persistent release-note source for the composite-model/object redesign and cleanup branch. Keep it factual: mark what is implemented, what is removed, and what is only planned.","title":"Release Notes Handoff"},{"category":"section","location":"dev/release_notes_handoff/#Implemented-Breaking-Cleanup","page":"Release notes handoff","text":"Source details live in code_cleanup_audit.md.\n\nRemoved ModelList, ModelMapping, GraphSimulation, MultiScaleModel, and the separate mapping dependency/runtime stack. Use CompositeModel, Object, and model applications.\nRemoved direct and batch mapping run! methods.\nRemoved string scale names. Use symbols, for example :Leaf.\nRemoved mapping-specific type-promotion configuration.\nRemoved ModelMapping completely; it is not retained as a qualified compatibility API.\nRemoved old multiscale output indexing helpers. Convert outputs explicitly before indexing.\nReplaced mapping-specific same-scale rename sentinels with inputs=(:local => One(within=Self(), var=:source),).\nRemoved unused parallel-executor traits after deleting the executor runtime.\nRemoved dead mapping-era wrappers and traits: UninitializedVar, RefVariable, TreeAlike, and StatusView.\nRemoved the unreleased CompositeModelTemplate(...; mapping=...) alias and dead selector-to-mapping conversion helpers.\nRemoved stale PlantSimEngine.Examples exports for the deleted ToyInternodeEmergence example.\nReplaced many source-side validation @asserts with explicit errors.\nAdded Updates(:var; after=:application) for ordered duplicate writers.\nAdded runtime_model(runtime) as the sanctioned live-model accessor for RunContext and Simulation; kernels no longer need to inspect context.compiled.model.\nAdded Diagnostics.explain_initialization(model) with structured :required, :defaulted, :supplied, :generated, :producer_bound, and :environment_bound dispositions.\nAdded CompositeModel(model, models...; status=...) as a thin one-object constructor that lowers to the normal object and ModelSpec representation.\nCalendar-aligned windows remain unsupported. Temporal windows use duration-based Dates.Period semantics.","title":"Implemented Breaking Cleanup"},{"category":"section","location":"dev/release_notes_handoff/#Removed-Unreleased-Scenario-Prototype","page":"Release notes handoff","text":"An experimental scenario runtime was developed and replaced on this branch before release. Its source, tests, examples, and documentation were removed rather than retained as compatibility code.\n\nThe removed API included Domain, SimulationMapping, Route, AllDomains, and HardDomains, together with the domain scheduler, run loops, route materialization, environment bridge, graph runner, and output publisher. Because this API was never released, there is no compatibility layer or user migration path for it.\n\nThe reusable behavior now lives in the composite-model/object runtime: object selectors, compiled ModelSpec(...; inputs=...), manual ModelSpec(...; calls=...), Dates-based scheduling, environment backends, dynamic object lifecycle handling, and structured explanations.\n\nDynamic MTG growth now has one public high-level operation: add_organ!. An MTG-backed CompositeModel retains the accessors and status initializer used during initial adaptation. By default, add_organ! reuses that policy for new nodes, merges explicit initial values, attaches the resulting Status, registers the model object, and invalidates runtime bindings. register_object! remains available as the low-level registry operation. XPalm and PlantGeom were migrated away from package-local wrappers that duplicated this lifecycle sequence.\n\nTopology engines that have already made a new node's attributes authoritative may call add_organ!(...; use_status_adapter=false). This advanced opt-out skips only the stored adapter status initializer; PlantSimEngine still copies the node attributes, applies explicit initial values with the normal precedence, forces the exact node identity, registers the object, and invalidates bindings. Callers must not disable the adapter when it contributes fields that are absent from the new node.","title":"Removed Unreleased Scenario Prototype"},{"category":"section","location":"dev/release_notes_handoff/#Implemented-MAESPA-Style-Example-Changes","page":"Release notes handoff","text":"The current examples/maespa_model_example.jl is the main executable example for multi-plant model coupling.\n\nUses copied PlantBiophysics subsample models: Monteith, Fvcb, and Tuzet.\nUses two plant instances with different parameters and shared scale names such as :Plant and :Leaf.\nUses a shared soil model.\nUses SceneEB with ModelSpec(...; calls=(...)) to manually run leaf :energy_balance and soil :soil_water targets.\nPorts MAESPA-style canopy air temperature and VPD update through the canopy_air_update(...) helper and gbcanms.\nTreats input meteorology as above-canopy forcing, runs trial leaves with run_call!(...; environment=trial_state), commits accepted canopy meteorology with commit_environment!, and writes below-canopy microclimate diagnostics to model status fields: canopy_tair, canopy_vpd, canopy_rh, canopy_htot, and canopy_gcanop.\nAdds LAIModel and declares plant leaf-area materialization with ModelSpec(...; inputs=(...)).\nComputes plant allocation daily from plant-local leaf_carbon vectors.\nAdds run_call! for manually executing compiled model call targets.\nAdds model-level Input(...) and Call(...) dependency defaults through dep(model), with scenario-level ModelSpec(...; inputs=...) and ModelSpec(...; calls=...) overriding those defaults in ModelSpec.\nAdds initial registry-backed model selector resolution with resolve_object_ids and resolve_objects for global, self-relative, plant-relative, ancestor-relative, and named-scope object selections.\nAdds Diagnostics.explain_scopes(model) for structured scope diagnostics. It reports the model scope, object subtree scopes, named Scope(...) entries, and scale/kind/species label groups with concrete object ids.\nSelector failures now include context, matched object ids, requested criteria, available labels, and near-match suggestions. Misspelled labels such as scale=:Leef therefore suggest :Leaf instead of returning only a cardinality count.\nRelation(...) now supports :self, :parent, :children, :ancestors, :descendants, and :siblings in object-relative input, call, and query selectors. Relation results are compiled to concrete object ids and may be constrained by an explicit scope. Application targets reject relations because they have no current object context.\nSelector labels now have one spelling (scale=:Leaf, kind=:plant, species=:oil_palm, and name=:leaf_1); the duplicate Scale, Kind, and Species wrappers were removed. Diagnostics.ObjectAddress preserves all normalized object, routing, temporal, and status-ordering fields, while positional topology selectors such as Relation(:parent) remain supported.\nAdds the first compiled composite-model/object view with Advanced.compile_composite_model, Advanced.CompiledCompositeModel, Advanced.CompiledModelApplication, Advanced.CompiledModelInputBinding, Advanced.CompiledModelCallBinding, Diagnostics.explain_applications, Diagnostics.explain_bindings, and Diagnostics.explain_calls.\nThe compiled model view resolves ModelSpec(...; on=...), ModelSpec(...; inputs=...), and ModelSpec(...; calls=...) to object ids ahead of runtime, and reports temporal policy, window, carrier hints, and callee application ids for agent-readable diagnostics.\nUnscoped composite-model/object dependency selectors now infer scope from the consumer: model consumers default to SceneScope(), while non-model consumers default to Self(). Cross-scope shared dependencies, such as leaf models reading soil state, should use within=SceneScope() explicitly.\nAdds status-backed compiled input carriers for the composite-model/object view: scalar shared refs, homogeneous RefVectors, and Advanced.ObjectRefVector fallback carriers. Diagnostics.input_carrier, Diagnostics.input_value, and Diagnostics.has_reference_carrier expose them for tests, diagnostics, and future runtime execution.\nSame-rate model inputs are now wired into consumer Status references once during compilation. Scalar and Many(...) inputs remain live references, missing bound input fields are compiler-generated without inventing canonical values for Required(T), and repeated non-temporal input materialization is allocation-free.\nSame-rate ModelSpec(...; inputs=...) carriers preserve arbitrary concrete value types. Regression coverage passes a dual-like BigFloat wrapper through a typed RefVector, model arithmetic, source mutation, and output publication without conversion to Float64.\nSame-object variable renaming now uses normal ModelSpec(...; inputs=...) syntax instead of SameScale(). Renamed inputs share the producer reference and contribute the expected producer-to-consumer scheduling edge.\nDiagnostics.explain_bindings now reports stable carrier kind and copy/reference semantics, making reference-wired inputs and materialized temporal values explicit for users and agents.\nAdds model binding cache helpers: Advanced.refresh_bindings!, Advanced.bindings_dirty, Advanced.compiled_bindings, and Advanced.model_revision. Object registration, removal, and reparenting invalidate cached compiled bindings before the next refresh.\nAdds composite-model/object environment binding cache helpers: Advanced.refresh_environment_bindings!, Advanced.compile_environment_bindings, Advanced.CompiledEnvironmentBinding, Advanced.CompiledEnvironmentBindings, Advanced.environment_bindings_dirty, Advanced.compiled_environment_bindings, Advanced.environment_revision, and Diagnostics.explain_environment_bindings.\nAdds geometry, position, and bounds accessors for model objects/statuses. Environment binding refreshes call EnvironmentAPI.update_index!(backend, changed_entities, removed_object_ids) before binding objects to backend cells/layers, so spatial backends can precompute model-wide lookup structures.\nSpatial environment binding now falls back to the nearest ancestor geometry for objects without their own geometry. Binding explanations expose the geometry provenance, and moving an ancestor refreshes only descendants that inherit its geometry.\nEnvironment binding refresh can now update changed environment_inputs_ metadata without repeating spatial indexing or cell lookup when the application/object/provider/geometry contract is otherwise unchanged.\nEnvironment(; sources=(CO2=:Ca,)) now remaps model-facing environment variables to backend source variables. CompositeModel environment binding refresh validates missing source variables for enumerable backends such as EnvironmentAPI.GlobalConstant, and explanations expose both required_inputs and source_inputs.\nvalidate_environment_inputs(model) and validate_environment_inputs(compiled_scene, environment_or_backend) now validate composite-model/object environment contracts directly. Missing-variable diagnostics use model application ids, and validation honors both scenario Environment(; sources=...) remaps and model-author environment_hint defaults.\nObject movement now invalidates environment bindings without rebuilding the structural object/model binding cache.\nAdds public geometry lifecycle helpers: update_geometry!(model, object, geometry; invalidate_environment=true) and object-scoped mark_environment_binding_dirty!(model, object). They currently invalidate the model environment binding cache and leave room for finer-grained dirty tracking later.\nAdds the first composite-model/object runtime with run!(model; steps=...). It materializes compiled ModelSpec(...; inputs=...) carriers, samples bound environment inputs, and executes generic model kernels on object Status values.\nCompositeModel/object compiler now infers simple same-object value bindings from inputs_/outputs_ when one producer is unambiguous. Diagnostics.explain_bindings reports each binding origin, including :model_default, :model_spec, and :inferred_same_object.\nCompiled input bindings now validate ModelSpec(...; inputs=...) process=/application= filters when they are provided, and Diagnostics.explain_bindings reports source_application_ids, process, and application.\nAdvanced.compile_composite_model now errors for required inputs_(model) variables that are neither bound through ModelSpec(...; inputs=...)/inference nor present on the target object Status.\nAdvanced.compile_composite_model now prepares model-owned status schemas automatically: model-targeted objects may omit Status, declared outputs and Default inputs are inserted from their initial values, and bound Required inputs are installed through their compiled carriers. External unbound Required inputs still need explicit initialization.\nAdvanced.compile_composite_model now rejects ModelSpec(...; inputs=...) entries whose receiving variable is not declared by the model's inputs_, making binding typos explicit at compile time.\nAdvanced.compile_composite_model now validates status-backed non-temporal ModelSpec(...; inputs=...) source availability, so bindings that select existing source objects but no source Status reference fail at compile time instead of becoming no-ops.\nCompositeModel/object runtime now publishes model outputs to model-local temporal streams and resolves temporal ModelSpec(...; inputs=...) with HoldLast, Integrate, and Aggregate policies before consumer execution.\nCompositeModel temporal ModelSpec(...; inputs=...) now use producer output_policy(...) traits as the default when the selector omits policy=... and resolves to a unique source application. Explicit selector policies override the trait.\nCompositeModel applications now infer model-author default environment source remaps from environment_hint(...).bindings when the scenario does not provide explicit environment bindings. Scenario Environment(; sources=...) remains the override.\nCompositeModel/object runtime exposes run_call!(...; environment=trial_state) for non-committing trial meteorology and commit_environment! for accepted mutable environment commits from model kernels.\nCompositeModel/object root applications now honor ModelSpec(...; every=...) values backed by Dates.Period scheduling. Diagnostics.explain_schedule reports normalized clocks and whether an application is root-scheduled or manual-call-only.\nCompositeModel/object root applications now also honor timespec(...) model traits when no explicit ModelSpec(...; every=...) is provided. Scenario-level ModelSpec(...; every=...) remains the override.\nCompositeModel/object root applications now validate timestep_hint(...) required bounds for clocks derived from the model base step. Hints remain compatibility constraints, not scheduling overrides.\nCompositeModel/object execution now uses a stable topological application order compiled from ModelSpec(...; inputs=...) producer edges and Updates(...) ordering. Dependencies on manual-call-only applications are redirected to their parent caller, same-timestep cycles fail during compilation, and Diagnostics.explain_schedule reports execution_index.\nAdvanced.CompiledCompositeModel now pre-indexes input and call bindings by application and object id. Runtime input materialization and hard-call lookup no longer scan all model bindings for every object/model invocation.\nAdvanced.CompiledCompositeModel now pre-indexes applications by application id, removing application scans from hard-call target resolution and dictionary rebuilding from ordered execution setup.\nAdvanced.CompiledEnvironmentBindings now pre-indexes environment bindings by application and object id, removing the model-wide binding scan from environment sampling and output scattering.\nAdds RunContext and CallTarget; composite-model/object models can use run_call!(context, :name) plus call_targets(context, :name) for fine-grained manual ModelSpec(...; calls=...) execution.\nApplications selected by ModelSpec(...; calls=...) are skipped by the root run!(model) loop and execute only through explicit run_call!, preserving parent-controlled hard-call execution.\nAdds composite-model/object duplicate-writer validation in Advanced.compile_composite_model. A variable may have only one canonical writer per object unless later writers declare Updates(:var; after=...), where after can match a previous application id/name or process.\nAdds Diagnostics.explain_writers(compiled) to report object-variable writer groups, duplicate writers, and the Updates(...) declarations that validate ordered updates.\nAdds the first reusable object-template path with CompositeModelTemplate and ObjectInstance. Templates bundle reusable ModelSpecs and default kind/species labels; instances mount them inside a named object subtree.\nCompositeModel(...) accepts mounted instances whose roots are either owned objects or references to separately supplied model objects.\nTemplate applications are scoped to their instance and receive stable instance-prefixed application ids. Unmodified instances share the template's model objects, while instance overrides can replace one application by name or process when the replacement implements the same process.\nAdds Override(...) and ObjectInstance(...; object_overrides=...) for exceptional organs. Overrides are resolved during compilation to concrete object ids without splitting the logical application or changing its dependency bindings.\nTemplate models, template parameter metadata, and replacement models are retained by reference. The runtime does not copy models or mutate fields to apply parameter overrides.\nOverride validation requires the same process and declared status/environment variable names. Application explanations report model storage, dispatch mode, overridden object ids, and replacement model types.\nAdds Diagnostics.explain_instances(model) and instance membership in Diagnostics.explain_objects(model). Instance rows expose roots, current object membership, mounted applications, overrides, template labels, and reference-based parameter ownership.\nObjects created below a mounted instance inherit missing template kind and species labels. Membership explanations use the current topology rather than a copied instance object list.\nCompositeModel hard calls now support trial microclimate through run_call!(context, name; environment=local_state), so hard-called descendants resample the temporary environment through their normal environment bindings.\nCompositeModel hard calls now default to publish=false. Trial calls mutate target status without publishing temporal samples or environment writes; accepted states must use run_call!(target; publish=true). Iterative-call tests verify that several trials followed by one accepted call publish exactly once.\nDiagnostics.explain_calls(compiled) now exposes the manual-call publication contract through publication_policy, default_publish, and accepted_publish fields.\nModelSpec now keeps provenance for ModelSpec(...; inputs=...) and ModelSpec(...; calls=...). Declarations coming from dep(model) are :model_default, scenario-level declarations and overrides are :model_spec, and structured explanations expose these origins for release-note and migration diagnostics.\nCompiled OptionalOne(...) inputs and calls now accept zero matches. Optional inputs keep their declared model default, optional calls return an empty target collection, and both remain visible in structured explanations.\nModel kernels read their own parameters from the model argument. Hard-call models and targets are available through focused context APIs such as call_targets, run_call!, and runtime_model.\nCompositeModel duplicate-writer validation now ignores manual-call-only applications when validating canonical root writers, so hard-dependency children are not treated as independent root writers for variables they update inside a parent call stack.\nAdds build_maespa_scene(...) and run_maespa_example(...). This unified composite-model/object MAESPA path uses CompositeModelTemplate, ObjectInstance, on, inputs, calls, and every=Dates.Period with two plant species, one shared soil object, model LAI, and model energy balance.\ntest/test-maespa-model-example.jl verifies the unified composite-model/object MAESPA path.\nrun!(model) now returns a Simulation wrapper containing the mutated model, compiled object bindings, compiled environment bindings, and model-local temporal output streams.\nAdds model output inspection helpers: outputs(sim::Simulation), collect_outputs(sim), and Diagnostics.explain_outputs(sim). These expose object ids, variables, publishing application ids, sample counts, time bounds, and value types.\nrun!(model; tracked_outputs=...) now accepts OutputRequest for composite-model/object runs. Requested outputs are collected from retained typed model streams after the run, can be read with collect_outputs(sim) or collect_outputs(sim, :request_name), support the standard temporal policies and Dates.Period export clocks, and respect dynamic object lifetimes by exporting each object only across its own sample interval. This now prunes retained streams at publisher level: tracked_outputs=nothing keeps all streams, explicit requests keep requested application/variable streams plus streams required by temporal ModelSpec(...; inputs=...), and tracked_outputs=OutputRequest[] keeps no streams unless temporal dependencies require them. Dependency-only streams now have bounded policy-specific histories: latest-only for HoldLast, the required window for Integrate/Aggregate, and sufficient recent source samples for Interpolate/PreviousTimeStep. Requested and default retain-all streams still preserve complete histories, and export remains post-run rather than fully online.\nAdds Diagnostics.explain_output_retention(sim) for structured diagnostics of retained model output streams, their reasons, and the compiled retention horizon for dependency-only streams.\nCompositeModel temporal streams are now keyed by application id, object id, and variable, so two applications can publish the same variable on the same object without overwriting each other's stream samples.\nCompositeModel output-export tests now cover requested-output DataFrame materialization, canonical publisher inference without process=..., rejection when only stream-only publishers exist, and ambiguity when an explicit process matches both a stream-only and a canonical publisher.\nOutputRequest(...) now accepts application=... for composite-model/object runs. This disambiguates repeated applications of the same process and permits explicit export of a named :stream_only publisher.\nCompositeModel temporal streams now retain a concrete value type per application/object/output stream. Type changes fail explicitly, while generic values such as BigFloat remain typed through publication, interpolation, and integration.\nCompositeModel temporal ModelSpec(...; inputs=...) now implement the complete Interpolate(...) policy used by the existing multirate runtime: linear interpolation when samples bracket the requested time, online linear extrapolation from the last two samples, and configurable hold behavior. Interpolation modes are validated during model compilation, and arithmetic preserves generic value types such as BigFloat instead of coercing model values to Float64.\nCompositeModel ModelSpec(...; inputs=...) accepts PreviousTimeStep(:input) => One(...) or Many(...) for explicit lagged dependencies. These bindings read the previous model timestep, use the consumer status initialization before history exists, and are excluded from same-timestep dependency edges so feedback loops can be compiled.\nCompositeModel output_routing=(var=:stream_only,) is honored by canonical writer validation and same-object input inference. Stream-only outputs are excluded from canonical ownership, but remain available in output streams and explicit inputs=(... One(application=:name), ...) bindings.\nCompositeModel execution now refreshes dirty structural bindings between timesteps. Objects created, removed, or reparented by a model update application target sets, input carriers, call targets, writer validation, and scheduling before the next timestep.\nGeometry-only changes refresh environment bindings at the next timestep without rebuilding structural bindings. Simulation returns the final compiled structural and environment state, including changes made during the last timestep.\nGeometry-only environment invalidation is now object-scoped. Moving or explicitly marking one organ dirty preserves unaffected compiled environment bindings and rebinds only model applications targeting that object; structural model changes still trigger a full rebuild.\nAdded runtime lifecycle coverage for organ creation, pruning, plant-local RefVector refresh, historical output retention for removed objects, and movement between mock microclimate cells.\nCompositeModel root execution now uses compiled homogeneous target batches. Models, statuses, input bindings, and environment bindings are prebound, so dynamic dispatch happens once per batch instead of once per object. Heterogeneous object overrides split into ordered concrete batches.\nAdds Diagnostics.explain_execution_plan(scene_or_simulation) and a zero-allocation warmed 128-leaf inner-loop regression gate.\nManual ModelSpec(...; calls=...) handles now use the public vector-like run_call!(context, name) execute-all API, with call_targets(context, name) followed by run_call!(target) for fine-grained control.\nRemoved the unreleased intermediate authoring and runtime subsystem after composite-model/object feature parity was established.\nAdds objects_from_mtg(root; ...) and CompositeModel(mtg; ...) so existing MTG topology can be adapted once into the unified registry while preserving node-derived identity, parent relations, labels, geometry, and existing status objects.\nCompositeModel applications now sample global tabular meteorology at their compiled Dates.Period clock. PlantMeteo reducers and windows from environment_hint are honored; Environment(; sources=...) overrides the source while preserving the reducer. Prepared samplers are shared, and one sampled row is cached per application/timestep for all selected objects.","title":"Implemented MAESPA-Style Example Changes"},{"category":"section","location":"dev/release_notes_handoff/#Downstream-and-Performance-Validation","page":"Release notes handoff","text":"The first complete remote XPalm performance artifact passed on 31 July 2026 in GitHub Actions run 30617676544. The Ubuntu 24.04 / Julia 1.12.1 single-threaded job resolved the four path-developed repositories, then passed all four correctness assertions in the complete 4,160-step profile. The correctness matrix took 12 minutes 10 seconds; the full job, including resolution and precompilation, took 16 minutes 55 seconds.\n\nThe retained artifact is xpalm-full-performance-30617676544-1 (artifact id 8788421206, 90-day retention). It contains the resolved Manifest.toml and 363 CSV measurement rows covering all 16 stages. Every output-retention mode reached the same committed final state: step 4,160, 344 phytomers, LAI 5.0587602356164405, and FTSW 0.7991179101191218.\n\nThe CSV records these exact source revisions:\n\nPlantSimEngine: a715e2bf4407870f7dcae5fedaca7eab00f2a826\nXPalm: 460d3b5161732a195517d5be9a4bdbb4cfc41846\nPlantBiophysics: b733e05032cde9b60e527cc2b33a472281c995fb\nPlantGeom: f53e5633da1a56d546c4a4433e6d1cd3a898ecf9\n\nOn that runner, the minimum complete-cycle measurements were 20.255 seconds with no retained outputs, 22.492 seconds with the small request, 23.246 seconds with the reference request, 61.324 seconds with all outputs, and 15.650 seconds for the historical end-to-end helper. These runner-specific measurements are a persisted comparison baseline, not a replacement for the faster local acceptance-machine targets.","title":"Downstream and Performance Validation"},{"category":"section","location":"dev/release_notes_handoff/#Compatibility-Boundary","page":"Release notes handoff","text":"The composite-model/object runtime and its MAESPA acceptance path are implemented. Historical mapping APIs and the unreleased intermediate prototype were removed. The design, implementation history, and completion evidence are documented in:\n\ncomposite_model_design.md\ncomposite_model_implementation_plan.md\ncomposite_model_completion_audit.md\n\nThe completed public migration is:\n\nreplace historical tutorials with native composite-model/object tutorials where long-term coverage is still valuable;\nmodel mappings should be described as model applications: ModelSpec(model; name=..., on=..., inputs=(...), calls=(...));\nMultiScaleModel(...) -> ModelSpec(...; inputs=...).\ndep(model) remains the model-level trait for default dependency intent: defaults can become Input(...) value bindings or Call(...) manual model calls, and scenario-level ModelSpec configuration overrides them.\nmodel target scales -> ModelSpec(...; on=...) object selectors.\nInputBindings(...) -> source, policy, and window information on ModelSpec(...; inputs=...).\nMeteoBindings(...) and MeteoWindow(...) -> automatic environment binding plus Environment(...) provider/source overrides.\nModelSpec(...; output_routing=...) -> model-application output policy.\nScopeModel(...) -> ModelSpec(...; on=...) plus selector scopes.\nPreviousTimeStep(...) remains supported as a temporal/cycle-breaking marker in the unified object-address graph.\nexplicit per-model environment wiring -> automatic environment resolver plus cached environment bindings.\n\nHistorical mapping examples, tests, and runtime files were removed after the composite-model/object acceptance path reached feature parity. Migration information is kept in this release-note handoff and the user-facing migration guide.","title":"Compatibility Boundary"},{"category":"section","location":"dev/release_notes_handoff/#Migration-Documentation-Added","page":"Release notes handoff","text":"Added docs/src/migration_composite_model.md as the user-facing migration guide from historical mappings to the composite-model/object API.\nUpdated documentation navigation, home-page guidance, multiscale warnings, and the canonical repository agent skill to direct new scenarios toward CompositeModel, Object, on, inputs, calls, Updates, every, and Environment.\nReplaced the documentation home-page quickstart with executable composite-model/object examples. The page now introduces CompositeModel, Object, ModelSpec, on, inputs, every, inferred same-object bindings, multi-object Many(...) inputs, and manual ModelSpec(...; calls=...) syntax before linking to the migration guide.\nReplaced the repository README examples with composite-model/object-first examples. The README now introduces CompositeModel, Object, model applications, multi-object ModelSpec(...; inputs=...), and ModelSpec(...; calls=...).\nAdded a native composite-model/object quickstart page to the main documentation navigation. It provides docs-tested examples for one-object model chaining, inferred bindings, requested output retention, multi-object ModelSpec(...; inputs=...), reference carrier explanations, and manual ModelSpec(...; calls=...) syntax.\nRewrote the model execution page as the current composite-model/object execution guide. It now covers compilation, reference carriers, temporal ModelSpec(...; inputs=...), manual ModelSpec(...; calls=...), Updates(...), ModelSpec(...; every=...), environment binding, retained outputs, lifecycle invalidation, and migration translations for historical mapping constructs.\nRewrote the detailed first simulation tutorial to use the composite-model/object API. It now introduces CompositeModel, Object, ModelSpec, on, every, compiled applications, inferred same-object bindings, model outputs, and a migration note for historical examples.\nRewrote the quick examples page to use native composite-model/object snippets for Beer light interception, degree-days/LAI/light coupling, biomass growth, and retained OutputRequest exports. Historical mapping usage is confined to migration records.\nRewrote the standard model coupling, model switching, and coupling more complex models tutorials around the composite-model/object API. These pages now show inferred same-object value bindings, switching one ModelSpec application, execution-plan explanations, and ModelSpec(...; calls=...) manual-call wiring.\nRemoved legacy mapping transforms and their runtime implementations: MultiScaleModel, SameScale, TimeStepModel, InputBindings, MeteoBindings, MeteoWindow, and ScopeModel.\nAdded a curated unified composite-model/object map to the public API page.","title":"Migration Documentation Added"},{"category":"section","location":"model_traits/#Model-Traits","page":"Model traits","text":"Model traits describe intrinsic model behavior. Scenario-specific coupling belongs in ModelSpec through on, inputs, calls, every, Environment, output_routing, and Updates.","title":"Model Traits"},{"category":"section","location":"model_traits/#Variables","page":"Model traits","text":"Implement inputs_(model) with an explicit declaration for every status input:\n\nPlantSimEngine.inputs_(::MyModel) = (\n    leaf_area=Required(Float64),\n    efficiency=Default(0.8),\n)\nPlantSimEngine.outputs_(::MyModel) = (assimilation=0.0,)\n\nRequired(T) means the value must be present on the target object's Status or bound from another application. T is an expected type, not a placeholder value. It may be abstract or parametric, so use the scientific type contract instead of forcing Float64.\n\nDefault(value) means the model can run without user initialization or a producer for that input. PlantSimEngine installs a private copy of value on each target object when the value is absent. Mutable defaults are therefore not shared between objects.\n\nOutput literals remain initial output-state values. In the example, assimilation starts at 0.0 before the first accepted model call.\n\nThese declarations are used for status initialization, dependency inference, validation, and type construction. Plain input literals are rejected because they do not say whether the value is required or genuinely optional.\n\nUse init_variables(model) to inspect only values PlantSimEngine can initialize by itself: Default input values and output initial values. Required inputs are intentionally omitted.\n\nBefore running a scenario, Diagnostics.explain_initialization(model) classifies inputs as :required, :defaulted, :supplied, or :producer_bound. A :required row must be resolved before compilation can succeed.","title":"Variables"},{"category":"section","location":"model_traits/#Scientific-meaning-and-dimensions","page":"Model traits","text":"Names and Julia types are not enough to distinguish, for example, daily PAR per ground area from daily PAR per plant. Add a VariableContract when a variable participates in scientific coupling:\n\nconst PLANT_DAILY_PAR = VariableContract(\n    unit=:mol_photon,\n    basis=:plant,\n    temporal=:day,\n    aggregation=:total,\n    extent=:extensive,\n)\n\nPlantSimEngine.variable_contracts_(::PlantLight) = (\n    absorbed_par=PLANT_DAILY_PAR,\n)\nPlantSimEngine.variable_contracts_(::PlantGrowth) = (\n    absorbed_par=PLANT_DAILY_PAR,\n)\n\nThe tokens are open symbols, but a connected producer and consumer must declare the same complete contract. Once either side declares one, a missing contract on the other side is also a compilation error. Renaming a variable with var=... does not convert its meaning; put unit, basis, or time conversion in an explicit model boundary.\n\nVariableContract is metadata only. Status and environment payloads remain ordinary numbers, arrays, unit-bearing values, or automatic-differentiation values, so the contract adds no wrapper to a model's numerical hot loop.\n\nUse variable_contracts(model) to inspect the validated declarations. Contract keys must occur in one of the model's declared status or environment traits, or in the compiled application's distributed outputs_to declaration.\n\nRole Declaration Ownership\nStatus input inputs_ object state or a compiled model producer\nEnvironment input environment_inputs_ the active forcing backend\nConstant the constants argument simulation configuration, not mutable status\nManual model call dep plus ModelSpec(...; calls=...) explicit callee execution; the callee keeps its own variable declarations\nLocal status output outputs_ the target object's runtime status\nDistributed output ModelSpec(...; outputs_to=...) explicitly selected destination objects","title":"Scientific meaning and dimensions"},{"category":"section","location":"model_traits/#Manual-Dependencies","page":"Model traits","text":"Implement dep(model) only when the model directly calls another process from inside its own run! method:\n\nPlantSimEngine.dep(::EnergyBalance) = (\n    photosynthesis=AbstractPhotosynthesisModel,\n)\n\nThe scenario binds the dependency with ModelSpec(...; calls=...). The parent executes all resolved targets with run_call!(context, :photosynthesis), which always returns a vector-like collection. Use call_targets plus run_call!(target) when the parent needs selective trials and accepted publication.","title":"Manual Dependencies"},{"category":"section","location":"model_traits/#Timing","page":"Model traits","text":"timespec(model) declares the model's default clock. The default is ClockSpec(1.0, 0.0).\n\nPlantSimEngine.timespec(::Type{<:DailyGrowth}) = ClockSpec(Dates.Day(1))\n\noutput_policy(model) declares the default temporal policy per output:\n\nPlantSimEngine.output_policy(::Type{<:MyModel}) = (\n    assimilation=Integrate(),\n    leaf_temperature=Aggregate(MeanReducer()),\n)\n\nUnspecified outputs use HoldLast(). A scenario can select another clock with ModelSpec(...; every=...) and another input policy in ModelSpec(...; inputs=...).\n\ntimestep_hint(model) can declare required or preferred timestep constraints. environment_hint(model) can provide default environment sampling configuration.","title":"Timing"},{"category":"section","location":"model_traits/#Environment-Variables","page":"Model traits","text":"Use environment_inputs_(model) for variables sampled from the active environment backend:\n\nPlantSimEngine.environment_inputs_(::LeafEnergyBalance) = (\n    T=0.0,\n    Rh=0.0,\n    Wind=0.0,\n    Ri_PAR_f=0.0,\n    CO2=400.0,\n)\n\nMutable microclimate updates should be committed explicitly by controller models:\n\ncommit_environment!(context, accepted_environment)\n\nFor trial solves, pass a backend-specific state with environment so each hard-called model keeps its compiled provider or spatial handle:\n\nrun_call!(context, :leaf_energy; environment=trial_environment, publish=false)\n\nDiagnostic variables such as canopy temperature or vapor-pressure deficit can still be regular outputs_, but status fields are not the transport mechanism for mutable environment state.","title":"Environment Variables"},{"category":"section","location":"model_traits/#Precedence","page":"Model traits","text":"Scenario configuration has precedence over model defaults:\n\nModelSpec(...; inputs=...) policy, then producer output_policy, then HoldLast().\nModelSpec(...; every=...), then timespec(model), then the environment base step.\nEnvironment(...), then environment_hint(model), then backend defaults.","title":"Precedence"},{"category":"section","location":"journeys/users/structure_changes/#Modify-Plant-Structure","page":"Modify plant structure","text":"","title":"Modify Plant Structure"},{"category":"section","location":"journeys/users/structure_changes/#New-concept:-lifecycle-changes-refresh-compiled-targets","page":"Modify plant structure","text":"Start with one plant, one branch, and two leaves. Each leaf computes carbon demand, then treats that fully met demand as accepted carbon allocation for ToyCBiomassModel. This small chain gives us a conserved quantity to check while topology changes.\n\nusing PlantSimEngine, DataFrames\nusing PlantSimEngine.Examples\n\nmodel = CompositeModel(\n    Object(:plant; scale=:Plant, kind=:plant),\n    Object(:branch; scale=:Axis, kind=:axis, parent=:plant),\n    Object(\n        :leaf_1;\n        scale=:Leaf,\n        kind=:leaf,\n        parent=:plant,\n        status=Status(TT=10.0),\n    ),\n    Object(\n        :leaf_2;\n        scale=:Leaf,\n        kind=:leaf,\n        parent=:plant,\n        status=Status(TT=15.0),\n    );\n    applications=(\n        ModelSpec(\n            ToyCDemandModel(\n                optimal_biomass=12.0,\n                development_duration=120.0,\n            );\n            name=:carbon_demand,\n            on=Many(scale=:Leaf),\n        ),\n        ModelSpec(\n            ToyCBiomassModel(1.2);\n            name=:biomass,\n            on=Many(scale=:Leaf),\n            inputs=(\n                :carbon_allocation => One(\n                    within=Self(),\n                    application=:carbon_demand,\n                    var=:carbon_demand,\n                ),\n            ),\n        ),\n    ),\n)\n\nsimulation = run!(model; outputs=:all)\ninitial_targets = only(\n    row for row in Diagnostics.explain_applications(simulation)\n    if row.application_id == :biomass\n).target_ids","title":"New concept: lifecycle changes refresh compiled targets"},{"category":"section","location":"journeys/users/structure_changes/#Add-one-leaf","page":"Modify plant structure","text":"Registering an object mutates the live model and marks affected compiled state dirty. Because this call happens between simulation steps, the new leaf is compiled before the next step:\n\nregister_object!(\n    model,\n    Object(\n        :leaf_3;\n        scale=:Leaf,\n        kind=:leaf,\n        status=Status(TT=20.0),\n    );\n    parent=:plant,\n)\n\ntargets_before_refresh = only(\n    row for row in Diagnostics.explain_applications(simulation)\n    if row.application_id == :biomass\n).target_ids\n\ncontinue!(simulation)\n\ntargets_after_refresh = only(\n    row for row in Diagnostics.explain_applications(simulation)\n    if row.application_id == :biomass\n).target_ids\n\n(\n    initial_targets=initial_targets,\n    before_refresh=targets_before_refresh,\n    after_refresh=targets_after_refresh,\n    leaf_3_parent=only(\n        object.parent.value\n        for object in model_objects(model)\n        if object.id == ObjectId(:leaf_3)\n    ),\n)\n\nWhen a lifecycle operation occurs inside a model kernel, PlantSimEngine refreshes after that application. A newly registered object may therefore run applications that remain later in the same timestep. It runs an application that already completed only through an explicit Initializer binding and run_initializer! call from its creator.","title":"Add one leaf"},{"category":"section","location":"journeys/users/structure_changes/#Reparent,-then-remove","page":"Modify plant structure","text":"Creation now works, so make two further changes in order. First move the new leaf under the branch and advance:\n\nreparent_object!(model, :leaf_3, :branch)\ncontinue!(simulation)\n\nleaf_3_parent = only(\n    object.parent.value\n    for object in model_objects(model)\n    if object.id == ObjectId(:leaf_3)\n)\n\nThen remove :leaf_2 and advance once more:\n\nremoved = remove_object!(model, :leaf_2)\ncontinue!(simulation)\n\n(\n    removed=removed.id.value,\n    current_leaves=sort!([\n        object.id.value\n        for object in model_objects(model; scale=:Leaf)\n    ]),\n    leaf_3_parent=leaf_3_parent,\n    current_targets=only(\n        row for row in Diagnostics.explain_applications(simulation)\n        if row.application_id == :biomass\n    ).target_ids,\n)","title":"Reparent, then remove"},{"category":"section","location":"journeys/users/structure_changes/#Check-conservation-and-history","page":"Modify plant structure","text":"For every retained leaf sample, accepted carbon allocation equals demand. The biomass model partitions it into biomass increment plus growth respiration:\n\nrows = collect_outputs(simulation; sink=nothing)\n\ndemand = Dict(\n    (row.timestep, row.object_id) => row.value\n    for row in rows\n    if row.application_id == :carbon_demand &&\n       row.variable == :carbon_demand\n)\nincrement = Dict(\n    (row.timestep, row.object_id) => row.value\n    for row in rows\n    if row.application_id == :biomass &&\n       row.variable == :carbon_biomass_increment\n)\nrespiration = Dict(\n    (row.timestep, row.object_id) => row.value\n    for row in rows\n    if row.application_id == :biomass &&\n       row.variable == :growth_respiration\n)\n\nall(\n    demand[key] ≈ increment[key] + respiration[key]\n    for key in keys(demand)\n)\n\nRemoved-object history remains queryable even though the object is no longer in the registry:\n\nhistory_counts = Dict(\n    id => length(collect_outputs(\n        simulation,\n        id,\n        :carbon_biomass;\n        sink=nothing,\n    ))\n    for id in (:leaf_1, :leaf_2, :leaf_3)\n)\n\n:leaf_2 keeps the three samples published before removal; :leaf_3 begins at step 2, after registration, and also has three samples.\n\nToyCAllocationModel is useful when supply is limiting and a plant controller must divide carbon among many organ demands. This journey deliberately assumes all demand is accepted so lifecycle timing and conservation stay visible without introducing a controller or hard calls.","title":"Check conservation and history"},{"category":"section","location":"journeys/users/structure_changes/#Page-recap","page":"Modify plant structure","text":"You added: one leaf, then one reparenting operation, then one removal.\nPlantSimEngine inferred: the affected application targets, status views, reference binding, execution batch extension, and retained stream keys.\nYou keep explicit: initialized status for a new object, its parent, conservation assumptions, and when removal or reparenting occurs.\nNew API names: register_object!, reparent_object!, remove_object!, and Diagnostics.explain_applications(simulation).","title":"Page recap"},{"category":"section","location":"journeys/modelers/hard_dependencies/#Implement-A-Hard-Dependency","page":"Hard dependencies","text":"New concept: a process-level hard dependency, used only when the parent kernel must control another model's execution. Value dependencies should stay in inputs_.\n\nSimulation users wire and inspect advanced calls in Control Advanced Execution.\n\nThe plain Julia blocks below are excerpts from the shipped, tested ToySelectiveCallControllerModel.","title":"Implement A Hard Dependency"},{"category":"section","location":"journeys/modelers/hard_dependencies/#Model-8:-declare-and-execute-the-call","page":"Hard dependencies","text":"ToySelectiveCallControllerModel declares a reusable default by process, scale, and relative scope. It cannot know future application names:\n\nPlantSimEngine.dep(::ToySelectiveCallControllerModel) = (\n    readers=Call(Many(\n        scale=:Leaf,\n        process=:toy_environment_reader,\n        within=Subtree(),\n    )),\n)\n\nInside run!, the controller inspects the vector-like collection, restricts the declared call by object id, runs trials without publication, and publishes one accepted execution:\n\ntargets = call_targets(context, :readers)\nselected = only(call_targets(\n    context,\n    :readers;\n    objects=(ObjectId(model.selected_object),),\n))\n\nfor temperature in model.trial_temperatures\n    run_call!(\n        selected;\n        sampled_environment=(T=temperature,),\n        publish=false,\n    )\nend\nrun_call!(\n    selected;\n    sampled_environment=(T=model.accepted_temperature,),\n    publish=true,\n)\n\nThe source excerpt is exercised by the example-model contract suite. Build a scenario without a calls keyword to use that model default:\n\nusing PlantSimEngine, DataFrames\nusing PlantSimEngine.Examples\n\nenvironment = ToySpatialEnvironment(\n    Dict(\n        :sun => (T=26.0,),\n        :shade => (T=18.0,),\n    );\n    step_seconds=3600.0,\n)\nmodel = CompositeModel(\n    Object(:plant; scale=:Plant),\n    Object(\n        :sun_leaf;\n        scale=:Leaf,\n        parent=:plant,\n        geometry=(cell=:sun,),\n    ),\n    Object(\n        :shade_leaf;\n        scale=:Leaf,\n        parent=:plant,\n        geometry=(cell=:shade,),\n    );\n    applications=(\n        ModelSpec(\n            ToyEnvironmentReaderModel();\n            name=:reader,\n            on=Many(scale=:Leaf),\n            environment=Environment(backend=environment),\n        ),\n        ModelSpec(\n            ToySelectiveCallControllerModel(\n                (28.0, 31.0),\n                22.0;\n                selected_object=:sun_leaf,\n            );\n            name=:controller,\n            on=One(scale=:Plant),\n        ),\n    ),\n)\n\nDataFrame(Diagnostics.explain_calls(model))\n\nsimulation = run!(model; outputs=:all)\n(\n    controller=final_state(simulation, :plant),\n    leaves=final_state(simulation, Many(scale=:Leaf)),\n    publications=filter(\n        row -> row.application_id == :reader,\n        DataFrame(Diagnostics.explain_outputs(simulation)),\n    ),\n)\n\norigin=:model_default identifies the dep(model) contract. A concrete scenario can replace it with ModelSpec(...; calls=...) when application identity or target selection differs.","title":"Model 8: declare and execute the call"},{"category":"section","location":"journeys/modelers/hard_dependencies/#Keep-the-common-path-bulk-and-concrete","page":"Hard dependencies","text":"The selective example above intentionally materializes one public target because it chooses an object and gives each trial its own sampled value. If the algorithm executes every resolved target with the same already-sampled environment, use the bulk path instead:\n\nrun_call!(\n    context,\n    :readers;\n    sampled_environment=environment,\n    publish=false,\n)\n\nThis executes the compiler's cached typed batches directly. It avoids creating or indexing CallTarget wrappers inside a timestep loop.\n\nSome iterative algorithms must inspect a singular dependency model before executing it. Keep that dispatch concrete with call_model, then execute the same declared call in bulk:\n\nreader_model = call_model(context, :reader)\ntrial = prepare_trial(reader_model, status, environment)\nrun_call!(context, :reader; sampled_environment=trial, publish=false)\n\ncall_model requires exactly one resolved target. Use call_targets when the algorithm also needs target status, object selection, a custom order, or several distinct sampled environments.\n\nThe dependency definition is immutable after compilation, while its selected objects are not. Growth, removal, and reparenting refresh the affected target buffers once at the lifecycle barrier; normal timesteps continue through the same compiled plan.","title":"Keep the common path bulk and concrete"},{"category":"section","location":"journeys/modelers/hard_dependencies/#Model-author-recap","page":"Hard dependencies","text":"You implemented: a process-level Call requirement and explicit execution inside the parent kernel.\nPlantSimEngine inferred: call-only scheduling, resolved targets, nested context, and publication boundaries.\nThe scenario author keeps explicit: application overrides and any architecture-specific target choice.\nNew API names: dep, Call, call_model, call_targets, CallTargets, run_call!, sampled_environment, and publish.","title":"Model-author recap"},{"category":"section","location":"guides/multiscale/concepts/#How-Multiscale-Composite-Models-Execute","page":"How composite models execute","text":"One application executes once for every object selected by its ModelSpec(...; on=...) selector. State belongs to the object, while topology and labels belong to the scenario. Self() is the current object, SelfPlant() is its plant-instance root, and SceneScope() is model-wide. Cardinality wrappers decide whether zero, one, or many matches are valid.\n\nMore objects mean more qualified streams. Removing an object stops future execution but preserves its accepted historical samples.\n\nCanonical selector patterns are:\n\nRelationship Pattern\napplication targets every leaf ModelSpec(model; on=Many(scale=:Leaf))\ninput from this same object omit inputs when the producer is unique\ninput from one ancestor One(Ancestor(scale=:Plant))\ninput from this plant's leaves Many(scale=:Leaf, within=SelfPlant())\ninput from shared soil One(scale=:Soil, within=SceneScope())\noptional named organ OptionalOne(name=:fruit, within=SelfPlant())\n\nSelf() always means the current target object. It never implicitly means the model, process, species, or plant. Prefer object IDs and labels for identity, and use Scope(name) only when the model explicitly defines that scope.\n\nOne application produces a separate stream for every selected object and output variable. Stream keys also include application identity, so repeated applications of the same process cannot overwrite each other. Use Diagnostics.explain_applications, Diagnostics.explain_objects, and Diagnostics.explain_bindings to verify target and source multiplicities before a long run.","title":"How Multiscale Composite Models Execute"},{"category":"section","location":"guides/data/forcing_observations/#Forcing-Observed-Variables","page":"Forcing observations","text":"Supply a constant observed value in object status when it does not vary. For a time-varying observation, use a small environment-driven source model that publishes the canonical variable; downstream applications remain unchanged.\n\nReplace a process for an entire template instance through instance overrides, or use Override for one exceptional object. The replacement must implement the same process and input/output contract.","title":"Forcing Observed Variables"},{"category":"section","location":"journeys/users/several_plants/#Instantiate-Several-Plants","page":"Several plants","text":"","title":"Instantiate Several Plants"},{"category":"section","location":"journeys/users/several_plants/#New-concept:-templates,-instances,-and-overrides","page":"Several plants","text":"The previous journey configured one plant explicitly. Its three applications can become a CompositeModelTemplate, then be mounted on several independent object topologies without duplicating that model configuration.\n\nusing PlantSimEngine, DataFrames\nusing PlantSimEngine.Examples\n\nplant_template = CompositeModelTemplate((\n    ModelSpec(\n        ToyLeafSurfaceModel(0.02);\n        name=:leaf_surface,\n        on=Many(scale=:Leaf),\n    ),\n    ModelSpec(\n        ToyPlantLeafSurfaceModel();\n        name=:plant_surface,\n        on=One(scale=:Plant),\n        inputs=(\n            :leaf_surfaces => Many(\n                scale=:Leaf,\n                within=Subtree(),\n                application=:leaf_surface,\n                var=:surface,\n            ),\n        ),\n    ),\n    ModelSpec(\n        ToyLightPartitioningModel();\n        name=:leaf_light,\n        on=Many(scale=:Leaf),\n        inputs=(\n            :aPPFD_larger_scale => One(\n                scale=:Plant,\n                within=SelfPlant(),\n                var=:aPPFD,\n            ),\n            :total_surface => One(\n                scale=:Plant,\n                within=SelfPlant(),\n                application=:plant_surface,\n                var=:surface,\n            ),\n        ),\n    ),\n))\n\nAn ObjectInstance supplies the concrete root and organs. These two instances reuse the same template while keeping different initial plant radiation and leaf biomasses:\n\nplant_a = ObjectInstance(\n    :plant_a,\n    plant_template;\n    root=Object(\n        :plant_a_root;\n        scale=:Plant,\n        kind=:plant,\n        status=Status(aPPFD=120.0),\n    ),\n    objects=(\n        Object(\n            :plant_a_leaf_1;\n            scale=:Leaf,\n            kind=:leaf,\n            parent=:plant_a_root,\n            status=Status(carbon_biomass=50.0),\n        ),\n        Object(\n            :plant_a_leaf_2;\n            scale=:Leaf,\n            kind=:leaf,\n            parent=:plant_a_root,\n            status=Status(carbon_biomass=100.0),\n        ),\n    ),\n)\n\nplant_b = ObjectInstance(\n    :plant_b,\n    plant_template;\n    root=Object(\n        :plant_b_root;\n        scale=:Plant,\n        kind=:plant,\n        status=Status(aPPFD=200.0),\n    ),\n    objects=(\n        Object(\n            :plant_b_leaf_1;\n            scale=:Leaf,\n            kind=:leaf,\n            parent=:plant_b_root,\n            status=Status(carbon_biomass=50.0),\n        ),\n        Object(\n            :plant_b_leaf_2;\n            scale=:Leaf,\n            kind=:leaf,\n            parent=:plant_b_root,\n            status=Status(carbon_biomass=50.0),\n        ),\n    ),\n)\n\nmodel = CompositeModel(plant_a, plant_b)\nsimulation = run!(model; outputs=:all)\nplant_states = final_state(simulation, Many(scale=:Plant))\nDict(id => (surface=state.surface, aPPFD=state.aPPFD) for (id, state) in plant_states)\n\nPlant A aggregates surfaces 1 + 2 = 3; plant B aggregates 1 + 1 = 2. Those totals prove that Subtree() did not mix leaves between instances. Likewise, each pair of leaf-level light outputs sums to its own plant's supplied light:\n\nleaf_states = final_state(simulation, Many(scale=:Leaf))\n(\n    plant_a_light=sum(\n        leaf_states[id].aPPFD\n        for id in (:plant_a_leaf_1, :plant_a_leaf_2)\n    ),\n    plant_b_light=sum(\n        leaf_states[id].aPPFD\n        for id in (:plant_b_leaf_1, :plant_b_leaf_2)\n    ),\n)\n\nThe instance diagnostic shows the mounted object and application ids. Template application names are prefixed automatically, so the two mounted graphs remain unambiguous:\n\nselect(\n    DataFrame(Diagnostics.explain_instances(model)),\n    :name,\n    :root_id,\n    :object_ids,\n    :application_ids,\n)","title":"New concept: templates, instances, and overrides"},{"category":"section","location":"journeys/users/several_plants/#Override-one-instance","page":"Several plants","text":"Only after the two unchanged instances work, override one application for a new instance. This plant uses a larger specific leaf area while retaining the same logical :leaf_surface application and all other template wiring:\n\nplant_c = ObjectInstance(\n    :plant_c,\n    plant_template;\n    root=Object(\n        :plant_c_root;\n        scale=:Plant,\n        kind=:plant,\n        status=Status(aPPFD=120.0),\n    ),\n    objects=(\n        Object(\n            :plant_c_leaf_1;\n            scale=:Leaf,\n            kind=:leaf,\n            parent=:plant_c_root,\n            status=Status(carbon_biomass=50.0),\n        ),\n        Object(\n            :plant_c_leaf_2;\n            scale=:Leaf,\n            kind=:leaf,\n            parent=:plant_c_root,\n            status=Status(carbon_biomass=100.0),\n        ),\n    ),\n    overrides=(leaf_surface=ToyLeafSurfaceModel(0.04),),\n)\n\noverride_simulation = run!(CompositeModel(plant_c))\noverride_state = final_state(override_simulation, One(scale=:Plant))\noverride_state.surface\n\nThere is no SceneScope() in this example because nothing is deliberately shared between plants. Introduce scene-wide scope only when adding a real shared source, such as a soil object or scene-level forcing controller.","title":"Override one instance"},{"category":"section","location":"journeys/users/several_plants/#Page-recap","page":"Several plants","text":"You added: one reusable CompositeModelTemplate, two independent ObjectInstances, and then one application override.\nPlantSimEngine inferred: instance-local selector scopes, prefixed mounted application ids, and the same compiled coupling graph for each plant.\nYou keep explicit: each instance's objects and initial values, plus the exact application replaced by an override.\nNew API names: CompositeModelTemplate, ObjectInstance, overrides, and Diagnostics.explain_instances.","title":"Page recap"},{"category":"section","location":"dev/public_api_refinement_decisions/#Public-API-refinement-decisions","page":"Public API refinement decisions","text":"This decision record defines the target public contract for the CompositeModel/Object API. The CompositeModel/Object compiler and runtime remain the only supported scenario runtime.","title":"Public API refinement decisions"},{"category":"section","location":"dev/public_api_refinement_decisions/#Terminology-and-identity","page":"Public API refinement decisions","text":"An object is one runtime entity with a stable ObjectId.\nA model is one scientific implementation of a process.\nA process is model metadata and may have several applications.\nAn application is one named, configured occurrence of a model in a model.\nUser declarations that identify a producer, writer, update predecessor, call target, or output stream use application identity.\nProcess queries are discovery filters. They are not substitutes for an application identifier when more than one application matches.\nEvery application receives a deterministic identifier. An explicit ModelSpec(...; name=...) is used verbatim. An unnamed application uses its process name only when that identifier is unique; repeated unnamed applications are rejected with instructions to name them.\nMounted template applications are qualified as instance_name__application_name.","title":"Terminology and identity"},{"category":"section","location":"dev/public_api_refinement_decisions/#Object-selectors-and-scope","page":"Public API refinement decisions","text":"The same One, OptionalOne, and Many selector values are accepted by application targeting, inputs, calls, object queries, and output requests.\n\nScope names have one meaning:\n\nSelf() selects only the current object.\nSubtree() selects the current object and all of its descendants.\nSelfPlant() selects the current object's plant root and its descendants.\nAncestor(...) selects the matching ancestor's subtree.\nSceneScope() searches the whole model.\nScope(name) searches the named object's subtree.\nRelation(...) selects objects with the requested topological relationship.\n\nSelectors that require a current object fail when used without a context. Cross-object coupling is always visible in the declaration through Subtree, SelfPlant, Ancestor, Scope, SceneScope, or Relation.","title":"Object selectors and scope"},{"category":"section","location":"dev/public_api_refinement_decisions/#Outputs","page":"Public API refinement decisions","text":"run! uses an explicit outputs keyword:\n\nrun!(model; outputs=:none)\nrun!(model; outputs=:all)\nrun!(model; outputs=request)\nrun!(model; outputs=requests)\n\nThe default is outputs=:none. Temporal dependency streams required by the runtime are still retained with bounded histories; they are not user-retained outputs.\n\nThe former tracked_outputs keyword has been removed. Use outputs directly; there is no dual spelling.\n\nAn OutputRequest contains an object selector, a variable, an optional application identifier, a unique result name, and optional temporal resampling policy. OutputRequest(:Leaf, :x) remains a convenience spelling that lowers to OutputRequest(Many(scale=:Leaf), :x) during migration.","title":"Outputs"},{"category":"section","location":"dev/public_api_refinement_decisions/#Execution-and-continuation","page":"Public API refinement decisions","text":"run!(model; steps=n, ...) starts a fresh result timeline at step one while mutating model status. It returns a live Simulation execution handle.\n\ncontinue!(simulation; steps=n) advances that simulation from its current step, preserving retained streams, temporal dependency history, environment position, and multirate clock phase. It returns the same simulation.\n\nstep!(simulation) is equivalent to continue!(simulation; steps=1).\n\nCalling run! on an already-mutated model intentionally creates a new result timeline. Users who intend temporal continuation use continue!; the distinct operation prevents an accidental step-index reset.\n\nLifecycle mutations between calls to continue! are compiled before the next timestep using the existing targeted invalidation contract.","title":"Execution and continuation"},{"category":"section","location":"dev/public_api_refinement_decisions/#Public-namespaces","page":"Public API refinement decisions","text":"The default namespace is organized around:\n\nmodel composition and execution;\nmodel-author declarations and kernel helpers;\nsupported structured explanations;\ndocumented environment extension interfaces.\n\nCompiled representation types, cache dirty flags, raw compiler stages, and low-level invalidation helpers are qualified advanced/internal APIs unless a documented external extension requires them. Removing an export does not make a symbol inaccessible through PlantSimEngine.Symbol; it removes the accidental promise that ordinary users should depend on it.","title":"Public namespaces"},{"category":"section","location":"dev/public_api_refinement_decisions/#Compatibility-policy","page":"Public API refinement decisions","text":"Removed legacy mapping/executor APIs are not restored.\nSuperseded CompositeModel/Object spellings are removed rather than retained as aliases or fallback methods.\nBenchmarks, examples, documentation, PlantBiophysics, and XPalm target the canonical API.","title":"Compatibility policy"},{"category":"section","location":"journeys/users/one_object/#Couple-Models-On-One-Object","page":"One object over time","text":"","title":"Couple Models On One Object"},{"category":"section","location":"journeys/users/one_object/#New-concept:-automatic-same-object-coupling-over-time","page":"One object over time","text":"This first executable simulation couples three existing models on one object:\n\nToyDegreeDaysCumulModel reads temperature and accumulates thermal time.\nToyLAIModel reads cumulative thermal time and computes LAI.\nBeer reads LAI and radiation and computes absorbed PAR.\n\nThe weather file is supplied forcing data for now. Environments get their own journey later.\n\nusing PlantSimEngine, PlantMeteo, Dates, DataFrames\nusing PlantSimEngine.Examples\n\nweather = read_weather(\n    joinpath(pkgdir(PlantSimEngine), \"examples/meteo_day.csv\");\n    duration=Day,\n)\n\nmodel = CompositeModel(\n    ToyDegreeDaysCumulModel(),\n    ToyLAIModel(),\n    Beer(0.6);\n    environment=weather,\n)\n\nNo ModelSpec or selector is needed when all models run on the one object made by the concise constructor. Run thirty daily steps and retain the model outputs:\n\nsimulation = run!(model; steps=30, outputs=:all)\nresults = collect_outputs(simulation)\n\nthermal_time = results[results.variable .== :TT_cu, :value]\nlai = results[results.variable .== :LAI, :value]\nevolution = DataFrame(\n    step=1:length(thermal_time),\n    TT_cu=thermal_time,\n    LAI=lai,\n)\nvcat(first(evolution, 3), last(evolution, 3))\n\nThe table is retained history. The latest values are also available directly, whether or not history was requested:\n\nstate_at_day_30 = final_state(simulation)\n(\n    current_step=current_step(simulation),\n    TT_cu=state_at_day_30.TT_cu,\n    LAI=state_at_day_30.LAI,\n    aPPFD=state_at_day_30.aPPFD,\n    retained_streams=length(outputs(simulation)),\n)\n\nPlantSimEngine inferred both status connections because each has one unambiguous producer on the same object. This focused diagnostic shows the resolved sources and the live reference carriers:\n\nselect(\n    DataFrame(Diagnostics.explain_bindings(model)),\n    :application_id,\n    :input,\n    :source_application_ids,\n    :carrier_kind,\n)\n\nA Simulation owns a continuing timeline. Advancing it does not rebuild a separate result object:\n\nstep!(simulation)\nstate_at_day_31 = final_state(simulation)\n(current_step=current_step(simulation), TT_cu=state_at_day_31.TT_cu)","title":"New concept: automatic same-object coupling over time"},{"category":"section","location":"journeys/users/one_object/#Page-recap","page":"One object over time","text":"You added: three models, supplied weather, a 30-step run, and retained outputs.\nPlantSimEngine inferred: the one object, three applications, their execution order, and the TT_cu and LAI connections.\nYou keep explicit: model parameters, forcing data, number of steps, and whether output history is retained.\nNew API names: CompositeModel, run!, Simulation, final_state, collect_outputs, outputs, current_step, step!, and Diagnostics.explain_bindings.","title":"Page recap"},{"category":"section","location":"journeys/users/mutable_environments/#Modify-The-Environment","page":"Modify the environment","text":"","title":"Modify The Environment"},{"category":"section","location":"journeys/users/mutable_environments/#New-concept:-trial-state-versus-accepted-state","page":"Modify the environment","text":"The previous environment journey sampled read-only global and spatial values. Now a controller evaluates one typed trial state, accepts a different state, and commits it explicitly.\n\nStart with one cell. ToyEnvironmentReaderModel declares T as an environment input. The controller declares T as an environment output: this is permission to commit that variable, not an ordinary object-status output.\n\nusing PlantSimEngine, DataFrames\nusing PlantSimEngine.Examples\n\n(\n    reader_inputs=PlantSimEngine.environment_inputs_(\n        ToyEnvironmentReaderModel(),\n    ),\n    controller_commit_permissions=PlantSimEngine.environment_outputs_(\n        ToyEnvironmentControllerModel(30.0, 22.0),\n    ),\n)\n\nThe controller's kernel uses the current typed trial-state path. A trial call changes the callee status for inspection but does not publish output history or commit backend state:\n\nfunction run_trial!(context, trial_environment)\n    return only(run_call!(\n        context,\n        :reader;\n        environment=trial_environment,\n        publish=false,\n    ))\nend\n\nAccepted state is explicit and separate:\n\nfunction commit_and_publish!(context, accepted_environment)\n    commit_environment!(context, accepted_environment)\n    return only(run_call!(\n        context,\n        :reader;\n        environment=accepted_environment,\n        publish=true,\n    ))\nend\n\nToyEnvironmentControllerModel applies those two operations in its kernel. Configure the reader as its one hard-call target and give only the controller a commit sink:\n\nenvironment = ToySpatialEnvironment(\n    Dict(:canopy => (T=20.0,));\n    step_seconds=3600.0,\n)\n\nmodel = CompositeModel(\n    Object(\n        :leaf;\n        scale=:Leaf,\n        kind=:leaf,\n        geometry=(cell=:canopy,),\n    );\n    applications=(\n        ModelSpec(\n            ToyEnvironmentReaderModel();\n            name=:reader,\n            on=One(scale=:Leaf),\n            environment=Environment(backend=environment),\n        ),\n        ModelSpec(\n            ToyEnvironmentControllerModel(30.0, 22.0);\n            name=:controller,\n            on=One(scale=:Leaf),\n            calls=(\n                :reader => One(\n                    scale=:Leaf,\n                    application=:reader,\n                ),\n            ),\n            environment=Environment(\n                backend=environment,\n                sink=:cells,\n            ),\n        ),\n    ),\n)\n\nsimulation = run!(model; outputs=:all)\nstate = final_state(simulation)\n(\n    trial_seen=state.trial_temperature_seen,\n    accepted_seen=state.accepted_temperature_seen,\n    committed=environment.cells[:canopy].T,\n)\n\nThe trial was 30, but the accepted and committed temperature is 22. Only the accepted reader call published:\n\nselect(\n    DataFrame(Diagnostics.explain_outputs(simulation)),\n    :application_id,\n    :variable,\n    :nsamples,\n)","title":"New concept: trial state versus accepted state"},{"category":"section","location":"journeys/users/mutable_environments/#Preserve-distinct-handles-under-Many","page":"Modify the environment","text":"Extend the same backend to two spatial cells. One Many application samples both, while each target retains its own compiled handle:\n\nspatial_environment = ToySpatialEnvironment(\n    Dict(\n        :sun => (T=26.0,),\n        :shade => (T=18.0,),\n    );\n    step_seconds=3600.0,\n)\n\nspatial_model = CompositeModel(\n    Object(\n        :sun_leaf;\n        scale=:Leaf,\n        geometry=(cell=:sun,),\n    ),\n    Object(\n        :shade_leaf;\n        scale=:Leaf,\n        geometry=(cell=:shade,),\n    );\n    applications=(\n        ModelSpec(\n            ToyEnvironmentReaderModel();\n            name=:temperature,\n            on=Many(scale=:Leaf),\n            environment=Environment(backend=spatial_environment),\n        ),\n    ),\n)\n\nspatial_simulation = run!(spatial_model)\nspatial_states = final_state(spatial_simulation, Many(scale=:Leaf))\nDict(id => state.temperature_seen for (id, state) in spatial_states)\n\nselect(\n    DataFrame(Diagnostics.explain_environment_bindings(spatial_model)),\n    :object_id,\n    :handle,\n)\n\nThis ordinary user page does not require the backend implementation protocol. Framework builders can follow Environment Backend Extensions; the complete MAESPA-style synthesis later combines mutable microclimate, several plants, and iterative leaf calls.","title":"Preserve distinct handles under Many"},{"category":"section","location":"journeys/users/mutable_environments/#Page-recap","page":"Modify the environment","text":"You added: one typed trial, one explicit accepted commit, one accepted publication, and then two spatial cells.\nPlantSimEngine inferred: the reader call target, publication boundary, commit permission check, and distinct Many handles.\nYou keep explicit: trial and acceptance logic, publish, the committed variables, controller sink, and backend state type.\nNew API names: environment_outputs_, run_call!, commit_environment!, publish, and calls.","title":"Page recap"},{"category":"section","location":"composite_model/quickstart/#CompositeModel/Object-Quickstart","page":"CompositeModel/Object Quickstart","text":"This page is the shortest path to a native composite-model/object simulation.\n\nUse this API for new multiscale, multi-plant, soil, microclimate, and model-scale simulations. Applications use one direct constructor:\n\nModelSpec(\n    model;\n    name=:application,\n    on=Many(scale=:Leaf),\n    inputs=(...),\n    calls=(...),\n    every=Dates.Hour(1),\n    environment=Environment(...),\n    output_routing=(...),\n    updates=Updates(...),\n)\n\nScenarios are defined with CompositeModel and model applications. A model is a reusable process implementation; an application is one configured use of that model, including its name, target objects, inputs, cadence, and environment. One application may run on many objects, and the same model may appear in several applications with different parameters or targets.","title":"CompositeModel/Object Quickstart"},{"category":"section","location":"composite_model/quickstart/#One-Object,-Several-Models","page":"CompositeModel/Object Quickstart","text":"For models that all run on one object, the concise constructor lowers directly to the ordinary CompositeModel/Object representation:\n\nmodel = CompositeModel(ModelA(), ModelB(); status=(initial_value=1.0,))\n\nUse the explicit form later in this guide when applications need names, selectors, or other scenario policies.\n\nThe first model has one object, :scene, and three model applications:\n\nToyDegreeDaysCumulModel computes daily thermal time;\nToyLAIModel consumes cumulative thermal time and computes LAI;\nBeer consumes LAI and meteorology to compute absorbed PAR.\n\nThe model implementations are ordinary PlantSimEngine kernels. The model application layer decides where they run. With no explicit every, these applications use the environment cadence.\n\nmeteo_day = read_weather(\n    joinpath(pkgdir(PlantSimEngine), \"examples/meteo_day.csv\");\n    duration=Dates.Day,\n)\n\nmodel = CompositeModel(\n    ToyDegreeDaysCumulModel(),\n    ToyLAIModel(),\n    Beer(0.6);\n    environment=meteo_day,\n)\n\nsim = run!(model; steps=30, outputs=:all)\nout = collect_outputs(sim; sink=DataFrame)\nfirst(out, 6)\n\nThe Simulation provides a snapshot of the latest status values:\n\nscene_status = final_state(sim)\n(TT_cu=scene_status.TT_cu, LAI=scene_status.LAI, aPPFD=scene_status.aPPFD)","title":"One Object, Several Models"},{"category":"section","location":"composite_model/quickstart/#Inspect-The-Compiled-Bindings","page":"CompositeModel/Object Quickstart","text":"Before running, Diagnostics.explain_initialization(model) classifies each variable as :required, :defaulted, :supplied, :producer_bound, :generated, or :environment_bound. The report remains available when required values are missing, so it can be used to finish configuring a model.\n\nThe compiler infers unambiguous same-object dependencies from declared model inputs and outputs:\n\n:LAI_Dynamic reads TT_cu from :Degreedays;\n:light_interception reads LAI from :LAI_Dynamic.\n\nselect(\n    DataFrame(Diagnostics.explain_bindings(model)),\n    :application_id,\n    :input,\n    :source_application_ids,\n    :carrier_kind,\n    :copy_semantics,\n)\n\ncarrier_kind = :ref and copy_semantics = :live_references mean the consumer sees a shared reference rather than a copied value.\n\nFor runtime performance diagnostics, inspect the execution plan:\n\nselect(\n    DataFrame(Diagnostics.explain_execution_plan(model)),\n    :application_id,\n    :object_ids,\n    :batch_size,\n    :inner_loop_dispatch,\n)","title":"Inspect The Compiled Bindings"},{"category":"section","location":"composite_model/quickstart/#Request-Outputs","page":"CompositeModel/Object Quickstart","text":"By default, model runs retain no user output streams. Pass outputs=:all to retain every publisher, or pass OutputRequest values to retain and materialize selected streams plus those required by temporal inputs.\n\nrequest = OutputRequest(\n    Many(scale=:Scene),\n    :LAI;\n    name=:lai_every_two_days,\n    application=:LAI_Dynamic,\n    policy=HoldLast(),\n    clock=Day(2),\n)\n\nrequested_sim = run!(\n    model;\n    steps=30,\n    outputs=request,\n)\n\ncollect_outputs(requested_sim, :lai_every_two_days; sink=nothing)[1:4]\n\nThe retention explanation reports why a stream was kept:\n\nDiagnostics.explain_output_retention(requested_sim)","title":"Request Outputs"},{"category":"section","location":"composite_model/quickstart/#Many-Objects-As-Inputs","page":"CompositeModel/Object Quickstart","text":"Use ModelSpec(...; inputs=...) when a model needs values from selected objects. This model-scale LAI model reads live references to the surface of every plant:\n\nplant_scene = CompositeModel(\n    Object(:scene; scale=:Scene, kind=:scene),\n    Object(\n        :plant_1;\n        scale=:Plant,\n        kind=:plant,\n        parent=:scene,\n        status=Status(surface=12.0),\n    ),\n    Object(\n        :plant_2;\n        scale=:Plant,\n        kind=:plant,\n        parent=:scene,\n        status=Status(surface=8.0),\n    );\n    applications=(\n        ModelSpec(ToyLAIfromLeafAreaModel(100.0); name=:scene_lai, on=One(scale=:Scene), inputs=(:plant_surfaces => Many(\n                    scale=:Plant,\n                    within=SceneScope(),\n                    var=:surface,\n                ),)),\n    ),\n)\n\nplant_sim = run!(plant_scene)\nplant_model_status = final_state(plant_sim, One(scale=:Scene))\n(total_surface=plant_model_status.total_surface, LAI=plant_model_status.LAI)\n\nThe compiled binding shows a RefVector carrier:\n\nselect(\n    DataFrame(Diagnostics.explain_bindings(plant_scene)),\n    :application_id,\n    :input,\n    :source_ids,\n    :carrier_kind,\n    :copy_semantics,\n)\n\nIf the consumer model runs on each plant, use within=Subtree() to read only objects inside the current plant. Use within=SceneScope() when a model model must aggregate all matching objects.","title":"Many Objects As Inputs"},{"category":"section","location":"composite_model/quickstart/#Manual-Calls","page":"CompositeModel/Object Quickstart","text":"Use ModelSpec(...; calls=...) when a parent model must directly run selected child models. This is the mechanism for iterative solvers such as model energy balance:\n\nModelSpec(SceneEnergyBalance(); name=:scene_energy, on=One(scale=:Scene), calls=(:leaf_energy => Many(\n            kind=:plant,\n            scale=:Leaf,\n            within=SceneScope(),\n            application=:energy_balance,\n        ),\n        :soil => One(\n            kind=:soil,\n            scale=:Soil,\n            within=SceneScope(),\n            application=:soil_water,\n        ),), every=Hour(1))\n\nFor a one-shot call, execute all targets directly:\n\ntargets = run_call!(context, :leaf_energy; publish=true)\n\ntargets is always vector-like regardless of whether the declaration uses One, OptionalOne, or Many. For iterative control, retrieve the same compiled targets without executing them:\n\nfunction PlantSimEngine.run!(model::SceneEnergyBalance, status, environment,\n                             constants, context)\n    trial = trial_environment(model, status)\n    run_call!(context, :leaf_energy; environment=trial, publish=false)\n\n    accepted = accepted_environment(model, status)\n    commit_environment!(context, accepted)\n    run_call!(context, :leaf_energy; publish=true)\n\n    return nothing\nend\n\nrun_call! defaults to publish=false, so trial calls mutate target statuses without publishing temporal streams or committing mutable environment updates. Pass non-committing trial state with environment=trial_state, then call commit_environment! before publishing the accepted state.","title":"Manual Calls"},{"category":"section","location":"composite_model/quickstart/#Next-Steps","page":"CompositeModel/Object Quickstart","text":"Migrating To The CompositeModel/Object API translates scenarios written with removed APIs.\nPublic API lists constructors, selectors, lifecycle helpers, environment helpers, and explanation helpers.\nModel traits documents inputs_, outputs_, dep, timespec, output_policy, and environment_inputs_.\nMAESPA-style model example handoff records the current multi-plant model energy-balance acceptance example.","title":"Next Steps"},{"category":"section","location":"step_by_step/simple_model_coupling/#Standard-model-coupling","page":"Standard model coupling","text":"This page shows the standard coupling case: one model computes a variable that another model reads. In the composite-model/object API, the user describes model applications on objects, and the compiler wires the value dependencies.","title":"Standard model coupling"},{"category":"section","location":"step_by_step/simple_model_coupling/#Setting-up-your-environment","page":"Standard model coupling","text":"Make sure you have a working Julia environment with PlantSimEngine and the recommended companion packages. Details are provided on the Installing PlantSimEngine page.","title":"Setting up your environment"},{"category":"section","location":"step_by_step/simple_model_coupling/#One-object-and-one-model","page":"Standard model coupling","text":"A model contains objects. A model application says where a model runs. Here a light interception model runs on the model object, uses the environment's daily cadence, and reads LAI from that object's status:\n\nlight_scene = CompositeModel(\n    Beer(0.5);\n    status=(LAI=2.0,),\n    environment=meteo_day,\n)\n\nlight_sim = run!(light_scene; steps=3, outputs=:all)\nfirst(collect_outputs(light_sim; sink=DataFrame), 3)","title":"One object and one model"},{"category":"section","location":"step_by_step/simple_model_coupling/#Coupling-two-models","page":"Standard model coupling","text":"Suppose we want ToyLAIModel to compute LAI for Beer. Both models can run on the same object. ToyLAIModel produces LAI, and Beer declares LAI as an input, so the model compiler infers the binding:\n\ncoupled_scene = CompositeModel(\n    ToyDegreeDaysCumulModel(),\n    ToyLAIModel(),\n    Beer(0.5);\n    environment=meteo_day,\n)\n\nselect(\n    DataFrame(Diagnostics.explain_bindings(coupled_scene)),\n    :application_id,\n    :input,\n    :source_application_ids,\n    :origin,\n    :carrier_kind,\n    :copy_semantics,\n)\n\nThe :inferred_same_object rows are soft dependencies: the consumer input is provided by another model output. Same-rate local links use live references, so the timestep loop does not copy values between models.\n\nRun the coupled model:\n\ncoupled_sim = run!(coupled_scene; steps=5)\ncoupled_status = final_state(coupled_sim)\n(TT_cu=coupled_status.TT_cu, LAI=coupled_status.LAI, aPPFD=coupled_status.aPPFD)","title":"Coupling two models"},{"category":"section","location":"step_by_step/simple_model_coupling/#Adding-another-model","page":"Standard model coupling","text":"Additional models are just additional applications. ToyRUEGrowthModel consumes aPPFD, which is produced by Beer, so the compiler infers another same-object binding:\n\ngrowth_scene = CompositeModel(\n    ToyDegreeDaysCumulModel(),\n    ToyLAIModel(),\n    Beer(0.5),\n    ToyRUEGrowthModel(0.2);\n    environment=meteo_day,\n)\n\ngrowth_sim = run!(growth_scene; steps=5)\ngrowth_status = final_state(growth_sim)\n(LAI=growth_status.LAI, aPPFD=growth_status.aPPFD, biomass=growth_status.biomass)","title":"Adding another model"},{"category":"section","location":"step_by_step/advanced_coupling/#Coupling-more-complex-models","page":"Advanced coupling and hard dependencies","text":"Most model coupling is a value dependency: one model writes an output, another model reads it as an input. Some models need tighter control. For example, an energy-balance model may call photosynthesis and stomatal-conductance models several times while it iterates leaf temperature.\n\nThat second case is a manual call dependency. In the composite-model/object API it is declared with ModelSpec(...; calls=...).","title":"Coupling more complex models"},{"category":"section","location":"step_by_step/advanced_coupling/#Soft-inputs-and-manual-calls","page":"Advanced coupling and hard dependencies","text":"Use ModelSpec(...; inputs=...) or inferred same-object bindings when a model only needs a value. Use ModelSpec(...; calls=...) when the parent model must directly run another model inside its own run! method.\n\nThe example process models in examples/dummy.jl contain both patterns:\n\nProcess4Model computes var1 and var2;\nProcess1Model consumes var1 and var2 and computes var3;\nProcess2Model manually calls process 1, then computes var4 and var5;\nProcess3Model manually calls process 2, then computes var6;\nProcess5Model, Process6Model, and Process7Model use regular soft value dependencies.","title":"Soft inputs and manual calls"},{"category":"section","location":"step_by_step/advanced_coupling/#Declaring-manual-calls-in-the-scenario","page":"Advanced coupling and hard dependencies","text":"ModelSpec(...; calls=...) is scenario-level wiring. The model kernel remains generic; the scenario decides which concrete application is called.\n\nUse application=... in scenario-level ModelSpec(...; calls=...) and ModelSpec(...; inputs=...) when you know which mounted model application should provide the value or be called. Use process identities in model-level contracts such as dep(model), where the model author only declares that a compatible process is required and cannot know the names chosen by future scenarios.\n\nThis split avoids ambiguity when several applications implement the same process. For example, two soil-water applications can share the same process but represent different layers, parameter sets, objects, or time steps. A scenario selector should name the application that has the intended role.\n\ncomplex_scene = CompositeModel(\n    Object(:scene; scale=:Scene, kind=:scene, status=Status(var0=2.0));\n    applications=(\n        ModelSpec(Process4Model(); name=:prepare_inputs, on=One(scale=:Scene), every=Day(1)),\n\n        ModelSpec(Process1Model(2.0); name=:process1, on=One(scale=:Scene), every=Day(1)),\n\n        ModelSpec(Process2Model(); name=:process2, on=One(scale=:Scene), calls=(:process1 => One(scale=:Scene, application=:process1)), every=Day(1)),\n\n        ModelSpec(Process3Model(); name=:process3, on=One(scale=:Scene), calls=(:process2 => One(scale=:Scene, application=:process2)), every=Day(1)),\n\n        ModelSpec(Process5Model(); name=:process5, on=One(scale=:Scene), every=Day(1)),\n\n        ModelSpec(Process7Model(); name=:process7, on=One(scale=:Scene), every=Day(1)),\n\n        ModelSpec(Process6Model(); name=:process6, on=One(scale=:Scene), every=Day(1)),\n    ),\n    environment=meteo_day,\n)\n\nselect(\n    DataFrame(Diagnostics.explain_calls(complex_scene)),\n    :application_id,\n    :call,\n    :callee_application_ids,\n    :callee_object_ids,\n    :publication_policy,\n)\n\nApplications selected by ModelSpec(...; calls=...) are not scheduled as independent root applications under their caller. They run only when the parent calls them. This gives the parent full call-stack control.","title":"Declaring manual calls in the scenario"},{"category":"section","location":"step_by_step/advanced_coupling/#Running-the-coupled-model","page":"Advanced coupling and hard dependencies","text":"The regular soft dependencies are still inferred from inputs_ and outputs_. The scheduler combines those soft edges with the call ownership rules:\n\nselect(\n    DataFrame(Diagnostics.explain_schedule(complex_scene)),\n    :application_id,\n    :manual_call_only,\n    :execution_index,\n    :clock,\n)\n\nRun one timestep:\n\ncomplex_sim = run!(complex_scene; steps=1)\ncomplex_status = final_state(complex_sim)\n(\n    var3=complex_status.var3,\n    var5=complex_status.var5,\n    var6=complex_status.var6,\n    var8=complex_status.var8,\n)","title":"Running the coupled model"},{"category":"section","location":"step_by_step/advanced_coupling/#Writing-new-hard-coupled-models","page":"Advanced coupling and hard dependencies","text":"For new composite-model/object models, execute all targets directly when they share meteorology and publication policy:\n\ntargets = run_call!(context, :leaf_energy; publish=true)\n\nThe result is always vector-like. Retrieve targets without executing them when an algorithm needs selective execution or an already sampled environment for each target:\n\ntargets = call_targets(context, :leaf_energy)\nfor (target, leaf_environment) in zip(targets, environments_by_leaf)\n    run_call!(\n        target;\n        sampled_environment=leaf_environment,\n        publish=false,\n    )\nend\n\nFor a provider-aware trial state shared by the call, keep the execute-all form. Each target still samples through its own compiled handle:\n\nfunction PlantSimEngine.run!(model::SceneEnergyBalance, status, environment,\n                             constants, context)\n    trial = trial_environment(model, status)\n    run_call!(context, :leaf_energy; environment=trial, publish=false)\n\n    accepted = accepted_environment(model, status)\n    commit_environment!(context, accepted)\n    run_call!(context, :leaf_energy; publish=true)\n\n    return nothing\nend\n\nrun_call! defaults to publish=false, which is useful for trial iterations. Pass non-committing trial state with the environment keyword. Use commit_environment! and publish=true for the accepted state so temporal streams and mutable environment state are published once.\n\nThe MAESPA-style example uses the same mechanism: a model energy-balance model calls all selected leaf energy-balance models and the shared soil model while it solves canopy microclimate.\n\nScenario wiring uses ModelSpec(...; calls=...). Model authors should keep kernels generic and only require manual calls when the model really needs call-stack control.","title":"Writing new hard-coupled models"},{"category":"section","location":"migration_composite_model/#Migrating-To-The-CompositeModel/Object-API","page":"From the mapping runtime","text":"","title":"Migrating To The CompositeModel/Object API"},{"category":"section","location":"migration_composite_model/#Refining-early-CompositeModel/Object-code","page":"From the mapping runtime","text":"The stabilized public surface makes several early CompositeModel/Object behaviors explicit:\n\nEarly spelling or behavior Stabilized API\nSelf() searched self and descendants Self() selects only the current object; use Subtree() for self plus descendants\nomitted tracked_outputs retained everything use explicit outputs=:all; the safe default is outputs=:none\ntracked_outputs=requests outputs=requests\nOutputRequest(:Leaf, :x; process=:p) OutputRequest(Many(scale=:Leaf), :x; application=:app)\nrepeated unnamed applications gained numbered IDs name every repeated application with ModelSpec(...; name=...)\ncalling run!(model) again implicitly looked like continuation use continue!(simulation) or step!(simulation)\ncompiler/cache types imported from the default namespace qualify them through PlantSimEngine.Advanced\n\ntracked_outputs has been removed. Use outputs=:all, outputs=:none, or outputs=requests directly. Singular scenario inputs and calls, OutputRequest, object overrides, and Updates(...; after=...) now identify the target by canonical application=... or application ID. Model-authored Input/Call defaults may still discover a process because they cannot know scenario application names, and Many(process=...) remains an explicit multi-application discovery query.\n\nCalling run!(model; ...) always creates a fresh result timeline starting at step one, even when object status has already been mutated by an earlier run. Continue the same timeline, environment position, temporal histories, and multirate phase with:\n\nsimulation = run!(model; steps=24, outputs=requests)\ncontinue!(simulation; steps=24)\nstep!(simulation)\n@assert current_step(simulation) == 49\n\nThe composite-model/object API replaces the historical multiscale mapping system with one object-address graph.\n\nNew scenario code should be organized around:\n\nCompositeModel\nObject\nModelSpec\nUpdates\nEnvironment\n\nProcess-model implementations do not need to know about composite models, plants, objects, or timesteps. They keep the existing kernel contract:\n\ninputs_(model)\noutputs_(model)\ndep(model)\nenvironment_inputs_(model)\nrun!(model, status, environment, constants, context)\n\nThis page maps the legacy configuration concepts to their composite-model/object equivalents.","title":"Refining early CompositeModel/Object code"},{"category":"section","location":"migration_composite_model/#Explicit-Input-Initialization","page":"From the mapping runtime","text":"Input literals no longer double as ambiguous placeholder values. Declare whether each model input is required or genuinely has a fallback:\n\n# Old, ambiguous\ninputs_(::GrowthModel) = (\n    temperature=-Inf,\n    efficiency=0.8,\n)\n\n# Current\ninputs_(::GrowthModel) = (\n    temperature=Required(Float64),\n    efficiency=Default(0.8),\n)\n\nRequired(T) means object state or another application must supply the value. Default(value) means PlantSimEngine may initialize it when absent. Output literals remain initial output-state values. Plain input literals are rejected; there is no compatibility interpretation of -Inf or another sentinel.\n\nReplace helpers that previously merged every input literal into initial state with init_variables(model). It returns only real input defaults and output initial values, omitting required inputs.","title":"Explicit Input Initialization"},{"category":"section","location":"migration_composite_model/#Scenario-Structure","page":"From the mapping runtime","text":"Legacy simulations split configuration between ModelMapping and MultiScaleModel. The unified API stores runtime entities in one CompositeModel:\n\nModelMapping has been removed. Historical code must be translated to the composite-model/object form below.\n\nmodel = CompositeModel(\n    Object(:scene; scale=:Scene, kind=:scene),\n    Object(:plant_1; scale=:Plant, kind=:plant, parent=:scene),\n    Object(:leaf_1; scale=:Leaf, kind=:plant, parent=:plant_1),\n    Object(:soil; scale=:Soil, kind=:soil, parent=:scene);\n    applications=(\n        ModelSpec(LeafModel(); name=:leaf_model, on=Many(scale=:Leaf)),\n\n        ModelSpec(SoilModel(); name=:soil_model, on=One(scale=:Soil)),\n    ),\n    environment=(T=25.0, Rh=0.6, Wind=1.0),\n)\n\nObject labels describe runtime entities. They do not prescribe plant topology. A plant may use any hierarchy of plants, axes, internodes, segments, leaves, roots, fruits, or application-specific objects.","title":"Scenario Structure"},{"category":"section","location":"migration_composite_model/#Existing-MTG-Topologies","page":"From the mapping runtime","text":"An existing MTG can be adapted without rebuilding its topology manually:\n\nmodel = CompositeModel(\n    mtg;\n    applications=applications,\n    environment=environment,\n    kind=node_kind,\n    species=node_species,\n    geometry=node_geometry,\n)\n\nobjects_from_mtg(mtg; ...) exposes the intermediate object list when it is useful to inspect or modify labels before constructing the model. By default, the adapter uses MTG node ids and scales. Runtime Status values belong to the CompositeModel registry and are never stored in MTG attributes. A deliberate import boundary may provide status=node -> import_status(node) explicitly; maintained workflows should initialize scientific state through model objects or model applications instead.","title":"Existing MTG Topologies"},{"category":"section","location":"migration_composite_model/#Multiscale-Inputs","page":"From the mapping runtime","text":"Replace MultiScaleModel(...) variable mappings with consumer-side ModelSpec(...; inputs=...).\n\nLegacy:\n\nMultiScaleModel(\n    AllocationModel(),\n    [:leaf_carbon => [:Leaf => :leaf_carbon]],\n)\n\nUnified:\n\nModelSpec(\n    AllocationModel();\n    name=:allocation,\n    on=Many(scale=:Plant),\n    inputs=(\n        :leaf_carbon => Many(\n            scale=:Leaf,\n            within=Subtree(),\n            var=:leaf_carbon,\n        ),\n    ),\n)\n\nSelf() selects only the object where the consumer runs. A plant-scale allocation model uses Subtree() to read leaves below that plant. Use SceneScope() for model-wide aggregation and SelfPlant() to select the nearest containing plant and its subtree from an organ.\n\nSame-object renaming uses the same syntax:\n\nModelSpec(\n    ConsumerModel();\n    inputs=(\n        :consumer_name => One(\n            within=Self(),\n            application=:producer,\n            var=:producer_name,\n        ),\n    ),\n)\n\nSame-rate bindings use shared references or reference vectors when possible. Cross-rate bindings use typed temporal streams.","title":"Multiscale Inputs"},{"category":"section","location":"migration_composite_model/#CompositeModel-Wide-Values","page":"From the mapping runtime","text":"Use an input selector on the consuming application:\n\nModelSpec(SceneWaterBalance(); name=:scene_water, on=One(scale=:Scene), inputs=(:leaf_transpiration => Many(\n            kind=:plant,\n            scale=:Leaf,\n            within=SceneScope(),\n            application=:transpiration,\n            var=:transpiration,\n        ),))\n\nThe compiler chooses the carrier. Scenario authors declare the source objects, source variable, and temporal policy rather than a route implementation.","title":"CompositeModel-Wide Values"},{"category":"section","location":"migration_composite_model/#Manual-Hard-Calls","page":"From the mapping runtime","text":"Use ModelSpec(...; calls=...) when a parent model must control child execution.\n\nModelSpec(SceneEnergyBalance(); name=:scene_energy, on=One(scale=:Scene), calls=(:leaf_energy => Many(\n            kind=:plant,\n            scale=:Leaf,\n            within=SceneScope(),\n            application=:energy_balance,\n        ),\n        :soil => One(\n            kind=:soil,\n            scale=:Soil,\n            within=SceneScope(),\n            application=:soil_water,\n        ),))\n\nThe parent model controls execution:\n\nfunction PlantSimEngine.run!(model::SceneEnergyBalance, status, environment,\n                             constants, context)\n    for iteration in 1:model.max_iterations\n        trial = trial_environment(model, status)\n        run_call!(context, :leaf_energy; environment=trial, publish=false)\n        converged(model, status) && break\n    end\n\n    accepted = accepted_environment(model, status)\n    commit_environment!(context, accepted)\n    run_call!(context, :leaf_energy; publish=true)\n    return nothing\nend\n\nrun_call! defaults to publish=false. Trial calls mutate target status but do not publish temporal samples or commit mutable environment updates. do not append temporal samples or write environment outputs. The accepted state must use publish=true.","title":"Manual Hard Calls"},{"category":"section","location":"migration_composite_model/#Multiple-Plants-And-Species","page":"From the mapping runtime","text":"Represent repeated plant configurations with CompositeModelTemplate and ObjectInstance.\n\noil_palm = CompositeModelTemplate(\n    (\n        ModelSpec(LeafEnergy(); on=Many(scale=:Leaf)),\n\n        ModelSpec(Allocation(); on=One(scale=:Plant), inputs=(:leaf_carbon => Many(\n                scale=:Leaf,\n                within=Subtree(),\n                var=:leaf_carbon,\n            ),)),\n    );\n    kind=:plant,\n    species=:oil_palm,\n)\n\npalm_1 = ObjectInstance(\n    :palm_1,\n    oil_palm;\n    root=Object(:plant_1; scale=:Plant, parent=:scene),\n    objects=(Object(:palm_1_leaf_1; scale=:Leaf, parent=:plant_1),),\n)\n\npalm_2 = ObjectInstance(\n    :palm_2,\n    oil_palm;\n    root=Object(:plant_2; scale=:Plant, parent=:scene),\n    objects=(Object(:palm_2_leaf_1; scale=:Leaf, parent=:plant_2),),\n)\n\nmodel = CompositeModel(\n    Object(:scene; scale=:Scene, kind=:scene),\n    palm_1,\n    palm_2,\n)\n\nUnmodified instances share model objects and parameters. Use instance overrides for one plant and Override(...) for exceptional organs.","title":"Multiple Plants And Species"},{"category":"section","location":"migration_composite_model/#Multirate-Inputs","page":"From the mapping runtime","text":"Replace TimeStepModel(...) with ModelSpec(...; every=...). Put temporal policy and window information on the consuming ModelSpec(...; inputs=...) selector.\n\nModelSpec(HourlyLeafModel(); name=:leaf_flux, on=Many(scale=:Leaf), every=Hour(1))\n\nModelSpec(DailyPlantModel(); name=:daily_plant, on=Many(scale=:Plant), inputs=(:leaf_fluxes => Many(\n            scale=:Leaf,\n            within=Subtree(),\n            application=:leaf_flux,\n            var=:flux,\n            policy=Integrate(),\n            window=Day(1),\n        ),), every=Day(1))\n\nUse HoldLast(), Interpolate(), Integrate(), or Aggregate() according to the physical meaning of the input. PreviousTimeStep(:x) => selector expresses an explicit lag and breaks a same-timestep dependency cycle.\n\nIf the input selector omits policy=..., the model compiler uses the producer's output_policy(...) trait for the selected source variable when the publisher is unique. An explicit selector policy always wins over the trait.\n\nIf a model defines timespec(::Type{<:MyModel}), the model scheduler uses that cadence when the application has no explicit ModelSpec(...; every=...). A scenario-level ModelSpec(...; every=...) always wins over the model trait.\n\nIf the clock falls back to the model base step, timestep_hint(...) required bounds are validated against that base step. The hint is a compatibility constraint, not a scheduling override.","title":"Multirate Inputs"},{"category":"section","location":"migration_composite_model/#Ordered-Variable-Updates","page":"From the mapping runtime","text":"When several models intentionally write the same variable, declare the order on the later application:\n\nModelSpec(CarbonAllocation(); name=:allocation, on=Many(scale=:Leaf))\n\nModelSpec(LeafPruning(); name=:pruning, on=Many(scale=:Leaf), updates=Updates(:leaf_biomass; after=:allocation))\n\nDo not encode this coupling in either model implementation. The scenario owns the writer order.","title":"Ordered Variable Updates"},{"category":"section","location":"migration_composite_model/#Environment-And-Microclimate","page":"From the mapping runtime","text":"Models declare sampled environment variables with environment_inputs_. The scenario binds each object/application to the active environment backend:\n\nModelSpec(LeafEnergy(); name=:leaf_energy, on=Many(scale=:Leaf), environment=Environment(provider=:grid))\n\nSpatial bindings are cached. The default resolver uses the object's geometry, then the nearest ancestor geometry, then the backend's global behavior. Changing geometry invalidates only affected environment bindings.\n\nPer-model source remapping also moves to Environment(...):\n\nModelSpec(LeafGasExchange(); name=:gas_exchange, on=Many(scale=:Leaf), environment=Environment(provider=:global, sources=(CO2=:Ca,)))\n\nThe model still declares and reads CO2; the model samples Ca from the active environment backend and exposes it to the model as environment.CO2. Diagnostics.explain_environment_bindings(...) reports both required_inputs and source_inputs, so remapped meteorology is visible to users and agents.\n\nModel authors can also provide default source remaps with environment_hint(::Type{<:Model}) = (bindings=(CO2=(source=:Ca,),),). CompositeModel applications use those defaults when the scenario does not provide an explicit source for the same variable. Environment(; sources=...) remains the scenario-level override.\n\nFor global Weather tables, sampling follows the application's ModelSpec(...; every=...). A slower model receives a PlantMeteo windowed sample using its environment_hint reducer and window instead of receiving only the current raw weather row. A scenario source override preserves that reducer:\n\nenvironment_hint(::Type{<:GasExchange}) = (\n    bindings=(CO2=(source=:Ca, reducer=MeanReducer()),),\n)\n\nModelSpec(GasExchange(); on=Many(scale=:Leaf), every=Hour(2), environment=Environment(provider=:global, sources=(CO2=:canopy_CO2,)))\n\nEvery leaf still reads environment.CO2; the two-hour mean is computed from :canopy_CO2. The sampled row is computed once per application and timestep, then reused for all selected leaves.","title":"Environment And Microclimate"},{"category":"section","location":"migration_composite_model/#Growth,-Pruning,-And-Movement","page":"From the mapping runtime","text":"Use the public lifecycle operations:\n\nregister_object!(model, new_leaf; parent=:plant_1)\nnew_leaf_status = add_organ!(\n    parent_node,\n    model,\n    :+,\n    :Leaf,\n    3;\n    initial_status=(biomass=0.0,),\n)\nremove_object!(model, :old_leaf)\nreparent_object!(model, :leaf_2, :axis_3)\nmove_object!(model, :leaf_3, new_geometry)\n\nFor MTG-backed growth, prefer add_organ!: it creates the MTG node and its model object together and, by default, reuses the status initialization policy from CompositeModel(mtg; status=...). Use register_object! when adapting another topology backend or when a complete Object already exists.\n\nStructural changes refresh application targets, input carriers, call targets, writer validation, and schedules after the application that made the change. New objects can therefore run applications that remain later in the current timestep, but never applications that already ran. Geometry-only changes refresh environment bindings without rebuilding unrelated structural bindings.\n\nThe refreshed runtime also rebuilds homogeneous execution batches. Use Diagnostics.explain_execution_plan(scene_or_simulation) to inspect the concrete model/status/carrier types and the objects grouped into each specialized inner loop. Exceptional per-object model overrides appear as separate ordered batches.","title":"Growth, Pruning, And Movement"},{"category":"section","location":"migration_composite_model/#Output-Collection","page":"From the mapping runtime","text":"run!(model; steps=...) returns a Simulation. Use final_state(sim) for the latest one-object state, outputs(sim) for retained typed streams, Diagnostics.explain_outputs(sim) for structured diagnostics, and collect_outputs(sim) for tabular rows.\n\nrequest = OutputRequest(\n    Many(scale=:Leaf),\n    :transpiration;\n    name=:leaf_transpiration_daily,\n    application=:leaf_energy,\n    policy=Integrate(),\n    clock=Day(1),\n)\n\nsim = run!(model; steps=48, outputs=request)\ndaily = collect_outputs(sim, :leaf_transpiration_daily)\n\nCompositeModel output requests are materialized from retained temporal streams after the run. They use the same temporal policies as multirate inputs and export dynamic objects only over the interval where that object published samples. If several model applications implement the same process, add application=:application_name to select one explicitly. This is also the way to request a named :stream_only publisher. outputs=:none retains no user streams. Passing explicit requests retains only their application/variable streams plus streams needed by temporal ModelSpec(...; inputs=...). Use Diagnostics.explain_output_retention(sim) to inspect why each retained stream was kept. Dependency-only streams retain a bounded policy-specific horizon, while requested streams keep complete histories for post-run export. Export is not yet a fully online path.","title":"Output Collection"},{"category":"section","location":"migration_composite_model/#Inspecting-The-Compiled-Scenario","page":"From the mapping runtime","text":"Use structured explanations instead of inspecting internal dictionaries:\n\nDiagnostics.explain_objects(model)\nDiagnostics.explain_instances(model)\nDiagnostics.explain_scopes(model)\nDiagnostics.explain_applications(model)\nDiagnostics.explain_bindings(model)\nDiagnostics.explain_calls(model)\nDiagnostics.explain_environment_bindings(model)\nDiagnostics.explain_schedule(model)\nDiagnostics.explain_writers(model)\n\nThese functions return structured rows with concrete object ids, application ids, processes, variables, temporal policies, carrier semantics, and resolved targets. They are intended for both users and coding agents.","title":"Inspecting The Compiled Scenario"},{"category":"section","location":"migration_composite_model/#Migration-Table","page":"From the mapping runtime","text":"Legacy configuration CompositeModel/object replacement\nModelMapping scale assembly CompositeModel objects plus model applications\nMultiScaleModel(...) consumer ModelSpec(...; inputs=...)\nTimeStepModel(...) ModelSpec(...; every=...)\nInputBindings(...) source, policy, and window on ModelSpec(...; inputs=...)\nMeteoBindings(...) automatic environment binding or Environment(...)\nScopeModel(...) ModelSpec(...; on=...) and selector scopes\nSameScale() rename inputs=(:local => One(within=Self(), var=:source),)\n\nThe executable MAESPA migration in examples/maespa_model_example.jl demonstrates two plant species, shared soil, plant-local aggregation, model-wide iterative energy balance, hourly and daily models, and automatic environment binding.","title":"Migration Table"},{"category":"section","location":"guides/multiscale/value_coupling/#Coupling-Values-Across-Objects","page":"Value coupling","text":"One(...) requires exactly one source.\nOptionalOne(...) accepts zero or one.\nMany(...) supplies a stable object-ID ordered carrier.\n\nUse var= to rename a source and application= to distinguish repeated processes. Homogeneous many-source values use a RefVector; heterogeneous values use an object-aware reference carrier. Inspect both through Diagnostics.input_carrier, Diagnostics.input_value, and Diagnostics.explain_bindings, not internal fields.","title":"Coupling Values Across Objects"},{"category":"section","location":"guides/multiscale/value_coupling/#Keep-identities-aligned-with-values","page":"Value coupling","text":"A model that only reduces or broadcasts over a Many input can keep using the ordinary status field. When a model must associate a value with the object that owns it, request the identity-aware view from the current RunContext:\n\nfunction PlantSimEngine.run!(model, status, environment, constants, context)\n    irradiance = bound_input(context, :irradiance)\n\n    @inbounds for index in eachindex(irradiance)\n        object_id = object_ids(irradiance)[index]\n        value = irradiance[index]\n        # Use object_id and value as one aligned pair.\n    end\n    return nothing\nend\n\nBoundMany wraps the same live RefVector or heterogeneous carrier already installed in status.irradiance; it does not copy values or identities. Positions follow compiled ObjectId order and have no botanical meaning. Identity lookup is unambiguous when written as irradiance[ObjectId(:leaf_12)]; integer indexing remains positional.\n\nObtain the view during each model invocation. Lifecycle refresh keeps the current view aligned when possible and may replace it after insertion, removal, or reparenting, so model code must not cache a BoundMany across a lifecycle barrier.","title":"Keep identities aligned with values"},{"category":"section","location":"guides/multiscale/value_coupling/#Publish-one-computation-to-many-objects","page":"Value coupling","text":"Some models execute once on a scene or plant but compute one value per organ. A light model is the typical example: the scene application owns the calculation and cadence, while each leaf owns its local irradiance values. Declare that relationship with outputs_to, then publish the solver result by object identity:\n\nPlantSimEngine.@process \"scene light\" verbose = false\n\nstruct SceneLightModel{F} <: AbstractScene_LightModel\n    solve::F\nend\n\nPlantSimEngine.inputs_(::SceneLightModel) = NamedTuple()\nPlantSimEngine.outputs_(::SceneLightModel) = NamedTuple()\n\nfunction PlantSimEngine.run!(\n    model::SceneLightModel,\n    status,\n    environment,\n    constants,\n    context,\n)\n    targets = output_targets(context, :leaves)\n    result = model.solve(\n        runtime_model(context),\n        environment,\n        object_ids(targets),\n    )\n    assign_outputs!(targets, result; id=:object_id)\n    return nothing\nend\n\nHere solve is an adapter around the scene solver. It returns any Tables.jl-compatible value with an object_id column and the declared result columns, for example incident_par and absorbed_par. Rows may arrive in any order because assign_outputs! maps them to destinations by ObjectId.\n\nDeclare the destinations on the scene application:\n\nlight_application = ModelSpec(\n    SceneLightModel(solve_light);\n    name=:scene_light,\n    on=One(scale=:Scene),\n    outputs_to=(\n        leaves=OutputTo(\n            Many(scale=:Leaf, within=SceneScope());\n            vars=(\n                incident_par=Default(0.0),\n                absorbed_par=Default(0.0),\n            ),\n        ),\n    ),\n)\n\nInside the kernel, targets.columns.incident_par and targets.columns.absorbed_par are the live destination carriers. object_ids(targets) is the aligned, read-only identity view. Direct positional writes are appropriate only when the producing algorithm is already using that exact identity order; identified external results should go through assign_outputs!.","title":"Publish one computation to many objects"},{"category":"section","location":"guides/multiscale/value_coupling/#Consume-those-values-normally","page":"Value coupling","text":"The receiving leaf model remains an ordinary PlantSimEngine model:\n\nPlantSimEngine.@process \"leaf assimilation\" verbose = false\n\nstruct LeafAssimilation <: AbstractLeaf_AssimilationModel end\n\nPlantSimEngine.inputs_(::LeafAssimilation) = (\n    absorbed_par=Required(Float64),\n)\nPlantSimEngine.outputs_(::LeafAssimilation) = (assimilation=0.0,)\n\nfunction PlantSimEngine.run!(\n    ::LeafAssimilation,\n    status,\n    environment,\n    constants,\n    context,\n)\n    # This coefficient is arbitrary and only illustrates value coupling.\n    status.assimilation = 0.01 * status.absorbed_par\n    return nothing\nend\n\napplications = (\n    ModelSpec(\n        LeafAssimilation();\n        name=:leaf_assimilation,\n        on=Many(scale=:Leaf),\n    ),\n    light_application,\n)\n\nThe compiler knows that :scene_light owns :absorbed_par on each selected leaf. It binds the leaf input and schedules the scene writer before the leaf consumer even though the consumer appears first in the tuple. No per-leaf copy model, from_status=true, or manual after=:scene_light declaration is needed.","title":"Consume those values normally"},{"category":"section","location":"guides/multiscale/value_coupling/#Assignment-contract","page":"Value coupling","text":"assign_outputs! supports two public forms:\n\nassign_outputs!(targets, result_table; id=:object_id)\nassign_outputs!(targets, result_ids, result_columns)\n\nThe first accepts any Tables.jl-compatible column or row table. The second accepts an AbstractVector of IDs and a NamedTuple of columns, avoiding the table adapter on a stable columnar path. Both forms use the same rules:\n\nResult content Behavior\nOne row for every current destination Required\nUnknown, duplicate, extra, or missing IDs Rejected before mutation\nEvery variable declared by OutputTo Required\nAdditional columns such as solver metadata Ignored\nSource columns overlapping destination storage Rejected, except exact-order self-assignment of the same column\n\nOnly coverage=:exact is supported. A filtered, abscised, or non-geometrized organ must therefore be handled deliberately by the scene adapter or excluded by the destination selector; PlantSimEngine never silently retains an old value or substitutes zero for a missing result.","title":"Assignment contract"},{"category":"section","location":"guides/multiscale/value_coupling/#Reuse-stable-columns-efficiently","page":"Value coupling","text":"PlantSimEngine caches the result-row permutation by the identity of the ID column. Reusing the same ID vector promises that its IDs and order remain unchanged; mutate only the result value columns in place. Replace the ID vector when membership or ordering changes. A lifecycle refresh invalidates this cache automatically, and the next invocation rebuilds it against the refreshed destinations.\n\nHomogeneous destination values use typed RefVector columns and the stable exact-order path can run without allocations after compilation. If selected statuses hold different concrete value types, PlantSimEngine falls back to an ObjectRefVector and converts against each destination reference. The public API is identical, but the homogeneous representation is the performance path to prefer for large per-organ assignments.\n\nLike BoundMany, an OutputTargets view belongs to the current invocation and lifecycle generation. Obtain it from RunContext each time rather than storing it in the model.","title":"Reuse stable columns efficiently"},{"category":"section","location":"dev/composite_model_design/#Unified-CompositeModel/Object-Design","page":"Composite model/object design","text":"This page records the target breaking design for one composite-model/object configuration and runtime API.\n\nThe central idea is:\n\nStructural groupings and scales are selections over objects in one model.\n\nThe engine should expose one way to say \"this model input comes from these objects\" and one way to say \"this model must manually call these models\". The compiler can then choose whether the runtime carrier is a Ref, RefVector, temporal stream, materialized value, or callable model handle.\n\nThe public API should be simple enough to remember as:\n\nModelSpec(\n    model;\n    name=:application,\n    on=Many(scale=:Leaf),\n    inputs=(...),\n    calls=(...),\n    every=Dates.Hour(1),\n    environment=Environment(...),\n    output_routing=(...),\n    updates=Updates(...),\n)\n\nEverything else should either be a selector, a trait declared by the model author, or an internal compiled carrier.","title":"Unified CompositeModel/Object Design"},{"category":"section","location":"dev/composite_model_design/#Core-Concepts","page":"Composite model/object design","text":"","title":"Core Concepts"},{"category":"section","location":"dev/composite_model_design/#CompositeModel","page":"Composite model/object design","text":"A CompositeModel is the whole simulation universe. It contains:\n\nsimulated objects;\nmodel applications;\nenvironment providers;\ntime/runtime state;\ncaches for object selections and environment bindings.\n\nPlants, soil, atmosphere, microclimate grids, organs, sensors, and artificial objects all live in the same model-level object graph.","title":"CompositeModel"},{"category":"section","location":"dev/composite_model_design/#Object","page":"Composite model/object design","text":"An object is any simulated entity with identity. It may have:\n\na unique object id;\none or more labels, such as scale=:Leaf, kind=:plant, species=:oil_palm;\nparent/child links;\ngeometry or position;\nstatus variables;\nmodel applications.\n\nThe engine must not prescribe a plant architecture. A plant can be described as Plant -> Internode -> Leaf, Plant -> Axis -> Segment -> Leaf, Plant -> Metamer -> Organ, or another topology. The engine only needs object identity, labels, and relations.\n\nExisting MultiScaleTreeGraph.Node topologies enter the same registry through objects_from_mtg(root; ...) or CompositeModel(root; ...). The adapter traverses once and accepts accessors for ids, labels, status, and geometry; the timestep runtime does not query the MTG topology.","title":"Object"},{"category":"section","location":"dev/composite_model_design/#Scale","page":"Composite model/object design","text":"A scale is a label on objects, not a separate runtime layer. Examples:\n\n:Scene\n:Plant\n:Axis\n:Internode\n:Leaf\n:Soil\n:SoilLayer\n:Voxel","title":"Scale"},{"category":"section","location":"dev/composite_model_design/#Scope","page":"Composite model/object design","text":"A scope is a named or inferred subset of objects. Examples:\n\nSceneScope()\nSelf()\nSelfPlant()\nAncestor(scale=:Plant)\nScope(:oil_palm)\nMany(kind=:plant)\nMany(species=:oil_palm)\n\nSelf() means only the current object: the object on which the consuming application runs. It never means the model, species, or plant unless that object is itself the plant. Subtree() means that object and its descendants. Neither spelling changes meaning with scale.\n\nSelfPlant() is the nearest containing plant scope. The more generic form is Ancestor(scale=:Plant). Use these when a model running below the plant scale must access siblings or state inside the containing plant.\n\nReusable plant models should use scope-relative queries. If an allocation model is applied to each :Plant, Many(scale=:Leaf, within=Subtree()) means \"the leaves inside this plant\", not all leaves in the model. The same query applied to an axis-scale model means \"the leaves inside this axis\".\n\nCompositeModel-level models widen the scope explicitly with within=SceneScope().\n\nTopology-relative selections use Relation(...):\n\nOne(Relation(:parent))\nMany(Relation(:children))\nMany(Relation(:ancestors); scale=:Plant)\nMany(Relation(:descendants); scale=:Leaf)\nMany(Relation(:siblings))\n\nSupported relations are :self, :parent, :children, :ancestors, :descendants, and :siblings. They resolve relative to the current model application object. An explicit within=... scope intersects the relation result; inferred default scopes do not hide parents or siblings. Relation results are normalized to stable object-id order before bindings are compiled.","title":"Scope"},{"category":"section","location":"dev/composite_model_design/#Object-Template-And-Instance","page":"Composite model/object design","text":"An object template is a reusable model/parameter bundle, for example one oil palm species model. An object instance is one concrete object in the model.\n\nThe same template can be mounted several times:\n\noil_palm = CompositeModelTemplate(\n    kind=:plant,\n    species=:oil_palm,\n    applications=oil_palm_applications,\n    parameters=oil_palm_parameters,\n)\n\nmodel = CompositeModel(\n    ObjectInstance(:palm_1, oil_palm; root=node1),\n    ObjectInstance(:palm_2, oil_palm; root=node2),\n    ObjectInstance(:palm_3, oil_palm; root=node3),\n    ObjectInstance(:palm_4, oil_palm; root=node4),\n)\n\nModels and parameters can be overridden at instance or object level:\n\nObjectInstance(:palm_2, oil_palm; overrides=(\n    stomatal_conductance = Tuzet(; g1=3.2),\n))\n\nOverride(\n    object=:leaf_12,\n    application=:photosynthesis,\n    model=Fvcb(; VcMaxRef=90.0),\n)\n\nOwnership is reference-based and explicit:\n\na template retains the supplied model and parameter objects without copying;\nunchanged instances share those exact objects;\nan instance override replaces one complete model application with another user-owned model object;\nan object override replaces that application only for the selected object;\nPlantSimEngine does not mutate model fields or implicitly merge parameter dictionaries.\n\nOverrides must preserve the model contract: process identity and declared status/environment variable names cannot change. Parameter-only overrides of the same concrete model type retain concrete runtime dispatch. Heterogeneous alternative implementations are supported but may require dynamic dispatch for the exceptional application.","title":"Object Template And Instance"},{"category":"section","location":"dev/composite_model_design/#Model-Kernel-And-Model-Application","page":"Composite model/object design","text":"A model kernel is the reusable model implementation written by a modeler. It defines a process, parameters, inputs_, outputs_, optional dep defaults, optional environment traits, and run!.\n\nA model application is the scenario-specific use of that kernel on selected objects, at a selected rate, with selected value inputs, model calls, update rules, output routing, and environment binding behavior.\n\nThe model kernel should not need to know:\n\nthe species it will be used with;\nthe model it will be embedded in;\nthe timestep chosen by the user;\nwhether its inputs come from local state, another scale, another object, a temporal stream, units, automatic differentiation values, or uncertainty wrappers.\n\nThe scenario owns those decisions through ModelSpec.\n\nTarget shape:\n\nModelSpec(LeafEnergyBalance(); name=:leaf_energy, on=Many(kind=:plant, scale=:Leaf), inputs=(...), calls=(...), every=Hour(1))\n\nApplication names are optional when a process occurs only once. Scenario declarations identify singular producers and call targets with application=:sunlit_photosynthesis; process=... is reserved for explicit discovery queries such as Many(process=:photosynthesis).","title":"Model Kernel And Model Application"},{"category":"section","location":"dev/composite_model_design/#Unified-Model-Configuration","page":"Composite model/object design","text":"ModelSpec is the single scenario wrapper. Released mapping-era configuration is replaced by explicit value inputs and callable model calls.","title":"Unified Model Configuration"},{"category":"section","location":"dev/composite_model_design/#Applies-To","page":"Composite model/object design","text":"Use ModelSpec(...; on=...) to declare the object set where a model application runs. This should be first-class, not inferred from a container or mapping key.\n\nModelSpec(LeafState(); on=Many(kind=:plant, scale=:Leaf))\n\nModelSpec(AllocationModel(); on=Many(kind=:plant, scale=:Plant))\n\nModelSpec(SceneEB(); on=One(scale=:Scene))\n\nThe same model kernel can be applied several times with different selectors, parameters, timesteps, or input bindings. The compiler should normalize each application to a stable application id.","title":"Applies To"},{"category":"section","location":"dev/composite_model_design/#Dependency-Defaults-From-Traits","page":"Composite model/object design","text":"Model authors should still declare inputs_, outputs_, and dep. In the final design, dep(model) is the model-level place for default dependency intent. Historical ModelMapping declarations are migration inputs to the CompositeModel/Object runtime, not a second supported path.\n\nThe rule is:\n\ninputs_(model) declares the variables the model needs;\noutputs_(model) declares the variables the model computes;\ndep(model) declares default value sources or manual model calls when the model author knows a sensible coupling pattern;\nModelSpec(...; inputs=(...)) and ModelSpec(...; calls=(...)) override or specialize those defaults for a specific simulation.\n\nFor example, a plant allocation model can provide a plant-local default:\n\ndep(::PlantAllocationModel) = (\n    leaf_carbon = Input(Many(scale=:Leaf, within=Subtree(), var=:leaf_carbon)),\n)\n\nAn energy-balance model can declare that it usually calls a stomatal conductance model manually:\n\ndep(::LeafEnergyBalanceModel) = (\n    stomatal_conductance = Call(process=:stomatal_conductance),\n)\n\nThese trait defaults are not absolute wiring. They are model-author defaults that make common cases work without repeating configuration, while scenario authors keep final authority through ModelSpec.\n\nCompiler order:\n\nread inputs_, outputs_, and dep;\ninfer simple same-object value dependencies when unambiguous;\napply dep(model) defaults for value inputs and model calls;\napply ModelSpec overrides last.\n\nThis order is part of the public contract. It keeps modeler defaults useful without making them final wiring. Missing or ambiguous inputs after this pass are errors, not incidental fallback behavior.","title":"Dependency Defaults From Traits"},{"category":"section","location":"dev/composite_model_design/#Value-Inputs","page":"Composite model/object design","text":"Use ModelSpec(...; inputs=...) when a model needs values before its run! method executes:\n\nModelSpec(LAIModel(ground_area); inputs=(:leaf_areas => Many(scale=:Leaf, within=SceneScope(), var=:leaf_area)))\n\nReusable plant allocation:\n\nModelSpec(AllocationModel(); on=Many(scale=:Plant), inputs=(:leaf_carbon => Many(scale=:Leaf, within=Subtree(), var=:leaf_carbon)))\n\nThe same declaration must compile to:\n\ndirect Ref/RefVector wiring when producer and consumer live in the same object graph and rate;\ntemporal stream reads when producer and consumer run at different rates;\nmaterialization when target status must be assigned before a model runs;\nsource-status lookup for graph-backed object selections.\n\nThe important user rule is:\n\nModelSpec(...; inputs=...) means \"give this model values\"; the runtime schedules or samples producers;\nthe receiving model never manually calls the producer because of an ModelSpec(...; inputs=...) declaration.","title":"Value Inputs"},{"category":"section","location":"dev/composite_model_design/#Carrier-And-Copy-Semantics","page":"Composite model/object design","text":"The compiler chooses the carrier, but the semantics must be documented and explainable:\n\nSituation Preferred carrier Copy behavior\nsame-rate scalar input shared Ref or local alias no copy when possible\nsame-rate Many(...) input RefVector or equivalent typed reference collection no copy for live values\ncross-rate input temporal stream sample value materialized for the consumer timestep\nIntegrate or Aggregate input temporal window reduction reduced value materialized\nmaterialized target status input compiler-generated assignment assigned before consumer run\nenvironment input cached EnvironmentBinding sample backend-defined value sample\n\nThis table is a required part of the design because performance, units, automatic differentiation, and error propagation depend on preserving user value types and avoiding hidden copies.\n\nPlantSimEngine should not force Float64 internally. Status values, parameters, environment values, and outputs must be allowed to use units, dual numbers, uncertainty wrappers, tracked arrays, or other numeric-like types. Compiled carriers should be parametric and type stable whenever the object set and value type are known at initialization.","title":"Carrier And Copy Semantics"},{"category":"section","location":"dev/composite_model_design/#Multirate-Inputs","page":"Composite model/object design","text":"Multirate must be supported by the same ModelSpec(...; inputs=...) declaration, not a separate mapping language. The public time language should remain Dates periods.\n\nExample:\n\nModelSpec(PlantAllocation(); on=Many(kind=:plant, scale=:Plant), inputs=(:leaf_assimilation => Many(\n        scale=:Leaf,\n        within=Subtree(),\n        var=:assimilation,\n        policy=Integrate(),\n        window=Day(1),\n    )), every=Day(1))\n\nPolicy precedence should stay explicit:\n\ninput-level policy in ModelSpec(...; inputs=...);\nproducer output_policy(model);\ndefault HoldLast().\n\nCross-rate links must go through temporal state even when they point to objects that could otherwise be reference-wired.\n\nSame-timestep feedback cycles are broken explicitly on the receiving input:\n\nModelSpec(CarbonState(); inputs=(PreviousTimeStep(:carbon_biomass) => One(\n            scale=:Plant,\n            application=:carbon_allocation,\n            var=:carbon_biomass,\n        ),))\n\nPreviousTimeStep(:x) removes the producer-to-consumer edge from the current timestep graph and reads the latest source sample at or before the previous model timestep. Before a source sample exists, the initialized consumer status value for x is used. This makes initialization part of the scenario contract instead of silently inventing a zero value.","title":"Multirate Inputs"},{"category":"section","location":"dev/composite_model_design/#Model-Calls","page":"Composite model/object design","text":"Use ModelSpec(...; calls=...) when a model must manually run selected models, typically inside an iterative solver. This is the required public API name and must be implemented as part of the unified composite-model/object redesign, not left as a later rename.\n\nModelSpec(SceneEB(); calls=(:leaf_energy => Many(\n        kind=:plant,\n        scale=:Leaf,\n        process=:energy_balance,\n    )), calls=(:soil => One(kind=:soil, application=:soil_water)))\n\nInside run!, the model model receives call handles and calls run_call!(call) during trial iterations, then run_call!(call; publish=true) for the accepted final solution. The default is deliberately publish=false: trial calls mutate target status for convergence checks but do not append temporal samples or write environment outputs.\n\nThe important user rule is:\n\nModelSpec(...; calls=...) means \"give this model callable model handles\";\nthe parent model owns the call stack and can iterate, reject, or accept trial calls;\ncall outputs are published only according to the call publication contract.\n\nDiagnostics.explain_calls(compiled) exposes this as publication_policy=:explicit_accept, with default_publish=false and accepted_publish=true.\n\nBinding and call explanations also report where each dependency declaration came from:\n\norigin=:inferred_same_object for compiler-inferred value dependencies;\norigin=:model_default for Input(...) or Call(...) declarations coming from dep(model);\norigin=:model_spec for scenario-level ModelSpec(...; inputs=...) or ModelSpec(...; calls=...), including declarations that override a model default.","title":"Model Calls"},{"category":"section","location":"dev/composite_model_design/#Multiplicity","page":"Composite model/object design","text":"Selection multiplicity is explicit:\n\nOne(...)\nMany(...)\nOptionalOne(...)\n\nOptionalOne(...) resolves to zero or one dependency. With zero matches, an input keeps its inputs_ default and a call returns an empty call_targets(...) collection. Explanations retain these unresolved optional bindings instead of hiding them.\n\nThe compiler validates that One(...) resolves to exactly one producer per consumer scope. Many(...) returns a vector-like value or target collection.","title":"Multiplicity"},{"category":"section","location":"dev/composite_model_design/#Address-Normalization","page":"Composite model/object design","text":"All source and target declarations normalize to an internal address:\n\nDiagnostics.ObjectAddress(\n    scope,\n    kind,\n    species,\n    scale,\n    name,\n    process,\n    application,\n    var,\n    relation,\n    policy,\n    window,\n    from_status,\n    after,\n    multiplicity,\n)\n\nOnly the compiler works with this normalized address. Users should not need to construct it manually. Diagnostics.object_address is the structured diagnostic view and preserves every normalized selector field, including temporal and status-routing fields.","title":"Address Normalization"},{"category":"section","location":"dev/composite_model_design/#Object-Lifecycle-And-Spatial-Contracts","page":"Composite model/object design","text":"Growth, pruning, organ creation, reparenting, and moving organs must all update the same compiled caches:\n\nobject selections used by on, inputs, and calls;\nRefVector or equivalent many-object carriers;\ntemporal stream ownership;\nwriter validation;\nenvironment bindings.\n\nThe public mutation API should make cache invalidation explicit and centralized:\n\nregister_object!(model, object; parent)\nremove_object!(model, object)\nreparent_object!(model, object, new_parent)\nmove_object!(model, object, geometry_or_position)\nAdvanced.refresh_bindings!(model)\n\nSpatial environment backends should depend on a small geometry contract, not on a particular plant representation:\n\nposition(object_or_status)\ngeometry(object_or_status)\nbounds(object_or_status)\n\nPackages can provide richer geometry, octrees, voxel grids, or layers, but PlantSimEngine should only require enough information to bind an object to an environment provider.","title":"Object Lifecycle And Spatial Contracts"},{"category":"section","location":"dev/composite_model_design/#Duplicate-Writers-And-Updates","page":"Composite model/object design","text":"Most variables should have one canonical writer per object and timestep. When a variable is intentionally updated by several models, the scenario should say so where the model applications are assembled:\n\nModelSpec(PruningModel(); on=Many(scale=:Leaf), updates=Updates(:leaf_biomass; after=:carbon_allocation))\n\nUpdates(...) should be rare and explicit. It is a scenario-level ordering rule, because a model author cannot predict every model that will later update the same variable.","title":"Duplicate Writers And Updates"},{"category":"section","location":"dev/composite_model_design/#Environment-And-Microclimate","page":"Composite model/object design","text":"Meteorology should remain automatic unless a model or scenario needs special behavior. Models declare environment variables:\n\nenvironment_inputs_(::LeafEnergyModel) = (\n    T=0.0,\n    Rh=0.0,\n    Wind=0.0,\n    Ri_PAR_f=0.0,\n    CO2=0.0,\n)\n\nThe runtime resolves those variables through the model environment service.\n\nDefault resolution:\n\nA global/table environment backend gives every object the current sampled row.\nA voxel, octree, layered, or grid backend samples the cell bound to the object.\nIf the object has no position, use the parent position.\nIf no spatial binding can be made, fall back to the global environment or error when the environment variable is required.\n\nUsers can override the binding contract:\n\nEnvironmentResolver(\n    bind=(model, object) -> containing_cell(model.microclimate, position(object)),\n)\n\nPlantSimEngine should define the protocol and caching hooks, not the voxel or octree implementation. Specialized packages should provide concrete spatial backends.\n\nThe environment backend protocol should be small and backend-oriented:\n\nhandle = EnvironmentAPI.bind_environment(backend, object, context, config)\nEnvironmentAPI.sample(backend, handle, variable, time)\nEnvironmentAPI.sample(backend, handle, trial_state, variable, time)\ncommit_environment!(backend, handle, accepted_state, time)\nEnvironmentAPI.update_index!(backend, changed_entities, removed_object_ids)\n\nenvironment_inputs_(model) declares what a model reads from the active environment provider, while environment_outputs_(model) declares what it may commit. Controllers commit accepted mutable microclimate state with commit_environment!(context, accepted_state) and run trial descendants with run_call!(context, name; environment=trial_state). Simple global meteorology remains the default provider.\n\nScenario-level environment source remapping belongs on Environment(...), for example:\n\nModelSpec(LeafGasExchange(); name=:gas_exchange, on=Many(scale=:Leaf), environment=Environment(provider=:global, sources=(CO2=:Ca,)))\n\nHere the model reads environment.CO2 because that is its declared generic contract, while the active environment backend samples the source variable :Ca. Environment binding refresh validates source availability when the backend can enumerate variables, and explanations report both required_inputs and source_inputs.\n\nGlobal tabular meteorology follows the model application's compiled ModelSpec(...; every=...). PlantMeteo samples the table with the reducer and window from environment_hint(...) when the model runs more slowly than the weather base step. An Environment(; sources=...) override replaces only the source variable; it does not discard the model-author reducer. The prepared weather sampler is compiled once, and one sampled row is reused by every object targeted by the same application at that timestep.\n\nSpatial or mutable backends retain control of their own temporal semantics. PlantSimEngine supplies the compiled object/cell binding and current simulation time; a specialized microclimate backend decides whether its local state is instantaneous, interpolated, or internally integrated.","title":"Environment And Microclimate"},{"category":"section","location":"dev/composite_model_design/#Cached-Environment-Bindings","page":"Composite model/object design","text":"Spatial lookup must not happen for every model call. At initialization and when objects are created, the runtime builds an environment binding cache:\n\nEnvironmentBinding(\n    object_id,\n    provider=:microclimate_grid,\n    cell_id,\n    variables=(:T, :Rh, :Wind, :Ri_PAR_f),\n)\n\nRuntime sampling is:\n\nobject -> cached binding -> environment cell -> current values\n\nInvalidation events:\n\nobject created;\nobject removed;\nobject moved;\ngeometry changed;\nenvironment grid rebuilt or refined;\nmodel environment requirements changed.\n\nGeometry APIs should provide ergonomic invalidation:\n\nmark_environment_binding_dirty!(model, object)\nupdate_geometry!(object, geometry; invalidate_environment=true)\n\nBefore each timestep, dirty bindings are refreshed in batch.","title":"Cached Environment Bindings"},{"category":"section","location":"dev/composite_model_design/#Compilation-Strategy","page":"Composite model/object design","text":"The compiler builds one global dependency graph over object addresses. The graph includes:\n\nvalue dependencies from ModelSpec(...; inputs=...);\ncallable dependencies from ModelSpec(...; calls=...);\nmodel update edges from Updates(...);\ntemporal policy edges;\nenvironment reads and writes;\nobject-scope selection caches.\n\nThe runtime representation is an implementation detail:\n\nsame-rate local links can stay as aliases;\ncross-rate links use temporal state;\nmany-object links use RefVector or node-value streams;\ncall links use immutable call plans with lifecycle-maintained target buffers;\nenvironment links use application-level plans and object-specific handles.","title":"Compilation Strategy"},{"category":"section","location":"dev/composite_model_design/#Immutable-plans-and-mutable-object-state","page":"Composite model/object design","text":"The application graph is immutable during a simulation. Growth changes which objects participate in that graph, not which applications, dependency declarations, cadence rules, or selector programs exist. The compiler keeps these two ownership layers separate:\n\nImmutable for the scenario Mutable at lifecycle barriers\napplication identity, model specification, and dense application slot application target object ids\ndependency edges and topological order value carriers and temporal source buffers\ncompiled input and hard-call selector programs hard-call target buffers\ncadence definitions and schedule entries schedule cursor and current execution groups\noutput-retention requirements and publication variables stream instances and request membership intervals\nenvironment backend/configuration, sampling rules, sampler, and prepared source object handles, geometry provenance, and spatial index entries\n\nCompiledScenarioPlan owns the immutable application, input, call, ordering, and cadence definitions. Object-level bindings delegate to these plans rather than copying their selector, policy, contract, or environment metadata. CompiledExecutionPlan uses dense application slots for root scheduling and keeps homogeneous target batches concrete.\n\nA LifecycleDelta journals additions, removals, reparenting, and movement once. At the safe barrier after the mutating application, the runtime consumes that shared delta to update affected application targets, input carriers, hard-call targets, temporal storage, output membership, environment handles, and execution groups. Applications still due later in the same timestep may run on new objects. The following unchanged timestep returns to the same precompiled schedule without selector resolution or graph reconstruction.\n\nThe final execution plan should group contiguous targets with the same concrete model, status, model-bundle, input-binding, and environment-binding types. Dynamic dispatch may occur once at the application/batch boundary, but not for every leaf in a homogeneous target set. Exceptional model overrides form separate concrete batches while preserving stable object order. Lifecycle or environment refreshes rebuild these batches before the next timestep.\n\nThe public explanation API must describe the normalized graph, not the internal carrier choice.\n\nUse run!(model; performance=true) only for coarse phase instrumentation. The structured Diagnostics.explain_runtime_performance(simulation) view groups the resulting counters into immutable-plan compilation, object-target instantiation, lifecycle-buffer updates, steady-state execution, output collection, and initial totals. Benchmark the normal runtime with performance=false, because enabled instrumentation deliberately calls time_ns() around compiler/runtime boundaries.","title":"Immutable plans and mutable object state"},{"category":"section","location":"dev/composite_model_design/#Agent-Facing-Requirements","page":"Composite model/object design","text":"The final design must be understandable by agents through structured explanation helpers:\n\nDiagnostics.explain_objects(model)\nDiagnostics.explain_instances(model)\nDiagnostics.explain_scopes(model)\nDiagnostics.explain_bindings(sim)\nDiagnostics.explain_calls(sim)\nDiagnostics.explain_environment_bindings(sim)\nDiagnostics.explain_schedule(sim)\nDiagnostics.explain_writers(sim)\nDiagnostics.explain_execution_plan(sim)\nDiagnostics.explain_output_retention(sim)\nDiagnostics.explain_runtime_performance(sim)\n\nThese helpers should return stable structured data, not only pretty text. A binding row should include at least:\n\nconsumer application id;\nconsumer object id;\nconsumer variable;\nsource selector;\nresolved producer application id or environment provider id;\nresolved producer object ids;\nprocess/name filters;\ntemporal policy and window;\ncarrier kind;\ncopy/reference semantics;\nreason the binding was chosen;\nwhether it came from inference, dep(model), or ModelSpec.\n\nExecution-plan rows should additionally expose the selected object ids, concrete model/status/carrier types, batch size, and whether the inner loop is homogeneous and specialized.\n\nOutput-retention rows should expose the retained application id, variable, retention reasons, compiled retention horizon, and current target count so agents can distinguish default retain-all behavior, requested output streams, and bounded temporal-dependency streams.\n\nErrors should report concrete object labels, scope selectors, process names, variables, and suggested fixes.","title":"Agent-Facing Requirements"},{"category":"section","location":"dev/composite_model_design/#API-Position","page":"Composite model/object design","text":"This is a breaking design. Model kernels use run!(model, status, environment, constants, context) while the scenario configuration surface uses CompositeModel, Object, ModelSpec, selectors, ModelSpec(...; inputs=...), ModelSpec(...; calls=...), ModelSpec(...; every=...), and Environment(...).","title":"API Position"},{"category":"section","location":"journeys/modelers/cross_object_values/#Implement-Cross-Object-Values","page":"Cross-object values","text":"New concept: scalar and vector-like inputs use the same one-step kernel contract. Object selection and topology remain scenario concerns.\n\nSee Build One Multiscale Plant for the simulation-user construction journey.\n\nThe plain Julia block below is an excerpt from a shipped example whose declarations and compositions are tested in test/test-toy_models.jl.","title":"Implement Cross-Object Values"},{"category":"section","location":"journeys/modelers/cross_object_values/#Model-4:-consume-one-scalar-from-another-object","page":"Cross-object values","text":"ToyDevelopmentModel already declares stress=Default(1.0). A scenario may replace that fallback with one live scalar from a soil object:\n\nusing Dates, PlantMeteo, PlantSimEngine, DataFrames\nusing PlantSimEngine.Examples\n\ncross_object = CompositeModel(\n    Object(:soil; scale=:Soil, status=Status(stress=0.4)),\n    Object(:leaf; scale=:Leaf, status=Status(TT=10.0));\n    applications=(\n        ModelSpec(\n            ToyDevelopmentModel(0.5);\n            name=:development,\n            on=One(scale=:Leaf),\n            inputs=(\n                :stress => One(\n                    scale=:Soil,\n                    within=SceneScope(),\n                    var=:stress,\n                    from_status=true,\n                ),\n            ),\n        ),\n    ),\n)\n\ncross_simulation = run!(cross_object)\n(\n    leaf=final_state(cross_simulation, :leaf),\n    binding=only(Diagnostics.explain_bindings(cross_object)),\n)\n\nThe model kernel still reads only status.stress. It does not search for soil, know object ids, or copy the scalar each step. The scenario's One selector resolves a shared Ref.","title":"Model 4: consume one scalar from another object"},{"category":"section","location":"journeys/modelers/cross_object_values/#Model-5:-consume-a-vector-like-multiscale-value","page":"Cross-object values","text":"ToyMaintenanceRespirationModel runs once per leaf and publishes Rm. ToyPlantRmModel declares one vector-like input and reduces it:\n\nPlantSimEngine.inputs_(::ToyPlantRmModel) = (\n    Rm_organs=Required(AbstractVector{<:Real}),\n)\nPlantSimEngine.outputs_(::ToyPlantRmModel) = (Rm=-Inf,)\n\nfunction PlantSimEngine.run!(\n    model::ToyPlantRmModel,\n    status,\n    environment,\n    constants,\n    context,\n)\n    status.Rm = sum(status.Rm_organs)\n    return nothing\nend\n\nThe scenario decides that Rm_organs means all descendant leaf outputs:\n\nrespiration = ToyMaintenanceRespirationModel(\n    2.0,\n    0.06,\n    25.0,\n    0.5,\n    0.02,\n)\n\nmultiscale = CompositeModel(\n    Object(:plant; scale=:Plant),\n    Object(\n        :leaf_1;\n        scale=:Leaf,\n        parent=:plant,\n        status=Status(carbon_biomass=10.0),\n    ),\n    Object(\n        :leaf_2;\n        scale=:Leaf,\n        parent=:plant,\n        status=Status(carbon_biomass=20.0),\n    );\n    applications=(\n        ModelSpec(\n            respiration;\n            name=:maintenance,\n            on=Many(scale=:Leaf),\n        ),\n        ModelSpec(\n            ToyPlantRmModel();\n            name=:plant_maintenance,\n            on=One(scale=:Plant),\n            inputs=(\n                :Rm_organs => Many(\n                    scale=:Leaf,\n                    within=Subtree(),\n                    application=:maintenance,\n                    var=:Rm,\n                ),\n            ),\n        ),\n    ),\n    environment=Atmosphere(\n        T=25.0,\n        Wind=1.0,\n        Rh=0.7,\n        duration=Hour(1),\n    ),\n)\n\nmultiscale_simulation = run!(multiscale)\n(\n    plant=final_state(multiscale_simulation, :plant),\n    leaves=final_state(multiscale_simulation, Many(scale=:Leaf)),\n)\n\nselect(\n    DataFrame(Diagnostics.explain_bindings(multiscale)),\n    :application_id,\n    :input,\n    :source_ids,\n    :carrier_kind,\n    :copy_semantics,\n)\n\nThe Many carrier is a live RefVector; the aggregation kernel operates on an AbstractVector and stays independent of object count and identity.","title":"Model 5: consume a vector-like multiscale value"},{"category":"section","location":"journeys/modelers/cross_object_values/#Model-author-recap","page":"Cross-object values","text":"You implemented: scalar or vector-compatible input schemas and ordinary one-step arithmetic.\nPlantSimEngine inferred: shared Ref and RefVector carriers plus producer-before-consumer order.\nThe scenario author keeps explicit: cross-object scope, multiplicity, source application, and variable remapping.\nNew API names: One, Many, SceneScope, Subtree, from_status, RefVector, and input_carrier.","title":"Model-author recap"},{"category":"section","location":"tutorials/growing_plant/part2_roots_water/#Adding-Roots-And-Water","page":"Part 2: roots and water","text":"Add root objects and gather absorption through a plant-local Many selector. Keep shared carbon and water stocks on the plant, while leaf and root state remains object-local. Environment precipitation is an environment input; root creation is an explicit register_object! operation with initialized status.\n\nWhen several plants share one soil object, select it explicitly with a model-wide One selector rather than relying on traversal order.\n\nKeep stocks at the scale that owns conservation. A root model may publish an absorption rate per root, while the plant model integrates all root rates and updates one plant water stock. A soil model owns soil water; plants read it through an explicit model-wide selector. This avoids copying one stock into every organ and makes duplicate writers visible.\n\nModelSpec(\n    PlantWaterModel();\n    inputs=(\n        :root_uptake => Many(\n            scale=:Root, within=Subtree(), application=:root_absorption,\n            var=:uptake, policy=Integrate(), window=Day(1),\n        ),\n        :soil_water => One(\n            scale=:Soil, within=SceneScope(), application=:soil_water,\n            var=:water,\n        ),\n    ),\n)\n\nPrecipitation, temperature, and radiation remain environment variables, not ordinary object outputs. Use Environment(sources=...) when provider column names differ from model-facing names. When growth creates a root, initialize all required root status values before register_object!; verify the next timestep's carrier with Diagnostics.input_value or Diagnostics.explain_bindings.","title":"Adding Roots And Water"},{"category":"section","location":"journeys/users/cadences/#Give-Models-Different-Cadences","page":"Different model cadences","text":"","title":"Give Models Different Cadences"},{"category":"section","location":"journeys/users/cadences/#New-concept:-application-clocks-and-temporal-input-policies","page":"Different model cadences","text":"Running a whole composite over many timesteps was introduced on the first journey. This page changes one thing: applications no longer all run at the environment base step.","title":"New concept: application clocks and temporal input policies"},{"category":"section","location":"journeys/users/cadences/#Hold-a-daily-state-for-an-hourly-model","page":"Different model cadences","text":"Reuse the thermal-time, LAI, and light chain. The environment advances hourly; thermal time and LAI run daily; light interception runs hourly. HoldLast makes each hourly light execution read the latest published daily LAI.\n\nusing PlantSimEngine, Dates, DataFrames\nusing PlantSimEngine.Examples\n\nhourly_forcing = [\n    (T=20.0, Ri_PAR_f=300.0, duration=Hour(1))\n    for _ in 1:25\n]\n\nmodel = CompositeModel(\n    Object(:plant; scale=:Plant, kind=:plant);\n    applications=(\n        ModelSpec(\n            ToyDegreeDaysCumulModel();\n            name=:degree_days,\n            on=One(scale=:Plant),\n            every=Day(1),\n        ),\n        ModelSpec(\n            ToyLAIModel();\n            name=:lai,\n            on=One(scale=:Plant),\n            every=Day(1),\n        ),\n        ModelSpec(\n            Beer(0.6);\n            name=:light,\n            on=One(scale=:Plant),\n            inputs=(\n                :LAI => One(\n                    within=Self(),\n                    application=:lai,\n                    var=:LAI,\n                    policy=HoldLast(),\n                    window=Day(1),\n                ),\n            ),\n            every=Hour(1),\n        ),\n    ),\n    environment=hourly_forcing,\n)\n\nsimulation = run!(model; steps=25, outputs=:all)\n\nThe schedule reports physical cadence in seconds and in base steps:\n\nselect(\n    DataFrame(Diagnostics.explain_schedule(model)),\n    :application_id,\n    :dt_seconds,\n    :dt_steps,\n)\n\nThe temporal binding is explicit even though the producer and consumer share an object:\n\nselect(\n    DataFrame(Diagnostics.explain_bindings(model)),\n    :application_id,\n    :input,\n    :policy,\n    :window,\n    :carrier_kind,\n)\n\nThe daily applications publish at steps 1 and 25; the hourly application publishes on all 25 steps:\n\nselect(\n    DataFrame(Diagnostics.explain_outputs(simulation)),\n    :application_id,\n    :variable,\n    :nsamples,\n)\n\nHoldLast is appropriate because LAI is a state: between daily updates, its latest value remains meaningful.","title":"Hold a daily state for an hourly model"},{"category":"section","location":"journeys/users/cadences/#Integrate-a-rate-into-an-amount","page":"Different model cadences","text":"Integrate has a different physical meaning. If a leaf publishes a constant rate in units per second, integrating 24 hourly samples produces a daily amount. The consumer below sums the independently integrated amounts from two leaves.\n\nPlantSimEngine.@process \"cadence_hourly_flux\" verbose = false\nPlantSimEngine.@process \"cadence_daily_amount\" verbose = false\n\nstruct CadenceHourlyFlux <: AbstractCadence_Hourly_FluxModel end\nstruct CadenceDailyAmount <: AbstractCadence_Daily_AmountModel end\n\nPlantSimEngine.inputs_(::CadenceHourlyFlux) = (rate=Required(Real),)\nPlantSimEngine.outputs_(::CadenceHourlyFlux) = (flux=0.0,)\nPlantSimEngine.run!(\n    ::CadenceHourlyFlux,\n    status,\n    environment,\n    constants,\n    context,\n) = (status.flux = status.rate)\n\nPlantSimEngine.inputs_(::CadenceDailyAmount) = (\n    leaf_amounts=Required(AbstractVector{<:Real}),\n)\nPlantSimEngine.outputs_(::CadenceDailyAmount) = (amount=0.0,)\nPlantSimEngine.run!(\n    ::CadenceDailyAmount,\n    status,\n    environment,\n    constants,\n    context,\n) = (status.amount = sum(status.leaf_amounts))\n\nflux_model = CompositeModel(\n    Object(:plant; scale=:Plant),\n    Object(\n        :leaf_1;\n        scale=:Leaf,\n        parent=:plant,\n        status=Status(rate=1.0),\n    ),\n    Object(\n        :leaf_2;\n        scale=:Leaf,\n        parent=:plant,\n        status=Status(rate=2.0),\n    );\n    applications=(\n        ModelSpec(\n            CadenceHourlyFlux();\n            name=:hourly_flux,\n            on=Many(scale=:Leaf),\n            every=Hour(1),\n        ),\n        ModelSpec(\n            CadenceDailyAmount();\n            name=:daily_amount,\n            on=One(scale=:Plant),\n            inputs=(\n                :leaf_amounts => Many(\n                    scale=:Leaf,\n                    within=Subtree(),\n                    application=:hourly_flux,\n                    var=:flux,\n                    policy=Integrate(),\n                    window=Day(1),\n                ),\n            ),\n            every=Day(1),\n        ),\n    ),\n    environment=[(duration=Hour(1),) for _ in 1:25],\n)\n\nflux_simulation = run!(flux_model; steps=25)\nfinal_state(flux_simulation, One(scale=:Plant)).amount\n\nThe result is (1 + 2) × 24 × 3600 = 259200 rate-seconds. Use Aggregate(reducer) instead when the desired quantity is a mean, minimum, maximum, or another reduction of observations rather than a time integral.\n\nThere is no same-step feedback cycle in either example, so PreviousTimeStep is not needed. It should be introduced only when a real scientific dependency intentionally reads the preceding step to break such a cycle.","title":"Integrate a rate into an amount"},{"category":"section","location":"journeys/users/cadences/#Page-recap","page":"Different model cadences","text":"You added: daily and hourly application clocks, HoldLast for a state, and then Integrate for a rate.\nPlantSimEngine inferred: the base-step ratios, publication schedule, and bounded temporal storage needed by the consumers.\nYou keep explicit: each application cadence, the physical meaning of its temporal policy, and the integration window.\nNew API names: every, HoldLast, Integrate, Aggregate, window, Diagnostics.explain_schedule, and Diagnostics.explain_outputs.","title":"Page recap"},{"category":"section","location":"guides/data/outputs_plotting/#Collecting-And-Plotting-Outputs","page":"Collecting and plotting outputs","text":"Run a model with outputs=:all or an explicit OutputRequest, then call collect_outputs(sim) for ordinary analysis. Rows identify application, object, variable, timestep/time, and value, so repeated processes cannot overwrite one another. Convert the rows to a DataFrame, filter by application/object/variable, group, and plot. Use final_state(sim) when only the latest values are needed.\n\nRuns default to outputs=:none. Use outputs=:all only when complete stream history is intentional; selected requests are the memory-safe choice for large composite models. Raw rows have the stable columns timestep, time, application_id, object_id, variable, and value. Requested/resampled rows additionally identify scale and process. A temporal request emits missing when its policy cannot produce a value for a scheduled output time. time is expressed in model base-step coordinates; application clock metadata is reported by Diagnostics.explain_schedule(simulation). Values retain their concrete types, so unit-bearing model outputs remain unit-bearing in collected rows.\n\nOutputRequest controls requested retention or resampling. Dependency streams may also be retained for runtime correctness. Use Diagnostics.explain_output_retention(sim) to see why each stream exists. Removed objects retain accepted historical rows.\n\nusing Dates\nusing DataFrames\nusing PlantSimEngine\n\nPlantSimEngine.@process \"docs_output_counter\" verbose = false\nstruct DocsOutputCounter <: AbstractDocs_Output_CounterModel end\nPlantSimEngine.inputs_(::DocsOutputCounter) = NamedTuple()\nPlantSimEngine.outputs_(::DocsOutputCounter) = (value=0,)\nPlantSimEngine.run!(::DocsOutputCounter, status, environment, constants, context) =\n    (status.value += 1)\n\nmodel = CompositeModel(DocsOutputCounter(); environment=(duration=Hour(1),))\nsimulation = run!(\n    model;\n    steps=3,\n    outputs=OutputRequest(\n        One(scale=:Scene),\n        :value;\n        name=:counter,\n        application=:docs_output_counter,\n    ),\n)\nrows = collect_outputs(simulation, :counter; sink=nothing)\ntable = DataFrame(rows)\n@assert table.value == [1, 2, 3]\ntable\n\nFor plotting, filter the table first and map time to the horizontal axis and value to the vertical axis. Group by application_id and object_id before drawing lines; grouping by variable alone can accidentally connect different objects. CairoMakie and other plotting packages consume the resulting columns without any PlantSimEngine-specific adapter.","title":"Collecting And Plotting Outputs"},{"category":"section","location":"step_by_step/detailed_first_example/#detailed-walkthrough-of-a-simple-simulation","page":"Detailed Walkthrough Of A Simple Simulation","text":"This page walks through a small composite-model/object simulation. It is written for readers who are still getting comfortable with Julia and PlantSimEngine.\n\nIf you only want examples to copy and modify, see Quick examples. For multi-object and multi-plant simulations, the same API scales up: add objects, select them with ModelSpec(...; on=...), connect values with ModelSpec(...; inputs=...), and use ModelSpec(...; calls=...) when a parent model must manually run child models.\n\nPages = [\"detailed_first_example.md\"]\nDepth = 3","title":"Detailed Walkthrough Of A Simple Simulation"},{"category":"section","location":"step_by_step/detailed_first_example/#Setting-Up-Your-Environment","page":"Detailed Walkthrough Of A Simple Simulation","text":"Every script needs a Julia environment with PlantSimEngine installed. Most examples also use companion packages such as PlantMeteo for weather data and DataFrames for tabular outputs. Installation details are in Installing PlantSimEngine.","title":"Setting Up Your Environment"},{"category":"section","location":"step_by_step/detailed_first_example/#The-Simulation-Pieces","page":"Detailed Walkthrough Of A Simple Simulation","text":"","title":"The Simulation Pieces"},{"category":"section","location":"step_by_step/detailed_first_example/#Processes-And-Models","page":"Detailed Walkthrough Of A Simple Simulation","text":"A process is something you want to simulate, such as light interception, photosynthesis, water flux, growth, yield, or energy balance.\n\nA model is one implementation of a process. In this page we use the example Beer model, which implements a Beer-Lambert light-interception equation. Its only parameter is the extinction coefficient k.\n\nfieldnames(Beer)\n\nThe model implementation declares the status variables it reads and writes:\n\ninputs(Beer(0.5))\n\noutputs(Beer(0.5))\n\nThese declarations are the modeler's contract. The composite-model/object layer decides where the model runs and where those values come from.","title":"Processes And Models"},{"category":"section","location":"step_by_step/detailed_first_example/#CompositeModel-Objects","page":"Detailed Walkthrough Of A Simple Simulation","text":"A CompositeModel contains simulated Objects. An object can represent a model, plant, axis, leaf, soil layer, sensor, voxel, or any other simulated entity.\n\nFor a first example, we use one object representing the whole model. The Beer model reads LAI, so we initialize that variable on the object status.\n\nmodel = CompositeModel(\n    Beer(0.5);\n    status=(LAI=2.0,),\n    environment=meteo_day,\n    timestep=Day(1),\n);\nnothing\n\nThe concise constructor creates one ordinary model object and one application for each supplied model. status initializes that object, timestep applies a common daily cadence, and environment supplies weather values such as radiation. Use explicit ModelSpec and selectors when applications need different policies or targets.","title":"CompositeModel Objects"},{"category":"section","location":"step_by_step/detailed_first_example/#Inspecting-The-Compiled-CompositeModel","page":"Detailed Walkthrough Of A Simple Simulation","text":"Before runtime, PlantSimEngine resolves selectors and builds a compiled model. This avoids resolving object selections inside the timestep loop.\n\nselect(\n    DataFrame(Diagnostics.explain_applications(model)),\n    :application_id,\n    :process,\n    :target_ids,\n)\n\nBeer has no model-to-model value input in this first model because LAI was initialized directly on the object status:\n\nDiagnostics.explain_bindings(model)\n\nThe schedule tells us when each application runs:\n\nselect(\n    DataFrame(Diagnostics.explain_schedule(model)),\n    :application_id,\n    :dt_seconds,\n    :root_scheduled,\n    :manual_call_only,\n)","title":"Inspecting The Compiled CompositeModel"},{"category":"section","location":"step_by_step/detailed_first_example/#Running-The-Simulation","page":"Detailed Walkthrough Of A Simple Simulation","text":"Run the model with run!:\n\nsim = run!(model; steps=3, outputs=:all)\nnothing\n\nFinal state is available independently of retained output history:\n\nscene_status = final_state(sim)\n(LAI=scene_status.LAI, aPPFD=scene_status.aPPFD)\n\nThe returned Simulation stores retained output streams:\n\nfirst(collect_outputs(sim; sink=nothing), 3)\n\nFor a table, use the default DataFrame sink:\n\nfirst(collect_outputs(sim), 3)","title":"Running The Simulation"},{"category":"section","location":"step_by_step/detailed_first_example/#Adding-A-Model-Coupling","page":"Detailed Walkthrough Of A Simple Simulation","text":"Now let a daily LAI model compute LAI before the light-interception model runs. ToyLAIModel reads cumulative thermal time TT_cu and writes LAI. Because Beer reads LAI, the compiler can infer the same-object binding.\n\ncoupled_scene = CompositeModel(\n    ToyDegreeDaysCumulModel(),\n    ToyLAIModel(),\n    Beer(0.5);\n    status=(TT_cu=0.0,),\n    environment=meteo_day,\n    timestep=Day(1),\n)\n\nselect(\n    DataFrame(Diagnostics.explain_bindings(coupled_scene)),\n    :application_id,\n    :input,\n    :source_application_ids,\n    :carrier_kind,\n    :copy_semantics,\n)\n\nThe LAI binding uses a live reference carrier, so the light-interception model sees the value written by the LAI model without copying it.\n\nRun the coupled model:\n\ncoupled_sim = run!(coupled_scene; steps=5, outputs=:all)\nfirst(collect_outputs(coupled_sim), 8)\n\nThe final object status contains the latest values from the coupled models:\n\ncoupled_status = final_state(coupled_sim)\n(TT_cu=coupled_status.TT_cu, LAI=coupled_status.LAI, aPPFD=coupled_status.aPPFD)","title":"Adding A Model Coupling"},{"category":"section","location":"step_by_step/detailed_first_example/#What-Needs-Initialization?","page":"Detailed Walkthrough Of A Simple Simulation","text":"Model inputs_(...) explicitly distinguishes Required(T) from Default(value). A required input needs user state or a producer binding; a defaulted input needs neither. In a coupled model, an upstream application can satisfy a required input.\n\nUse the compiler explanations to distinguish the two cases:\n\n:supplied means the object Status already provides the value;\n:producer_bound means another application supplies it;\n:defaulted means Default(value) initialized it;\n:required means it still has no source and compilation will fail.\n\nFor example, if we remove TT_cu from the model status, compilation fails because no model in this model computes it before ToyLAIModel reads it:\n\nbad_scene = CompositeModel(\n    ToyLAIModel();\n    environment=meteo_day,\n)\n\ntry\n    Diagnostics.explain_bindings(bad_scene)\ncatch err\n    first(sprint(showerror, err), 300)\nend","title":"What Needs Initialization?"},{"category":"section","location":"step_by_step/detailed_first_example/#Next-Steps","page":"Detailed Walkthrough Of A Simple Simulation","text":"Standard model coupling shows more coupling patterns.\nCompositeModel/Object Quickstart is the shortest copy-pasteable path for the new API.\nModel execution explains scheduling, temporal inputs, hard calls, output retention, and lifecycle refreshes.","title":"Next Steps"},{"category":"section","location":"journeys/users/mental_model/#A-Mental-Model-For-PlantSimEngine","page":"Mental model","text":"","title":"A Mental Model For PlantSimEngine"},{"category":"section","location":"journeys/users/mental_model/#New-concept:-composition","page":"Mental model","text":"PlantSimEngine is a framework for composing and running scientific models. It does not prescribe a plant architecture, and it is not itself a library of crop, tree, or organ equations. Model packages provide those equations; PlantSimEngine connects them to simulated entities, orders their execution, and manages time, environments, and retained results.\n\nSeven ideas are enough to read a PlantSimEngine simulation:\n\nIdea Meaning\nProcess A scientific responsibility, such as thermal time or light interception\nModel One reusable implementation of a process\nApplication One configured use of a model in a simulation\nObject A simulated entity with stable identity, such as a scene, plant, leaf, or soil layer\nStatus The current values owned by an object\nEnvironment Values sampled from outside object status, such as weather or microclimate\nSimulation A running timeline, including the live model, current step, and any retained output streams\n\nThe distinction between a model and an application is important. A model author writes an equation once. A simulation author can then apply it to one whole plant, every leaf, selected soil layers, or several named groups without putting an object loop inside the equation.\n\nObjects are equally general. Scales and parent/child links describe the topology chosen by the simulation author; PlantSimEngine does not require a particular hierarchy. A simple simulation can have one object. A detailed one can have scenes, several plants, organs, voxels, and shared resources.\n\nValues reach a model in two ways:\n\nstatus inputs come from the target object or from outputs of other model applications;\nenvironment inputs are sampled from the environment selected for that application and object.\n\nBefore running, PlantSimEngine compiles applications into concrete application/object targets, resolves value connections, and determines a valid execution order. During the timestep loop, model kernels work with their own parameters, the resolved status view, the sampled environment, constants, and a runtime context.","title":"New concept: composition"},{"category":"section","location":"journeys/users/mental_model/#Page-recap","page":"Mental model","text":"You added: no configuration yet—only the vocabulary used by every later journey.\nPlantSimEngine infers: application order and unambiguous value connections once a simulation is assembled.\nYou keep explicit: scientific equations, object topology, ambiguous cross-object connections, time policies, and requested output history.\nNew API names: none yet. The next page introduces CompositeModel, run!, Simulation, final_state, and collect_outputs.","title":"Page recap"},{"category":"section","location":"API/API_private/#API-internal-functions","page":"Internal API","text":"","title":"API - internal functions"},{"category":"section","location":"API/API_private/#Un-exported","page":"Internal API","text":"Private functions, types or constants from PlantSimEngine. These are not exported, so you need to use PlantSimEngine. to access them (e.g. PlantSimEngine.DataFormat). Most of them are developer code, but some may be useful for tinkerers, or to have greater control over some simulation parameters (future versions of this documentation might break those categories into separate pages for clarity).","title":"Un-exported"},{"category":"section","location":"API/API_private/#Index","page":"Internal API","text":"Pages = [\"API_private.md\"]","title":"Index"},{"category":"section","location":"API/API_private/#API-documentation","page":"Internal API","text":"","title":"API documentation"},{"category":"method","location":"API/API_private/#PlantMeteo.TimeStepTable-Union{Tuple{DataFrame}, Tuple{Status}, Tuple{DataFrame, Any}} where Status","page":"Internal API","text":"TimeStepTable{Status}(df::DataFrame)\n\nMethod to build a TimeStepTable (from PlantMeteo.jl)  from a DataFrame, but with each row being a Status.\n\nExamples\n\nusing PlantSimEngine, DataFrames\n\n# A TimeStepTable from a DataFrame:\ndf = DataFrame(\n    Tₗ=[25.0, 26.0],\n    aPPFD=[1000.0, 1200.0],\n    Cₛ=[400.0, 400.0],\n    Dₗ=[1.0, 1.2],\n)\nTimeStepTable{Status}(df)\n\n# A TimeStepTable can also be built directly from Status values:\nTimeStepTable(\n    [\n        Status(Tₗ=25.0, aPPFD=1000.0, Cₛ=400.0, Dₗ=1.0),\n        Status(Tₗ=26.0, aPPFD=1200.0, Cₛ=400.0, Dₗ=1.2),\n    ]\n)\n\n\n\n\n\n","title":"PlantMeteo.TimeStepTable"},{"category":"type","location":"API/API_private/#PlantSimEngine.AbstractEnvironmentBackend","page":"Internal API","text":"AbstractEnvironmentBackend\n\nBackend protocol for meteorology and mutable microclimate providers.\n\nPlantSimEngine defines the protocol, not the spatial indexing strategy. External packages subtype PlantSimEngine.EnvironmentAPI.AbstractEnvironmentBackend and extend the functions in PlantSimEngine.EnvironmentAPI, including sample, commit_environment!, update_index!, get_nsteps, and base_step_seconds.\n\n\n\n\n\n","title":"PlantSimEngine.AbstractEnvironmentBackend"},{"category":"type","location":"API/API_private/#PlantSimEngine.AbstractTargetedTopologyRuntime","page":"Internal API","text":"Append-only object lifecycle events pending the next runtime refresh barrier. The dirty-id sets are derived indices shared by every runtime consumer.\n\n\n\n\n\n","title":"PlantSimEngine.AbstractTargetedTopologyRuntime"},{"category":"type","location":"API/API_private/#PlantSimEngine.CompiledApplicationPlan","page":"Internal API","text":"Immutable scenario-level metadata for one model application. Runtime object membership is stored separately in CompiledModelApplication.\n\n\n\n\n\n","title":"PlantSimEngine.CompiledApplicationPlan"},{"category":"type","location":"API/API_private/#PlantSimEngine.CompiledApplicationSchedule","page":"Internal API","text":"Immutable cadence definitions for root-scheduled applications.\n\n\n\n\n\n","title":"PlantSimEngine.CompiledApplicationSchedule"},{"category":"type","location":"API/API_private/#PlantSimEngine.CompiledApplicationScheduleEntry","page":"Internal API","text":"One immutable root-scheduler rule in stable topological order.\n\n\n\n\n\n","title":"PlantSimEngine.CompiledApplicationScheduleEntry"},{"category":"type","location":"API/API_private/#PlantSimEngine.CompiledModelApplication","page":"Internal API","text":"Object-dependent runtime state for one compiled application.\n\n\n\n\n\n","title":"PlantSimEngine.CompiledModelApplication"},{"category":"type","location":"API/API_private/#PlantSimEngine.CompiledModelCallPlan","page":"Internal API","text":"Immutable authored hard-call declaration for one application.\n\n\n\n\n\n","title":"PlantSimEngine.CompiledModelCallPlan"},{"category":"type","location":"API/API_private/#PlantSimEngine.CompiledModelInputPlan","page":"Internal API","text":"Immutable authored input declaration for one application.\n\n\n\n\n\n","title":"PlantSimEngine.CompiledModelInputPlan"},{"category":"type","location":"API/API_private/#PlantSimEngine.CompiledModelOutputDestinationBinding","page":"Internal API","text":"Compiled columnar references for one execution object and output group.\n\n\n\n\n\n","title":"PlantSimEngine.CompiledModelOutputDestinationBinding"},{"category":"type","location":"API/API_private/#PlantSimEngine.CompiledModelOutputDestinationPlan","page":"Internal API","text":"Immutable authored cross-object output declaration for one application.\n\n\n\n\n\n","title":"PlantSimEngine.CompiledModelOutputDestinationPlan"},{"category":"type","location":"API/API_private/#PlantSimEngine.CompiledNamedScope","page":"Internal API","text":"A named topology scope resolved once while the scenario plan is compiled.\n\n\n\n\n\n","title":"PlantSimEngine.CompiledNamedScope"},{"category":"type","location":"API/API_private/#PlantSimEngine.CompiledScenarioPlan","page":"Internal API","text":"Immutable application, dependency-declaration, and timeline metadata shared by every lifecycle refresh of a compiled scenario.\n\n\n\n\n\n","title":"PlantSimEngine.CompiledScenarioPlan"},{"category":"type","location":"API/API_private/#PlantSimEngine.CompiledSelectorMatcher","page":"Internal API","text":"Normalized selector criteria used by lifecycle membership checks without reinterpreting the authored selector at every structural refresh.\n\n\n\n\n\n","title":"PlantSimEngine.CompiledSelectorMatcher"},{"category":"type","location":"API/API_private/#PlantSimEngine.CompiledWriterOwner","page":"Internal API","text":"One declared owner of a status variable on a destination object.\n\n\n\n\n\n","title":"PlantSimEngine.CompiledWriterOwner"},{"category":"type","location":"API/API_private/#PlantSimEngine.CompositeModelCompilationReport","page":"Internal API","text":"CompositeModelCompilationReport\n\nBest-effort result of compiling a CompositeModel for inspection. Unlike compile_composite_model, this representation preserves successful earlier phases when a later phase reports an invalid selector, binding, writer, or cycle.\n\n\n\n\n\n","title":"PlantSimEngine.CompositeModelCompilationReport"},{"category":"method","location":"API/API_private/#PlantSimEngine.DataFormat-Tuple{Type{<:AbstractDataFrame}}","page":"Internal API","text":"DataFormat(T::Type)\n\nReturns the data format of the type T. The data format is used to determine how to iterate over the data. The following data formats are supported:\n\nTableAlike: The data is a table-like object, e.g. a DataFrame or a TimeStepTable. The data is iterated over by rows using the Tables.jl interface.\nSingletonAlike: The data is a singleton-like object, e.g. a NamedTuple   or a TimeStepRow. The data is iterated over by columns.\n\nThe default implementation returns TableAlike for AbstractDataFrame, TimeStepTable, AbstractVector, and Dict; and SingletonAlike for Status, NamedTuple, and TimeStepRow.\n\nThe default implementation for Any throws an error. Users that want to use another input should define this trait for the new data format, e.g.:\n\nPlantSimEngine.DataFormat(::Type{<:MyType}) = TableAlike()\n\nExamples\n\njulia> using PlantSimEngine, PlantMeteo, DataFrames\n\njulia> PlantSimEngine.DataFormat(DataFrame)\nPlantSimEngine.TableAlike()\n\njulia> PlantSimEngine.DataFormat(TimeStepTable([Status(a = 1, b = 2, c = 3)]))\nPlantSimEngine.TableAlike()\n\njulia> PlantSimEngine.DataFormat([1, 2, 3])\nPlantSimEngine.TableAlike()\n\njulia> PlantSimEngine.DataFormat(Dict(:a => 1, :b => 2))\nPlantSimEngine.TableAlike()\n\njulia> PlantSimEngine.DataFormat(Status(a = 1, b = 2, c = 3))\nPlantSimEngine.SingletonAlike()\n\n\n\n\n\n","title":"PlantSimEngine.DataFormat"},{"category":"type","location":"API/API_private/#PlantSimEngine.EnvironmentContext","page":"Internal API","text":"EnvironmentContext(application, object_id, scale, process)\n\nImmutable model-target metadata passed to bind_environment. Backends return an opaque handle from that method and use the handle for all subsequent sampling and commits. Runtime status and geometry are intentionally absent: object-to-environment routing belongs to the compiled handle.\n\n\n\n\n\n","title":"PlantSimEngine.EnvironmentContext"},{"category":"type","location":"API/API_private/#PlantSimEngine.GlobalConstant","page":"Internal API","text":"GlobalConstant(source)\n\nEnvironment backend that preserves the current PlantSimEngine behavior: every model receives the same source object or source row at a given timestep.\n\n\n\n\n\n","title":"PlantSimEngine.GlobalConstant"},{"category":"type","location":"API/API_private/#PlantSimEngine.LifecycleMoveEvent","page":"Internal API","text":"One geometry mutation and every environment handle it can affect.\n\n\n\n\n\n","title":"PlantSimEngine.LifecycleMoveEvent"},{"category":"type","location":"API/API_private/#PlantSimEngine.LifecycleObjectSnapshot","page":"Internal API","text":"Labels and topology captured for one object at a lifecycle event.\n\n\n\n\n\n","title":"PlantSimEngine.LifecycleObjectSnapshot"},{"category":"type","location":"API/API_private/#PlantSimEngine.LifecycleReparentEvent","page":"Internal API","text":"One subtree reparenting recorded before runtime buffers refresh.\n\n\n\n\n\n","title":"PlantSimEngine.LifecycleReparentEvent"},{"category":"type","location":"API/API_private/#PlantSimEngine.ModelApplicationRef","page":"Internal API","text":"ModelApplicationRef(scope, application_id; instance=nothing)\nGlobalApplicationRef(application_id)\nTemplateApplicationRef(instance, application_id)\n\nIdentify the declaration that owns an editable model application. Global applications are owned directly by the CompositeModel; template applications are owned by the shared CompositeModelTemplate mounted by instance.\n\nCompiled mounted ids such as plant_a__leaf_area are deliberately not accepted as declaration identities. The graph DTO carries both the compiled id and this owner reference.\n\n\n\n\n\n","title":"PlantSimEngine.ModelApplicationRef"},{"category":"type","location":"API/API_private/#PlantSimEngine.ModelGraphDiagnostic","page":"Internal API","text":"ModelGraphDiagnostic\n\nStructured diagnostic emitted while compiling a model for visualization. A graph report keeps diagnostics attached to stable application, object, and variable identities so an editor can present actionable controls.\n\n\n\n\n\n","title":"PlantSimEngine.ModelGraphDiagnostic"},{"category":"type","location":"API/API_private/#PlantSimEngine.ModelGraphView","page":"Internal API","text":"ModelGraphView\n\nJSON-oriented, renderer-independent representation of a Composite Model/Object graph. Applications are the default visual unit; executions optionally expands them into concrete (application, object) targets.\n\n\n\n\n\n","title":"PlantSimEngine.ModelGraphView"},{"category":"type","location":"API/API_private/#PlantSimEngine.NoCompiledDistributedOutputPlans","page":"Internal API","text":"Zero-cost marker used when a scenario has no distributed outputs.\n\n\n\n\n\n","title":"PlantSimEngine.NoCompiledDistributedOutputPlans"},{"category":"type","location":"API/API_private/#PlantSimEngine.NoCompiledDistributedOutputs","page":"Internal API","text":"Zero-cost marker used when a compiled scene has no distributed outputs.\n\n\n\n\n\n","title":"PlantSimEngine.NoCompiledDistributedOutputs"},{"category":"type","location":"API/API_private/#PlantSimEngine.ObjectAddress","page":"Internal API","text":"ObjectAddress(selector)\n\nReturn the normalized, structured diagnostic address for an object selector. The address preserves scope, object labels, producer/callee routing, temporal policy/window, live-status ordering, and multiplicity.\n\n\n\n\n\n","title":"PlantSimEngine.ObjectAddress"},{"category":"type","location":"API/API_private/#PlantSimEngine.OutputAssignmentCache","page":"Internal API","text":"Mutable row-mapping cache owned by one simulation execution target.\n\n\n\n\n\n","title":"PlantSimEngine.OutputAssignmentCache"},{"category":"type","location":"API/API_private/#PlantSimEngine.RefVector","page":"Internal API","text":"RefVector(field::Symbol, sts...)\nRefVector(field::Symbol, sts::Vector{<:Status})\nRefVector(v::Vector{Base.RefValue{T}})\n\nA vector of references to a field of a vector of structs. This is used to efficiently pass the values between scales.\n\nArguments\n\nfield: the field of the struct to reference\nsts...: the structs to reference\nsts::Vector{<:Status}: a vector of structs to reference\n\nExamples\n\njulia> using PlantSimEngine\n\nLet's take two Status structs:\n\njulia> status1 = Status(a = 1.0, b = 2.0, c = 3.0);\n\njulia> status2 = Status(a = 2.0, b = 3.0, c = 4.0);\n\nWe can make a RefVector of the field a of the structs st1 and st2:\n\njulia> rv = PlantSimEngine.RefVector(:a, status1, status2)\n2-element PlantSimEngine.RefVector{Float64}:\n 1.0\n 2.0\n\nWhich is equivalent to:\n\njulia> rv = PlantSimEngine.RefVector(:a, [status1, status2])\n2-element PlantSimEngine.RefVector{Float64}:\n 1.0\n 2.0\n\nWe can access the values of the RefVector:\n\njulia> rv[1]\n1.0\n\nUpdating the value in the RefVector will update the value in the original struct:\n\njulia> rv[1] = 10.0\n10.0\n\njulia> status1.a\n10.0\n\nWe can also make a RefVector from a vector of references:\n\njulia> vec = [Ref(1.0), Ref(2.0), Ref(3.0)]\n3-element Vector{Base.RefValue{Float64}}:\n Base.RefValue{Float64}(1.0)\n Base.RefValue{Float64}(2.0)\n Base.RefValue{Float64}(3.0)\n\njulia> rv = PlantSimEngine.RefVector(vec)\n3-element PlantSimEngine.RefVector{Float64}:\n 1.0\n 2.0\n 3.0\n\njulia> rv[1]\n1.0\n\n\n\n\n\n","title":"PlantSimEngine.RefVector"},{"category":"type","location":"API/API_private/#PlantSimEngine.ResolvedModelOutputDestination","page":"Internal API","text":"Resolved destination membership before status references are constructed.\n\n\n\n\n\n","title":"PlantSimEngine.ResolvedModelOutputDestination"},{"category":"type","location":"API/API_private/#PlantSimEngine.RuntimeDistributedOutputStream","page":"Internal API","text":"Columnar retained streams for one distributed output variable.\n\n\n\n\n\n","title":"PlantSimEngine.RuntimeDistributedOutputStream"},{"category":"type","location":"API/API_private/#PlantSimEngine.RuntimePerformanceCounters","page":"Internal API","text":"RuntimePerformanceCounters\n\nOpt-in coarse runtime instrumentation used by the performance regression suite. Pass performance=true to run!, then inspect a copy of the recorded counters with runtime_performance.\n\nThe disabled path stores nothing in the simulation and does not call time_ns(). Counters deliberately cover compiler/runtime boundaries rather than individual scientific kernels so instrumentation does not change kernel dispatch or allocation behavior.\n\n\n\n\n\n","title":"PlantSimEngine.RuntimePerformanceCounters"},{"category":"type","location":"API/API_private/#PlantSimEngine.RuntimeTemporalInput","page":"Internal API","text":"RuntimeTemporalInput\n\nPer-simulation temporal input state compiled into an execution target. It shares direct references to the producer streams with every compatible consumer, avoiding a global stream-dictionary lookup in the per-target loop.\n\n\n\n\n\n","title":"PlantSimEngine.RuntimeTemporalInput"},{"category":"type","location":"API/API_private/#PlantSimEngine.SelectorCandidateIndex","page":"Internal API","text":"Reverse candidate index for compiled selector matchers.\n\n\n\n\n\n","title":"PlantSimEngine.SelectorCandidateIndex"},{"category":"type","location":"API/API_private/#PlantSimEngine.TemporalDependencyBuffer","page":"Internal API","text":"TemporalDependencyBuffer{T} <: AbstractVector{Tuple{Float64,T}}\n\nTyped, bounded storage for an output retained only to satisfy temporal model dependencies. Requested and outputs=:all streams continue to use append-only vectors. The logical indexing order is oldest to newest, independently of the physical circular-buffer layout.\n\n\n\n\n\n","title":"PlantSimEngine.TemporalDependencyBuffer"},{"category":"type","location":"API/API_private/#PlantSimEngine.TimelineContext","page":"Internal API","text":"Internal timing context for one model run.\n\n\n\n\n\n","title":"PlantSimEngine.TimelineContext"},{"category":"method","location":"API/API_private/#PlantSimEngine.EF-Tuple{Any, Any}","page":"Internal API","text":"EF(obs,sim)\n\nReturns the Efficiency Factor between observations obs and simulations sim using NSE (Nash-Sutcliffe efficiency) model. More information can be found at https://en.wikipedia.org/wiki/Nash%E2%80%93Sutcliffemodelefficiency_coefficient.\n\nThe closer to 1 the better.\n\nExamples\n\nusing PlantSimEngine.Evaluation\n\nobs = [1.0, 2.0, 3.0]\nsim = [1.1, 2.1, 3.1]\n\nEF(obs, sim)\n\n\n\n\n\n","title":"PlantSimEngine.EF"},{"category":"method","location":"API/API_private/#PlantSimEngine.NRMSE-Tuple{Any, Any}","page":"Internal API","text":"NRMSE(obs,sim)\n\nReturns the Normalized Root Mean Squared Error between observations obs and simulations sim. Normalization is performed using division by observations range (max-min).\n\nExamples\n\nusing PlantSimEngine.Evaluation\n\nobs = [1.0, 2.0, 3.0]\nsim = [1.1, 2.1, 3.1]\n\nNRMSE(obs, sim)\n\n\n\n\n\n","title":"PlantSimEngine.NRMSE"},{"category":"method","location":"API/API_private/#PlantSimEngine.RMSE-Tuple{Any, Any}","page":"Internal API","text":"RMSE(obs,sim)\n\nReturns the Root Mean Squared Error between observations obs and simulations sim.\n\nThe closer to 0 the better.\n\nExamples\n\nusing PlantSimEngine.Evaluation\n\nobs = [1.0, 2.0, 3.0]\nsim = [1.1, 2.1, 3.1]\n\nRMSE(obs, sim)\n\n\n\n\n\n","title":"PlantSimEngine.RMSE"},{"category":"method","location":"API/API_private/#PlantSimEngine._call_model_target_count_error-Tuple{Any, Any}","page":"Internal API","text":"call_model(context::RunContext, name::Symbol)\n\nReturn the concrete model for a declared hard call that currently resolves to exactly one execution target. This is the allocation-free singular access path for a controller that needs to dispatch on or inspect its fixed dependency model before executing it.\n\nUse call_targets when status access, selective execution, or more than one target is required. The returned model is the model stored in the compiled execution target and remains valid until a lifecycle refresh barrier.\n\n\n\n\n\n","title":"PlantSimEngine._call_model_target_count_error"},{"category":"function","location":"API/API_private/#PlantSimEngine._model_graph_deepcopy","page":"Internal API","text":"apply_model_graph_edit(model, edit; preserve=())\n\nApply one declarative graph edit transactionally. The input model is not modified; a deep-copied, cache-invalidated CompositeModel is returned. Configuration that is temporarily incomplete or cyclic is retained so the editor can display and repair it. Structural edit errors leave the original model unchanged. Values listed in preserve retain their identity inside the copy; editor sessions use this for server-side template and environment catalogs.\n\n\n\n\n\n","title":"PlantSimEngine._model_graph_deepcopy"},{"category":"method","location":"API/API_private/#PlantSimEngine._run_call!-Tuple{CallTarget, Bool, Any, Any}","page":"Internal API","text":"run_call!(target::CallTarget; publish=false, sampled_environment)\n\nRun one manually selected model call. By default, the call mutates its target status without publishing outputs or environment updates, which is suitable for trial iterations. Pass publish=true once for the accepted state. When sampled_environment is provided, it is forwarded directly to this target instead of using its compiled environment binding. This is the fine-grained escape hatch for one selected target; execute provider-aware trial states for all targets with run_call!(context, name; environment=state).\n\nThe method returns the same CallTarget.\n\nPublication permission is inherited through the call stack. A descendant cannot publish outputs or environment writes while any ancestor is running as a trial.\n\nFor fine-grained control, obtain a collection with call_targets, then select or iterate its targets:\n\ntargets = call_targets(context, :leaf_energy)\nfor (target, target_environment) in zip(targets, environments_by_leaf)\n    run_call!(\n        target;\n        sampled_environment=target_environment,\n        publish=false,\n    )\nend\n\naccepted = accepted_environment(model, status)\ncommit_environment!(context, accepted)\nfor target in targets\n    run_call!(target; publish=true)\nend\n\n\n\n\n\n","title":"PlantSimEngine._run_call!"},{"category":"method","location":"API/API_private/#PlantSimEngine.available_models-Tuple{}","page":"Internal API","text":"available_models()\navailable_models(process::Symbol)\navailable_models(process_type::Type{<:AbstractModel})\n\nReturn concrete model implementation types visible in the current Julia session.\n\n\n\n\n\n","title":"PlantSimEngine.available_models"},{"category":"method","location":"API/API_private/#PlantSimEngine.available_processes-Tuple{}","page":"Internal API","text":"available_processes()\n\nReturn process abstract model types visible in the current Julia session. Loading another package with using PackageName makes its process and model types discoverable without a separate registry.\n\n\n\n\n\n","title":"PlantSimEngine.available_processes"},{"category":"method","location":"API/API_private/#PlantSimEngine.base_step_seconds-Tuple{PlantSimEngine.AbstractEnvironmentBackend}","page":"Internal API","text":"base_step_seconds(backend)\n\nReturn the duration of one simulation base step in seconds.\n\n\n\n\n\n","title":"PlantSimEngine.base_step_seconds"},{"category":"function","location":"API/API_private/#PlantSimEngine.bind_environment","page":"Internal API","text":"bind_environment(backend, object, context, config=nothing)\n\nCompile and return an opaque backend-specific handle for one model target. context contains immutable application/object metadata and config is the payload declared with Environment(...). Spatial backends should resolve the object's geometry here and store the resulting cell, voxel, layer, provider, or commit sink in a concrete handle. Runtime sampling and commits receive that handle directly.\n\n\n\n\n\n","title":"PlantSimEngine.bind_environment"},{"category":"method","location":"API/API_private/#PlantSimEngine.compile_composite_model-Tuple{CompositeModel}","page":"Internal API","text":"compile_composite_model(model[, applications...])\n\nCompile model applications, selectors, value bindings, hard calls, writer ordering, and schedules into the single Composite Model/Object runtime representation. Most callers can use run! directly, which compiles as needed.\n\n\n\n\n\n","title":"PlantSimEngine.compile_composite_model"},{"category":"method","location":"API/API_private/#PlantSimEngine.compile_model_graph-Tuple{CompositeModel}","page":"Internal API","text":"compile_model_graph(model; level=:applications, strict=false,\n                    templates=NamedTuple(), environments=NamedTuple())\ncompile_model_graph(compiled::CompiledCompositeModel; level=:applications,\n                    templates=NamedTuple(), environments=NamedTuple())\n\nBuild a renderer-independent graph view from a Composite model or an existing compiled model. Named template and environment catalogs add schema-v2 descriptors without serializing runtime environment values.\n\n\n\n\n\n","title":"PlantSimEngine.compile_model_graph"},{"category":"method","location":"API/API_private/#PlantSimEngine.compile_model_report-Tuple{CompositeModel}","page":"Internal API","text":"compile_model_report(model; strict=false)\n\nCompile a Composite model for visualization while retaining partial graph information and structured diagnostics. With strict=true, call the simulation compiler and propagate its errors unchanged.\n\n\n\n\n\n","title":"PlantSimEngine.compile_model_report"},{"category":"method","location":"API/API_private/#PlantSimEngine.diff_vars-Tuple{Any, Any}","page":"Internal API","text":"diff_vars(x, y)\n\nReturns the names of variables that have different values in x and y.\n\n\n\n\n\n","title":"PlantSimEngine.diff_vars"},{"category":"method","location":"API/API_private/#PlantSimEngine.dr-Tuple{Any, Any}","page":"Internal API","text":"dr(obs,sim)\n\nReturns the Willmott’s refined index of agreement dᵣ. Willmot et al. 2011. A refined index of model performance. https://rmets.onlinelibrary.wiley.com/doi/10.1002/joc.2419\n\nThe closer to 1 the better.\n\nExamples\n\nusing PlantSimEngine.Evaluation\n\nobs = [1.0, 2.0, 3.0]\nsim = [1.1, 2.1, 3.1]\n\ndr(obs, sim)\n\n\n\n\n\n","title":"PlantSimEngine.dr"},{"category":"method","location":"API/API_private/#PlantSimEngine.edit_graph-Tuple","page":"Internal API","text":"edit_graph([model]; kwargs...)\n\nStart the HTTP-backed interactive CompositeModel editor. Call edit_graph() to begin with an empty CompositeModel. The implementation is provided by the HTTP package extension.\n\n\n\n\n\n","title":"PlantSimEngine.edit_graph"},{"category":"method","location":"API/API_private/#PlantSimEngine.environment_backend-Tuple{PlantSimEngine.AbstractEnvironmentBackend}","page":"Internal API","text":"environment_backend(environment_or_backend)\n\nReturn an environment backend. Plain environment data is wrapped in GlobalConstant; existing environment backends are returned unchanged.\n\n\n\n\n\n","title":"PlantSimEngine.environment_backend"},{"category":"method","location":"API/API_private/#PlantSimEngine.environment_variables-Tuple{PlantSimEngine.AbstractEnvironmentBackend}","page":"Internal API","text":"environment_variables(backend)\n\nReturn a set of variable names that the backend can provide, or nothing when the backend cannot enumerate them cheaply.\n\n\n\n\n\n","title":"PlantSimEngine.environment_variables"},{"category":"method","location":"API/API_private/#PlantSimEngine.explain_environment-Tuple{Any}","page":"Internal API","text":"explain_environment(simulation)\n\nReturn a compact description of the environment backend used by a model simulation.\n\n\n\n\n\n","title":"PlantSimEngine.explain_environment"},{"category":"method","location":"API/API_private/#PlantSimEngine.explain_initialization-Tuple{CompositeModel}","page":"Internal API","text":"explain_initialization(model::CompositeModel)\n\nReturn structured rows describing how every application variable is initialized. disposition is one of:\n\n:supplied: present on the object's status before compilation;\n:generated: created from a model output declaration;\n:producer_bound: connected through an explicit or inferred inputs binding;\n:environment_bound: provided by the selected environment backend;\n:unresolved: still requires user or scenario configuration.\n\nUnlike compile_composite_model, this report does not fail solely because a required status or environment value is unresolved. Selector, writer, call, and other invalid configuration errors remain errors.\n\n\n\n\n\n","title":"PlantSimEngine.explain_initialization"},{"category":"method","location":"API/API_private/#PlantSimEngine.explain_output_bindings-Tuple{PlantSimEngine.CompiledCompositeModel}","page":"Internal API","text":"Diagnostics.explain_output_bindings(model_or_compiled)\n\nReturn one structured row per compiled cross-object output destination. Rows separate the application execution object from current destination object IDs and report declared variables, carrier types, coverage, and lifecycle generation.\n\n\n\n\n\n","title":"PlantSimEngine.explain_output_bindings"},{"category":"method","location":"API/API_private/#PlantSimEngine.explain_runtime_performance-Tuple{Simulation}","page":"Internal API","text":"explain_runtime_performance(simulation)\n\nGroup the opt-in counters produced by run!(...; performance=true) into immutable plan compilation, object-target instantiation, lifecycle buffer updates, steady-state execution, output collection, and initial-total rows. Each row reports either or both of count and elapsed_seconds. The function returns an empty vector when performance instrumentation was disabled.\n\n\n\n\n\n","title":"PlantSimEngine.explain_runtime_performance"},{"category":"function","location":"API/API_private/#PlantSimEngine.fit","page":"Internal API","text":"fit()\n\nOptimize the parameters of a model using measurements and (potentially) initialisation values. \n\nModellers should implement a method to fit for their model, with the following design pattern:\n\nThe call to the function should take the model type as the first argument (T::Type{<:AbstractModel}),  the data as the second argument (as a Table.jl compatible type, such as DataFrame), and the  parameters initializations as keyword arguments (with default values when necessary).\n\nFor example, the method for fitting the Beer model from the example script (see examples/Beer.jl) uses this mathematical identity after validating each observation:\n\nJ_to_umol = PlantMeteo.Constants().J_to_umol\nincident_ppfd = J_to_umol .* df.Ri_PAR_f\nf_abs = df.aPPFD ./ incident_ppfd\nk = Statistics.mean(-log1p.(-f_abs) ./ df.LAI)\n\nThis is only the inversion, not a complete implementation to copy. The shipped Beer method also rejects empty data and invalid LAI, incident flux, and absorbed fractions with row-specific errors.\n\nHere, Ri_PAR_f is incident PAR in W m[ground]⁻², aPPFD is the PAR absorbed by the canopy in μmol[PAR] m[ground]⁻² s⁻¹, and LAI is in m[leaf]² m[ground]⁻². A mean leaf-area-basis PPFD is a different quantity and must not be passed to this fit.\n\nThe function should return the optimized parameters as a NamedTuple of the form (parameter_name=parameter_value,).\n\nHere is an example usage with the Beer model, where we fit the k parameter from \"measurements\" of aPPFD, LAI  and Ri_PAR_f. \n\n# Including example processes and models:\nusing PlantSimEngine.Examples;\nusing PlantSimEngine.Evaluation;\n\nmeteo = Atmosphere(\n    T=20.0,\n    Wind=1.0,\n    P=101.3,\n    Rh=0.65,\n    Ri_PAR_f=300.0,\n    duration=Hour(1),\n)\nmodel = CompositeModel(\n    Beer(0.6);\n    status=(LAI=2.0,),\n    id=:plant,\n    scale=:Plant,\n    environment=meteo,\n)\nsimulation = run!(model)\nplant = final_state(simulation, One(scale=:Plant))\ndata = DataFrame(\n    aPPFD=[plant.aPPFD],\n    LAI=[plant.LAI],\n    Ri_PAR_f=[meteo.Ri_PAR_f[1]],\n)\nEvaluation.fit(Beer, data)\n\nThis is a synthetic round trip: it simulates canopy-absorbed aPPFD with k=0.6, then recovers the same value from the ground-area-basis fluxes.\n\n\n\n\n\n","title":"PlantSimEngine.fit"},{"category":"method","location":"API/API_private/#PlantSimEngine.model_constructor_descriptor-Union{Tuple{Type{T}}, Tuple{T}} where T<:AbstractModel","page":"Internal API","text":"model_constructor_descriptor(::Type{<:AbstractModel})\n\nReturn constructor metadata inferred from struct fields and an optional zero-argument constructor. Fields that share a type parameter share a type choice in the editor.\n\n\n\n\n\n","title":"PlantSimEngine.model_constructor_descriptor"},{"category":"method","location":"API/API_private/#PlantSimEngine.model_descriptor-Union{Tuple{Type{T}}, Tuple{T}} where T<:AbstractModel","page":"Internal API","text":"model_descriptor(::Type{<:AbstractModel})\n\nReturn JSON-compatible discovery metadata for a model implementation type. Input and output metadata is inferred best-effort from a zero-argument or dummy instance when the type can be constructed safely.\n\n\n\n\n\n","title":"PlantSimEngine.model_descriptor"},{"category":"method","location":"API/API_private/#PlantSimEngine.object_address-Tuple{PlantSimEngine.AbstractObjectMultiplicity}","page":"Internal API","text":"object_address(selector)\n\nReturn an ObjectAddress containing every normalized selector field.\n\n\n\n\n\n","title":"PlantSimEngine.object_address"},{"category":"method","location":"API/API_private/#PlantSimEngine.runtime_performance-Tuple{Simulation}","page":"Internal API","text":"runtime_performance(simulation)\n\nReturn a stable snapshot of opt-in runtime performance counters, or nothing when the simulation was not started with performance=true. Elapsed values are reported in seconds while the internal counters retain nanosecond resolution.\n\n\n\n\n\n","title":"PlantSimEngine.runtime_performance"},{"category":"method","location":"API/API_private/#PlantSimEngine.sample-Tuple{PlantSimEngine.AbstractEnvironmentBackend, Any, Symbol, Any}","page":"Internal API","text":"sample(backend, handle, variable, time)\nsample(backend, handle, state, variable, time)\n\nSample one environmental variable through an opaque compiled backend handle. The four-argument method reads the backend's committed state. The five-argument method reads a transient backend-specific state supplied through run_call!(context, name; environment=state).\n\n\n\n\n\n","title":"PlantSimEngine.sample"},{"category":"method","location":"API/API_private/#PlantSimEngine.sample_environment-Tuple{PlantSimEngine.AbstractEnvironmentBackend, Any, Any, Any}","page":"Internal API","text":"sample_environment(backend, handle, time, variables)\nsample_environment(backend, handle, state, time, variables)\n\nSample a model-facing source row through a compiled backend handle. GlobalConstant returns the original row; other backends return a NamedTuple assembled from sample calls. The overload containing state preserves the same handle while sampling a transient environment.\n\n\n\n\n\n","title":"PlantSimEngine.sample_environment"},{"category":"method","location":"API/API_private/#PlantSimEngine.set_variables_at_timestep!-Tuple{Status, Status, Any, Any}","page":"Internal API","text":"set_variables_at_timestep!(status_timestep::Status, user_status::Status, variables_to_update, timestep)\n\nUpdate status_timestep to the current values at the timestep for all variables_to_update in the status provided by the user (user_status). The variables to update are given in variables_to_update, which is a vector of symbols.\n\nstatus_timestep is a status representing a single time-step. user_status is the status provided that gives values for variables that are not computed by any model. It may give constant values or vectors of values, in which case the timestep is used to select the value to use for the current time step.\n\n\n\n\n\n","title":"PlantSimEngine.set_variables_at_timestep!"},{"category":"method","location":"API/API_private/#PlantSimEngine.update_index!-Tuple{PlantSimEngine.AbstractEnvironmentBackend, Any, Any}","page":"Internal API","text":"update_index!(backend, changed_entities, removed_object_ids)\n\nUpdate the backend spatial/entity index after topology or geometry changes. changed_entities contains only new or changed objects and removed_object_ids contains stable ObjectIds that no longer exist. The initial compilation supplies every entity and an empty removal vector.\n\n\n\n\n\n","title":"PlantSimEngine.update_index!"},{"category":"method","location":"API/API_private/#PlantSimEngine.variable_contracts_-Tuple{AbstractModel}","page":"Internal API","text":"variable_contracts_(model::AbstractModel)\n\nTrait declaring the scientific contracts of status and environment variables used by model. Return a named tuple whose keys are variables declared by inputs_, outputs_, environment_inputs_, or environment_outputs_, and whose values are VariableContracts. A model that produces values only through distributed output groups may also declare those names; each compiled ModelSpec must then include them in outputs_to.\n\nThe default is empty for incremental adoption. Once either side of a compiled model-to-model input binding declares a contract, the other side must declare the same complete contract. This prevents a contracted variable from silently falling back to name-only coupling.\n\n\n\n\n\n","title":"PlantSimEngine.variable_contracts_"},{"category":"method","location":"API/API_private/#PlantSimEngine.variables_typed-Tuple{T} where T<:AbstractModel","page":"Internal API","text":"variables_typed(model)\nvariables_typed(model, models...)\n\nReturns a named tuple with the name and the types of the variables needed by a model, or a union of those for several models.\n\nExamples\n\nusing PlantSimEngine;\n\n# Load the dummy models given as example in the package:\nusing PlantSimEngine.Examples;\n\nPlantSimEngine.variables_typed(Process1Model(1.0))\n(var1 = Float64, var2 = Float64, var3 = Float64)\n\nPlantSimEngine.variables_typed(Process1Model(1.0), Process2Model())\n\n# output\n(var4 = Float64, var5 = Float64, var1 = Float64, var2 = Float64, var3 = Float64)\n\nSee also\n\ninputs, outputs and variables\n\n\n\n\n\n","title":"PlantSimEngine.variables_typed"},{"category":"method","location":"API/API_private/#PlantSimEngine.write_model_graph_view-Tuple{AbstractString, Any}","page":"Internal API","text":"write_model_graph_view(path, scene_or_view; level=:applications,\n                       strict=false, renderer=:react)\n\nWrite a self-contained static Model graph viewer. The default renderer uses the bundled frontend when available and otherwise falls back to the standalone viewer.\n\n\n\n\n\n","title":"PlantSimEngine.write_model_graph_view"},{"category":"section","location":"working_with_data/fitting/#Parameter-Fitting","page":"Parameter fitting","text":"PlantSimEngine.Evaluation.fit is the shared interface for model-specific calibration. Model packages implement a method whose first argument is the model type and whose second argument is Tables.jl-compatible observations.\n\nThe mathematical core of the Beer fit is:\n\nJ_to_umol = PlantMeteo.Constants().J_to_umol\nincident_ppfd = J_to_umol .* data.Ri_PAR_f\nf_abs = data.aPPFD ./ incident_ppfd\nk = Statistics.mean(-log1p.(-f_abs) ./ data.LAI)\n\nThis snippet shows the inversion only. The implementation in examples/Beer.jl validates every observation and returns (k=k,); do not use the snippet alone as an unchecked fitting method.\n\nThe result should be a NamedTuple of fitted parameters.\n\nIn this Beer example, Ri_PAR_f is an incident flux per unit ground area and aPPFD is the flux absorbed by the whole canopy, also per unit ground area. LAI is leaf area per unit ground area. Do not use a PPFD expressed per unit leaf area in this inversion. The fit rejects empty data, non-finite or non-positive LAI and incident PAR, and absorbed fractions outside [0, 1).\n\nusing PlantSimEngine, PlantMeteo, Dates, DataFrames\nusing PlantSimEngine.Examples\n\nmeteo = Atmosphere(\n    T=20.0,\n    Wind=1.0,\n    P=101.3,\n    Rh=0.65,\n    Ri_PAR_f=300.0,\n    duration=Hour(1),\n)\n\nmodel = CompositeModel(\n    Beer(0.6);\n    status=(LAI=2.0,),\n    id=:plant,\n    scale=:Plant,\n    environment=meteo,\n)\n\nsimulation = run!(model)\nplant = final_state(simulation, One(scale=:Plant))\ndata = DataFrame(\n    aPPFD=[plant.aPPFD],\n    LAI=[plant.LAI],\n    Ri_PAR_f=[meteo.Ri_PAR_f[1]],\n)\n\nPlantSimEngine.Evaluation.fit(Beer, data)\n\nThis example recovers the parameter used to generate the synthetic observation. Real calibration methods can use any optimizer or uncertainty framework and may return additional diagnostics.","title":"Parameter Fitting"},{"category":"section","location":"guides/multiscale/manual_calls/#Manual-Calls-Across-Objects","page":"Manual calls","text":"Declare parent-owned execution with ModelSpec(model; calls=(:name => One(...),)) or a Many(...) selector. In the kernel, execute every resolved target with run_call!(context, :name). The returned CallTargets collection is always vector-like, including for One and OptionalOne.\n\nUse the narrowest execution path that matches the algorithm:\n\nrun_call!(context, :name; sampled_environment=environment) executes all targets directly through cached typed batches. Prefer it when the caller has already sampled one model-facing environment for every target.\ncall_model(context, :name) returns the concrete model for a call that resolves to exactly one target. It is useful when dispatch or model parameters must be inspected before the bulk call.\ncall_targets(context, :name) followed by run_call!(target) supports object selection, custom ordering, target status inspection, or a different sampled environment per target.\n\nenvironment=trial_state has different semantics from sampled_environment=value. The former is a transient backend state that each target samples through its compiled environment handle. The latter is already in the model-facing form and is forwarded without sampling.\n\nA target used only by calls is absent from root scheduling. Trial calls default to publish=false; publish only an accepted execution.","title":"Manual Calls Across Objects"},{"category":"section","location":"guides/multiscale/manual_calls/#Initialize-a-newly-registered-object","page":"Manual calls","text":"Use Initializer, not an ordinary manual Call, when the target application must remain in the root schedule but a creator needs to run it once on a new object after its normal slot already passed:\n\ncreator = ModelSpec(\n    GrowthModel();\n    name=:growth,\n    on=One(scale=:Plant),\n    calls=(\n        leaf_state=Initializer(\n            One(\n                scale=:Leaf,\n                within=Subtree(),\n                application=:leaf_state,\n            ),\n        ),\n    ),\n)\n\nfunction PlantSimEngine.run!(::GrowthModel, status, environment, constants, context)\n    leaf = register_object!(\n        runtime_model(context),\n        Object(:new_leaf; scale=:Leaf, parent=object_id(context)),\n    )\n    initialized_status = run_initializer!(context, :leaf_state, leaf)\n    return nothing\nend\n\nInitializer selectors follow the ordinary contextual-scope rules. A call from a plant object defaults to Self(), so a creator targeting a new descendant must state within=Subtree() explicitly. A scene creator may instead use within=SceneScope() when the target is scene-wide.\n\nThe target application must use on=Many(...), an explicit application=, and exactly the caller's cadence and phase. It remains root-scheduled and owns its canonical outputs. The compiler orders it before the creator and orders the execution owners of same-step consumers after the creator. That owner is the consumer itself for an ordinary scheduled application, the root hard-call owner for a manual callee, or the consumer's creator when it is another initializer target. If both calls belong to the same creator, its kernel must invoke the initializers in dependency order; there is no meaningful self-edge to impose that intra-kernel order. Targeted newborn execution supports the global environment, canonical local outputs, non-temporal inputs, and PreviousTimeStep inputs, including canonical input sources written through another application's outputs_to. It rejects nested calls, distributed or stream-only outputs on the initializer target itself, other temporal policies, mixed manual ownership, multiple initializer owners, and any overlapping local or distributed canonical writer for the target's outputs. Each initialized output must have one canonical writer; Updates ordering cannot make a later writer safe because targeted execution occurs inside the creator's kernel.\n\nOnly direct, non-temporal downstream consumers may observe the initialized output later in that same step. run_initializer! deliberately publishes no mid-step stream sample, so the compiler rejects downstream HoldLast windows, Interpolate, Integrate, Aggregate, and PreviousTimeStep bindings that could consume an initializer target's newborn output. This is distinct from a PreviousTimeStep input used by the initializer itself, whose newborn fallback is supported. This first initializer contract does not admit a temporal downstream binding at all. When later history is required, publish the value from a distinct scheduled application and consume that application's history on a later timestep.\n\nAn initializer binding stores only its statically validated application identity. It does not collect pre-existing target objects or build cached execution batches; run_initializer! resolves only the explicit newborn.\n\nrun_initializer! accepts exactly one object added by the current pure addition event, mutates its canonical local status without adding a mid-step output-history sample, and returns that canonical Status. A second call for the same application/object pair is an error. The pair is reserved before the model runs, so a failed initializer remains marked and cannot be retried after an unknown partial mutation in the same lifecycle event. Existing, reparented, foreign, or refresh-fallback targets are also errors. Use ordinary Call and run_call! for trial execution or repeated controller-owned calls.","title":"Initialize a newly registered object"},{"category":"section","location":"guides/multiscale/manual_calls/#Compiled-plans-and-changing-objects","page":"Manual calls","text":"The call declaration is compiled once with the scenario. Its call name, applications, selector, multiplicity, ordering, and execution batches remain fixed during the simulation. Ordinary calls therefore do not resolve selectors or rebuild public target wrappers in their execution loop.\n\nObjects may still be created, removed, or reparented during growth. At the structural refresh barrier, PlantSimEngine updates only the affected resolved target buffers. Later applications in the same timestep see the new targets; applications that already ran are not repeated. The following ordinary timestep returns to the cached execution path.\n\ncall_targets(context, name; objects=newborn) can build a targeted partial view without crossing that barrier only while the pending lifecycle delta is a pure addition. If the same event also removes or reparents an object, the manual-call API performs the full binding and environment refresh before it resolves the requested objects. This preserves current topology membership but has the cost of a mid-kernel refresh and consumes the pending dirty state. run_initializer! is stricter: it rejects such a mixed structural event.\n\nExplicit target cadence must match the caller. A target without an explicit cadence inherits the caller's invocation timing.","title":"Compiled plans and changing objects"},{"category":"section","location":"dev/public_api_refinement_completion_audit/#Public-API-Refinement-Completion-Audit","page":"Public API refinement completion audit","text":"This audit records the supported contract and the evidence used to stabilize it. It complements the decision record and the public symbol inventory.","title":"Public API Refinement Completion Audit"},{"category":"section","location":"dev/public_api_refinement_completion_audit/#Contract-evidence","page":"Public API refinement completion audit","text":"Requirement Supported contract Evidence\nPublic boundary Composition, model-author, diagnostic, and extension symbols are exported by default; compiler/cache representations live under PlantSimEngine.Advanced. test-model-api-stabilization.jl checks the namespace boundary; Documenter's missing-doc check covers exported docstrings.\nApplication identity Repeated process applications require explicit names. Inputs, calls, outputs, overrides, and Updates(...; after=...) use canonical application IDs. Singular process references are rejected; Many(process=...) remains an explicit discovery query. Stabilization, binding-inference, hard-call, output, override, and update tests cover repeated applications and actionable errors.\nSelector grammar Self() is one object, Subtree() is that object plus descendants, SelfPlant() is the containing plant, and SceneScope() is the model. One, OptionalOne, and Many share the same criteria across targeting, coupling, lookup, and outputs. Multi-plant selector tests, instance/template tests, lifecycle tests, and XPalm downstream tests.\nOutputs outputs=:none is the safe default; :all and selector-based OutputRequests are explicit. Request names are unique, application identity is preserved, and removed-object history remains collectable. Output-boundary, runtime-matrix, multirate, lifecycle-history, and allocation tests.\nExecution ownership run! starts a fresh simulation; continue! and step! advance its live handle without resetting time, streams, schedules, or environment position. Split-run equivalence, multirate-boundary, environment-resume, and lifecycle-continuation tests.\nConstruction and initialization CompositeModel(models...; status=...) lowers to ordinary objects and ModelSpecs. Diagnostics.explain_initialization reports application, object, origin, defaults, expected/provided types, and remedies without running kernels. Concise/explicit lowering equivalence and initialization report tests.\nDiagnostics Supported explanation functions accept CompositeModel directly and compiled views where useful; simulation overloads avoid field inspection. Results are structured vectors that can be filtered with ordinary Julia predicates. Structured explanation assertions throughout the model test matrix and documentation examples.\nLifecycle Registration, MTG growth, removal, reparenting, movement, and geometry updates are the supported mutation paths. Cycle/self-parent failures are atomic; structural and geometry invalidation remain targeted. Stabilization, unified integration, environment, and lifecycle-output tests.\nModel-author API The kernel is run!(model, status, environment, constants, context). Model parameters come from model; runtime_model, call-target accessors, traits, and lifecycle helpers are the supported context surface. test-model-contract.jl, hard-call tests, growing-plant tutorial, and downstream model suites.\nCompatibility tracked_outputs, singular scenario process= references, output-request process=, and process-only overrides are removed. Mapping runtimes are not restored. Migration guide plus rejection tests.","title":"Contract evidence"},{"category":"section","location":"dev/public_api_refinement_completion_audit/#Validation-matrix","page":"Public API refinement completion audit","text":"The release gate is:\n\ncomplete PlantSimEngine package tests, including allocation gates and doctests;\na full Documenter build with missing-doc and executable-example checks;\nfull PlantBiophysics and XPalm downstream suites against this checkout;\nbenchmark smoke tests for native, multirate, PlantBiophysics, and XPalm paths;\ngit diff --check and searches for transitional spellings outside explicit migration/history documentation.\n\nThis matrix covers one/many objects, all selector multiplicities, soft inputs, hard calls, duplicate writers, temporal policies, global/spatial environments, templates, instances, overrides, lifecycle mutation, generic values, output retention modes, fresh/continued execution, and homogeneous hot-loop allocation.","title":"Validation matrix"},{"category":"section","location":"dev/public_api_refinement_completion_audit/#Deliberate-compatibility-boundary","page":"Public API refinement completion audit","text":"Compiled structs and cache controls are qualified advanced APIs and may evolve. Direct mutation of Object or CompositeModel fields is unsupported. Historical ModelMapping, executor, and status-vector runtimes are outside the compatibility surface and must not be reintroduced.","title":"Deliberate compatibility boundary"},{"category":"section","location":"step_by_step/model_switching/#Model-switching","page":"Composition and switching","text":"One main objective of PlantSimEngine is to let users switch between model implementations for a process without changing the engine or the other model kernels.\n\nIn the composite-model/object API, the switch happens at the model-application layer: replace the model inside a ModelSpec, keep the same ModelSpec(...; on=...) selector, and keep the same input contract when the replacement model needs the same variables.","title":"Model switching"},{"category":"section","location":"step_by_step/model_switching/#A-first-simulation","page":"Composition and switching","text":"This model computes degree-days, LAI, absorbed PAR, and growth on one model object:\n\nfunction plant_model_with_growth(growth_model; growth_name=:growth)\n    CompositeModel(\n        Object(:scene; scale=:Scene, kind=:scene);\n        applications=(\n            ModelSpec(ToyDegreeDaysCumulModel(); name=:degree_days, on=One(scale=:Scene), every=Day(1)),\n\n            ModelSpec(ToyLAIModel(); name=:lai, on=One(scale=:Scene), every=Day(1)),\n\n            ModelSpec(Beer(0.5); name=:light_interception, on=One(scale=:Scene), every=Day(1)),\n\n            ModelSpec(growth_model; name=growth_name, on=One(scale=:Scene), every=Day(1)),\n        ),\n        environment=meteo_day,\n    )\nend\n\nrue_scene = plant_model_with_growth(ToyRUEGrowthModel(0.2))\nrue_sim = run!(rue_scene; steps=10)\nrue_status = final_state(rue_sim)\n(growth_model=:ToyRUEGrowthModel, biomass=rue_status.biomass)\n\nThe compiler infers the same-object bindings from the model declarations. The growth model reads aPPFD, which is produced by the light interception model:\n\nselect(\n    DataFrame(Diagnostics.explain_bindings(rue_scene)),\n    :application_id,\n    :input,\n    :source_application_ids,\n    :origin,\n    :carrier_kind,\n)","title":"A first simulation"},{"category":"section","location":"step_by_step/model_switching/#Switching-the-growth-model","page":"Composition and switching","text":"ToyAssimGrowthModel implements the same :growth process, reads the same aPPFD input, and computes additional outputs such as carbon assimilation and respiration. The rest of the model does not need to change:\n\nassim_scene = plant_model_with_growth(ToyAssimGrowthModel())\nassim_sim = run!(assim_scene; steps=10)\nassim_status = final_state(assim_sim)\n(\n    growth_model=:ToyAssimGrowthModel,\n    carbon_assimilation=assim_status.carbon_assimilation,\n    Rm=assim_status.Rm,\n    biomass=assim_status.biomass,\n)\n\nThe dependency graph and execution plan are rebuilt from the new application set:\n\nselect(\n    DataFrame(Diagnostics.explain_execution_plan(assim_sim)),\n    :application_id,\n    :object_ids,\n    :batch_size,\n    :inner_loop_dispatch,\n)\n\nThis is the same principle used in larger composite models: switch one process implementation by replacing one ModelSpec or by using an ObjectInstance(...; overrides=...) when the change applies to one plant instance or one organ.","title":"Switching the growth model"},{"category":"section","location":"troubleshooting_and_testing/downstream_tests/#Automated-tests-:-downstream-dependency-checking","page":"Downstream testing","text":"PlantSimEngine is open sourced on Github, and so are its other companion packages, PlantGeom.jl, PlantMeteo.jl, PlantBioPhysics.jl, MultiScaleTreeGraph.jl, and XPalm.\n\nOne handy CI (Continuous Integration) feature implemented for these packages is automated integration and downstream testing: after changes to a package, its known downstream dependencies are tested to ensure no breaking changes were introduced. \n\nFor instance, PlantBioPhysics uses PlantSimEngine, so the integration workflow checks that PlantBioPhysics's tests do not break unexpectedly after changes to PlantSimEngine. The repository also keeps a separate benchmark workflow and benchmark scripts in the benchmark/ directory for performance tracking.\n\nIf you maintain a package that depends on PlantSimEngine, you can propose adding it to the downstream integration workflow through a pull request.","title":"Automated tests : downstream dependency checking"},{"category":"section","location":"guides/multiscale/visualizing_structure/#Visualizing-Composite-model-Structure","page":"Visualizing structure","text":"Use Diagnostics.explain_objects, Diagnostics.explain_scopes, and Diagnostics.explain_instances to obtain stable rows for plotting. Draw nodes by object ID and edges from parent to child; color by scale, species, or instance. Keep simulation visualization outside model kernels so model code remains independent of topology packages.","title":"Visualizing Composite model Structure"},{"category":"section","location":"dev/code_cleanup_audit/#Code-Cleanup-Audit","page":"Code cleanup audit","text":"","title":"Code Cleanup Audit"},{"category":"section","location":"dev/code_cleanup_audit/#Status","page":"Code cleanup audit","text":"The compatibility cleanup is implemented on the multi-plants branch.\n\nThe package now has one scenario compiler and runtime: the composite-model/object API. The following superseded implementations were removed rather than deprecated:\n\nModelList and SingleScaleModelSet;\nModelMapping and MultiScaleModel;\nDependencyGraph, HardDependencyNode, and SoftDependencyNode;\nGraphSimulation and the MTG mapping runner;\nmapping-specific multirate input resolution and output export;\nthe unreleased domain prototype that preceded the composite-model/object API, including Domain, SimulationMapping, Route, AllDomains, HardDomains, and its separate scheduler, runtime, environment bridge, and output publisher;\nmapping-only initialization, dataframe, dimension, and topology helpers;\nunused parallel-executor traits after removal of the executor runtime;\ndead UninitializedVar, RefVariable, TreeAlike, and StatusView types;\nthe unreleased CompositeModelTemplate(...; mapping=...) alias and legacy selector-to-mapping conversion helpers;\ncompatibility tests, tutorials, and executable examples.\n\nMigration details are retained in migration_composite_model.md and release_notes_handoff.md.","title":"Status"},{"category":"section","location":"dev/code_cleanup_audit/#Current-Ownership","page":"Code cleanup audit","text":"Concern Owner\nObject registry, selectors, compilation, execution, lifecycle src/composite_model_api.jl\nModel application configuration src/ModelSpec.jl\nStatus and reference vectors src/component_models/Status.jl, src/component_models/RefVector.jl\nDates-based clocks and policies src/time/multirate.jl, src/time/runtime/clocks.jl\nEnvironment sampling src/time/runtime/environment_sampling.jl\nEnvironment backends src/time/runtime/environment_backends.jl\nOutput request definition src/time/runtime/output_export.jl","title":"Current Ownership"},{"category":"section","location":"dev/code_cleanup_audit/#Remaining-Review-Rules","page":"Code cleanup audit","text":"Future cleanup should reject:\n\na second scenario/runtime abstraction parallel to CompositeModel;\ncompatibility wrappers for unreleased APIs;\npackage-specific behavior in PlantSimEngine;\nmodel kernels that know their scenario object, timestep, or coupling unless the scientific algorithm requires a hard call;\ndynamic per-object dispatch or copying in hot loops when compiled typed batches and reference carriers are available;\nundocumented public names or agent instructions that describe removed APIs.","title":"Remaining Review Rules"},{"category":"section","location":"dev/code_cleanup_audit/#Verification","page":"Code cleanup audit","text":"Current cleanup evidence:\n\nthe src tree contains only the composite-model/object runtime and supporting status, time, environment, fitting, trait, and example files;\nempty directories left by removed subsystems were deleted;\ngit diff --check passes;\nPlantSimEngine precompiles and loads from a clean Kaimon session;\ntest/test-unified-model-object-api.jl passes 576 tests;\nthe complete package environment passes 885 tests, including Aqua and doctests;\ntest/test-fitting.jl passes;\nthe documentation build passes, including executable examples and cross-reference checks;\nrepository search finds no superseded scenario-runtime definitions or references in source, tests, examples, README, public docs, or the packaged agent skill;\nremaining ModelMapping and MultiScaleModel references outside development notes are migration text that explicitly points historical code to the composite-model/object API.\nrepository search finds no Domain, SimulationMapping, Route, AllDomains, or HardDomains implementations, exports, tests, examples, or public documentation. The only remaining references are development/release notes that record their removal from this unreleased branch;\nignored .DS_Store files were removed from the working tree outside .git.\nModelSpec pipe-helper boilerplate was consolidated behind shared internal helpers while keeping the public on, inputs, calls, every, Environment, Updates, and output_routing grammar unchanged.\nthe duplicate Advanced.TimeStepTable export was removed from the PlantMeteo re-export block; Advanced.TimeStepTable remains exported once from the core status export list.\nstale PlantSimEngine.Examples exports for the deleted ToyInternodeEmergence example were removed.\n\nDownstream verification:\n\nPlantBiophysics passes 117/117 tests against this working tree.\nXPalm's uncommitted CompositeModel/Object migration executes 74/75 assertions. The remaining assertion retains the removed runtime's first-step LAI value, while the explicit current dependency order produces zero from the initial zero-biomass leaf before plant/model aggregation. PlantSimEngine intentionally contains no package-specific compatibility workaround for that stale fixture.","title":"Verification"},{"category":"section","location":"guides/time/multirate_concepts/#Understanding-Model-Cadence","page":"Understanding Model Cadence","text":"ModelSpec(...; every=Dates.Period) sets application cadence. Without every, an application uses timespec(model) when non-default and otherwise the environment base step. timestep_hint validates a scientifically acceptable range; it does not silently change cadence.\n\nTemporal input policies have different physical meanings: HoldLast samples the latest state, Aggregate reduces observations, and Integrate multiplies rates by sample durations. PreviousTimeStep deliberately breaks a same-step cycle. Windows are rolling fixed-duration windows. Civil-day or previous-complete-calendar-period alignment is not a supported public feature.","title":"Understanding Model Cadence"},{"category":"section","location":"journeys/modelers/environment_and_cadence/#Implement-Environment-And-Cadence-Traits","page":"Environment and cadence traits","text":"New concept: model-authored runtime traits. Environment declarations name the fields a kernel samples, while cadence and output-policy traits state when the same kernel runs and how its values cross clocks.\n\nSimulation users configure providers in Understand Environments and application clocks in Give Models Different Cadences.","title":"Implement Environment And Cadence Traits"},{"category":"section","location":"journeys/modelers/environment_and_cadence/#Model-6:-declare-sampled-environment-inputs","page":"Environment and cadence traits","text":"ToyMaintenanceRespirationModel reads object state and sampled temperature. The tested contract names that environment field explicitly:\n\nPlantSimEngine.inputs_(::ToyMaintenanceRespirationModel) = (\n    carbon_biomass=Required(Real),\n)\nPlantSimEngine.environment_inputs_(\n    model::ToyMaintenanceRespirationModel,\n) = (T=zero(model.T_ref),)\nPlantSimEngine.outputs_(model::ToyMaintenanceRespirationModel) = (\n    Rm=oftype(model.Rm_base, -Inf),\n)\n\nThe kernel reads model parameters from model, object state from status, and forcing from environment:\n\nfunction PlantSimEngine.run!(\n    model::ToyMaintenanceRespirationModel,\n    status,\n    environment,\n    constants,\n    context,\n)\n    status.Rm =\n        status.carbon_biomass *\n        model.P_alive *\n        model.nitrogen_content *\n        model.Rm_base *\n        model.Q10^((environment.T - model.T_ref) / 10)\n    return nothing\nend\n\nusing Dates, PlantMeteo, PlantSimEngine, DataFrames\nusing PlantSimEngine.Examples\n\nrespiration = ToyMaintenanceRespirationModel(\n    2.0,\n    0.06,\n    25.0,\n    0.5,\n    0.02,\n)\nmodel = CompositeModel(\n    Object(\n        :leaf;\n        scale=:Leaf,\n        status=Status(carbon_biomass=10.0),\n    );\n    applications=(\n        ModelSpec(\n            respiration;\n            name=:maintenance,\n            on=One(scale=:Leaf),\n        ),\n    ),\n    environment=Atmosphere(\n        T=25.0,\n        Wind=1.0,\n        Rh=0.7,\n        duration=Hour(1),\n    ),\n)\n\n(\n    declared=PlantSimEngine.environment_inputs_(respiration),\n    final=final_state(run!(model)),\n)\n\nThe model does not name a provider or inspect raw weather storage. Environment(...) remains scenario configuration.","title":"Model 6: declare sampled environment inputs"},{"category":"section","location":"journeys/modelers/environment_and_cadence/#Model-7:-declare-cadence-and-output-semantics","page":"Environment and cadence traits","text":"ToyDailyDevelopmentModel accumulates one increment whenever it runs. Its model-level traits say “every 24 simulation steps, starting at step 1” and “consumers may hold the last daily value between publications”:\n\nPlantSimEngine.timespec(::Type{<:ToyDailyDevelopmentModel}) =\n    ClockSpec(24.0, 1.0)\nPlantSimEngine.output_policy(\n    ::Type{<:ToyDailyDevelopmentModel},\n) = (daily_growth=HoldLast(),)\n\nClockSpec is expressed in simulation steps. Use ModelSpec(...; every=Day(1)) when a scenario should express a duration-relative cadence or override the model default.\n\ndaily = ToyDailyDevelopmentModel(2.0)\ndaily_model = CompositeModel(\n    Object(:plant; scale=:Plant);\n    applications=(\n        ModelSpec(\n            daily;\n            name=:daily_development,\n            on=One(scale=:Plant),\n        ),\n    ),\n    environment=[\n        (duration=Hour(1),)\n        for _ in 1:25\n    ],\n)\n\ndaily_simulation =\n    run!(daily_model; steps=25, outputs=:all)\n(\n    traits=(\n        clock=PlantSimEngine.timespec(daily),\n        outputs=PlantSimEngine.output_policy(daily),\n    ),\n    schedule=DataFrame(Diagnostics.explain_schedule(daily_model)),\n    final=final_state(daily_simulation),\n    retained=DataFrame(\n        Diagnostics.explain_outputs(daily_simulation),\n    ),\n)\n\nThe model runs at steps 1 and 25. A downstream application can override HoldLast with an explicit selector policy when its scientific interpretation requires Integrate, Aggregate, or Interpolate.","title":"Model 7: declare cadence and output semantics"},{"category":"section","location":"journeys/modelers/environment_and_cadence/#Model-author-recap","page":"Environment and cadence traits","text":"You implemented: declared environment reads, a model default clock, and per-output temporal meaning.\nPlantSimEngine inferred: environment validation/sampling, execution steps, and the default cross-clock policy.\nThe scenario author keeps explicit: concrete environment provider, simulation base step, cadence overrides, and input-policy overrides.\nNew API names: environment_inputs_, timespec, ClockSpec, output_policy, and HoldLast.","title":"Model-author recap"},{"category":"section","location":"troubleshooting/runtime_contracts/#Runtime-Contracts-And-Diagnostics","page":"Runtime contracts","text":"Use Diagnostics.explain_initialization, Diagnostics.explain_bindings, Diagnostics.explain_calls, Diagnostics.explain_schedule, Diagnostics.explain_environment_bindings, and Diagnostics.explain_output_retention as the supported inspection surface. Do not inspect compiled internal fields.\n\nTargets, carriers, calls, writer checks, and schedules refresh after the application that made a structural change. New objects join applications still remaining in that timestep; they do not retroactively run earlier applications. Movement and geometry changes invalidate affected spatial environment bindings. Accepted streams are append-only.","title":"Runtime Contracts And Diagnostics"},{"category":"section","location":"journeys/users/maespa_synthesis/#MAESPA-Style-Synthesis","page":"MAESPA-style synthesis","text":"","title":"MAESPA-Style Synthesis"},{"category":"section","location":"journeys/users/maespa_synthesis/#New-concept:-synthesis-without-another-runtime-mechanism","page":"MAESPA-style synthesis","text":"This page is an integrated reference, not an onboarding example. It combines the ideas developed independently in the earlier journeys into a small MAESPA-style stand: two species, five leaves, hourly canopy and soil exchange, daily allocation and LAI, iterative leaf calls, and accepted mutable canopy air.\n\nIf any individual mechanism is unfamiliar, follow its focused link in How the pieces compose before reading the implementation.","title":"New concept: synthesis without another runtime mechanism"},{"category":"section","location":"journeys/users/maespa_synthesis/#Run-the-reference-case","page":"MAESPA-style synthesis","text":"The complete, tested source lives in examples/maespa_model_example.jl. Run 25 hours so both hourly and daily applications cross a day boundary:\n\nusing PlantSimEngine, DataFrames\n\ninclude(joinpath(\n    pkgdir(PlantSimEngine),\n    \"examples\",\n    \"maespa_model_example.jl\",\n))\n\nresult = run_maespa_example(; nhours=25, check=true)\nsimulation = result.simulation\nmodel = result.model\nnothing\n\nThe model contains one scene, one soil object, and two template instances with different species parameters and leaf counts:\n\n(\n    instances=DataFrame(Diagnostics.explain_instances(model)),\n    plants=length(model_objects(model; scale=:Plant)),\n    leaves=length(model_objects(model; scale=:Leaf)),\n    species_A=length(model_objects(model; scale=:Leaf, species=:A)),\n    species_B=length(model_objects(model; scale=:Leaf, species=:B)),\n)","title":"Run the reference case"},{"category":"section","location":"journeys/users/maespa_synthesis/#Inspect-the-compiled-architecture","page":"MAESPA-style synthesis","text":"The execution schedule makes the two cadences and parent-controlled applications visible. Leaf energy balance and soil water are call-only under the scene controller; allocation and LAI run daily:\n\nschedule = DataFrame(Diagnostics.explain_schedule(result.compiled))\nselect(\n    filter(\n        row -> row.application_id in (\n            :scene_eb,\n            :soil_water,\n            :lai_dynamic,\n            :plant_A__energy_balance,\n            :plant_A__allocation,\n            :plant_B__allocation,\n        ),\n        schedule,\n    ),\n    :application_id,\n    :root_scheduled,\n    :manual_call_only,\n    :dt_steps,\n)\n\nThe scene energy-balance application resolves all five leaves through one Many hard call and the soil through one One hard call:\n\ncalls = DataFrame(Diagnostics.explain_calls(result.compiled))\nselect(\n    filter(row -> row.application_id == :scene_eb, calls),\n    :call,\n    :callee_application_ids,\n    :callee_object_ids,\n    :publication_policy,\n)\n\nEach plant allocation application receives a live vector of only its own descendant leaves. The scene receives stand-wide vectors and one scalar soil potential:\n\nbindings = DataFrame(Diagnostics.explain_bindings(result.compiled))\nselect(\n    filter(\n        row -> (\n            row.application_id in (\n                :plant_A__allocation,\n                :plant_B__allocation,\n            ) && row.input == :leaf_carbon\n        ) || (\n            row.application_id == :scene_eb &&\n            row.input in (:leaf_areas, :psi_soil)\n        ),\n        bindings,\n    ),\n    :application_id,\n    :input,\n    :source_ids,\n    :carrier_kind,\n    :copy_semantics,\n)","title":"Inspect the compiled architecture"},{"category":"section","location":"journeys/users/maespa_synthesis/#Follow-trial-canopy-air-to-its-accepted-state","page":"MAESPA-style synthesis","text":"The scene controller reads above-canopy :forcing, iterates leaf models against typed trial canopy air with publish=false, commits the converged state to sink=:canopy, and then publishes one accepted leaf execution. Leaf applications read the committed :canopy provider. Those routes are compiled into opaque handles:\n\nenvironment_bindings = DataFrame(\n    Diagnostics.explain_environment_bindings(result.environment),\n)\nselect(\n    filter(\n        row -> row.application_id in (\n            :scene_eb,\n            :plant_A__energy_balance,\n            :plant_B__energy_balance,\n        ),\n        environment_bindings,\n    ),\n    :application_id,\n    :object_id,\n    :handle,\n    :required_inputs,\n    :produced_outputs,\n)\n\nMaespaSingleLayerEnvironment is intentionally a one-layer canopy backend. Its handle still separates forcing, canopy, and commit-sink routes per application/object. A voxel or multilayer backend can replace it without changing the model-facing environment contract; the two-cell proof is in Modify The Environment, and backend implementation belongs in Environment Backend Extensions.","title":"Follow trial canopy air to its accepted state"},{"category":"section","location":"journeys/users/maespa_synthesis/#Check-the-scientific-handoffs","page":"MAESPA-style synthesis","text":"The final snapshots expose canonical state independently of retained history:\n\nscene = final_state(simulation, :model)\nsoil = final_state(simulation, :soil)\nplants = final_state(simulation, Many(scale=:Plant))\n\n(\n    lai=scene.lai,\n    canopy_temperature=scene.canopy_tair,\n    transpiration=scene.scene_transpiration,\n    soil_water_potential=soil.psi_soil,\n    daily_growth=Dict(\n        id => state.daily_growth\n        for (id, state) in plants\n    ),\n)\n\nRetained output counts confirm the cadence boundary: hourly scene and leaf variables have 25 samples, while daily LAI and allocation variables have two:\n\noutput_summary = DataFrame(Diagnostics.explain_outputs(simulation))\nselect(\n    filter(\n        row -> (\n            row.object_id == :model &&\n            row.variable in (:scene_transpiration, :lai)\n        ) || (\n            row.object_id in (:plant_A, :plant_B) &&\n            row.variable == :daily_growth\n        ) || (\n            row.object_id == :plant_A_leaf_1 &&\n            row.variable == :λE\n        ),\n        output_summary,\n    ),\n    :application_id,\n    :object_id,\n    :variable,\n    :nsamples,\n)","title":"Check the scientific handoffs"},{"category":"section","location":"journeys/users/maespa_synthesis/#How-the-pieces-compose","page":"MAESPA-style synthesis","text":"Construct in this synthesis Role here Focused journey\nCompositeModelTemplate and two ObjectInstances Reuse one species-specific application set across several plants Instantiate Several Plants\nScene, plant, internode, leaf, and soil objects Represent one registry without prescribing plant architecture Build One Multiscale Plant\nScalar and Many live-reference bindings Couple soil-to-scene and leaf-to-plant/scene values Build One Multiscale Plant\nHourly and daily applications with HoldLast Keep canopy exchange and allocation on scientific cadences Give Models Different Cadences\nForcing and canopy providers with compiled handles Sample global forcing and committed canopy state through one contract Understand Environments\nTyped trials and explicit accepted commit Iterate canopy air without publishing rejected states Modify The Environment\nNested hard calls and accepted publication Let scene energy balance control leaf and soil execution Control Advanced Execution\nSimulation, final state, and retained streams Separate current canonical state from requested history Couple Models On One Object\n\nThis 25-hour reference keeps plant topology fixed because organogenesis is not part of its scientific question. A growth model can add, reparent, or remove organs through the same registry and refresh machinery; that independent lifecycle is demonstrated in Modify Plant Structure. Keeping it out of this synthesis prevents canopy iteration, daily allocation, and topology mutation from becoming one inseparable example.","title":"How the pieces compose"},{"category":"section","location":"journeys/users/maespa_synthesis/#Reference-invariants","page":"MAESPA-style synthesis","text":"The automated example test verifies the important handoffs rather than exact floating-point trajectories:\n\nthe stand contains two isolated instances and five correctly routed leaves;\nplant allocation vectors contain only descendant leaves;\nthe scene call resolves every leaf and the shared soil application;\nhourly and daily output counts match their cadences;\naccepted canopy air is committed separately from above-canopy forcing;\nleaf fluxes are finite and aggregate consistently at scene scale;\nboth species grow, while their parameterized allocations remain distinct.","title":"Reference invariants"},{"category":"section","location":"journeys/users/maespa_synthesis/#Page-recap","page":"MAESPA-style synthesis","text":"You added: no new primitive; you assembled the earlier topology, cadence, hard-call, environment, and output mechanisms into one stand.\nPlantSimEngine inferred: application order, plant-local bindings, concrete call targets, environment handles, and the hourly/daily schedule.\nYou keep explicit: species parameters, topology, scientific iteration, accepted state, output requests, and the invariants used to validate the result.\nNew API names: none. Every API in this synthesis was introduced on a focused earlier journey.","title":"Page recap"}]
}
